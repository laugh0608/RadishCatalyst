# Current Plan

更新时间：2026-09-12

## 当前阶段

```text
正式三维工厂基础已实现 · 原生关闭与独立恢复补验完成 · 渲染优化已有收益，前台持续性能待收口
```

方向以 [Production-Centered Direction](../product/production-centered-direction.md) 为产品真相源：化工生产经营为核心，探索提供资源、空间和长期目的。既有二维切片、schema 10 和历史验证保留，旧整改 S0 暂缓。

## 最新决定与实现

萝卜SAMA已认可 Godot Demo 的画面和操作，明确精细度暂缓，并确认正式接入、旧二维世界保留、工厂独立存档和 P1–P3 扩建 / 规模方案。9 月 12 日确认继续按首包收口顺序推进，无需重复请求同范围实施或画面授权。

正式基础 `978d6250` 已实现 Boot 工厂入口、64×64 / 25 矿点、多设备物流、相机跟随、独立 schema 1、保存恢复与单写入锁。9 月 9 日修正活动计时和失败退出；9 月 12 日省去未变生产状态的重复渲染提交，状态灯共享同形网格和只读材质。产品默认 Metal / Forward+、VSync、4× MSAA、实际像素、生产规则和存档格式未改。

整包尚未验收，不把实现、诊断段或短测等同于长期稳定经营。

## 当前活跃专题

- [Factory Foundation And Persistence V1](../features/factory-foundation-and-persistence-v1.md)：玩家路径、供给、P1–P3 合同、已测状态和验收缺口。
- [Factory World State And Save V1](../architecture/factory-world-state-and-save-v1.md)：目录 / 入口、状态权威、独立存档、锁、保存失败、时钟及视图同步。
- [Godot Demo](../features/godot-first-production-line-demo-v1.md) 和 [Web 产线](../features/web-first-production-line-v1.md) 保留作参照；[路线评估](../architecture/production-client-technology-assessment-v1.md) 保留决策依据。

## 当前边界

- 保留已认可的造型、50° 视角、配色、光照、接口箭头、拖铺与真实弯道；精细美术另包推进。
- 当前只有采集器、反应器、终端仓和固体带；预供电、预发构件用于基础验证，不混入旧切片电力 / 仓储规则。
- 普通世界与工程 100 台 / 1,000 带负载明确区分；工程夹具不出现在普通新建选项中。
- 工厂与旧世界入口、状态和存档隔离，不自动转换或删除 schema 10。
- 未解冻新设备、经济解锁、液气、战斗、多人、随机地图、多星球、离线收益、量产美术、分发和发布。

## 当前验证与限制

- 状态 / Web 对照、存档故障、跨进程、旧入口 / 存档、双线操作、100 台 / 1,000 带满载存读及汇流已有定向证据。窗口长帧计时、暂停恢复、规模拆改、地图四侧 / 相机 / HUD 及原生失败返回已复核。批次与历史失败见 [W37 周志](../devlogs/2026-W37.md)。
- 30 分钟隔离 headless Boot 长测通过：交付 4,375 件、59 次自动保存成功，独立 Store 读取完整状态及后续轨迹一致。测试注入活动焦点，不替代前台渲染或原生操作。
- 9 月 12 日原生系统关闭保存 17 项、独立 Boot 重进 15 项通过；完整状态在首帧前核对，验证写入锁释放与新进程获取，两张原图已审阅。先前原生取消 / 重试 / 最终放弃 / 返回 / 同进程重进 53 项证据保留。
- 同状态同步开启 / 关闭 / 恢复各 90 秒完整执行，三份有效调用栈保留且标记采样干扰；帧 p95 `33.550 / 17.459 / 28.654ms` 均超预算，不能以关闭同步为最终解法。
- 两项视图优化与旧实现 28,806 项比较通过，最终基准原图字节相同。优化后原配置 90 秒交付 225 件，绘制调用 p95 从 557 降至 508，模拟步 p95 `1.113ms`、节点处理跨度 p95 `3.324ms`；帧 p95 `21.237ms` 仍超 `16.7ms`。各批次存在运行时波动，不把全部帧差归于代码优化。
- Godot 内置 Metal GPU 时间接口仍不可用。临时 Vulkan 对照取得 GPU 时间但也超预算；SSAO / 阴影分项诊断及恢复已完成，未永久更改驱动或画质。恢复段也有改善，尚不能唯一归因于某项效果或平台驱动。
- 后续同状态重复对照固定每段预热 15 秒、生产 45 秒：默认 Metal 无额外 GPU 计时的帧 p95 为 `20.595 / 20.853ms`，超预算可重复。Vulkan 分项采样带来明显干扰，不据其分项值永久关闭效果或换驱动；六段生产、计时和保存正常。随后用户点击准备页取得有效原生 Metal 轨迹：drawable 等待 p95 `19.225ms`、合并每帧 GPU 活动 p95 `15.073ms`，二者重叠；完整 90 秒帧 p95 `21.227ms`。等待链路已可观察，不能据此断言唯一根因。
- 同状态原不限帧 / 临时 60 FPS / 恢复对照完成：帧 p95 `20.131 / 20.772 / 18.104ms`，均超预算；限帧未改善，不写入正式配置。三段生产、保存和基准图一致，原设置恢复；已有轨迹离线核对已收口：等待无 drawable 标识，未建立回收唤醒因果；时钟修正与已核对显示依赖见 [Metal 呈现诊断记录](../reference/factory-metal-presentation-review-20260912.md)。
- 四档前台长测未完成；本轮未重跑完整客户端 Godot 套件，Windows / PowerShell 运行未验证。不能把关闭效果的诊断段、后台时长或中断样本写成正式通过。

## 下一步与退出条件

按 [Daily Start](daily-start.md) 只读审计实际 Godot 构建的 Metal 获取 / 提交 / 释放顺序，给出具体修正证据或必要取证位置；不继续盲试参数或重复导出无标识事件；原配置短测稳定后，再安排 1080 / 最大化、局部 / 拉远各 450 秒前台持续生产。已通过的原生关闭 / 独立重进不重复列为未实现。

首包退出仍以 P1–P3 合同为准：正式玩家路径、跨进程完整恢复、故障保护、多线扩建及本机持续性能证据齐备后交付亲测。Windows 目标硬件未定，保持单列待验；当前不切换专题或宣布首包完成。
