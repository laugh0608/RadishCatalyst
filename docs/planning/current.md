# Current Plan

更新时间：2026-05-12

## 入口约束

本文是新会话的阶段入口，只保留当前阶段、最近进度摘要、短期重点、验证入口和退出条件。历史过程、长完成清单和详细复盘优先查看：

`AGENTS.md` 和 `CLAUDE.md` 只保留长期协作约束与稳定入口引用；当前阶段、当前不做、当前验证重点和退出条件统一以本文为准。

- `docs/planning/daily-start.md`
- `docs/devlogs/README.md` 中列出的最新一期周志
- `docs/planning/vertical-slice.md`
- `docs/design/onboarding-and-first-hour.md`
- `docs/planning/milestone-review-checklist.md`

## 当前阶段

RadishCatalyst 当前处于「首小时引导可玩」阶段。

第一切片冷启动审计已于 2026-05-05 通过，上一阶段「Godot 第一可玩切片原型骨架实现」退出条件已满足。当前重点不是按分钟数堆内容，而是验证玩家是否能理解并完成：

```text
外出获得资源 -> 回基地加工 -> 远征产物改变下一次外勤结果
```

2026-05-05 空存档试玩显示完整流程扩展后仍不足 10 分钟。该时长仅作为内容密度、理解断点和工程摩擦的观察信号，不再作为阶段硬目标。

## 最近进度

- `client/` 已形成可反复复测的 Godot 4.x 原型：第一切片主流程、任务链、静态数据、地图实例、存档运行时、HUD 关键面板和验证脚本都已接入。
- 深段主线已扩到 `S14`：从外圈推进、更深遗迹入口、前线回传锚点、相位井各断面，到锚场回稳完成态和余响片解析入口，当前都已有可运行链路和代码生成的开发复测基线。
- `S13` GUI 人工复测已于 2026-05-12 通过：锚核后配方自动跳转、锚场回稳目标顺序、部署后失败直接重试和局部稳定窗口提示均正常。
- 锚场回稳包已按阶段校准结果改成“前线短守场 + 局部稳定窗口”结构：已部署的 `井系校锚桩` 会保留在现场，失败后允许直接重试，回稳完成后会在现场回充生命与防护。
- 前线回传仍保持“多已部署落点 + 单当前回投目标”口径：基地相位回投台默认返回当前选中的锚点，并可按 `R` 在已部署落点间切换，不扩成全图自由传送网。
- HUD、任务链、地图目标、交互 prompt、旧存档补激活、保存校验和 GM 资源调试口径已足够支撑后续内容扩展；这些护栏改为被动维护项，不再默认占据主线。

## 当前最高优先级

1. `S13` GUI 人工复测已通过；当前默认下一步是继续强化锚场回稳完成态和余响片解析后的前线收益，确认局部稳定窗口能带来真实容错，而不只是把同构链换成另一种收尾文案。
2. 保护「基地服务冒险，冒险反哺基地」的核心闭环；每一包新内容都应让基地加工、补给、模块或通行能力真实改变下一次外勤结果。
3. 任务链数据维护、验证脚本职责、正式 HUD / 调试 HUD 边界和配方选择体验改为被动维护项；只在新内容开发暴露真实同步成本、回归风险或严重摩擦时补强。
4. 人工复测默认先用开发基线定位段落：当前深段开发优先使用 `S14` 或 `S13`，需要回看前一跳时再退回 `S12`、`S11`、`S10`、`S9`、`S8`、`S6` / `S7` 或 `S5`；若只差关键资源或生命 / 防护，直接使用调试 HUD 的 GM 区，而不是回头重打整条链。
5. 保持入口文档简约；首小时文档只保留首小时教学，`S6` 之后的深段连续主线改由 `docs/planning/vertical-slice.md`、开发复测基线和周志维护，不再把入口文档写成长链脚本。

## 当前不做

- 不把联机、官方账号、交易、公会、排行榜、云存档或复杂服务端作为首版前置条件。
- 不把完整 Web 客户端、Android 或官方工具作为 MVP 必须交付项。
- 不把 Godot 客户端核心逻辑押到 C# / .NET。
- 不大规模生产美术资产，不提前批量编写玩家 Wiki 百科条目。
- 不在首小时同时解锁完整管线、多设备自动化、多天气、多角色 Build、大型任务日志、复杂技能树或联机入口。
- 不主动扩展存档边界校验、电网、管线、自动物流和多设备调度，除非真实改动需要。
- 不把参考仓库或历史旧仓库代码整包迁入本仓库。

## 代码实践约束

正式规范见 `docs/architecture/code-style-and-language-practices.md`。当前阶段尤其需要避免为了拆分 HUD 或验证脚本而制造晦涩封装；新抽象必须能降低复杂度、减少真实重复或隔离清晰边界。

## 当前默认验证

客户端相关改动优先执行聚合验证：

```powershell
pwsh ./scripts/check-client.ps1
```

文档、规划、协作规则或仓库入口改动额外执行：

```powershell
pwsh ./scripts/check-docs.ps1
```

该脚本按顺序执行：

```powershell
pwsh ./scripts/check-client-data.ps1
pwsh ./scripts/check-client-scenes.ps1
pwsh ./scripts/check-client-save.ps1
pwsh ./scripts/check-client-quests.ps1
pwsh ./scripts/check-client-flow.ps1
pwsh ./scripts/check-godot-client.ps1
```

其中 `check-client-data.ps1` 已覆盖静态数据引用、本地化键、配方解锁来源一致性、任务激活重复声明、区域任务索引、主线任务图一致性和任务目标来源，避免 `unlock_conditions`、`next_quest_ids`、任务 `unlock_effects`、`quest_refs`、主线前后置和目标可获得性脱节；`check-client-scenes.ps1` 已覆盖第一切片任务目标与地图交互实例、建造点、加工设备和敌人实例的对应关系，并会按场景真实区域落点校验任务目标与 `quest_refs` 的区域归属同步。

提交前仍需执行仓库文本卫生和 diff 检查：

```powershell
pwsh ./scripts/check-text-files.ps1
git diff --check
```

脚本通过不等于首小时体验通过；文案理解、节奏感受、配方查找成本和重复跑图仍需空存档人工试玩判断。

## 下一步建议

- `S13` 已于 2026-05-12 人工复测通过；当前默认下一步是继续让锚场回稳完成态和余响片解析读数带来前线容错收益，并观察该收益是否足以支撑“基地先产出稳场工具，再改变外勤状态”的体验。
- 基地控制的前线回传锚点 / 相位回投台继续采用“多前线锚点实例 + 多已部署落点 / 单当前回投目标”口径：当前深段可同时保留多个已校准锚点，基地回投默认返回当前选中的落点，并可在回投台按 `R` 切换，不扩成全图自由传送网。
- “相位井锚核之后”的非同构闭环已收口到 `docs/planning/vertical-slice.md` 并接入主线：`相位井锚核` 现会在基地转成 `相位井归谱片` 和 `锚核落尘`，再经污染过滤器与基础反应器做成 `井系校锚桩`，把下一次外勤改成一次短守场，并在井系桥东侧制造局部稳定窗口。
- 这包已经明确改变外勤目标结构和前线节奏；回稳完成后的局部稳定窗口应继续承载可感知收益，后续继续扩展时默认禁止退回“一个门禁 + 一个怪物 + 两处收集 + 过滤器 + 反应器 + 下一门禁”模板。
- `docs/design/onboarding-and-first-hour.md` 现应收回真正首小时范围；`S6` 之后的深段连续主线改由 `docs/planning/vertical-slice.md`、`docs/design/development-retest-baselines.md` 和最新周志维护。
- 继续执行文档篇幅治理：入口文档维持 120 行硬上限，超长的活跃专题文档优先拆成“总览 + 子文档”，不要把新一轮历史过程重新堆回入口。
- 恢复后的前哨核心现提供正式基地整备，修复凝胶的价值继续保留在前线续战；后续若继续提高战斗消耗，优先让“回基地整备再出发”的判断成立，而不是依赖有限旧图采集物兜底。
- 若后续再出现晚期战斗压得生命 / 防护比例失衡，优先继续回修敌人数值和阶段成长，不靠额外多送修复凝胶掩盖问题。
- 调试面板现已附带 GM 资源 / 状态调整区，可在开发复测时直接补关键物资并回满生命 / 防护，但该入口不面向正式玩家。
- 继续约束复杂度：新深段包已经证明污染过滤器和基础反应器还能继续承载推进；短期仍不为深段包额外引入电网、管线、第三台设备或复杂系统。
- HUD 可读性、验证脚本和任务链护栏继续改为被动维护项；只在新内容开发出现真实阻断时再处理，不单独占据主线。
- 每完成一个小阶段，按 `docs/planning/milestone-review-checklist.md` 做兼容、存档、数据和范围复核。

## 阶段退出条件

- 玩家能在无开发者口头解释下完成至少一次「外出获得资源 -> 回基地加工 -> 远征产物改变下一次外勤结果」闭环。
- 玩家能理解基地后勤、设备缺料、地图目标、采集、基础战斗、制造远征产物、污染与防护、处理点建设、危险区域挑战和后续入口。
- 失败或被挡回时，UI、日志、地图标记或设备状态能给出具体补救方向。
- HUD、日志、方向提示、设备面板、小地图、任务完成反馈、撤离反馈和常驻状态摘要之间不遮挡、不信息过载。
- `check-client.ps1`、`check-text-files.ps1`、`git diff --check` 持续通过，阶段边界和协作规则同步更新到 `docs/`、`AGENTS.md` 和 `CLAUDE.md`。
