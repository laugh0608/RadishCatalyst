# Current Plan

更新时间：2026-08-25

## 入口约束

本文只保留当前阶段、活跃专题、边界、验证入口和退出条件。

- 当前活跃专题：[试玩反馈整改 V1](../features/slice-playtest-remediation-v1.md)。2026-08-16 连续试玩确认既有“功能全链通过”不等于玩家体验通过，阶段验收与陌生玩家盲测重新冻结。
- 功能基线仍是[第一可玩切片全链串通](../features/slice-first-playable-journey-v1.md)的 `delivered / 120`、现行 schema 10、单地图基地 / 外勤、二值供电和实体物流；本轮先整改，不扩新内容层。
- 美术口径：[Pixel Art And Grid Standard](../reference/pixel-art-and-grid-standard.md)（32px 网格、高斜角像素世界）。旧 Demo V1 路线继续留在归档，不复活。

## 阶段状态

已完成的工程基线：

- 2026-07-12 至 08-15 已依次接通核心修复、建造、供电、物流、反应器、多世界存档、首次战斗、远程武器、五阶段首程引导和探索小地图。
- 自动专项、正式 `Boot` 与当时人工复核仍作为回归证据保留，但不再作为当前 Demo 体验已达标的结论。
- schema 10 是现行写盘合同；schema 2—9 继续通过确定性迁移与来源感知备份兼容。

当前阶段：

```text
第一可玩切片——首轮试玩反馈整改设计与实施
```

问题归因、参考 UI 审阅、源码体验审计、整改排期与包 0—2 已完成；当前进入包 3 开工五问，不提前改造库存和设备容器交互。

包 0 已冻结空间和保存事件合同，提取 `SliceSaveScheduler` 与无状态快照构造职责。高频变化使用约 `2s` 合并保存，关键财产、里程碑与退出路径立即写盘；`SliceWorld` 为 1487 行。包 0 专项 41 项及完整 Godot 回归通过，schema 9 与 schema 2—8 迁移保持不变。

包 1 已完成 Demo 完成卡、传送带地表层与通行、反应器三格轮廓、设备足印碰撞和采集器显式过渡格。专项 `26` 项及完整 Godot 回归通过；正式 `Boot` 两种窗口比例各通过 `28` 项，隔离新档验证两条真实采集物流、角色碰撞、连续开火合并保存与交付收尾。`SliceWorld` 为 `1491` 行，仍低于硬上限但只余极小空间，后续包不得继续向世界根堆业务。

包 2 已完成制造 / 放置分离、无状态完整依赖诊断、采集器 `50`、地板 `1→8`、中继接力 / 供能 `8/6`、最小角色装备页和 schema 10。专项 `42` 项及完整 Godot 回归通过；正式 `Boot` 两种窗口比例各通过 `80` 项，B 轮以两座中继和一批地板获得首个通电采集器并余 `3` 晶体，完整首线的 `21` 格自动补板只需三批，步枪与切割器选择分别通过完全重启恢复。`SliceWorld` 为 `1492` 行，没有承接新增制造计算或角色 UI 业务。

## 当前问题结论

1. 主流程的显式 Demo 完成态已由包 1 治理；完成事实仍来自 `delivered`，卡片只在本次交付时出现，HUD 持续说明可自由建设。
2. 传送带层级 / 通行、设备足印碰撞、反应器轮廓和采集器端口已由包 1 按统一空间合同治理，并通过两种窗口比例正式入口验证。
3. 制造自动进入放置、资源依赖不可预见、地板与中继过度消耗、步枪缺少装备页已由包 2 一组治理，并通过新档完整首线与装备双重启验证。
4. 背包 / 仓库默认展示空物品，设备面板以“取出 / 放置”按钮为主；需要改为“仅持有 + 全部平铺 / 分类筛选 + 成对容器拖拽”。
5. 核心和设备反馈静态，HUD 深底细字、硬编码偏移和常驻输电线降低可读性；参考《终末地》的信息架构，不照搬体量与移动端外观。
6. 每发步枪同步写盘与世界根职责风险已由包 0 治理；包 1 正式入口确认连续射击只进入共享防抖，约 `2s` 后发布合并结果。
7. 窗口化 16:10 基线确认生产配置仍把内容保持为 16:9 并出现上下黑边；该事实进入 A07，留到包 4 与 HUD 安全区一组处理。

## 下一推进包

包 2 已完成。下一动作是为包 3 回答并确认五问；未确认前不进入容器实现。

1. 包 3：统一背包、核心仓库和设备容器的持有物视图、分类筛选与拖拽交互；先冻结权威容器模型、主路径和键鼠等价动作。
2. 包 4：修正 HUD 安全区、非 16:9 窗口与可读性，补核心 / 设备状态动画、电子音和上下文电力叠层。

包 4 通过后再评估传送带拖铺、无线回传语义和基地级工业总览，不将下一阶段效率系统偷偷并入当前整改。

## 边界与冻结

- 不增加第二只敌人、后续剧情、新地图、新建筑、新配方、Boss、技能树、联机或完整有限电力。
- Demo 完成继续从既有 `field_encounter.state == delivered` 派生；schema 10 只新增 `equipped_weapon_id`，其他 UI 状态保持会话态。
- 采集器容量、存档校验和面板必须共用同一常量；保存防抖必须在制造、放置、拆除、交付、暂停与退出等关键点强制落盘。
- 传送带和地板为可通行地表层；设备按“基座足印阻挡、上部轮廓参与排序”处理，不以整张透明画布充当碰撞。
- UI 只移植参考图的状态分离、容器并排、直接操纵、强层级和可诊断结果，不复制角色编队、复杂成长、移动端按钮或完整工业简报。
- `SliceWorld` 不得继续增长；新行为先确定权威所有者，不通过更多按类型分支兜底。
- 陌生玩家盲测、试玩分发、打包和发布继续冻结，直到本专题 S0 全链退出条件通过。

## 当前默认验证

- 文档：`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check`。
- 客户端默认：`sh ./scripts/check-client.sh`；玩家可见包按 [Godot Runtime Verification Guide](../reference/godot-runtime-verification-guide.md) 补正式 `Boot`、有窗口截图和人工路径。
- 本专题涉及早期链、HUD、任务、存档与跨系统状态，最终必须使用真实新档执行 S0 20—40 分钟全链；16:9 全屏和窗口化非 16:9 均需截图。

## 阶段退出条件

- 无口头提示完成核心修复、首条产线、首次充能、步枪装备、战斗和样本交付，并理解 Demo 已结束且可自由建设。
- 采集器、传送带、反应器、储物箱和核心的端口可自然接通；角色可走上传送带但不能穿过设备基座。
- 制造后仍留在面板，资源与补板缺口提前可见；地板和中继不再迫使玩家反复返工。
- 背包、核心仓库和设备默认只显示持有物，分类筛选和鼠标拖拽路径统一；装备选择可读档恢复。
- 小地图贴合安全区，HUD 正文清楚；核心与常用设备仅看世界画面即可区分主要状态。
- 连续射击不再逐发同步写盘，关键财产与退出路径仍能确认保存成功；schema 2—9 到 schema 10 的迁移无回归。
- `SliceWorld` 已完成职责提取且没有继续增长；专项检查覆盖层级、碰撞、接驳、容量、UI 视图和存档节流。
