# Current Plan

更新时间：2026-05-18

## 入口约束

本文是新会话的阶段入口，只保留当前阶段、最近进度摘要、短期重点、验证入口和退出条件。历史过程、长完成清单和详细复盘优先查看：

`AGENTS.md` 和 `CLAUDE.md` 只保留长期协作约束与稳定入口引用；当前阶段、当前不做、当前验证重点和退出条件统一以本文为准。

- `docs/planning/daily-start.md`
- `docs/planning/base-action-choice-prototype.md`
- `docs/planning/resource-and-frontline-loop-rework.md`
- `docs/devlogs/README.md` 中列出的最新一期周志
- `docs/planning/vertical-slice.md`
- `docs/design/onboarding-and-first-hour.md`
- `docs/planning/milestone-review-checklist.md`

## 当前阶段

RadishCatalyst 已于 2026-05-13 完成 `S0` 新档冷启动人工复测，并于 2026-05-16 宣布「资源循环与前线节奏重构」阶段通过。

「基地-前线行动核心循环原型」已通过 `S17` / `S18` / `S19` 证明最小模板可以重复成立。下一阶段定为「基地行动选择原型」：不再横向新增同构短行动，而是把“基地确认”推进成出发前的真实取舍。

```text
当前主线终点 -> 基地选择下一趟行动方案 -> 前线短目标体现选择差异 -> 前线结果反哺基地
```

后续阶段只让 `P0` / `P1` 阻塞推进：崩溃、主线卡死、坏档、任务无法完成或明显资源断档必须立刻修；目标机械、局部拥挤、文案不够顺等 `P2` / `P3` 进入 backlog 或专门打磨周。

## 最近进度

- `client/` 已形成可反复复测的 Godot 4.x 原型：第一切片主流程、任务链、静态数据、地图实例、存档运行时、HUD 关键面板和验证脚本都已接入。
- 深段主线已扩到 `S16`：从外圈推进、更深遗迹入口、前线回传锚点、相位井各断面，到锚场回稳、余响片解析、稳窗读数和稳窗相位序校准，当前都已有可运行链路和代码生成的开发复测基线。
- `S13` / `S14` / `S15` GUI 人工复测已于 2026-05-12 通过：锚核后配方跳转、锚场回稳目标顺序、部署后失败直接重试、余响片解析配方推荐、读数制造完成进度、稳窗读数完成态和局部稳定窗口提示均正常。
- `S0` 新档冷启动于 2026-05-13 暴露的主线阻塞已回修：晶体簇余量、污染浆液断档、隐藏对象误交互、回投深段区域恢复、裂相碎屑补记、读数矩阵完成后锚点刷新等问题均已纳入验证。
- 处理晶体矿物产出已从 `基础零件 x2` 调整为 `x4`，作为早期教学和应急缓冲；关键前线阶段现在会通过前哨核心回收口径发放 `基础零件` 批次，缺料时加工日志和设备面板会指向前哨核心回收 / 阶段补给批次、晶体矿脉区北侧富晶残脉，以及相位中继锚点后开放的 `回收基础零件` 轻量产线。
- `check-client-data.ps1` 已新增主线资源预算检查，覆盖中间加工、建筑消耗、任务件部署和 3 个基础零件误用缓冲，随 `check-client.ps1` 聚合执行。
- 前线回传仍保持“多已部署落点 + 单当前回投目标”口径：基地相位回投台默认返回当前选中的锚点，并可按 `R` 在已部署落点间切换，不扩成全图自由传送网。
- 前线回传锚点后的节奏重排已覆盖 9 处单件制造节点：裂相碎屑远征整备、故障残渣井锁整备、井涌碎屑探针整备，以及井底穿钉、井心分流栓、井纺梭栓、井纹架键栓、井系定桩、井系校锚桩的连续整备包。
- 后段 `analysis` 类配方已移除固定 `基础零件 x2` 输入，带样本 / 坐标 / 读数回基地解析不再因为缺基础零件而强制转成补件往返；装配类配方继续保留基础零件消耗。
- 从 `裂相脊` 到 `井底裂口` 的重复现场模板已第一轮改造：裂相脊改为两点共振读数后回收，故障残渣改为脉冲读数后回收，井涌改为泄压后回收，井底改为清余烬壳后回收；地图节点也已打散，不再按“一怪、两物、一个门禁”复制排布。
- `S9` 到 `S13` 已继续拆同构：井心室新增两处分流读数后开放心棘回收，井纺室新增两处张力绕轮后开放纬束回收，井系桥新增两端结点后开放系索回收，锚场部署后需清两处压力钉才刷出守脉体；井纹架侧路清障继续保留。
- `S5` 到 `S13` 第一轮提示复核已回修：读数点 prompt 与写入反馈会说明进度和解锁关系，井底余烬壳也已改成专用清障文案，避免玩家把新增目标理解成又一层无意义打卡。
- 第一版非同构现场目标已落地：稳窗读数完成后，玩家返回井系桥东侧按西侧、中央、东侧顺序校准三处稳窗节点，`S15` 变为现场校准起点，`S16` 代表校准完成态。
- 第一版路线清障目标已落地：`回收边缕残条` 改为进入井纹架边缘后先任选一条侧路清障，再击退井纹刮裂体并回收两处边缕残条；HUD、交互提示、地图对象显隐、旧进度补记和自动检查已同步。
- `S5` 到 `S16` 已完成一轮人工复测：逻辑上暂无明显问题，末段井系桥 / 锚场 / 稳窗节点拥挤的地图可读性问题已回修。
- 基础零件稳定供给、后段整备包、现场目标差异化和资源预算检查已足够支撑阶段通过；后续不再因 `P2` / `P3` 打磨项拖住新循环开发。
- `S17` 第一版轻量前线行动已接入：`S16` 稳窗校准完成后回基地确认前线行动台，回访井系桥东侧稳窗回波探点，再回基地解析成前线行动回报；回报目标完成补记和反应器自动推荐回报配方已纳入回归检查。
- 2026-05-17 `S16` / `S17` 短复核通过后，前线行动回报已补上第一版基地反馈收益：回报归档会整理出下一趟短行动补给，并同步 HUD 完成态、`S17` 基线和回归检查。
- 2026-05-17 根据复测反馈，当前地图尽头已补第三个前线回传锚点；`S14` 到 `S17` 默认回投目标切到井系桥前线，减少锚场 / 稳窗 / 回波探点往返跑图成本。
- 第二、第三条轻量前线行动已从 `S17` / `S18` 反馈收益连续接出，`S19` 后已改接基地行动二选一：稳场补给走一处低风险投放点，相位测绘走两处前线读数点；开发复测基线已扩到 `S20`。
- `S17` 到 `S19` 已足够验证基地-前线-基地最短循环；`S20` 已完成第一轮行动前取舍验证，自动检查已覆盖补给 / 测绘互斥、目标数量、返回收益和分支收束。

## 当前最高优先级

1. 收口「基地行动选择原型」首轮：`S20` 已达到阶段退出条件，下一步记录阶段通过和后续边界。
2. 不补压力清障、不扩完整队列、不继续新增同构短行动；只处理 `P0` / `P1` 或必要自动检查。
3. 下一阶段改为“选择结果反哺下一轮准备”：让测绘 / 补给反馈影响后续基地整备或路线选择，而不是继续验证“能否二选一”。
4. 保持资源预算、开发基线、HUD / 任务链和存档检查持续通过。

## 当前不做

- 不把联机、官方账号、交易、公会、排行榜、云存档或复杂服务端作为首版前置条件。
- 不把完整 Web 客户端、Android 或官方工具作为 MVP 必须交付项。
- 不把 Godot 客户端核心逻辑押到 C# / .NET。
- 不大规模生产美术资产，不提前批量编写玩家 Wiki 百科条目。
- 不继续沿相位井后段横向堆内容包或为了 `P2` / `P3` 体验项反复打磨。
- 不新增第 4 条同构轻量前线行动，不急着把多个前线行动 console 收束成完整队列系统。
- 不新增电网、管线、自动物流、多设备调度或完整教程弹窗框架，除非它们直接服务下一组基地-前线行动核心循环原型。
- 不把参考仓库或历史旧仓库代码整包迁入本仓库。

## 代码实践约束

正式规范见 `docs/architecture/code-style-and-language-practices.md`。当前阶段尤其需要避免为了拆分 HUD 或验证脚本而制造晦涩封装；新抽象必须能降低复杂度、减少真实重复或隔离清晰边界。

## 当前默认验证

客户端相关改动优先执行聚合验证：

```powershell
pwsh ./scripts/check-client.ps1
```

文档、规划、协作规则或仓库入口改动额外执行：

```powershell
pwsh ./scripts/check-docs.ps1
```

提交前仍需执行仓库文本卫生和 diff 检查：

```powershell
pwsh ./scripts/check-text-files.ps1
git diff --check
```

脚本通过不等于体验完全通过；但个人开发阶段只要求 `P0` / `P1` 得到处理，`P2` / `P3` 体验项不阻塞新循环推进。人工短复测改为阶段闸门或风险触发，不再要求每条轻量行动都手动短跑。

## 下一步建议

- 下一步宣布「基地行动选择原型」首轮通过，并同步 `daily-start`、周志和必要设计入口。
- 下一阶段只定义“选择结果如何改变后续准备”的最小闭环，不做完整远征系统、行动队列、长期派遣或复杂数值构筑。
- 把压力清障、前线行动台完整队列、更多短行动分支和短行动 polish 记录为 backlog。
- 只有遇到 `P0` / `P1`，或准备宣布新阶段通过时，才安排完整 `S0` 新档复测。

## 阶段退出条件

- `S16` 当前终点能自然引出一个基地新能力或下一趟前线行动选择。
- 玩家能完成至少一次新的基地选择 -> 前线目标 -> 返回反馈闭环；当前 `S20` 已达到首轮可玩验证目的。
- 玩家能在基地做出一个可感知取舍，并在下一趟前线行动中看到该取舍的结果；当前补给 / 测绘已分别改变目标数量、距离和收益口径。
- 新循环只要求最短可玩路径成立，不要求完整数值打磨、美术表现或所有分支。
- `P0` / `P1` 问题已处理；`P2` / `P3` 进入 backlog。
- `check-client.ps1`、`check-docs.ps1`、`check-text-files.ps1`、`git diff --check` 持续通过，阶段边界同步更新到 `docs/`。
