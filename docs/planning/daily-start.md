# Daily Start

更新时间：2026-09-09

计划日期：2026-09-10

## 当前任务

先读 [Current Plan](current.md)、[Factory Foundation And Persistence V1](../features/factory-foundation-and-persistence-v1.md) 和 [Factory World State And Save V1](../architecture/factory-world-state-and-save-v1.md)。9 月 9 日已收工，以下事项留待次日接续；今晚不再开窗或启动长测。首包尚未整体验收。

## 当前交接

- Godot Demo、最大化清晰度修正、首包设计分别为 `cfa2fee4`、`5e1701b9`、`d75075ce`；正式工厂基础为 `978d6250`。均为本地提交，未推送。
- 正式 Boot 可新建 / 继续工厂；64×64、25 矿点、多实例与独立 schema 1 已实现，旧二维入口 / schema 10 保留。
- 9 月 9 日复现引擎 delta 在长帧中漏时，已改用单调活动时钟；Boot 时钟与失败退出 28 项通过。二次确认层级冲突已修正，自动保存使用实际经过时间。
- 双线 5 张原图已审阅；100 台 / 1,000 带满载跨进程写 / 读各 107 项、规模汇流 210 项通过。准备 / 采样失焦注入均中断后续阶段。Windows 未测；详见 [W37 周志](../devlogs/2026-W37.md)。
- 计时修正已提交 `cff8b0ef`，窗口 / 后台记录已提交 `2eef577a`。窗口补验取得长帧、三次规模拆改、四侧边缘移动 / HUD 原图；历史失败批次保留。最新原生保存复验 53 项通过，系统鼠标完成重试保存、最终放弃及返回 / 同进程重进，原图已审阅。
- `0f787b5a` 已提交中断采样补齐、原生保存复验与前台诊断记录。中断检查共 8 项通过；性能超预算和对照中断均保留为未完成，不修改产品 VSync、画质或负载。
- `background-soak-long-20260909-a` 完成隔离 headless Boot 真实 `1800.001092s`，写入 133 项 / 独立进程恢复 5 项通过，交付 4,375 件、59 次自动保存全部成功。产品失焦暂停不变，测试注入活动焦点；该结果覆盖生产、计时与存档稳定性，前台性能与原生关闭保存仍待验。

## 9 月 10 日事项（按顺序）

1. **补齐性能归因**：90 秒短测已复现帧 p95 `33.719ms`，模拟步 p95 `2.508ms`、交付 100 件。保留相同负载、实际像素、画质与生产阶段，完成同步开启 / 关闭 / 恢复对照；在进程存活且采样稳定时取得调用栈，标记采样工具的干扰时段。GPU 零读数仍记不可用，不凭局部样本永久改同步设置或降低画质。
2. **补原生关闭保存路径**：使用双根隔离 Boot，实际点击系统关闭按钮保存，再由独立进程进入，核对完整状态及写入锁释放。已有一次性检查脚本仅通过语法检查，须复核后运行；原生保存返回 / 同进程重进 53 项不重复充当这条证据。
3. **满足前置条件后跑四档长测**：原因明确、必要修正完成且短测稳定后，再安排 1080 / 最大化、局部 / 拉远各 450 秒真实前台生产。记录完整帧、模拟步、保存、时钟守恒与积压；先告知并确认桌面可用，失焦即停，不自动重开，不把后台或拼接时长当连续前台通过。
4. **核对退出缺口后交付亲测**：已有规模拆改、四侧边缘 / 相机 / HUD、失败返回与重进证据按合同核对；按实际修改选择回归，不重复所有旧短测。P1–P3 本机证据齐备后提供隔离世界和路径，完整客户端 Godot 套件及 Windows 未测项如实单列。

## 暂缓事项

- 萝卜SAMA已认可当前画面与操作，精细度暂缓；不重复索要同范围认可，不追加模型润色。
- 不增加设备种类、经济解锁、液气、多人、随机地图或战斗，不转换 / 删除 schema 10 旧档。
- 不推进 1,000 台 / 10,000 带扩展档，不安装依赖、打包、发布或推送。

## 入口与验证

- 正式路径：`client/scenes/boot/Boot.tscn` → “工厂世界” → 新建 / 继续。运行验证必须注入工厂与旧切片两个隔离存档根，操作方法见[运行时指南](../reference/godot-runtime-verification-guide.md)。
- 无窗口定向：`state` 检查规则 / 存档，`clock` 检查时钟及失败退出，`interruption` 检查阶段中断，`process` 检查跨进程 / 锁，`legacy` 检查旧入口 / 存档，`scale` 生成满载状态，`scale-process` / `scale-merge` 补规模存读 / 拆改 / 汇流。使用 `sh tools/check-factory-foundation.sh <mode>`，Godot 启动前仍先告知。
- 有窗口 `boot`、`operation-write/read`、`performance`、`sustained` 的运行条件和证据见首包专题；不在收工或用户占用桌面时自动执行。
- 治理与静态：`./scripts/check-repo.sh`、`sh ./scripts/check-client.sh`、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check`。
- 独立 Godot Demo 与 Web `/play/` 只保留作参照，前者关闭重置；正式工厂已有持久化。需要追溯时再读旧专题。
