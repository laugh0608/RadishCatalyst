# Daily Start

更新时间：2026-05-16

## 用途

当提示是“根据项目规划和开发进度，今天要来做什么以推进开发”时，优先阅读本文。本文只回答今天如何推进，不承载完整历史。

## 当前阶段一句话

`S0` 新档冷启动人工复测已于 2026-05-13 收口；「资源循环与前线节奏重构」已于 2026-05-16 按阶段通过处理。下一阶段进入「基地-远征核心循环原型」。

## 最近进度

- 相位井织核、井纹架、井系桥、锚场回稳、余响片解析、稳窗读数和稳窗相位序校准链路已接入主线，开发复测基线已扩到 `S16`。
- `S13` / `S14` / `S15` 已于 2026-05-12 人工复测通过：配方跳转、锚场目标顺序、部署后失败重试、读数制造进度和完成态回访均正常。
- `S0` 空档测试于 2026-05-13 暴露的主线阻塞已回修：晶体簇、污染浆液、隐藏对象误交互、回投深段区域恢复、裂相碎屑补记和前线回传锚点刷新都已纳入检查。
- 基础零件稳定供给的玩家可见闭环已补上第一版：关键前线阶段会发放前哨核心回收批次，资源预算检查已接入 `check-client.ps1`，缺基础零件时加工日志和设备面板会指向前哨核心回收 / 阶段补给批次、晶体矿脉区北侧富晶残脉，以及相位中继锚点后开放的 `回收基础零件` 轻量产线。
- 前线回传锚点后的节奏重排已覆盖 9 处单件制造节点：裂相碎屑远征整备、故障残渣井锁整备、井涌碎屑探针整备，以及井底穿钉、井心分流栓、井纺梭栓、井纹架键栓、井系定桩、井系校锚桩的连续整备包。
- 后段解析类配方已移除固定 `基础零件 x2` 消耗，回基地解析不再因为缺基础零件而转成补件往返。
- 2026-05-14 日终已回看当天提交：缺料提示、远征 / 井锁 / 探针整备、富晶残脉、污染浆液回收产线、解析减税和后段装配整备包均已同步到相关设计、规划和周志文档。
- `裂相脊` 到 `井底裂口` 已第一轮改掉复制模板：裂相脊两点共振读数、故障残渣脉冲读数、井涌泄压、井底余烬壳清理都已接入任务和地图排布。
- `S9` 到 `S13` 已继续拆同构：井心室要先写入两处分流读数，井纺室要先检查两处张力绕轮，井系桥要先检查两端结点，锚场部署后要先清两处压力钉再出守脉体；井纹架侧路清障保留。
- `S5` 到 `S13` 提示复核已回修第一轮：共振 / 脉冲 / 泄压 / 分流 / 张力 / 结点读数点现在会显示当前进度和“读完打开什么”，写入反馈也会说明部分完成或回收线打开；`S8` 余烬壳清理已改为井底专用清障提示，不再误报成通用建造地块。
- 第一版非同构现场目标已落地为 `校准稳窗相位序`：稳窗读数完成后，玩家返回井系桥东侧按西侧、中央、东侧顺序校准三处节点，阶段收口顺延到 `S16`。
- 第一版路线清障目标已接入 `回收边缕残条`：玩家进入井纹架边缘后需任选一条侧路清障，再回收两处边缕残条；相关任务、HUD、交互提示、地图对象、旧进度补记和检查脚本已同步。
- `S5` 到 `S16` 人工复测已走完：现场目标逻辑暂无明显问题，末段井系桥 / 锚场 / 稳窗节点拥挤问题已通过地图右侧展开回修。
- 当前项目节奏调整为粗粒度里程碑推进：只让 `P0` / `P1` 阻塞阶段，`P2` / `P3` 体验打磨进入 backlog 或后续 polish。

## 今日事项

1. 先定义 `S16` 之后基地获得的一个新能力，以及它如何打开下一趟远征选择。
2. 给下一阶段只保留一条最短可玩路径：基地选择 -> 前线目标 -> 返回反馈。
3. 新增实现时同步维护任务链、HUD、开发复测基线和 `check-client`；不要为了 `P2` / `P3` 体验项阻塞主线推进。
4. 若发现崩溃、坏档、主线卡死、任务无法完成或明显资源断档，按 `P0` / `P1` 立刻回修。

## 今日不做

- 不扩展联机、账号、云存档、交易、公会、排行榜或复杂服务端。
- 不新增电网、管线、自动物流、多设备调度或完整教程弹窗框架。
- 不为了内容时长堆等待时间、长文本说明或纯 UI 抛光。
- 不用 `P2` / `P3` 细节打磨阻塞新循环原型。
- 不再默认按“一个门禁 + 一个怪物 + 两处收集 + 两台旧设备”模板继续堆深段包。
- 不把验证脚本、HUD 护栏或任务链口径收敛继续当作默认主线。

## 必读与选读

日常推进必读：

- `docs/planning/current.md`
- `docs/planning/resource-and-frontline-loop-rework.md`
- `docs/devlogs/` 下最新一期周志中的“风险与未完成项”和“下周建议”

按任务选读：

- 首小时体验：`docs/design/onboarding-and-first-hour.md`
- 开发复测基线：`docs/design/development-retest-baselines.md`
- 代码结构和重构：`docs/architecture/code-style-and-language-practices.md`
- 存档、联机或边界：`docs/architecture/multiplayer-and-save-architecture.md`
- 阶段复核：`docs/planning/milestone-review-checklist.md`

## 验证入口

客户端相关改动优先执行：

```powershell
pwsh ./scripts/check-client.ps1
```

提交前执行：

```powershell
pwsh ./scripts/check-text-files.ps1
pwsh ./scripts/check-docs.ps1
git diff --check
```
