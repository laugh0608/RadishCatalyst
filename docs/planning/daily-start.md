# Daily Start

更新时间：2026-08-25

计划日期：2026-08-26

## 用途

当提示是“根据项目规划和开发进度，今天要来做什么以推进开发”时，先读本文，再读 [Current Plan](current.md) 与当前活跃专题。历史实现与验证留在 W31—W35 周志。

## 当前阶段

阶段：第一可玩切片首轮试玩反馈整改设计与实施。

- 2026-08-16 连续试玩暴露 19 项玩家问题；既有 `delivered / 120` 全链仍是功能基线，但阶段验收结论已撤回，陌生玩家盲测继续冻结。
- 当前活跃专题为[试玩反馈整改 V1](../features/slice-playtest-remediation-v1.md)，问题归因、参考 UI 审阅、源码玩家体验审计、五包排期、包 0—2 已完成。
- 包 2 已收口制造 / 放置分离、完整依赖诊断、采集器 `50`、地板 `1→8`、中继 `8/6`、最小角色装备页与 schema 10；当前进入包 3 开工五问，不提前改造库存和设备容器。
- schema 10、多世界存档、二值供电、实体物流、首次战斗与探索小地图继续作为回归基线，不重复造系统。

## 最近完成

1. 把原始反馈整理为 R01—R11、R13—R20，保留缺失的原编号 12，避免后续对照错位。
2. 代码确认传送带阻挡、层级、反应器足印 / 贴图、采集器端口地表冲突、制造后自动放置、容量 `10`、地板 `1 → 4`、中继 `6 / 4`、空物品默认展示和核心仅换色等根因。
3. 快速审阅参考图库中的任务、制造 / 放置、角色装备、背包仓库、设备容器、工业俯瞰和生产链路，提炼“状态分离、容器并排、直接操纵、强层级、结果可诊断”五项可移植原则。
4. 追加发现每发步枪同步完整存档、设置页占位、输电线常驻、传送带不能拖铺、储物箱无线回传语义、小地图持续渲染和 HUD 绝对偏移等高频游玩风险。
5. 确认 `SliceWorld` 为 1499 行；后续包不得再向世界编排器追加业务分支，包 0 必须先处理保存调度和职责边界。
6. 2026-08-25 冻结地表 / 设备 / 角色层级、足印 / 端口 / 过渡格和保存事件合同；提取 `SliceSaveScheduler` 与无状态快照构造职责。
7. 高频变更改为约 `2s` 合并保存，关键财产和退出路径强制落盘；包 0 专项 41 项与完整 Godot 回归通过，`SliceWorld` 为 1487 行。
8. 正式 `Boot` 已保留 16:9 与实际 16:10 窗口基线；后者确认游戏内容仍锁为 16:9 并出现上下黑边，留给包 4 的 A07 处理。
9. 2026-08-25 完成包 1：传送带进入固定地表层并可通行，反应器收回三格宽轮廓，采集器显式过渡格接通储物箱 / 核心，交付后显示 Demo 完成卡与持续自由建设 HUD。
10. 包 1 专项 `26` 项、完整 Godot 回归和两种窗口比例正式 `Boot` 均通过；角色真实穿带 / 设备阻挡、连续开火合并保存和截图已复核。
11. 2026-08-25 完成包 2：制造后停留、直接 / 基础折算与两种可制造数、补板上限、采集器 `50`、地板 `1→8`、中继 `8/6`、一个武器槽和 schema 10 装备恢复全部接通。
12. 包 2 专项 `42` 项与完整 Godot 回归通过；正式 `Boot` 在 16:9 与实际 16:10 各通过 `80` 项，B 轮从 25 晶体以两座中继得到首个通电采集器并余 3 晶体，21 格补板仅三批，完整产线与两次装备重启通过。

## 明日事项（2026-08-26）

明天只推进包 3 的设计闸门与获确认后的首个可分割实现，不直接把五类容器面板一起改完：

1. **玩家结果：**冻结默认“全部”只平铺实际持有物、分类只作视图筛选、左侧随身背包 / 右侧目标容器的共同读法，并定义空容器、满容量和目标不接受该物品时的就地反馈。
2. **权威状态：**确认 `Inventory`、`SliceInventoryProfile` 和现有转移 API 继续拥有数量、容量、白名单与原子转移；采集器整数缓冲、反应器输入 / 输出缓冲只适配同一物品格展示，不伪装成第二套库存或改写 schema 10。
3. **直接操纵：**以现有核心仓库“整堆拖拽、`Ctrl / Control` 逐次减半”为候选共同语义，冻结鼠标拖拽、点击和键盘的等价动作、取消路径、跨容量部分转移和失败不丢物合同。
4. **实现介质：**审计 `SliceInventoryReadModel`、`SliceCoreStoragePanel`、`SliceBuildingPanelSnapshot` 与 `SliceBuildingActionPanel` 的复用边界；优先提取无状态成对容器读模型和窄 UI 协作者，不向已达 `1492` 行的 `SliceWorld` 增加库存视图、筛选或拖拽业务。
5. **成功证据：**先形成包 3 开工提案并由萝卜SAMA确认；确认后首包至少覆盖背包 ↔ 核心、背包 ↔ 储物箱、采集器取出和反应器回收的守恒 / 容量 / 白名单检查，以及 16:9 与非 16:9 正式 `Boot` 的仅持有、筛选、拖拽、键鼠等价和无裁切证据。

包 3 不改变库存容量、物品所有权、设备模式、无线回传语义或 schema 10。包 4 的 HUD 安全区、窗口比例、核心 / 设备状态动画与音效继续后置。

## 防跑偏规则

- 不把“样本交付后没任务”解释为立即扩第二段剧情；先给明确 Demo 完成态与自由建设入口。
- 不用接驳贴片、长提示或更大碰撞掩盖空间合同错误；先统一设备基座、外接格和地表规则。
- 不照搬《终末地》的体量、移动端 HUD、角色编队或完整工业简报，只借用信息架构原则。
- 不在旧布局上先批量生成动画或音频；代表设备必须逐个通过世界画面人工复核。
- 玩家可见目标以正式 `Boot`、实机截图与人工路径为主证据，自动检查只兜底。

## 当前不做

- 新地图、新敌人、后续剧情任务、新建筑 / 配方、Boss、技能树、联机和完整有限电力。
- 陌生玩家盲测、试玩分发、打包或发布。
- 立绘对话、八方向动画、全屏地图、完整按键重映射和基地级工业总览。

## 继续前必读

- [Current Plan](current.md)
- [试玩反馈整改 V1](../features/slice-playtest-remediation-v1.md)
- [第一可玩切片全链串通](../features/slice-first-playable-journey-v1.md)
- [Development Decision Gates](../process/development-decision-gates.md)
- [Godot Runtime Verification Guide](../reference/godot-runtime-verification-guide.md)
- [Pixel Art And Grid Standard](../reference/pixel-art-and-grid-standard.md)

## 验证入口

- 文档：`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check`。
- 代码：`sh ./scripts/check-client.sh`；启动 Godot、`--with-godot` 或有窗口复核前先告知萝卜SAMA。
