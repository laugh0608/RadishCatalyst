# 2026-W32 开发周志

更新时间：2026-08-09

## 本周目标

- 完成视觉方向 Gate A 世界目标稿候选与人工审阅，再决定是否进入 Gate B UI 目标稿。
- 锁定前哨核心与化工反应器的独立设备身份，避免继续在视觉和场景中合并两者。

## 完成情况

- 把核心 / 反应器分离写入视觉方向框架和活跃专题：核心负责修复、仓储、首次充能与样本交付；反应器负责 `2 晶体 → 1 催化剂` 的独立加工链。
- 使用 built-in `image_gen` 完成世界目标稿首轮 `3 / 3`；每次只生成 1 张，并在下一次生成前复制到 `assets/art-intake/2026-08-03-batch01/`、记录源路径、尺寸、SHA-256 与硬判据。
- 三张 `1672×941` 候选虽然把核心与反应器分开，并容纳角色、设备、平台、物流和边缘 HUD，但整体依赖预渲染 2.5D / 类 3D 的体积、连续光照和细节密度；人工复核后否决其目标稿资格，只保留构图证据。
- 盘点正式地表、核心、采集器、中继、储物箱、传送带、反应器、角色和敌人素材，以 `1:1`、无重采样方式确定性拼装原生 `960×540` 世界目标 V1—V3。
- V1 如实暴露当前核心与反应器精灵近乎同型，V2 只加高核心桅杆仍不足；V3 虽换用同密度矩形工业精灵作反应器代理，但地表、平台、材质、比例、色彩与 HUD 仍是当前视觉。萝卜SAMA确认其与现状几乎没有区别，V1—V3 全部否决。
- 萝卜SAMA明确授权当前会话切换到关键设备小样类别后，built-in `image_gen` 完成新一轮 `3 / 3`：V1 建立纵向服务塔 / 横向加工舱，V2 收敛相对角色比例，V3 只增加反应器厚度；每张均在下一次前复制源图、去背并记录提示、尺寸、Alpha 与 SHA-256。
- 关键设备 V3 按约 4 倍生成像素估算为核心 `92×118px`、角色可见 `35×62px`、反应器 `117×59px`；这些比例和剪影分离证据保留。
- 萝卜SAMA独立审阅否决 V1—V3：三稿仍由深青旧家族主导，整体看过去仍是深色；生成合同又主动禁止平台，导致核心和反应器没有明显自有基座，机身小接头也不能替代可接传送带的物流出入口。
- 萝卜SAMA明确授权关键设备小样第 2 轮最多 3 次后，建立 `assets/art-intake/2026-08-03-batch03/`；输入只使用现行工程师锁定角色尺度、像素簇、投影和左上光，不把首轮深色设备继续作为风格参考。
- 第 2 轮 V1 从零建立工程白 / 浅暖灰主导的完整设施，核心与反应器各有独立基座；核心仓储 / 交付宽口、反应器前 / 后物流喉口都进入占地轮廓。V2 只收敛两台设施相对角色尺度，V3 只放大反应器；每张均在下一次前复制源图、去背、量测并记录提示与哈希。
- V2 曾因尺度更接近现有足印成为批次内部推荐：按角色映射约 `48×64px` 换算，核心约 `134×137px`、反应器约 `113×82px`；V3 反应器未按约 `110%` 执行，只保留为物流体量上限参考。
- 萝卜SAMA人工终审否决第 2 轮共用几何：核心整机向屏幕下方斜伸，反应器过度扁平、缺少设备高度；V1—V3 全部作为几何废稿。浅色明亮主体、深色底座与物流出入口获得认可并锁定为下一轮不变量。
- 萝卜SAMA明确授权关键设备第 3 轮最多 3 次后，建立 `assets/art-intake/2026-08-03-batch04/`；不再输入第 2 轮设备图，只用现行工程师锁定角色尺度、像素簇、投影和左上光。
- 第 3 轮 V1 从零建立屏幕竖直、左右平衡的核心，以及具备高前侧壁、肩部、上盖和下部承重层的反应器；V2 只补后 / 上侧输出宽口，V3 只收敛反应器横向体量。每张均在下一次前复制源图、去背、量测并记录提示与哈希。
- V3 的核心 / 反应器高度分别为角色 `2.20× / 1.60×`，统一缩尺估算约 `133×141px / 111×103px`；萝卜SAMA条件认可其竖直轴、高体量、浅色机身、深色底座和整体比例。
- V3 的端口细节未通过：设施自带滚轮的方向彼此冲突，无法稳定区分输入与输出；四向对称接口也不符合固定视角下只稳定展示左、右、前三侧的画面事实。
- 日终代码—文档审计确认：8 月 3 日在本次日终文档提交前没有 Git 提交，也没有受跟踪的客户端代码改动；当天成果为忽略目录中的 5 组视觉证据和待提交的规划、专题、参考与周志更新。
- 代码事实与新方向已分层记录：当前核心只有背包—`core_storage` 手动转移，物流网没有核心端点；当前 `building_rotation` 仍控制贴图、端口、供电、调整与存档。旧专题保留现行实现，新增后续演进说明。
- 8 月 4 日端口细化轮次完成 `3 / 3` 并停手：V1 建立核心左入右出、反应器前入右出的布局；V2 把黑色大口收成浅框短槽并将箭头放回浅色顶面；V3 只尝试恢复反应器高体量。
- V1 / V2 的反应器约为 `121–122×93px`，V3 又过度修正为约 `98×118px`；与昨日母参考约 `111×103px` 相比，两端都不成立。初步 AI 复核曾保留 V2 端口语法，但萝卜SAMA随后人工判定整批为废稿。
- 人工反馈锁定三项新要求：方向标记位于物流口内部并使用两到三个 `》` 形楔纹；端口拓扑在所有大型设施间统一；核心与反应器从结构和功能剪影上重做，不能继续像储物箱加点缀。人工裁图已保存到本批 manifest。
- 萝卜SAMA授权新一轮设备重设计最多 3 次后建立 `assets/art-intake/2026-08-04-batch02/`；旧设备废稿未作为输入，只使用工程师锁定尺度 / 像素介质，并用人工裁图锁定端口内部标识位置。
- batch02 完成 `3 / 3`：V1 从零建立开放能量脊柱核心与横向承压化工反应器，V2 / V3 只校准相对尺度；三稿的四个端口均统一左进右出，并以深色口内三楔纹代替直白箭头。
- V3 按人物高 `64px` 归一约为核心 `113×137px`、反应器 `179×79px`；核心高度已收至人物 `2.14×`，反应器仍仅约 `1.24×`。V3 作为造型与端口候选等待人工复核，不裁切、归一、接入客户端或重组 Gate A。
- 萝卜SAMA确认 batch02 V3 整体不错、基本没有问题，只指出基座与物流口相对整机过大、端口视觉过于突出；随后建立 batch03 局部比例优化轮次。
- batch03 V1 只收紧两台设施基座外伸与四个端口：核心完整宽度由 `365` 收至 `270` 源像素、反应器由 `579` 收至 `506`，四口改为短槽和口内两枚浅灰楔纹；萝卜SAMA随后指出口面像直接竖起来面对玩家。
- V2 只把四口改为沿左右基座斜边嵌入、带薄顶唇和镜像透视收缩的梯形凹槽；V3 的楔纹增强收益有限，去背又出现洋红边缘，不能覆盖 V2。batch03 按 `3 / 3` 停止，推荐 V2 等待人工确认。
- 萝卜SAMA继续退回 V2：物流口比例又过小，斜角终端也无法与传送带形成可信对接。审计确认现行传送带为 `32×32px` 一格组件，反应器端口通过本体内格与正交相邻连接格派生；下一轮必须提供带宽平直停靠边，而非装饰斜槽。
- 下一轮锁定“斜面过渡肩 + 平直停靠领口”：预计原生完整宽 `26–30px`、深色有效开口 `20–24px`、外伸 `8–12px`，同时保留口内两楔纹、左进右出和已认可设备本体。batch03 已达 `3 / 3`，等待新轮明确授权。
- 萝卜SAMA授权对接式物流口新轮次最多 3 次后建立 `assets/art-intake/2026-08-04-batch04/`，并锁定 batch03 V2 本体与现行 `32×32px` 右向传送带为尺度证据；本轮只编辑四个端口。
- batch04 V1 建立平直终端但缺少核心右口且端口过大，V2 补齐四口却仍像外接大耳朵；V3 将终端和肩部压回中等体量，按人物高 `64px` 归一约为核心 `96×136px`、反应器 `162×70px`。
- 萝卜SAMA人工复核推翻 V3 的内部推荐：端口口面仍是设备侧壁上的垂直舱门，无法与平放地面的传送带共面；本轮只校准了终端宽度与中心线，没有建立水平承接托盘和进入机身的后置喉口。batch04 三稿全部判废。
- batch06 / batch07 随后分别锁定核心与反应器的短接口和设备身份；batch08 视角校准完成 `3 / 3`，V3 首次通过高机位、人物两格与设备网格密度，但把开放核心压成低矮环井，仍无完整通过稿。
- 萝卜SAMA于 8 月 5 日确认“换介质”，建立 `assets/art-intake/2026-08-05-batch09-native-redraw/`；本批不调用图像生成，只对已通过透明设计稿做确定性分段最近邻重投影、Alpha 硬化、18 色子集归一和运行时资产拼合。
- 原生主体候选为核心 `144×128px`、反应器 `176×88px`；核心保留开放能量脊柱和跨层负空间，反应器使用 batch08 V3 的宽顶盖 / 薄侧带投影并保留横向承压身份。
- 四口 `12×24px` 版本与相邻传送带 Alpha 可见区均完整重叠 `24px`，但两枚楔纹被压成竖牌白痕；此前约 `14×18px` 版本又过窄。按两次失败规则停止盲试，待萝卜SAMA确认后才进入 V5。
- V5 获批为 `16×24px` 后继续核对运行时代码，确认现行已实现的反应器 / 储存设备左入料、右出料相邻格分别使用 `conveyor_sink_right` / `conveyor_source_right`；此前预览两侧误用普通 `conveyor_right`，其封闭端框放大了“接不上”的观感。核心只复用目标稿语法，物流功能仍未实现。
- V5 以现行带面像素组成中性半格托盘，去除托盘内琥珀驱动语义，并让真实端点与托盘交叠 `2px`；含端口画布为核心 `152×128px`、反应器 `184×88px`，排除旧端面后的主体逐像素差异均为 `0`。
- 萝卜SAMA确认核心端口接入基座自然，但反应器仍像插进承压腔。量测定位托盘为 `y=38–61`、承重基座自 `y=62` 开始；V6 锁定核心，只把反应器两口移至 `y=62–85` 并收成 `180×88px` 画布。
- 萝卜SAMA进一步用局部截图指出 V6 虽位于基座层，仍缺少核心已有的设备侧过渡。V7 原样裁取核心左右 `14×24px` 停靠颈 / 浅色斜肩，插在反应器托盘与基座之间；反应器主体未改，组合画布为 `208×88px`。
- V7 继续被指出有前后错层；量测确认同源过渡肩与反应器只在 `7 / 24` 行直接相邻，下半段有 `1–10px` 空隙。V8 虽沿反应器自身基座边缘补齐 `87` 个像素并使 24 行机械间隙归零，人工仍确认两端像高处骤降的“断崖”，证明局部连通不等于空间连续。
- 连续三版局部接口修正失败后按闸门停手；萝卜SAMA批准 V9 扩大到整个下部基座。V9 保留高机位上部，整体换入 batch07 同源的连续低平台、浅前裙、内嵌桁架与弧形卡口，画布由 V8 的 `208×88px` 收回 `184×88px`。
- 萝卜SAMA确认 V9 已能连接但造型不好看：两侧高 U 形端框像白色夹具。复核发现核心自然过渡实际为 `32×39px` 的完整“窄颈 + 低斜肩 + 基座收口”，并非 V7 只复制的 `14×24px` 短颈；V10 原样复用这两段完整结构替换 U 形端框。
- 萝卜SAMA随后确认 V10“非常好”；核心 V5 与反应器 V10 作为 batch09 关键设备候选正式锁定，接口不再迭代。该人工通过尚未扩展为整个世界 Gate A、正式方向帧或客户端接入授权。
- 萝卜SAMA确认继续下一步后，将两张锁定 PNG 逐字节复制为固定正面候选帧，并以现行原生地表和其余设备重组 `960×540` 世界目标 V4；没有改变核心 / 反应器像素，也没有调用图像生成。
- V4 首版暴露深色采集器与工业平台粘连；根因是采集器被摆在错误功能表面。最终恢复完整 `128×128px` 晶矿宏块作为采集舱位，在不提亮或重绘设备的前提下让彩色 / 灰度轮廓恢复可读。
- V4 已同屏覆盖六类设备、分离的核心 / 生产岛、真实传送带端点、细电网、下方接敌通道与四角 HUD；固定帧、彩色图、灰度图、联系表、构建脚本和 SHA-256 均记录在忽略提交的 `assets/art-intake/2026-08-05-world-target-v4/`，等待 Gate A 人工判断。
- 萝卜SAMA退回 V4：采集器、储物箱和电力中继仍明显属于旧深色资产家族，不能只靠晶矿底面补救；电力关系也应由中继直接连向设备，不使用折线、公共干线或接线盒。核心 V5 / 反应器 V10 不受影响。
- 经明确点名后把三台辅助设备作为一个统一家族类别，built-in `image_gen` 完成独立 `3 / 3`：V1 建立工程白大面、蓝灰侧壁和实心四端中继，V2 收紧相对尺度 / 投影 / 托盘，V3 只把储物箱连续青窗改成三层封闭货格与稀疏库存灯。
- V3 作为结构审阅候选，三张 `1771×888` RGB 源图、完整提示、SHA-256、角落背景色与判断位于忽略提交的 `assets/art-intake/2026-08-05-batch10-support-device-family/`。背景不是严格同色，当时未去底、裁切、归一、重组世界或启动 Godot。
- 后续反馈保留 batch10 V3 的采集器 / 储物箱方向，但退回低矮十字中继：没有抬高接线位，就无法解释直线电力线如何机械接入。batch11 因而只重做中继，不改变其他设备。
- batch11 V1 建立粗立柱与四向端子但过度读成输电塔；V2 只收紧基座、柱身和横担，预计原生包围约 `48×66px`，与 `48×64px` 工程师约同高。本类在 `2 / 3` 主动停止，随后获萝卜SAMA确认。
- batch12 采集器 `96×112px`、储物箱 `104×80px` 暂时通过；中继 V1 的造型 / 接线结构通过但因与工程师近乎等高而退回。确定性脚本保留 V1 全部原始行，仅插入 `14×10px` 居中柱身形成 `48×76px` V2；没有调用图像生成、重采样或覆盖 V1，V2 随后获萝卜SAMA人工通过。
- 沿用 V4 双岛、物流、下方外勤通道和边缘 HUD 关系，以五张锁定设备重组 `960×540` 世界目标 V5。内部首版先清除中央晶簇遮线；萝卜SAMA随后要求设备端统一连接顶部中心、只表示连接关系，四条线改为 `1px` 青线。后续反馈把核心校正到上部核心环视觉中心；储物箱一度被误放在上边缘，最新红框截图进一步明确连接点应在浅色顶部面内部中央。最终修订撤掉下层遮线与端帽，完整单段线终止于局部 `(52, 18)`；其他锚点、设备摆位、锁定 PNG 和中继端子不变。萝卜SAMA随后确认 Gate A 通过，获批候选逐字节固定为 `assets/reference/visual-direction/target-world-v01.png`。
- Gate B V1 以获批世界目标为锁定背景，不继续给旧大表单换色；制造 / 背包和反应器两个互斥状态复用同一右侧现场舷窗。七类配方改为紧凑对象导航，只为当前对象展开物料、流程、阻塞、主操作和结果；反应器沿同一三列层级显示身份、电力 / 端口、固定配方、进度、回收和维护。萝卜SAMA确认可以继续后，两态分别固定为 `target-ui-v01.png`、`target-ui-v01-reactor.png`。

## 关键决策

- 核心与反应器不仅逻辑职责不同，视觉上也必须使用不同剪影、独立占位、独立平台和明确间隔；它们只能通过电力、物流和地面网络建立系统关系，不能共壳或互为附属模块。
- 世界目标必须在原生 `960×540` 尺度证明可读并能由固定网格像素管线复制；整屏类 3D 生成图不能因构图好看而通过 Gate A。
- 现有素材拼装只能证明容纳关系，不能解决资产家族和材质目标；不追加 V4 摆位或调色，先做核心 / 反应器关键设备小样。
- 关键设备生成用角色作为固定尺度尺，并以单一变化迭代比例；达到 `3 / 3` 后停手，不把授权扩展到裁切、归一、整屏世界或 UI。
- 下一轮设备判断单位改为完整设施模组而非孤立机身：明亮大面先行，暗色退到骨架 / 凹槽；核心有独立枢纽基座和仓储 / 交付吞吐位，反应器有独立加工基座及相对两侧 `IN / OUT`。
- 首轮“剪影不共底座”判据不完整：禁止共享底座不等于允许没有底座。后续同时锁定“不可共享”和“每台必须自有”，物流接口也必须参与占地轮廓与连接，而非贴图装饰。
- 候选选择不按调用顺序：V3 若偏离单一修正或现有足印，即使视觉结构仍成立，也不自动覆盖约束更完整的 V2。
- 核心高斜角透视不能破坏屏幕竖直主体轴；反应器的设备高度必须由抬起的主腔体、上盖、肩部和前侧壁建立，不能靠等比放大整块基座解决。
- 第 3 轮继续按单一变量迭代：先证明轴向 / 高度，再补缺失输出口，最后收窄横向体量；后一次调用只有在前一张落盘和量测后才执行。
- 大型设施主体固定正面朝向玩家，传送带等方向性网络构件继续旋转；下一轮统一采用所有大型设施左侧输入、右侧输出，多个真实通道只在对应侧扩展。
- 设施停靠口采用中性深色结构，连续两到三个 `》` 形方向楔纹直接位于口内；不再使用基座顶面箭头、文字或设施自带滚轮，颜色只作冗余。
- 核心与反应器都有物流端口，但职责不同：核心是可存入和流出多种物品的仓储枢纽，反应器是固定配方加工设备。当前核心物流尚未实现，不能只画假箭头后宣称功能完成。
- 固定正面、显式端口与核心物流会改变现行建造 / 物流 / 供电 / 存档边界；8 月 4 日端口小样只验证视觉语法，正式实现前另建功能专题和兼容方案。
- 世界目标稿与 UI 目标稿属于不同生成类别。本会话达到世界类别 `3 / 3` 后停止，不把一次“开始”授权扩展为 UI 类别续跑。
- 本轮没有复制 `target-world-vNN.png`、修改 Godot 正式资产 / 场景或启动运行时验证。
- 端口轮次达到 `3 / 3` 后停止，不把一次开始授权扩展为第 4 张；人工复核否决旧几何与端口语法，下一轮不得继续编辑废稿，而应只用工程师尺度和人工端口裁图从零重设计。
- batch09 的路线决策为“换介质”：生成稿只提供身份与投影真相源，目标稿改由原生网格确定性重投影建立；该决定不修改 32px 网格、960×540 相机、色板或客户端实现介质。
- `12px` 外伸与两枚独立楔纹存在像素容量冲突；获批放宽到 `16×24px` 后不合并楔纹、不移回外接带面。V5 锁定核心后，V6—V8 证明局部短颈无法改变内收底座读法；V9 替换整个下部平台后，V10 再用核心完整低斜肩统一连接语法，仍不重新生成整台设备。
- 电力中继不能在“细杆”与“低矮台座”之间二选一；目标结构固定为实心一格基座、短粗绝缘立柱和抬高四向端子。V2 只延长中央柱身，不缩放或重画端子 / 眼孔 / 基座；世界供电只画端子—设备顶部中心的单段示意线，不用折线、公共干线或接线盒。
- Gate A V5 保留一个中继辐射四台设备的明确系统读法；四条 `1px` 关系线均位于设备上层，储物箱线明确终止于浅色顶部面内部中央；环境不得制造隐藏折点，本版也不通过增加第二中继缩短距离。

## 验证记录

- 整屏生成稿均核对为 `1672×941` PNG RGB 并记录 SHA-256；介质否决结论保留在批次 manifest。
- 原生联系表及 V1—V3 均完成肉眼复核；V3 由脚本断言 `960×540`、32px 网格、世界资产 `1:1` 和无重采样，文件 SHA-256 为 `21ff4ac8d8a8a7ca7fc7f8ca339c6e99226012c27899899b3ce4fd52f9f87ad0`。
- V3 核心约位于 `(192, 272)`、反应器约位于 `(848, 320)`，独立平台和约 `650px` 空间距离成立；细电力线、正式方向传送带和下方移动 / 接敌区没有共用读法。
- 关键设备 V3 去背图为 `1779×884` RGBA，四角 Alpha 均为 `0`，三件 Alpha 包围盒互不相连；SHA-256 为 `624f76bf3e52fd8c9b6affa9993f7a269a73aa972708720dce8e4d6806f6c720`。
- 第 2 轮 V1—V3 均完成 flat chroma 去背与四角 Alpha 检查；V2 为 `1903×826` RGBA，核心 / 角色 / 反应器包围盒互不相连，SHA-256 为 `8070598a13a5cda177f0778db435fb2586938c5beb2a12debadad70866ffa986`。技术完整性不改变人工几何否决。
- 第 3 轮 V1—V3 均完成 flat chroma 去背与四角 Alpha 检查；V3 为 `2022×778` RGBA，核心 / 角色 / 反应器包围盒互不相连，SHA-256 为 `48b438b89f6b2641140b32301563b978cece71f0aec7c77136c5d81033aceda8`。
- 8 月 4 日 batch01 V1—V3 均完成源图复制、flat chroma 去背、Alpha 包围量测、SHA-256 和原生尺度复核；V2 去背图 SHA-256 为 `b5e248f46082e839b9fef584a90c4614bc12e0bf65d7ba6e16f04a306c6c0e8e`，V3 为 `9b8b8e5ebb7db5ae92220263a00a8dbed84e97e4debeebcf8e51c8f559030871`。
- batch02 V1—V3 同样完成逐张源图复制、flat chroma 去背、Alpha 包围量测、SHA-256 和预计原生尺度视觉复核；V3 为 `1821×864` RGBA，三主体包围盒互不相连，去背图 SHA-256 为 `f7c413538647f0c1c682aa17692b3dc8e299660e3f65e7ae358101cf9ebb64ab`。
- batch03 V1—V3 均完成源图复制、flat chroma 去背、Alpha 包围量测、SHA-256 和预计原生尺度复核；推荐 V2 去背图 SHA-256 为 `5da5a0e3948634bad410243f94bc8e206f11e0e356f3e1ff2cb0041852a0d43e`。V3 洋红边缘与无效 `edge-contract 1` 复试已记录在批次 manifest。
- batch04 V1—V3 均完成源图复制、flat chroma 去背、Alpha 包围量测、SHA-256 和预计原生尺度复核；V3 源图 SHA-256 为 `0f76090d32cddeeead9c7d3df641e74f5e9b1349631091921b37a43893a41941`，去背图为 `8d8baa083e8ac655ba13513acbefffcdda028e13016de5137000b864dc154d96`。透明边缘风险已记录，没有用清边处理掩盖。
- batch09 候选 Alpha 均严格为 `0 / 255`，核心使用 17 个、反应器使用 18 个锁定 RGB；`core_native_redraw_v2.png` 与 `reactor_native_redraw_v4.png` SHA-256 分别为 `21e0d0b3df94cf2d84680402c23facc03db2300253ceb12e21b9da87ccfc6a72`、`b1283eaabec892fd872620ad08cf5205ecdb17682935e7d282b818f00ec8c042`。
- `native_redraw_v4_world_960x540.png` 在现行岩地、工程师和四格真实传送带中完成 1:1 视觉复核，SHA-256 为 `3253bc5b470daeea81449259e48ac8bcd6d55a54264e65b8642aeb39ccdbddcb`；没有启动 Godot。
- V5 托盘、核心与反应器 Alpha 均严格为 `0 / 255` 且 RGB 只来自锁定色板；`native_redraw_v5_world_960x540.png` 与 `native_redraw_v5_detail_4x.png` SHA-256 分别为 `a1c01fddefeb47ec6321f340cee6c284be2b7917fbc0de7e3bcf024f1e3c47c2`、`c6e44f0a125117f1f25b501ed8c34ed3a41acf0ec4ec67f66371530da070b619`，四处带面 / 色环 / 托盘 / 喉口连续；没有启动 Godot。
- V6 反应器 Alpha 严格为 `0 / 255`、18 个 RGB 全属锁定色板；排除旧端面和新托盘覆盖区后主体差异为 `0`。`native_redraw_v6_world_960x540.png` 与 `native_redraw_v6_detail_4x.png` SHA-256 分别为 `c21944f5a9aa355cb72559f527e7a916be50ae77cb9496f26414ca21d83c2bd6`、`06eb835381aab713d4f6a78f11b0f12d0b7e07d948ea9072950941168b2cb528`；没有启动 Godot。
- V7 两枚过渡件与核心对应裁区逐像素完全一致，反应器主体差异仍为 `0`；`native_redraw_v7_world_960x540.png` 与 `native_redraw_v7_detail_4x.png` SHA-256 分别为 `1a959293ac167023efb923831d7dd39b9628af6a2c10b1462ce20bc8698732b7`、`68668c5a2a3282e619f31c98eae99ff7aeda60127f48370d9b5e4b1bb75721dc`，没有启动 Godot。
- V8 相对 V7 只改 `87` 个像素，全部位于两处接口区，逐行接缝最大空隙为 `0`；`native_redraw_v8_world_960x540.png` 与 `native_redraw_v8_detail_4x.png` SHA-256 分别为 `19a121ec5e042ba770b750c7aa66ad6bac9ff978074a74c8093aa2a181b993a5`、`ca6d4ec55f3de6b3d7644d6519fe16377fe51570ce281d5dd07cef54281c8317`，没有启动 Godot。
- V9 Alpha 严格为 `0 / 255`、17 个 RGB 全属锁定色板；排除废止端面后 V4 上部像素差异为 `0`，排除两枚托盘后 batch07 下部平台像素差异为 `0`，左右托盘与中央平台属于同一 8 邻接连通结构。世界图与反应器 4 倍细节图 SHA-256 分别为 `13937d004610f9c79ab2750b50ef74f08888c4d15cb5896fe59fb8138c5f50df`、`68ef61063f3b82f9c28355db3b96aad301560207b31be76278c65192926b9297`；没有启动 Godot。
- V10 Alpha 严格为 `0 / 255`、17 个 RGB 全属锁定色板；两段 `32×39px` 过渡与核心裁区逐像素一致，相对 V9 的差异只位于两个过渡区，平台仍左右贯通。世界图与反应器 4 倍细节图 SHA-256 分别为 `a03739d02bbdfb445e8c668796ec3506926e15933f6e0812db211436460c54ad`、`db0f6f24049e17c5f5e451082e0e59081c5c320fa7eb13b2e6ead51d0dea7a68`；没有启动 Godot。
- batch11 V1 / V2 均为 `1122×1402` RGB PNG，SHA-256 分别为 `654837a816f92b90683a44092175f791dd25a46971d64d9f90b31710b3d072f4`、`de27b5a66ed5ca76e3087a9ec12b42f0e0f87894529d99ffd58263c65471e935`；洋红色域包围盒分别为 `731×1093px`、`544×747px`，V2 宽归一为 `48×66px`。背景仍非严格同色，未误记为透明通过。
- batch12 中继 V2 为 `48×76px`，可见像素 `2149`、18 色、Alpha `{0, 255}`、洋红残边 `0`，SHA-256 为 `22608d88ec311929c3e4b7c051caa3dbd2a8fe365cf944a00c2d902ce02d408f`；V1 / V2 原生同屏和 4 倍图 SHA-256 为 `566be67a5ec5e1c0b5c71e8b66253a0013c510989ac4826abd0e3d68b7f37a9c`、`9d04002ad9fc647d71fdd87a8ac9da46b66ccc11268b50fa008d6d7e33f8f2fe`。既有三张 V1 和两张旧审阅图哈希运行前后不变。
- 世界目标 V5 锁定输入为核心 `152×128px`、反应器 `184×88px`、采集器 `96×112px`、储物箱 `104×80px`、中继 `48×76px`；五张 SHA-256 均与来源一致，Alpha 为 `{0, 255}`、颜色均属 18 色设备子集且洋红残边为 `0`。储物箱顶部面中央当前修订的彩色候选、彩色 / 灰度同屏、供电区 4 倍图和储物箱专用 4 倍局部图 SHA-256 分别为 `ffe56b4c99b0f5d53cf13490ad9f5cd5f742fae84942cc0ab846b3e73541647d`、`192c8e4adba80a8edf0e3211eb4fc0963f3cc7f3c9940bb3ec04ead14616ac43`、`91d19e3f3e6801516cce7090a976f43c5f2efd234efa9756614b1ee7b3dae84d`、`12622b547d2817dda1654f9939084d3a0ebf9242e56f112e97857b51d7526cb3`；四条供电线均为一个线段、零折点、零接线盒且彼此不交叉，没有启动 Godot。
- Gate B V1 两张候选均为 `960×540px` RGB，右侧舷窗 `608×384px`、面板外世界可见面积 `55.0%`；制造态、反应器态、彩色同屏、灰度同屏和 2 倍面板图 SHA-256 分别为 `045a1dd5ab9841bb85e9bfbf16f7e199f5d0bbe4ac3ce58fda8a925f73e65ca8`、`563feb83666c236609c320e7f2a3e29ba5c5a2065265015dc45e8d79c914345f`、`f4416a0c5e3dbaefdab5abe5c1f809716b44e5e1496bee7773f3801bdfe62f49`、`9e126302639b3ab61fb3d8e7b0b84170daabb10ce58d289133444f904906d410`、`f565bddceffaec83666601f261adf7f4481c10857efc4a0e1dff806ff78d7753`；洋红异常均为 `0`，没有启动 Godot。
- `git log --all` 的 8 月 3 日区间在日终提交前为空；`git diff` 与 `git status` 确认没有受跟踪的 `client/`、`server/`、`scripts/`、`tools/` 或 `assets/` 改动。当天生成图和确定性辅助脚本均位于仓库既有忽略目录。
- `./scripts/check-text-files.sh`、`./scripts/check-docs.sh` 与 `git diff --check` 通过；没有启动 Godot。

## 风险与未完成项

- 五张锁定候选与 `target-world-v01.png` 不得重画、重生成、旋转或缩放；正式接入只允许逐字节复制与运行时锚点 / 端点校准。
- 联合包 2 / 3 已提交，包 3 / 4 已人工通过；schema 2–7 兼容与来源感知发布继续作为回归基线。
- 包 5 自动、正式入口、AI 逐图复核和萝卜SAMA综合人工确认均已完成；下一步须先做视觉专题与阶段退出复核，不直接切换专题。
- Gate B 与 UI 结构已人工通过；陌生玩家盲测继续冻结，直到视觉专题与当前阶段退出条件完成架构级复核。

## 2026-08-04 进展与停点

- batch02 重设计、batch03 局部缩比 / 透视优化和 batch04 对接式物流口均按各自 `3 / 3` 停止；没有启动 Godot，也没有修改客户端、schema、现行四向端口或全链。
- 下一步若获新轮授权，只重做“地面水平承接托盘 / 短坡 + 后置机身喉口”，并在生成阶段就用一格带面验证共面连续性。当前不生成第 4 张，也不进入清边 / 裁切 / 归一。
- 完整设施小样通过前仍不建立固定正面 / 核心物流实现专题，也不制作正式帧或重组 Gate A。

## 2026-08-05 至 08-06 进展与停点

- 完成 batch08 卡住会话的落盘核验，并确认三稿只留下 V3 高机位证据；没有补做第 4 次生成。
- 经萝卜SAMA批准转入 batch09 原生网格路线，设备主体和共面量测成立；V5 锁定核心接口，V6—V8 定位并证明反应器内收底座不能靠局部接口补丁修复。
- V9 整体低平台解决连通但端框造型退回，V10 换入核心同源完整低斜肩后获人工通过；世界目标 V4 随后因辅助设备断代和折线电网退回。batch12 三台辅助设备收口后，世界目标 V5 已按单段直线电网重组；储物箱端点最终落在浅色顶部面中央，Gate A 人工通过并固定为 `target-world-v01.png`。本步仍未进入客户端或启动 Godot。
- Gate B V1 随后以确定性侧舷窗重组制造 / 背包和反应器操作，保留世界可见性并移除整页表单主读法；彩色、灰度与 2 倍细节证据完成后获人工通过，当前进入 UI 结构实现子包。
- UI 结构实现把制造面板收为七个对象导航、单一当前配方和九格背包，把建筑面板收为身份、物料流程、现场操作三列；两者共用 `1224×768px` 右侧舷窗和深色终端抬头 / 页脚，没有改玩法、端口、旋转、schema 或存档。
- 正式 `Boot` 隔离世界专项以 `41` 项断言通过，保留制造阻塞 / 成功、储物箱断链、反应器加工四张 `3024×1701px` 原图、联系表、主档与三份备份；人工逐图未见截断、互叠、HUD 遮挡或外框越界。
- 完整 `check-client --with-godot` 通过；旧首小时检查对制造面板私有 `_rule` 的耦合改为读取 `current_journey_rule_text()`，继续证明三阶段权威规则而不要求目标稿已删除的重复长行。
- 萝卜SAMA人工确认制造 / 背包与建筑设备舷窗整体通过；UI 结构实现据此收口，下一步转入世界设备家族接入架构闸门，不把人工通过扩展为替换世界素材、端口、旋转或存档的授权。
- UI 收口随后以 `4086c2be` 提交；提交前静态 `check-client`、文档篇幅、文本卫生与 `git diff --check` 全部通过，两个明确排除的视觉参考文档保持未修改。
- 世界设备接入审计确认：`building_rotation` 同时驱动贴图、足印 / 锚点、物流口、供电探针、放置调整与 schema 7；核心仍是地图固定 sprite，`core_storage` 未登记物流端点，采集器缓冲也只能手动取出。Gate A 的储物箱供电线还与现行 `POWER_PASSIVE` 定义冲突，不能只画线伪装为功能。
- 新建 [世界设备家族接入 V1](../features/slice-world-device-family-integration-v1.md)，提出“方向 / 端点等价重构 → 可放置设备 + schema 8 原子切换 → 核心 V5 + 核心物流 → 真实供电线 / 锚点 → 正式入口与人工闸门”的五包顺序；确认前未复制锁定资产、未改端口 / schema / 存档，也未启动 Godot。
- 8 月 8 日萝卜SAMA确认储物箱应成为真实用电设备：`supply` 模式由玩家手动存入并从右口供货，`transfer` 模式从左口接收并按固定周期无线汇入核心仓库；手动访问不因断电锁住，自动供货 / 无线回库受二值供电约束。
- 同时确认分类库存口径：核心仓库不限制类型、每类 `99999`；背包不设类型数硬上限、普通物品每类 `200`；储物箱最多四类、每类 `200`；机器专用缓冲保持现值。新增 [分类库存与通电储物箱 V1](../features/slice-category-inventory-and-powered-storage-v1.md) 保存容量、UI、模式、无线传输与迁移合同。
- schema 8 改为两个专题共同拥有的联合原子切换：先以 legacy profile 做不改变 schema 7 / 行为的包 1 地基，再一次启用逐类容量、储物箱模式 / 供电、固定正面、显式端口和旧档迁移；本次仍未修改客户端或启动 Godot。
- 联合包 1 随后完成：九类现行物品统一进入 `SliceItemCatalog`，容器用 legacy profile 保持 `30 / 120 / 20 / 2 / 1`，采集、制作、拆除、手动转移、反应器回收和面板容量判断迁到逐物品、原子批次与显式部分转移 API；九格和 `0 / 30` 玩家结果不变。
- 建筑定义新增独立方向 / 视觉模式、端点定义、逻辑供电探针和画线锚点；非持久化 runtime endpoint 统一了物流登记、传输、拓扑、放置预览和面板快照。六类旧建筑仍为 legacy `cardinal`，储物箱仍 `POWER_PASSIVE`，采集器 / 核心未新增端点。
- schema 7 专项补充根 `14` 键、库存 `2` 键、建筑 `5` 键、禁止字段递归缺席、schema 6、五类设施四旋转及保存幂等；合同 helper 从大型专项拆出，主检查保持低于 1500 行。
- 首次 Godot 导入拦到一处设备面板 `bool` 类型推断并当场修复；随后库存 `79`、放置 `124`、建筑操作 `131`、供电 `96`、反应器 `125`、物流 `141`、存档 `203`、首条全链 `70` 项断言及完整 `check-client --with-godot` 全部通过。内部等价重构没有另做玩家可见截图。

## 2026-08-08 联合包 2 实施与复核

- 背包、核心仓库和储物箱已切到逐类 profile：普通物品每类 `200`、核心每类 `99999`、储物箱最多四类且每类 `200`；制造 / 背包与核心仓库沿人工通过的侧舷窗改为动态分类格，关键样本继续由遭遇状态独立呈现，旧未知物品保留兼容分组。
- 储物箱成为二值电网消费者并启用 `supply / transfer` 开关：存储模式只开放右 `OUT` 与筛选供货，传输模式只开放左 `IN` 并按 `5s / 50件` 无线回核心；手动访问不受断电影响，断电、核心未修复和满核心均暂停进度且不丢物。
- 采集器 V1、反应器 V10、储物箱 V1 和中继 V2 已逐字节复制到正式客户端，尺寸与锁定 SHA-256 全等；固定设施禁用 `R`，传送带继续四向。核心 V5、核心左 `IN` / 右 `OUT` 和完整四端子拓扑明确未进入本包。
- schema 8 已联合保存逐类库存、储物箱五项权威状态与固定设施规范旋转；schema 2–7 先走冻结旧合同，再于世界、供电和物流重建成功后来源感知发布。合法旧储物箱的五至九类祖父状态、坏主档回退、备份哈希与重复迁移均保留。
- 首轮正式截图虽通过自动断言，但萝卜SAMA人工复核指出反应器面板图标把世界接驳段读成额外传送带，且相邻传送带压住设备接口并出现连接错位；该轮因此不通过，撤销“无互叠”的 AI 结论。
- 修正版保持四张锁定 PNG 字节不变：UI 使用独立 `icon_region`，反应器左右端口显式跨过一格自带接驳段，放置态保留该非持久接驳格，物流按真实连接格绑定，末端带面降到设备接口下层。
- 修正版完整 `sh ./scripts/check-client.sh --with-godot` 通过；放置 `139`、建筑操作 `174`、库存 `121`、通电储物箱 `55`、供电 `115`、反应器 `132`、物流 `167`、存档 `157`、schema 8 `121`、存档目录 `92` 和首条全链 `71` 项专项断言全绿。
- 正式 `Boot` 隔离自动化增至 `100` 项，保留九张原图、schema 8 主档与三份备份、schema 7 升级主档与备份；当前等待萝卜SAMA再次人工复核。
- 截图位于 `assets/art-intake/2026-08-08-category-inventory-powered-storage-preview/`；隔离新档与升级档位于 `tools/runtime-intake/2026-08-08-category-inventory-powered-storage/`。修正版随后按萝卜SAMA指令提交为 `b4803e5a`。

## 2026-08-08 核心 V5 与实体物流

- 联合包 2 修正版按萝卜SAMA指令提交为 `b4803e5a`，随后进入包 3；原九态截图、schema 8 新档、schema 7 升级档及备份继续保留。
- 修复态正式路径逐字节切至锁定核心 V5，尺寸 `152×128px`、SHA-256 `e5ab5a430e4f725b4ac61eeb1d170d93b0f9d217b794e4716cab7ed5fc447bc3`；受损核心素材、位置、2×2 足印和碰撞不变。
- 修复后派生左 `IN` / 右 `OUT` 并绑定同一 `core_storage`。首次实现误判 V5 侧接头覆盖完整一格，端点因而跨过透明格连接外侧第二格；逻辑可传但截图出现肉眼断链。该证据作废，改为相邻终端带直接落在 `(18,10)` / `(21,10)` 并置于核心 sprite 下层。
- 相邻接驳修正版的核心专项 `54`、供电专项 `116` 及完整 `sh ./scripts/check-client.sh --with-godot` 全绿；正式 `Boot` `48` 项覆盖受损态、真实修复、实体入 / 出库、满仓回压、保存与重载。
- 第一轮截图因镜头过低裁掉 V5 顶部而主动作废；调整构图后的第二轮又被萝卜SAMA指出实体带中间存在透明断链，撤销“接头与带面连续”的 AI 结论。第三轮相邻接驳截图已逐图复核，左右透明断段消失、输入催化剂 / 输出晶体可辨，重载一致。
- 最终主档、三份备份和复核入口保留在 `tools/runtime-intake/2026-08-08-core-v5-logistics/`，截图位于 `assets/art-intake/2026-08-08-core-v5-logistics-preview/`；包 3 已以 `f41f93a7` 提交，随后获萝卜SAMA人工通过。

## 2026-08-08 真实二值供电线

- 包 3 人工通过后进入包 4；电网继续保持二值可达模型，新增的消费者父边只为每台通电设备稳定选择核心或最近已通电中继，不新增发电量、负荷、储能或存档字段。
- 中继 V2 四端子按 Gate A PNG-local 锁值换算为足印中心偏移；新的狭义视觉解析器按父子相对方向选端子，不让视觉锚点反向改变供电半径或父树。
- 画线层删除原有下垂中点和暗色粗底，每条真实父边只画一条设备上层 `1px` 青色直线；四端子不是连接容量，多个同方向关系仍可共用同一视觉端子。
- 完整 `sh ./scripts/check-client.sh --with-godot` 通过，供电专项从 `116` 增至 `139` 项；正式 `Boot` `64` 项覆盖中继链、中央四端子、三消费者、调整断线、储物箱传输模式、schema 8 重载和非持久字段递归缺席。
- `01-powered-four-terminal-network.png`、`02-central-relay-disconnected.png`、`03-powered-transfer-storage-panel.png`、`04-schema8-reloaded-network.png` 均为 `3024×1701px`；逐图复核未见下垂 / 折点 / 公共干线、端子错位、设备互叠或文字截断。截图在 `assets/art-intake/2026-08-08-real-power-links-preview/`，可载入主档与三备份在 `tools/runtime-intake/2026-08-08-real-power-links/save-root/`。
- 萝卜SAMA随后直接确认包 4 无问题；真实四端子供电线、消费者顶部锚点、断电同帧反馈和 schema 8 重载据此人工通过。

## 2026-08-09 包 5 综合迁移闸门

- 正式 `Boot` 双档演练以 `138` 项断言通过：真实新档串联完整自然产线、储物箱双模式 / 无线回库、核心实体入 / 出库与保存重载；schema 7 隔离旧档串联断链诊断、带货回收、固定设施调整、端口修复和重启。
- 旧档不自动搬动布局；玩家路径最终保持储物箱 / 旧带稳定 ID，晶体 `38` 与催化剂 `11` 守恒。两套主档、各三备份均可解析为 schema 8，八份哈希彼此独立。
- 首轮逐图复核发现放置 / 调整态 `OUT` 被裁成 `OU`；将绘制文本改为使用完整 `22px` 标记矩形后，完整 `check-client --with-godot` 与正式演练再次通过。
- 最终 11 张原图逐图复核未见 HUD / 面板截断、遮挡或设备主体互叠；迁移终态的储物箱、真实终端带与核心 V5 边界清楚。截图、可载入主档与三备份均保留在对应忽略目录。
- 萝卜SAMA于 2026-08-09 确认包 5 综合人工闸门通过；没有重画锁定资产，没有增加 schema 字段、有限电力、负荷、储能或新内容。

## 下周建议

- 包 5 收口提交后，下一步先按决策闸门复核当前视觉专题与阶段退出条件，不直接追加同层视觉包或擅自切换专题。
- 继续冻结有限电力、负荷、储能、新内容和 schema 字段；包 5 只做既有合同的综合收口，不借机扩系统。
