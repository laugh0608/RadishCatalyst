# 2026-W36 开发周志

更新时间：2026-09-06

## 本周目标

- 将已确认的生产核心方向落入正式文档，并把当前入口切换到建造体验验证定义。

## 完成情况

- 新增 [Production-Centered Direction](../product/production-centered-direction.md)，明确化工生产经营是核心，探索为生产提供资源、空间和长期目的。
- 更新 [Project Definition](../product/project-definition.md)，移除每 10 分钟必须同时满足工程、冒险、成长以及 Boss、天气、秘境等旧硬承诺；战斗体量保持待定。
- 更新 Current 与 Daily：原试玩整改 S0 暂缓，下一步改为定义设备放置、真实物流接通、产出／堵塞观察和布局调整组成的工厂片段。
- 既有第一可玩切片、整改包 0—4、schema 10 和历史验证证据全部保留；没有改写旧专题或旧周志，也没有宣称新方向的体验验证已经完成。
- 新增 [Factory Building Experience Validation V1](../features/factory-building-experience-validation-v1.md)，定义固体“采集器 → 反应器 → 储存”片段、真实玩家输入、缺料 / 堵塞恢复、固定高斜角与建造模式画面合同，以及系统证据和玩家评价的双重通过条件。
- 完成现有设备、物流、放置、相机和隔离入口的只读源码审计；Godot 局部实验已获授权并完成建造会话 / 视图实现，隔离场地采用采集器 `(40,11)`、反应器 `(47,10)`、储物箱 `(53,11)`。传送带仍逐格放置，本轮未实现直线拖铺。
- 完成正式 `Boot` 输入链：输入带 `(43..46,12)` 先缺 `(46,12)`，输出带 `(50..52,12)` 先缺 `(50,12)`；三台设备各 `1` 件套件、共 `9` 条传送带，建造缩放冻结为 `1.5`。`1.25` 倍率出现上下灰条的结果保留为历史失败记录。

## 关键决策

- 常态首选固定高斜角主视图；建造模式拉远并显示网格、足印、接口和物流流向，同时弱化遮挡，不强制切换真实正俯视。
- 近期优先复用现有像素素材和 Godot 实现；是否更换现行像素介质、是否采用 3D 或双角度，以及 Web 迁移与整仓重写均未决定。
- 长期愿景允许多星球，近期只在小区域验证生产核心。
- 随机消耗工业资源以增加期待感是产品诉求；工业研发、试制、勘探和传统抽卡只是候选表达，具体规则尚未冻结。

## 2026-09-06 视觉对照审阅

- 萝卜SAMA同意开始当日评审与下一开发包范围整理；当前仍是小型化工厂视觉目标评审，未据此推定视觉定稿或授权资产 / 客户端接入。
- 本次直接查看 v1、v2、上一轮三机产线重载截图和本轮接缝 / 建造态夹具图，并只读核对设备目录。夹具中的储物箱排列不作为正式场景密度证据；上一轮三机截图也不冒充最新修复后的全链画面。
- 在 [Small Chemical Factory Visual Target V1](../design/small-chemical-factory-visual-target-v1.md) 增补四项审阅、实现差距和“三机产线的代表性厂坪片段”草案。执行会话建议保留 v2 场景组织与比例倾向，但前排带 / 管混接无法证明完整固体路径，不能认定物流可读性已通过。
- 草案优先验证连续厂坪、通道、原生比例和真实端口；三台设备不足以复现概念图完整双排密度，新增模块或资产生产须单独定义范围。没有新增功能专题、切换当前阶段、生成第三张图或修改客户端。
- 本次未启动 Godot、未运行客户端测试、未进行像素网格 / 色板验收或真人手感复评；用户视觉评价仍待回填。
- 本日文档变更的 `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh` 均通过；仓库治理检查 `1481` 个文件。
- 后续收到萝卜SAMA的局部视觉反馈：透明管段、金属连接处，以内部流体的颜色与状态表达物质和状态。已记入视觉专题，并将执行会话提出的物质识别色、物态纹理与输送动画分工明确标为建议；整体视觉定稿、具体配色与液气实现仍未决定。本次没有生成新图或修改客户端。
- 萝卜SAMA随后同意继续局部样板。使用内置 `image_gen` 新建独立 `2026-09-06-transparent-pipe-study` 批次，调用 `1 / 3`，生成一张两种匿名流体 × 空管 / 静止 / 输送的对照板，原文件保留在默认生成目录，项目副本与完整提示词 / 尺寸 / 哈希 / 审阅落在 `assets/art-intake/2026-09-06-transparent-pipe-study/`。它不计作前一工厂整图批次的第三次修订。
- `transparent-pipe-states-v1.png` 为 `1672×941`，SHA-256 `5dafc03e476d6b815fb7dc3e97e03fde2349f1bf8f10491f10d9e95c740dd08d`。空 / 满、物质示例色和金属分区成立；接头偏宽、视角偏正面、硬件有漂移，保留为材质候选而非游戏精灵。静态横纹和箭头不能证明流动动画；未做客户端接入、网格归一或运行时验证，用户样板评价待定。
- 萝卜SAMA指定本地终末地参考图 `20260730215315.png`，要求在管道上增加动态流向标识。已只读查看其管段白色双箭头，制作可切换物质、方向和管道状态的独立动态示意，副本落在同批 `pipe-flow-markers-v1.html`；没有修改参考图、生成 PNG 或客户端。箭头移动、停流变淡与空管隐藏是本次建议，不冒称参考截图能够证明动画节奏。
- 动态预览通过离线模拟 DOM / 帧回调检查，覆盖流动、反向、停流、空管、颜色、暂停、减少动态效果及三档宽度几何。浏览器本地文件预览被 URL 安全策略拒绝，未绕过；没有取得浏览器截图、主题适配或玩家辨识证据，也未启动 Godot。
- 萝卜SAMA随后明确同意动态标识方向并询问下一步。专题已记录局部表达方向通过，未扩大为整图、像素资产或液气功能验收；下一包建议是“管道模块与游戏比例验证”，覆盖直管、弯头、设备端连接与原生比例下的状态辨识，素材制作及 Godot 视觉验证范围尚待确认。

## 2026-09-06 管道模块与游戏比例验证

- 萝卜SAMA确认继续推进有限模块制作、机械归一与 Godot 隔离视觉验证；新增 [Transparent Pipe Module Visual Validation V1](../design/transparent-pipe-module-visual-validation-v1.md) 作为本包合同。Current / Daily 路由到该包，真实液气物流仍未解冻。
- 本模块批使用内置图像工具 `2` 次，同会话管道类别累计 `3 / 3`，已停止生成。v1 比例过细长而失败，v2 粗短模块可作归一源；源稿、提示词、哈希及失败审阅保留在 `assets/art-intake/2026-09-06-pipe-modules/`。
- `tools/normalize_pipe_visual_study.py` 从 v2 固定哈希源图做分区像素块采样、去除伪透明棋盘、共用色板和源接头边缘对齐。产出水平 / 竖直直管、左入下出弯头、右出端接头及独立 mask；均为 `32×32`、`10px` 内容通道、`15` 色，`10` 项机械检查通过，原生 / 4 倍装配预览已查看。
- 新增独立 `Sprite2D` 管体样板、内容 / 双箭头 shader 与控制面板，提供两种匿名内容色、输送 / 停流 / 空管、反向和暂停。箭头按统一路径距离推进并沿代表弯头改变朝向；没有加入正式设备目录、库存、输送逻辑或存档字段。
- 新增 `sh tools/run-pipe-visual-study.sh`；通过正式 `Boot` 菜单信号进入隔离世界，由工具准备地板和储物箱比例参照，样板按钮及现有建造按钮经鼠标输入分发。定向验证通过 `91` 项断言；初次运行发现 A 管标签被任务 HUD 遮挡，下移样板后再次通过 `91` 项，最终日志 `tools/runtime-intake/2026-09-06-pipe-module-validation/logs/review-20260906T032737Z-13419.log`。
- 已查看最终 `assets/art-intake/2026-09-06-pipe-modules/runtime-preview/run-1788665258/` 中常态、停流、空管、建造反向及运动序列首尾原图。六帧固定相位序列每次推进 `4px`，相邻 A 管区域变化 `541 / 524 / 478 / 558 / 553` 像素，B 停流和空管区域均无变化；另由运行断言验证实际帧暂停 / 恢复及路径连续性。
- 执行会话观察：透明通道、接头分层及内容区别可辨，拼接没有明显断缝；建造网格与地板纹理较抢眼，停流淡箭头、动画节奏和材质融合仍待用户评价。截图为 `1440×810` 内部渲染，时序帧是受控相位证据，不冒称真人建造、游玩录像或真实生产。
- 初次 headless 导入虽退出 `0`，但沙箱拒绝写 Godot 全局编辑器设置，未计成功；说明具体设置文件并获授权后重跑导入无错误。第一次工具脚本解析因目录方法名不匹配失败，修为既有 `SliceBuildingCatalog.find` 后解析通过。失败与重跑日志均保留，不用后续通过覆盖前次失败。
- 默认复核入口已实际创建并进入 `tools/runtime-intake/review-worlds/pipe-module-visual/`，窗口保留给萝卜SAMA；不覆盖其他复测档与正式存档。用户评价待回填，当前不宣称本包视觉验收或工厂整图通过。
- 本包 `sh ./scripts/check-client.sh` 静态检查、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh` 均通过，仓库治理检查 `1509` 个文件。本轮未重跑完整 `check-client --with-godot`，不扩大为全客户端运行时回归通过。
- 萝卜SAMA随后要求提交工作区并暂停思考，反馈“我总感觉没达到我想象中的那种效果”。本包保留为未验收的视觉试验记录，具体差距待后续讨论；不追加同类生成、视觉微调或正式液气开发。

## 验证记录

### 2026-09-06 局部体积候选

- `280abdde` 已按萝卜SAMA要求保存上一轮管道视觉试验，随后用户明确差距为“感觉没有那种 2.5D 的感觉”，并同意从“设备＋架起的管道＋地面”的局部体积样板继续。
- 新增 [Industrial Volume Visual Study V1](../design/industrial-volume-visual-study-v1.md)，优先校正统一高斜角、机器顶侧面、圆管截面、管下负空间、支撑与投影。保持现行像素与矩形网格方向，不把 2.5D 诉求解释为真实 3D 或预渲染管线迁移。
- 使用内置 `image_gen` 生成 v1，再做一次投影 / 等高管道关系的定向编辑得到 v2；第二轮 `2 / 3`，管道类别累计 `5` 次，已停止追加。源文件、完整提示词、尺寸 / 哈希与审阅保留在 `assets/art-intake/2026-09-06-industrial-volume-study/`。
- 两稿均为 `1672×941`；v1 SHA-256 `a47c09b07ade46cfab36b9ea40023b1f977a03a3dbed0915ff7e2b6a94aff323`，v2 SHA-256 `ea177f60276e26eec08180360d51b9fdd8df730d74b1b8b0ce4594f091f1ae0d`。两稿均已直接审阅并立即复制到仓库规定的忽略目录。
- v1 虽有离地体积，但视角仍偏低、右端管路读成下折立管，作为失败对照。v2 顶面更主导、管道向前延伸并有圆管端面，推荐用户判断体积方向；设备相对人物偏大、前端支撑遮挡较多、材质细节偏密与严格装配一致性仍未解决，不宣称视觉定稿。
- 本轮只生成和审阅局部候选，没有修改客户端、像素标准或正式资产，没有归一、运行 Godot 或重跑客户端检查。`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh` 通过，仓库治理检查 `1510` 个文件。

### 2026-09-06 独立体积素材与游戏比例复核

- 萝卜SAMA对体积 v2 反馈“有点那种感觉了”，随后明确同意拆成少量独立像素素材，放回正常游戏比例，先验证一台设备和一段架空管道。该授权已补入体积合同，Current / Daily 更新到实机复核。
- 使用内置 `image_gen` 以 v2 为体积 / 材质参考重出六个独立部件：设备、水平管、支撑及三类地面阴影。第二轮第 `3 / 3` 次、管道类别累计 `6` 次后停生成，继续已授权的归一与引擎阶段。源图为 `1024×1536`，SHA-256 `9b1347ba5908f7e14a449474974fd899cf3befa79a27b01bd42cd8295f7a5e86`，原件、副本与完整提示词保留在 `assets/art-intake/2026-09-06-industrial-volume-assets/`。
- `tools/normalize_industrial_volume_study.py` 固定源框、哈希与等向整数采样因子，去品红底、共用 `26` 色并保留小面积琥珀 / 青色语义锚点；生成阴影轮廓独立于主体，清玻璃区域使用有限 alpha。设备内容 `98×81` / 画布 `128×128`，管体内容 `82×24` / 画布 `96×32`，未拉伸管径或放大设备掩盖原生尺度损失。`7` 项机械检查通过，装配原生与 nearest 4 倍预览已直接审阅。
- 首次归一检查误把清玻璃半透明判作背景，修正检查谓词；随后原图发现暗品红边缘和小灯失色，修正去底判据与色板锚点后重出。首次客户端静态检查因新脚本尚未导入、缺少 `.uid` 未通过；执行获授权的 Godot 导入后，静态检查通过，工具脚本解析通过，导入 / 解析日志无实际错误。
- 新增 `IndustrialVolumeStudy.tscn` 与 `sh tools/run-industrial-volume-study.sh`，正式 `Boot` 隔离世界使用真实角色与既有相机倍率，设备 / 管道按接地深度参与现有 `y_sort`，阴影与支撑可独立开关。默认低纹理地面对照复用仓库已有 raster 的原比例区域，可切回当前工业地板；不修改全局地面资产或建造政策。
- 有窗口定向验证通过 `38` 项断言，日志 `tools/runtime-intake/2026-09-06-industrial-volume-validation/logs/review-20260906T050322Z-19365.log`。`assets/art-intake/2026-09-06-industrial-volume-assets/runtime-preview/run-1788671002/` 的八张原图已全部查看，覆盖 `1440×810` 常态、`960×540` 一倍素材像素、去支撑 / 阴影、现有地板、角色前后遮挡、建造态及正式保存重载。
- 执行会话观察：顶侧面、支脚、管下空隙与真实角色前后遮挡在正常尺寸下仍可辨，空间线索较上一平面模块改善；玻璃精致感弱于概念图。当前地板纹理抢眼，建造网格与既有弱化遮挡明显压淡体积，未将建造态体积可读性记为通过。用户实机视觉评价仍待回填。
- 菜单由正式信号进入，场地与初始位置由夹具准备，样板按钮 / 建造按钮经过鼠标输入分发，遮挡跨线使用实际 `move_down`；不冒称纯人工路径、真实管道碰撞或输送。管内色带是静态材质，上一动画样板独立保留；没有修改正式设备目录、液气库存或存档字段。
- 默认入口已创建并进入 `tools/runtime-intake/review-worlds/industrial-volume-visual/`，窗口保留给萝卜SAMA，不覆盖之前复核档。客户端静态、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh` 通过，仓库治理检查 `1529` 文件；未重跑完整客户端 Godot 套件。

### 2026-09-06 体积样板建造显示收口

- 在 `e4f63d5d` 保存后，萝卜SAMA同意继续收口当前样板。补充体积合同，范围限定为地面 / 主体分层、建造对照与低纹理默认，不自动扩展管道族或生成新资产。
- 根因是样板地面与主体一起被普通非必要节点策略淡化，网格与每格地板粗足印又重复覆盖机身。建造控制器新增显式复合主体登记及可选地面网格通道；隔离样板启用，普通入口默认绘制口径不变。样板新增原模式 / 分层按钮，保留相同相机与素材比例。
- 首轮窗口检查 `56` 项通过，但截图仍见重复地板框干扰；取消分层通道的重复框后，发现 `960×540` 建造缩放下正宽度细线丢失，再改为固定屏幕细线。三次运行分别保存在 `run-1788671928`、`run-1788672114`、`run-1788672180`，不累加断言充当覆盖。
- 最终正式 Boot 窗口验证 `56` 项通过，日志 `tools/runtime-intake/2026-09-06-industrial-volume-validation/logs/review-20260906T052300Z-20854.log`；截图在 `assets/art-intake/2026-09-06-industrial-volume-assets/runtime-preview/run-1788672180/`。已实际查看常态、原生建造、角色前后和过程中的模式 / 地面 / 放置对照。分层显示改善，推荐低纹理地面；半透明主体仍能透见地面细线，用户最终体积评价未代办。
- 首次解析命令误用了工程内相对路径，日志明确报脚本缺失，未算通过；改绝对路径后解析通过。既有 `slice_building_placement_check.gd` headless 回归 `152` 项通过，只有既知 macOS `noErr` 证书噪声。客户端静态、文档 / 文本、差异空白与仓库治理通过；未重跑完整 Godot 客户端套件。

### 2026-09-06 可拼接架空管道模块

- `6264e17d` 保存建造分层后，萝卜SAMA同意继续有限横管、纵管、弯头与设备接口，接回动态内容并做正式入口隔离验证。新增 [Raised Pipe Module Visual Validation V1](../design/raised-pipe-module-visual-validation-v1.md)，不扩大到真实流体、碰撞、配方或存档 schema。
- 内置图像工具生成 v1，因直管过长、弯头长尾影响对接，定向编辑为 v2；第三轮 `2 / 3`、累计 `8` 次后停生成。两稿与完整提示词保留在 `assets/art-intake/2026-09-06-raised-pipe-modules/`，v2 SHA-256 `25548eb4ffd497819b4ed15b98360845aea480bb6035d2ba49e96caa89577997`。
- 固定源块锚点、同源接头边和材质 alpha，归一四种 `32×32` 主体及各自内容 / 投影掩码。首次宽度检查暴露外轮廓多算一至两像素，修正源边界后 `12` 项机械检查通过；保留 16px 管体与 16 色主材质。归一原图与装配预览已查看，不把棋盘源背景或整景当作正式资产。
- 新增架空路径场景与复核入口，复用之前的路径距离 / 相位逻辑、独立设备和支撑；九段 L 形链按接地深度排序，管轴抬高 16px。动态分层材质保留玻璃明暗，正式主体透明度只作用于新模式，旧 shader 默认效果保持。
- 正式 Boot 首轮 `71` 项通过，补齐建造停流 / 空管后最终 `78` 项通过；日志 `tools/runtime-intake/2026-09-06-raised-pipe-validation/logs/review-20260906T065512Z-25422.log`，最终截图 `runtime-preview/run-1788677713/`，隔离存档 `check-runs/raised-pipe-modules/run-1788677713-473482/`。窗口检查包括真实按钮、真实跨遮挡线移动、保存与重载；场地由夹具准备，菜单通过正式信号进入。
- 已查看常态三种内容状态、原生尺寸、琥珀反向、前后遮挡、建造流动 / 停流 / 空管及七相位序列中的关键帧。相邻帧弯头区域均有变化，地面与金属接头对照区域始终不变，结果保留在 `motion-comparison.json`；不把受控相位帧写成连续录像。连续体积与状态可辨，建造态仍较淡、玻璃细度和末端读法仍待用户评价。
- Godot 导入 / 解析、客户端静态与旧管道窗口回归 `91` 项通过；旧回归存档 `check-runs/pipe-module-visual/run-1788678280-493093/`。本轮没有重跑全套客户端 Godot 检查，没有新增真实管道存档字段。
- 默认入口已创建并进入 `tools/runtime-intake/review-worlds/raised-pipe-modules/`，窗口保留给萝卜SAMA；文档 / 文本、差异空白与仓库治理检查通过（`1563` 文件）。

### 2026-09-06 俯视角 3D 独立对照

- 从干净的 `dev / 2486fa65` 开始，按萝卜SAMA明确授权建立 [Topdown 3D Comparison Demo V1](../design/topdown-3d-comparison-demo-v1.md)。先记录范围，再新增 `tools/visual-studies/topdown-3d/` 独立工程和 `sh tools/run-topdown-3d-demo.sh`；没有修改正式 Boot、客户端玩法、存档、流体代码或全局像素标准。
- 使用本地编写的真实网格构建厂坪、工业罐设备、五段空心玻璃管与三副支架、可移动工装角色；最终为 Forward+ / Metal、斜俯视正交相机、统一光源、真实阴影与 3D 碰撞。无资产下载、依赖安装或图像生成；管内内容只是静态展示。
- 首次导入枚举错误、Compatibility 过曝、厂坪共面和两次初始按钮漏点均保留为过程失败。阴影检查状态与画面不符时转专用图集诊断，最终采用固定小场景的单幅正交覆盖，并按真实角色高度修正管下净空，不用素材装饰掩盖。
- 导入的自包含 Godot 副本放在忽略目录，复制后签名失效经本地临时签名解决，未修改系统证书或原可执行文件。自包含模式只隔离编辑器数据；Godot 自身 shader cache / 空 objectdb 目录曾写入本 Demo 专属用户目录，没有游戏存档。所有实际验证记录、日志和截图均在仓库忽略目录。
- 最终有窗口 `28` 项通过，含 `12` 张截图落盘；日志 `tools/runtime-intake/check-runs/topdown-3d/20260906T082203Z-49693.log`，结构化结果 `run-1788682924-49693/result.json`，原图 `assets/art-intake/2026-09-06-topdown-3d-preview/run-1788682924-49693/`。逐张审阅正常 / 空管 / 不透明、投影开关、改光向、设备前后、玻璃后 / 不透明遮挡、管前、转角与拉远；按钮走鼠标事件，移动走 Input / 物理，初始点由夹具定位，不冒称纯人工复测。
- 世界区域阴影开关变化 `40699` 像素，改光向变化 `116006` 像素；玻璃 / 不透明的角色局部区域变化 `2148` 像素，阈值和区域记录在 `image-comparison.json`。与二维 `run-1788677713/01-normal-flow.png` 直接对照，两边同为 `1440×810`，但模型、角色屏幕高度与地板不同，不声称严格等资产或性能 A/B。
- 客户端静态、文档 / 文本、差异空白与仓库治理通过（1577 文件）；未重跑完整客户端 Godot 套件或正式 Boot。默认交互窗口已打开并经原生窗口截图确认、置前保留。机制已经可以对照，量产美术、复杂透明交叉和大工厂性能尚未验证；等待萝卜SAMA选择画面路线，不宣布视觉通过或正式迁移。

### 历史验证

- 本轮小型化工厂视觉目标与文档同步完成后，`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh` 均通过，仓库治理检查 `1481` 个文件；未执行客户端或 Godot 检查。
- 产品方向文档批的 `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh` 均通过，仓库总门禁检查 `1473` 个文件；本工厂方案批四项检查再次通过，仓库总门禁检查 `1474` 个文件。该句只记录实现前的文档批，不代表后续客户端实现与 Godot 运行状态。
- 最新 `sh scripts/check-client.sh` 静态检查通过；最终 `./scripts/check-repo.sh`、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh` 与 `git diff --check` 均通过，仓库治理检查 `1480` 个文件。旧完整 `check-client --with-godot` 在焦点 / 缩放最后小修前通过，焦点专门 probe 通过；修后的 `formal-16x9-mouse-final.log` 在 16:9 窗口 `1440×810` 记录 `67` 项断言通过并包含储物箱真实鼠标点击，`formal-16x10-calibrated.log` 在 16:10 窗口 `1440×900` 记录 `6` 项断言通过。
- 自动复测通过传送到箱旁后真实按 `E` 打开设备面板；其他测试准备 / 清理直接调用建造模式进入 / 退出，部分菜单操作使用菜单信号，因此不冒称全链纯人工。截图记录的是内部渲染 `1440×810`，未抓取操作系统窗口外框。玩家操作读法与手感尚未评价。

## 风险与下一步

- 当前阶段转入小型化工厂视觉目标探索：只授权一张 16:9 概念图及有限修订，用于评审规模密度、生产可读性、投影模块感和材质气质；暂停新增玩法系统，用户评审前不批量生产 / 接入正式资产，不迁移 Web、不切换 3D、不重构客户端。
- 新增 [Small Chemical Factory Visual Target V1](../design/small-chemical-factory-visual-target-v1.md)。首轮使用内置 `image_gen` 默认模式调用 `1` 次，候选与完整提示词清单落在 `assets/art-intake/2026-09-05-small-chemical-factory-target/`；图像为 `1672×941` RGB PNG，SHA-256 为 `dead959c01776e521c3d0cc56669ba07e0696b326e87b04563cb4d9d61c8bfa4`。
- 首轮候选已形成连续厂坪、两排设备、中央贯通通道、后沿架空主管和前排传送物流。主模型审阅认为场景组织成立，但人物约为前排设备三分之一高、后排立面偏高；随后批准第 2 次内置编辑调用，仅放大人物、缩短前排侧壁并把后排罐体改得更短粗。v2 已单独落盘，SHA-256 为 `204ba5f72d8a493bdc4b2a21bd7afb3c424f6880c6d274a6e7ea4bcd6ff08d31`；本轮不执行第 3 次生成。
- 主模型最终推荐 v2 供萝卜SAMA评审，v1 保留为比例修订对照：连续厂坪、双排设备、贯通通道和三层管线已形成整体小厂组织，v2 的人物 / 低矮设备比例与顶面读法优于 v1。尚未解决画面细密度高于现有可点修资产、后排罐体重复，以及部分传送带 / 短接管混杂；两稿均不构成可实施物流蓝图，未做 32px 网格归一、色板量化、资产拆分、Godot 接入、运行时或功能验证。萝卜SAMA视觉评审结论待回填。

- 萝卜SAMA在隔离世界实测中指出两项未通过反馈：设备可见接口与相邻传送带没有形成连续连接感；建造态冻结移动且依赖近身 `E` 打开设备面板，使错放设备难以重新选中和调整。当前已把修复边界写入活跃专题：接缝必须服从真实端口、方向和物流邻接，断路 / 错向不得伪装成连接；建造态保留 `WASD`、无放置预览时允许鼠标选中已有设备并进入既有调整流程，同时保留近身 `E`、战斗屏蔽、UI 防穿透、放置预览点击优先与 `Esc` 分层退出。
- 已完成两项反馈实现与定向系统验证。储物箱使用 sprite local `(-46, 18)` / `(46, 18)` 的 `12×24` 左右接缝，由真实端口、当前模式、传送带邻接和方向共同决定显示；建造态保留 `WASD` / 近身 `E`，并支持无预览时鼠标点击已有设备进入既有调整流程。物流 / 建造专项分别通过 `180` / `152` 项断言，显式窗口 `1440×810` 的正式 `Boot` 路径通过 `59` 项断言，并实际验证载货传送带的 Adjust 禁用、点击不进入调整、原位置与货物保持；正确 supply 右接、transfer 左接、无带与错向原图已由主会话审阅，萝卜SAMA尚待实机复评完整操作手感。
- 本轮使用独立 `/Users/luobo/Code/RadishCatalyst/tools/runtime-intake/review-worlds/factory-feedback-fix-initial` / `factory-feedback-fix-final`，日志与原图分别保存在 `tools/runtime-intake/2026-09-05-factory-feedback-fix/logs/` 和 `assets/art-intake/2026-09-05-factory-feedback-fix-preview/`，没有覆盖上轮存档。首次物流定向运行暴露 `slice_player.gd` 类型推断编译失败，修成显式 `bool` 后才重跑取得通过；失败日志保留，不把其尾部误打印的通过文案计作证据。上一轮 `67 + 6` 项断言也不冒充本轮反馈证据。
- 本轮反馈收口后的 `sh ./scripts/check-client.sh` 静态检查、`./scripts/check-repo.sh`、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh` 与 `git diff --check` 均通过，仓库治理检查 `1480` 个文件。
- 其他旧产品、设计和功能文档仍可能包含 ARPG 优先叙述，后续按实际任务逐项评估；本轮不做全仓改写。
- 工厂片段上一轮合同、客户端实现与系统路径证据已完成；当前不宣称操作顺手或体验验证通过。
- 下一步从独立反馈初始档复评 `WASD`、鼠标 / `E` 双入口、调整与左右接缝读法；若反馈整改后的玩家证据仍不成立，再缩小 / 暂缓或重新评估同范围 Web 对照。
