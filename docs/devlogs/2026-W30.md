# 2026-W30 开发周志

更新时间：2026-07-26

## 本周目标

- 收口 L1 手持合成面板，启动 L2 核心功能化。
- 让修复后的核心成为真实的基地直供电源与中央仓库，并完成存档闭环。
- L2 人工收口后建立 L3 建造放置 + 扩展供电网可执行专题。
- 完成 L3-A 工业地板逐格归一、terrain atlas 接入与多尺寸平台视觉复核。
- 完成 L3-B 电力中继双态审阅、归一接入与工业地板实景视觉复核。
- 完成 L3-C 传送带四向直段生成、32px 归一入库与工业地板实景视觉复核。
- 完成 L3 通用建筑放置、扩展供电网、schema 4、自然资源自举和最终视觉修正收口。
- 建立并完成 L4 传送带物流，实现直线、转角、端点、三路合流、公平轮询、回压与 schema 5。
- 按文档先行建立 L5 反应器接入自动化专题，固定素材闸门、状态模型、存档边界与三包顺序。
- 完成 L5-A 催化剂货物生成、目标尺寸审阅、归一和运行时资产接入。
- 完成 L5 包 1–3、自然完整产线、双状态重启与人工收口，并启动最多 30 个世界的多存档列表专题。

## 完成情况

- 2026-07-21 萝卜SAMA确认 L1 已收口；三入口、L1 细专题与 arc 层状态同步更新。
- 新建 [Slice Core Functionalization V1](../features/slice-core-functionalization-v1.md)，完成 L2 开工前十问、玩家路径、状态 / 存档 / HUD / 验收与分包设计。
- 完成 L2 代码：核心修复后同一交互点切换为中央仓库；新增文本仓库面板（晶体 / 零件双向批量转移）、容量守恒转移 API、6 格核心直供查询、HUD 在线 / 仓库用量读法和合成 / 仓库面板互斥。
- `SliceSaveService` 升至 schema 3 保存中央仓库，同时兼容读取 schema 2 并以空仓迁移；存档路径改为可注入目录，正式运行仍使用原 `user://saves/slice`，运行时检查使用 `/private/tmp` 隔离目录。
- `Boot` 改为把菜单读取摘要所用的同一个 `SliceSaveService` 注入 `SliceWorld`，消除菜单与世界各自构造服务实例的重复接线，也让正式入口存读测试可安全注入隔离目录。
- 2026-07-21 萝卜SAMA正式入口人工复测 L2 通过：新档采集全部晶体，合成机械零件修复核心，打开中央仓库存取晶体与机械零件，完全重启后载入并再次核对数量、修复态与直供状态均成立；L2 正式收口。
- 建立 [Slice Building Placement And Power Grid V1](../features/slice-building-placement-and-power-grid-v1.md)，完成专题十问、建筑配方 / 套件、六类通用网格放置、错放纠正、核心 + 中继供电图、schema 4、旧档迁移、正式入口证据、五类素材批次和五个实现包定义。
- 审计当前采集器专用放置：`SliceWorld` 同时承载携带布尔、固定纹理 ghost、2×2 晶体地与 62×62 物理查询、生成、HUD / 玩家分支和 `collectors` 存档；L3 定档为切片专用“建筑定义 + 放置控制器 + 通用实例 + 窄行为组件”，不复活冻结旧 `BuildSystem`，不建设过度通用 ECS。
- L3-A 审阅定稿 V2 为唯一源；新增 `normalize_l3a_industrial_floor.py` 校验源哈希并按 gutter 边界裁切 16 格，共享色板归一为 32px，输出 128x128 `industrial_floor_terrain.png` 与 `industrial_floor_tileset.tres`。三种尺寸平台 QA 覆盖外角、内角与中心变体，本包不编写建造玩法代码。
- L3-B 电力中继生成 V1 / V2 两组断电 / 通电双态；初步联系表审阅曾误选状态读法清楚的 V2，萝卜SAMA按主体原型与投影方向复核后将两张全部判废：V1 斜向下偏航，V2 是矮胖平台设备而非电线杆式中继。
- L3-B 重新生成 V4 / V5：V4 已纠正为细高电线杆但仍有斜向偏航，修订为正交网格相机方位后 V5 通过“细高电线杆 + 高机位屏幕轴对齐”两个首轮硬判据；原图与生成源、哈希和完整提示词已记录到 `2026-07-21-batch03/_manifest.md`，目标尺寸与共享色板仍待独立审阅。
- L3-B 独立审阅确认 V5 在 64px 高仍可辨认，但原图两态存在顶部琥珀 / 青色电极不一致和脉冲过长；新增 `normalize_l3b_power_relay.py`，固定断电半边为唯一公共几何，以 13 倍整数块缩放归一到 32×64，再只替换红 / 青状态灯、双电极窗口和 4px 短脉冲。输出 `power_relay_unpowered.png` / `power_relay_powered.png`，不追加生成调用。
- 完成 L3-C 传送带素材开工闸门与正式提示词：锁定同图四帧、左到右上 / 右 / 下 / 左、`1×1` 直段、32px 目标尺寸，以 belt slat + 驱动 / 从动滚筒表达方向；排除程序旋转、文字 / UI 箭头、物品、动画、转角、交叉、合流与分流。本会话只准备真相源，不跨素材类别调用图像生成。
- 2026-07-25 L3-C V1 挂像素锚点生成并一次通过主体 / 投影硬判据；独立审阅确认同一设备四向、屏幕轴对齐和 32px 方向读法成立。新增 `normalize_l3c_conveyor.py` 锁定源哈希，以统一 `384×384` 窗口和 `12×` 整数块降采样输出四个 32×32 帧，共享 17 色，未追加 V2。
- 补齐 L3-D 反应器四向端口提示词与首张硬判据：锁定现有反应器为唯一主体，四帧按琥珀出料口朝上 / 右 / 下 / 左排序，暗青入料口在对侧；排除主体漂移、斜向端口、货物、连接管网和加工状态变化。
- L3-D V1 首张硬判据失败并按规则停手：四帧主体相互接近，但相较定稿侧壁被拉高、框架和底座被重设计；琥珀出料口大致四向变化，青色入料口却固定在屏幕下缘或缺失。提示词改为 precise-object-edit：先像素级复制母版四次，每帧只添加恰好两个刚性 180° 对置并一起旋转的端口，禁止第三固定服务口。
- L3-D V2 端口基数、严格对置和上 / 右 / 下 / 左顺序通过，但青色顶腔、结构灯与机身细节仍逐帧重绘；萝卜SAMA确认停止生成 V3并换为确定性离线派生。新增 `normalize_l3d_reactor_ports.py` 锁定现有 `96×90` 反应器哈希，只覆盖两个受控端口区域，输出 `reactor_up/right/down/left.png` 与工业地板联系表。
- 建立 L3-E 储物箱开工闸门与正式提示词：储物箱使用单个双向物流舱口，不复制反应器双端口；参考图核对后把主体身份纠正为四罐 `2×2` 储存组。V1 / V2 的舱口基数与上 / 右 / 下 / 左顺序均通过，但主体两次都被简化为三罐三角布局；第二次已使用独立高分辨率四罐参考并显式锁定四个罐位，仍重复失败，故停止且不生成 V3，升级给萝卜SAMA做路线复盘。
- 萝卜SAMA确认 L3-E 换介质后，新增 `normalize_l3e_storage_hatch.py` 锁定现有 `87×88` 四罐主体，每帧只覆盖一个受控舱口区，输出 `storage_up/right/down/left.png` 与工业地板联系表；四向静态素材硬闸门完成，未调用 V3。
- L3 实现包 1 完成：建筑套件纳入 `Inventory`，新增切片专用建筑定义、目录、占用索引、放置控制器与通用实例；工业地板和采集器共用真实素材预览、网格校验与提交管线，支持 `R` 旋转、连续放置、`Esc` 无损取消及短失败原因，旧 `carrying_collector` 仅保留存档迁移读取。
- L3 实现包 2 完成：补齐反应器、中继、传送带、储物箱配方和定义，方向性设备按旋转值选择审定上 / 右 / 下 / 左固定帧；工业地板成为四类设施完整足印的强制支撑。新增建筑操作面板与储物箱窄状态，调整保留实例 ID / 内容，取消恢复原位，拆除须二次确认并全额返还；地板承载、背包容量、采集器缓冲和储物箱库存均有明确门控。
- L3 实现包 3 完成：新增派生式 `SlicePowerGrid`，修复核心是唯一源，中继按 6 格欧氏距离多跳可达并提供 4 格设备覆盖；建筑定义承载可旋转供电端口。核心修复、建筑放置 / 调整 / 拆除后统一全量重算，不保存供电布尔或边。中继切换审定青 / 红双态 sprite，采集器 / 反应器只增加小型状态灯；采集器改为逐实例 tick 进度，断电暂停、恢复后续跑。
- L3 最终人工审阅未放行：运行时采集器虽来自 07-18 修正轮，仍明显向右下偏航；三段中继只有状态灯、没有关系连线；角色与设备缺少统一接地阴影且建筑按足印中心排序，整体像贴在地面上。决定不切 L4，新增包 5 修正采集器、供电树和空间接地。
- L3 包 5 第一段实现：`SlicePowerGrid` 为每个通电中继派生唯一稳定父边，`SlicePowerLinkLayer` 只渲染核心到中继的低透明暗线 + 青线供电树，调整 / 拆除 / 重建 / 核心离线同帧刷新；连线仍不进存档。角色、核心与通用建筑复用 `GroundShadow`，该组件补齐 `AnimatedSprite2D` 支持；可放建筑根节点改在足印底边参与 y-sort，碰撞、交互、sprite 和状态灯保留在逻辑中心。
- L3-F V1–V4 分别暴露身份、高度、轴向与承重路径问题；V5 以 V4 为精确编辑目标，把支撑重构为外围四角立柱、高位方框、正交横梁、中央滑台与向下钻轴，四项首轮硬判据同时通过。新增 `normalize_l3f_collector.py` 锁定 V5、晶体地和工业地板哈希，生成 `99×120` / `89×108` / `81×98` 三档透明共享色板候选；联系表复核选择 `89×108`，累计调用 `5`、第二轮停在 `2 / 3` 后转入运行时接入。
- L3 包 5 换装完成：`89×108` RGBA 候选以原哈希 `3c224fae...7184a1` 替换运行时采集器；sprite / ghost 统一底边居中，碰撞与交互维持 `64×64`，右前状态灯、`GroundShadow` 和足印底边 y-sort 完成对齐，不改配方、采集、供电、碰撞规则或存档结构。
- 建立 [Slice Conveyor Logistics V1](../features/slice-conveyor-logistics-v1.md) 并完成开工十问、货物槽 / 方向 / 转角 / 合流、储物箱端点、回压守恒、schema 5、素材闸门和三个实现包定档。
- 新增 `normalize_l4a_crystal_cargo.py`，锁定已审定 `21×22` 小型晶体簇哈希并按固定逐行掩码分离中央单晶，输出 `10×12` RGBA `cargo_crystal.png`；不缩小整簇、不调用 image_gen。
- L4 包 1 完成：新增 `SliceConveyor`、`SliceLogisticsGrid` 与储物箱物流端口，单格单槽晶体按方向逐格移动；源箱输出、目标箱入库、满箱回压、带货调整 / 拆除门控和储物箱 `[3]/[4]` 装取成立。初版外部大步长会降低吞吐，专项断言没有放宽，改为内部固定 60Hz 子步进后 0.10s / 0.25s 结果一致。
- `SliceSaveService` 升至 schema 5，传送带白名单保存 `cargo {item_id, progress}` 与未来合流游标；schema 4 的空带状态直接兼容，schema 2 / 3 迁移保持。派生物流邻接、端口连接和货物世界坐标不入存档。
- L4 包 2 完成：`normalize_l4b_conveyor_topology.py` 锁定四向直段哈希，离线派生 8 张转角、4 张源端点和 4 张目标端点 `32×32` RGBA 固定帧；运行时按单上游、储物箱连接与输出方向选择，不强转 sprite 或程序画轨道。
- `SliceLogisticsGrid` 同帧派生端点 / 直段 / 转角，货物按入口边→格心→出口边的正交折线移动；双转角、四角满环路回压、布局重建去除旧分类和资源路径接线使物流专项增至 50 项，schema 115 项保持通过。
- L4 包 3 完成实现与自动 / 正式入口证据：同一归一脚本新增 16 张双 / 三路合流固定帧；`SliceLogisticsGrid` 改为成熟货物先计划、按接收带 `merge_cursor` 和稳定上游 `instance_id` 轮询后提交，失败源保持原归属。实际赢家入射侧驱动折线路径，重启时由持久游标恢复，不扩 schema 5 cargo 结构。
- 双路 6 轮、三路 9 轮公平性、步长独立、输入节点逆序、满带守恒和 merge cursor 存读使物流专项增至 82 项、schema 专项增至 117 项。
- 萝卜SAMA载入三源各 20 晶体、目标空箱的预制正式槽演示档，复核持续合流、目标满载和整线回压后确认无问题；复核前主档 / 备份档先独立备份，结束后按原 SHA-256 一一恢复。L4 正式收口。
- 建立 [Slice Reactor Automation V1](../features/slice-reactor-automation-v1.md)，完成 L5 十问 / 五问、固定 `2 crystal → 1 catalyst / 10s`、四向对置双端口、独立双缓冲、断电暂停、输出回压、schema 6 迁移、素材硬闸门和三个实现包定档。
- L5-A 首轮三次生成中 V1 因方形机座过强判废，V2 / V3 进入目标尺寸审阅；六档联系表与晶体对照表确认 V2 `12×8` 在货物身份、带面比例和方向留白间最平衡。
- 新增 `normalize_l5a_catalyst_cargo.py`，把审定 V2 机械复现为 5 色透明 `cargo_catalyst.png`，并生成跟踪的 Godot `.import` sidecar；未启动 Godot、未进入 L5 包 1。
- L5 包 1 完成：新增 `SliceReactor` 场景 / 窄脚本，输入 `2`、输出 `1` 的独立 `Inventory` 缓冲执行固定 `2 crystal → 1 catalyst / 10s`；开工原子扣料、断电暂停、恢复续跑、输出占用阻止下一批，调整 / 拆除沿内容门控拒绝在制或带货设备。
- `SliceBuildingDefinition` 新增命名机器输入 / 输出端口及四向旋转派生；基准朝上的出料位于上边中格、入料位于严格对侧，连接格均在足印外一格。包 1 只建立端口契约，不提前接入包 2 物流。
- `SliceSaveService` 升至 schema 6，精确保存反应器双缓冲、`processing` 和进度，停止写入旧 `catalyst_count` / `reactor_active`；schema 2–5 旧催化剂使用受信路径无损并入核心仓库，旧激活标记丢弃。
- L5 包 2 完成：`SliceLogisticsGrid` 把反应器注册为输入 sink 与输出 source，晶体只进输入、催化剂只从输出上带；运输白名单和 `SliceConveyor` 扩为晶体 / 催化剂独立 sprite，错误方向、满缓冲、满带和满箱均保持货物原所有权。
- 建筑操作面补齐断电、缺料、就绪、加工中、待出料、出料堵塞，显示输入 `x/2`、输出 `x/1` 与秒数；储物箱可见并可存取催化剂，传送带提示区分双货物。在制、输入和输出可在背包容量足够时一次性回收，容量不足整次不动，带内容 / 在制反应器禁止调整或拆除。
- L5-B 不调用图像生成：`derive_l5b_reactor_processing_overlay.py` 确定性输出两张 `12×7` 固定像素脉冲帧，只覆盖顶腔加工反馈；运行时仅切帧和在断电时压暗，四向主体、端口、足印与锚点不变。
- L5 包 3 自动 / 正式入口证据完成：真实 `Boot → 新游戏` 逐簇采集 25 个有限晶体，以 `B / 数字键 / R / E` 恰好完成核心修复、三段中继和首台采集器；随后分批领取 37 个可再生晶体，在 30 格背包容量内自然合成并放置 19 格工业地板、两箱、两带与通电反应器，没有注入背包、套件或最终世界状态。
- 完整产线装入两批 4 晶体后，在制约 2 秒保存并从真实载入按钮恢复；首件到箱后由玩家把空出口带调成错误方向，第二批完成为待出料并再次重启，输出 `1/1` 与错误带向均保留。玩家纠正带向后第二枚催化剂入箱，五段产线最终无隐藏货物。
- 萝卜SAMA使用保留的 L5 隔离主档 / 备份档和人工启动器，从真实 `Boot → 载入存档` 复核最终五段产线并确认通过；L5 与配方加工 arc 正式收口。
- 建立 [Slice Multi-World Save List V1](../features/slice-multi-world-save-list-v1.md)：最多 30 个世界，一个世界一个稳定目录 / autosave / 三份备份；列表读轻量元数据，当前单档无损导入，回收可恢复，自动化可注入独立世界根并保留命名复核世界。
- 多世界存档包 1 完成：新增 `SliceSaveCatalog` 管理可注入根、创建 / 枚举 / 重命名 / 回收 / 恢复、稳定排序和 30 世界上限；单个元数据损坏项留在列表但不可载入，不阻断其他世界。世界 ID 不随显示名变化，路径穿越输入被拒绝。
- `SliceSaveService.for_world` 以单世界目录使用 `autosave.json` 与 `backups/autosave.bak.1–3.json`；第四次保存后主档与三代备份分别还原第 4 / 3 / 2 / 1 代状态。旧构造方式仍使用 `slice_world.json` / `.bak`，既有正式脚本和测试隔离目录不被破坏。
- 当前单档迁移先走现有 schema 2–6 校验，在 `worlds/` 外隐藏暂存目录写入两代世界档并读回关键状态，全部成立后才原子发布；旧主档 / 备份保持原位，重复迁移在已有世界时安全跳过。
- 多世界存档包 2 完成：`StartupMenu` 以可滚动列表和详情区接入创建并进入、载入、重命名、二次确认回收、恢复、30 上限与损坏反馈；`Boot` 不再持有默认固定单档，只在创建或选择稳定世界 ID 后注入对应 `SliceSaveService`。仅剩损坏 / 回收项时仍可进入列表治理。

## 关键决策

- 修正 arc 层间依赖倒置：L2 收窄为固定核心的“直供电源 + 中央仓库”，不依赖通用放置；电线杆 / 中继的制作、放置与扩展供电网并入 L3。
- L2 复用现有核心修复态素材，不生成新素材、不使用程序绘制范围圈；L3 新素材仍需萝卜SAMA另行授权。
- 核心电源在线由 `core_repaired` 推导，不新增冗余存档布尔值；中央仓库使用通用 `Inventory`，schema 3 兼容读取 schema 2。
- 萝卜SAMA指出仓库已有正式入口自动运行与截图方案后，将 `godot-runtime-verification-guide.md` 从参考方法提升为 `AGENTS.md` / `CLAUDE.md` 硬约束：玩家可见功能必须补真实 `Boot`、输入链、有窗口截图、实际审图与周志证据，纯 headless 断言不得替代。
- 复盘一次在同一 Codex 工具调用中读取 5 张截图导致会话卡顿的失误；新增单会话默认最多 3 张图片进入上下文的统一上限，多图改用磁盘联系表审阅。
- 新增 `scripts/create-screenshot-contact-sheet.sh`，把任意数量自动化截图在磁盘压成一张带序号总览，并输出序号到原图路径映射，避免再次把整批原图送入会话。
- 2026-07-21 经萝卜SAMA明确要求，将单会话 3 张图片读取限制改为默认软上限：达到后先报告并停手，用户明确要求当前会话继续时可按每次至多 3 张追加读取。
- 2026-07-24 经萝卜SAMA架构决策，将图像生成的绝对 3 次硬上限改为分轮授权：单会话默认每轮至多 3 次，每轮结束必须先落盘、更新 manifest 并报告；只有获得当前会话继续生成的明确授权后才能追加下一轮至多 3 次，且一次授权不得解释为无限续跑。一个素材类别一个会话、生成与审阅 / 接入分离等边界保持不变。
- 2026-07-25 经萝卜SAMA进一步明确，将“生成与后续阶段必须分会话”改为默认护栏：生成落盘并更新 manifest 后，用户可点名授权当前会话切换到审阅、归一等具体阶段；授权只覆盖被点名阶段，不自动扩展到其他素材、Godot、提交或无限续跑。
- 2026-07-25 经萝卜SAMA再次明确，将“不同素材类别必须分会话”也改为默认护栏：前一类别收口并更新 manifest 后，用户可点名授权当前会话切换到下一类别；新类别必须重新读取真相源、使用独立批次与 manifest，并从自己的首轮 `0 / 3` 计数，授权不自动覆盖再下一个类别。
- 2026-07-26 萝卜SAMA明确自动化测试后的人工复核边界：隔离存档可在开跑前清理以建立新档基线，但通过后不得删除最终主档 / 备份档；必须报告绝对路径并保留可直接载入的复核状态，不覆盖用户正式存档。规则已同步到运行时验证指南、`AGENTS.md` 与 `CLAUDE.md`。
- 2026-07-26 萝卜SAMA确认多存档升级方向：槽位单位定为“世界”而非手动快照，最多 30 个；`SliceSaveService` 保持单世界职责，新增 `SliceSaveCatalog` 管理目录、元数据、选择、迁移和回收，符合长期世界 / 玩家 / 角色归属模型。
- L3 配方产出统一为 `Inventory` 中建筑套件，放置提交时才消费，取消不损失；错放设施可调整或全额拆回套件，带内容设备与被支撑地板受门控。
- L3 电网定为二值可达图：核心 6 格直供，中继 6 格接力、4 格设备覆盖，结构变化后从核心全量重算；供电布尔与邻接边不进存档，载入建筑拓扑后派生。
- L3 存档规划升 schema 4，以稳定建筑实例统一承载地板和设施；schema 2 / 3 迁移保留旧采集器与携带套件，不因新供电规则删旧布局。
- L3-A terrain atlas 固定行契约：中心四变体；北 / 东 / 南 / 西边；西北 / 东北 / 东南 / 西南外角；同序四个内角。逐格裁切后再缩放，禁止把生成图 gutter 或整张 128px 平台压进单格。
- L3-B 中继原型锁定为窄 1×1 底座的细高电线杆：杆体直立居中、横担双绝缘子、总高至少为底座宽 1.5 倍；任何矮胖平台设备、斜向下偏航、倾斜杆体或正立面主导稿直接判废。首张未过原型 / 投影硬判据时必须先修提示词，不沿原提示继续消耗生成额度。
- L3-B 双态不直接使用生成图的两份独立主体；运行时资产共享同一份杆体与结构灯像素，仅允许故障灯、通电灯、电极窗口和短脉冲变化，避免状态切换时形体抖动。
- L3-C 四向帧使用同一尺寸源窗口和同一整数缩放因子，不按各对象最大边分别适配；高饱和暖灯 / 青色状态像素在共享量化前先吸附到项目语义锚点，避免四向色义漂移。
- L3-D 生成失败的根因是生成模型无法保证母版 raster 的逐像素复用，而不是端口方向设计失败；路线由“继续收紧生成提示词”切换为“固定已审定主体 + 受控端口覆盖层”。离线脚本只派生静态资产，运行时不绘制、不旋转，也不改变像素风格锚点。
- L3-E 储物箱在 L4 作为可收可发的物流端点，只预留一个双向舱口；L3 不提前定义输入 / 输出优先级、搬运或空 / 满状态动画。
- L3-E 与 L3-D 采用同一介质治理原则：生成候选可验证方向语义，但不能替代 raster 身份不变量；最终四向帧固定复用定稿主体，只允许单个舱口覆盖层变化，运行时不绘制、不旋转。
- L5 不复活旧全局 `catalyst_count` / `reactor_active` 记账玩法：每台反应器以独立输入 / 输出缓冲和在制批次为权威，schema 6 把旧顶层催化剂无损迁入中央仓库，供电与物流邻接仍只派生。
- L5-A 定稿为扁横深青金属壳 + 琥珀核芯的 `12×8` 货物，与细长亮青晶体形成形状和色义双重分离；硬闸门关闭后不追加同类生成。

## 验证记录

- Godot 4.7 headless `--import` 完成，新脚本全局类注册成功并生成 `.uid`；仅出现沙箱内系统 CA / editor settings 写入告警，无项目解析错误。
- 隔离运行时真实 `SliceWorld` 重启验证 25 项断言全过：未修复门控、6 格供电边界、存取与容量守恒、schema 3 仓库还原、schema 2 背包保留与空仓迁移。
- 按 [Godot Runtime Verification Guide](../reference/godot-runtime-verification-guide.md) 补跑有窗口正式入口：真实 `Boot → 新游戏`、`E` 修核心、`E` 开仓、数字键 `1/3` 存入、`Esc` 关闭、重建 `Boot → 载入存档`、`E` 重开、数字键 `2` 取出，25 项断言全过。
- 有窗口测试抓取 `l2_core_online.png`、`l2_core_storage_open.png`、`l2_core_storage_filled.png`、`l2_core_storage_loaded.png`、`l2_core_storage_withdrawn.png` 至忽略提交的 `assets/art-intake/2026-07-21-l2-preview/`；AI 视觉复核确认核心在线提示、面板文字、存入 / 载入 / 取出数量均清楚且无截断。
- 萝卜SAMA人工路径复测覆盖新档采集 / 合成 / 修复、晶体与机械零件双向存取、完全重启后的载入复核，明确回复通过；L2 人工证据闭环。
- `sh ./scripts/check-client.sh` 全绿；`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 全绿。
- L3-A 归一脚本确认 V2 SHA-256 为 `f7463ba...b36b948c815b86eb36ea`，产物为 128x128、16 个独立 32px tile、实际 11 色、16 格像素指纹均不同；中心变体最差边缘平均 RGB 差为水平 45.59 / 垂直 37.47。
- 实际读取 `l3a-normalization-qa-contact-sheet.png` 复核 3x3、6x4、10x6 带 2x2 开孔平台：外圈连续、内角方向正确、中心变体能打散重复，无错格、拉伸或 gutter 泄漏；机械格线与铆接工业地板语义一致。
- L3-A 最终执行归一脚本复跑、Python 编译检查、`sh ./scripts/check-client.sh`、`./scripts/check-text-files.sh`、`./scripts/check-docs.sh` 与 `git diff --check`，均通过；按本包边界未启动 Godot。
- L3-B 联系表编号 01 V1·64px、02 V2·64px、03 V1·48px、04 V2·48px；联系表证明 V1 缩小后细节粘连、V2 状态灯可读，但萝卜SAMA进一步按更高优先级的原型 / 投影标准复核：V1 斜向偏航、V2 非电线杆式剪影，均判废且不进入归一。
- L3-B V5 原图为 1536×1024，SHA-256 `310357e5007feec940b077864230ae36cc632e71a5836c7690f2768e912c6ec0`；归一脚本校验两个 292×789 对象边界，以整数因子 13 输出 32×64 双态帧。断电帧 22 色、通电帧 23 色，两态仅受控发光像素不同；输出 SHA-256 分别为 `1832fa38...1c0558bae39294f5` 与 `2c6ca956...61a6617358f483118cc4396`。
- 实际读取 `power_relay_states_contact_sheet.png` 做两轮视觉复核：首轮发现细杆在工业地板上过暗，将公共主体亮度从平均 `54.14` 调至 `63.41`；第二轮确认杆身 / 底座与地板稳定分离，红色故障灯、青色通电灯和 4px 顶部短脉冲均无需文字可辨。本包未启动 Godot。
- L3-C 源图为 2172×724，SHA-256 `513dd1e...e7271b29`；归一脚本校验四个源对象边界，输出上 / 右 / 下 / 左四个 32×32 RGBA 帧，共享 17 色、平均明度 `62.88–69.23`，对向帧 Alpha 外接尺寸差不超过 1px。
- 实际读取 `conveyor_cardinal_contact_sheet.png` 复核隔离帧与工业地板实景：四组人字带板和琥珀输出辊分别明确指向上 / 右 / 下 / 左，足印均为屏幕轴对齐，未出现货物、转角、合流、分流或色域跳变。本包未启动 Godot。
- L3-D 四帧均为 `96×90`、22 色；遮掉四个受控端口区后的主体 SHA-256 统一为 `dfbdd2db...13d010b9`，每帧仅对应的两个对置区域可与母版不同。实际读取 `reactor_cardinal_ports_contact_sheet.png` 复核隔离帧与工业地板实景：琥珀出料分别朝上 / 右 / 下 / 左，青色缺口入料固定在对侧，主体无抖动，无第三彩色服务口、箭头、货物、管线或加工状态。本包未启动 Godot。
- L3-E V1 原图为 `2172×724`，SHA-256 `1004b1b4...2a1915a`；实际读取运行时储物箱、高分辨率双对象源图与生成结果，确认批准身份为四罐 `2×2`，而 V1 四帧均错误减少为三罐，因此不进入目标尺寸或工业地板审阅。
- L3-E V2 原图为 `2172×724`，SHA-256 `b86aa38f...d629564a`；独立四罐参考与四罐位强约束仍未改变三罐结果，证明继续追加同类提示词不成立；V2 同样不进入目标尺寸或工业地板审阅。
- L3-E 四帧均为 `87×88`、23 色；遮掉四个受控舱口区后的主体 SHA-256 统一为 `33b788d6...d9baceda1`，每帧仅对应的一个区域可与母版不同。实际读取 `storage_cardinal_hatch_contact_sheet.png` 复核隔离帧与工业地板实景：四罐主体无抖动，单个双向舱口分别位于上 / 右 / 下 / 左，无第二舱口、货物、管线、开盖或容量状态变化。本包未启动 Godot。
- L3 实现包 1 的隔离运行时检查通过 42 项断言；有窗口正式入口从真实 `Boot → 新游戏` 使用 `B / 2 / 3 / E / Esc` 完成地板制造与连续放置、取消保留、采集器岩地拒绝 / 晶体地放置及 10 秒产出，25 项断言全过。
- 有窗口测试抓取 `01_floor_valid_preview.png` 至 `05_collector_placed.png` 到忽略提交的 `assets/art-intake/2026-07-25-l3-package1-preview/`；实际读取联系表（SHA-256 `b837eac312352c8fc80bf30ee19e67bc0105358529a551a04ec004fcac3d6b4f`），确认真实 sprite / tile 的绿红预览、工业地板落地、短失败原因、HUD 与操作提示均清楚且无截断遮挡。
- Godot 4.7 导入与 `sh ./scripts/check-client.sh --with-godot` 全绿；新建筑放置检查已接入 Bash / PowerShell 客户端入口。联系表脚本改为把 Godot 内部日志写入 `/tmp`，避免沙箱环境因默认日志目录不可写而中止。
- L3 实现包 2 隔离运行时检查通过 81 项断言，覆盖六类建筑定义 / 七项配方、固定方向帧、工业地板支撑、保 ID 调整、取消恢复、三类拆除门控、二次确认返还和中央仓库套件往返；包 1 的 42 项断言保持通过。
- 有窗口正式入口从真实 `Boot → 新游戏` 使用 `B / 4–7 / R / E / 1 / 2` 完成七配方面板、缺地板红色预览、四类建筑同场放置、反应器调整换向和传送带二次确认拆除，40 项断言全过；六张截图位于 `assets/art-intake/2026-07-25-l3-package2-preview/`。
- 实际读取 `l3-package2-contact-sheet.png`（SHA-256 `ccc608e65ed4b1abb8d81d105de7746d2078abb04b9d1b7983503545622b8ea1`）：配方无截断，红 / 青真实素材预览、四类设备、操作面板、换向结果和拆除确认均清楚，无关键遮挡；完整 `sh ./scripts/check-client.sh --with-godot` 全绿。
- L3 实现包 3 隔离检查通过 67 项断言，覆盖端口旋转、三跳中继、孤立中继拒绝、设备覆盖、断连影响数、同帧掉电、双态素材和采集器断电进度；包 1 / 2 的 42 / 81 项保持通过。
- 有窗口正式入口从真实 `Boot → 新游戏` 实际修复核心并以 `B / 3–5 / E / Esc / 1 / 2` 搭建三段电网、调整 / 拆除桥接中继和同格重建，42 项断言全过；七张截图位于 `assets/art-intake/2026-07-25-l3-package3-preview/`。
- 实际读取 `l3-package3-contact-sheet.png`（SHA-256 `4510a3a45d2938f7f7a940282db3b818aa0cb660e81e0c57d15745fa7a672a1d`）：孤立中继红色预览、三段青色中继、两台设备小状态灯、影响 2 台文案、断连红态和重建青态均清楚；无常驻范围圈、调试连线、物流或加工表现。完整 `sh ./scripts/check-client.sh --with-godot` 全绿。
- L3 实现包 4 完成：新增独立 `SliceBuildingSaveCodec`，schema 4 统一序列化六类建筑的稳定 ID、定义 ID、原点格、旋转、白名单内部状态和 `next_building_serial`；供电布尔、邻接边与覆盖结果不落盘。schema 2 / 3 旧采集器按顺序获得稳定 ID，缓冲保留、tick 进度从零开始，旧携带态回收到背包套件，schema 2 以空核心仓库迁移。
- schema 4 对重复 ID、未知建筑、非法旋转、越界、同层重叠、工业地板支撑、状态字段和储物箱物品 / 容量做候选级校验；坏主档整体失败后读取备份，不静默丢建筑。加入自然资源预算后专项检查 102 项断言通过，完整 `sh ./scripts/check-client.sh --with-godot` 全绿。
- 有窗口正式入口沿真实 `Boot → 新游戏` 完成多跳电网、桥接调整 / 拆除 / 重建，写入 schema 4 后销毁 Boot，再从真实 `Boot → 载入存档` 还原全部建筑、稳定 ID、采集器缓冲 / tick 和派生电网，78 项断言通过；另以 45 项断言完成无注入自然采集 / 合成 / 修核心 / 三跳采集器自举。十张截图位于 `assets/art-intake/2026-07-25-l3-package4-preview/`。实际读取 `l3-package4-contact-sheet.png`（1280×1960，SHA-256 `0bd323164087801ee99462f2082e628dcbc99fcb004c99b9ecbe9e9143e68bd4`），确认非法短原因、多跳青态、断连红态、重建、读档还原与自然自举画面一致，无物流 / 加工越界表现。
- 包 5 新增断言后，建筑放置检查 47 项、供电图检查 79 项通过；完整 `sh ./scripts/check-client.sh --with-godot` 全绿。真实 `Boot → 新游戏` 快速自举截图 `assets/art-intake/2026-07-25-l3-package5-preview/01_power_links_and_grounding.png` 已实际查看：三段供电树连续且低干扰，角色、核心和设备右下接地阴影可见；旧采集器斜向问题仍成立，等待 L3-F 换装，不把本图当作包 5 收口证据。
- 换装后完整 `./scripts/check-client.sh --with-godot` 全绿：建筑放置 54 项、供电图 79 项、schema 102 项；真实 `Boot → 新游戏` 使用 `/private/tmp/radishcatalyst-l3-package5-v5-save` 隔离存档，包 5 场景 44 项断言通过并在退出时清理隔离目录。
- 三张 `3024×1701` 正式入口截图与 `1280×784` 联系表位于 `assets/art-intake/2026-07-25-l3-package5-v5-preview/`，联系表 SHA-256 `01da4031...c81d02`。实际读取联系表与前景关键原图：采集器不再斜向偏航，钻轴 / 暗孔 / 四角框架与高度清楚；角色前后遮挡、接地阴影、右前状态灯及合法 ghost 无漂移，供电树连续且不抢主体，未见贴地、悬浮或严重遮挡。
- 萝卜SAMA人工确认包 5 画面无阻碍问题；L3 的实现包 1–4、25 晶体自然自举和最终视觉修正证据闭环，专题正式收口。下一步按文档先行建立 L4 传送带物流细专题。
- L4-A 归一复跑确认 `cargo_crystal.png` 为 `10×12`、76 个不透明像素，SHA-256 `e47cae97c2d145a31b36a31d42747132f789af6b2a0d422a784d614c42a879bd`；联系表 SHA-256 `6283e8804b300f6aa9d11c48e6064ae25386ce029877de134451b028522d9a67`。实际读取联系表确认单晶在四向带面均清楚，带板方向和琥珀输出端未被遮没。
- 完整 `sh ./scripts/check-client.sh --with-godot` 全绿：L4 物流专项 25 项、schema 5 / schema 2–4 兼容 115 项；既有 L3 放置 54 项、建筑操作 81 项、供电 79 项保持通过。
- L4 包 2 完整 Godot 检查全绿：物流专项 50 项、schema 115 项，既有 L3 54 / 81 / 79 项保持通过。真实 `Boot → 新游戏` 使用新建 `/private/tmp/radishcatalyst-l4-package2-save.*` 隔离目录，玩家 `E` / `[3]` 装入 4 晶体后通过双转角；满箱四格回压、解除堵塞、最终 `4/20` 和 schema 5 共 26 项断言通过，未接触现有存档。
- 包 2 三张 `3024×1701` 截图位于 `assets/art-intake/2026-07-25-l4-package2-preview/`；联系表 SHA-256 `0fa67d5bded91e93e3990c0cd1fb6cd8c562995877c90e2411a429e3b51fd957`。实审确认路线读作“右→下→右”，货物不斜穿、不悬浮、不贴箱，端点与目标箱面板无漂移。
- L4 包 3 完整 Godot 检查全绿；真实 `Boot → 新游戏` 使用新建 `/private/tmp/radishcatalyst-l4-package3-save.*` 隔离目录，三只源箱均以 `E` / `[3]` 装入 3 晶体。三路运动、目标满载五格堵塞、schema 5 重启、游标 / 入射侧恢复、续跑至目标 `9/20` 共 45 项断言通过，未接触现有存档。
- 包 3 五张 `3024×1701` 截图位于 `assets/art-intake/2026-07-25-l4-package3-preview/`；联系表 SHA-256 `ba1e3a3e686706c6c8941e03ff420d1e95d1c537d8bba2a31a2c35e02ecdc824`。AI 实审确认三向入口与唯一出口清楚，货物无斜穿、悬浮、贴箱或中心吞没；随后萝卜SAMA通过预制演示档完成人工确认，L4 证据闭环。
- 有窗口正式入口使用 `/private/tmp/radishcatalyst-l4-package1-save-windowed` 隔离存档，从真实 `Boot → 新游戏` 以 `E` 打开源箱、`3` 存入 4 晶体，完成直线搬运、schema 5 保存、销毁 Boot、正式“载入存档”与目标箱入库，26 项断言通过。
- 四张 `3024×1701` 原图与联系表位于 `assets/art-intake/2026-07-25-l4-package1-preview/`，联系表 SHA-256 `af715f1729487e1e8b1cda3c85d12e4258e81ca922228432e39f78da724e94e7`。实际读取联系表和重启关键原图：多件晶体落在带面轨道内，大小与 1×1 带格协调，方向滚筒仍可读；重启货物不丢，目标箱面板为晶体 `4` / 容量 `4/20`，无漂浮、压带或箱体遮挡。
- 今日收尾复核覆盖 2026-07-25 的 17 个提交、179 个变更文件及其代码 / 素材 / 文档关系；对照当前实现确认 `SliceRecipes` 为七项配方、L3 人工路径已随包 5 完成、L4 已收口且 schema 5 为当前运行时事实，并据此修正阶段入口、L3 专题与本周周志中的过期口径。
- L5-A 生成累计 `3 / 3`；审阅累计读取 5 张图片（3 张生成结果 + 2 张联系表）。`cargo_catalyst.png` 为 `12×8` RGBA、92 个不透明像素、5 色、18 个琥珀核芯像素，SHA-256 `76187e97bcd025c2a12ad2348e3922a320165dfc9abd21f059da5c5c053f6a02`；归一复跑、Alpha / 色板 / 晶体差异和 `.import` UID 唯一性检查通过。
- L5 包 1 Godot 4.7 隔离验证通过：反应器专项 42 项、schema 6 / schema 2–5 迁移专项 137 项，建筑操作 81 项、供电 79 项、既有物流 82 项保持通过；`sh ./scripts/check-client.sh` 静态与场景检查全绿。专项步长测试发现 `0.1s` 浮点累积边界并以近似完成判定修正，`0.1 / 0.25s` 结果一致。
- L5 包 2 反应器专项增至 73 项、schema 增至 138 项，建筑操作 81 项、供电 79 项、既有物流 82 项保持通过。正式有窗口入口从真实 `Boot → 新游戏` 使用隔离存档覆盖通电加工、断电暂停、错误方向、输出堵塞、解除堵塞、催化剂上带 / 入箱和面板原子回收，共 23 项断言通过。
- 四张 `3024×1701` 正式截图及 `1280×784` 联系表位于 `assets/art-intake/2026-07-26-l5-package2-preview/`，联系表 SHA-256 `0a69d30b9b056fdf0400053418299fb8e9d809fab90183432f9fd0d6c592ffe3`。实际读取联系表确认加工 `1.3/10.0s`、堵塞输入 `2/2` / 输出 `1/1`、左右严格对置货物流、琥珀催化剂上带和目标箱催化剂 `1` 均清楚，无文字截断、穿机、漂浮或关键遮挡。
- L5 包 3 有窗口正式入口使用 `/private/tmp/radishcatalyst-l5-package3-formal-save` 隔离存档，覆盖自然资源预算、七项配方输入、19 格地板、五段产线、加工中重启、待出料重启、错误带向纠正与最终目标箱，共 228 项断言通过；隔离存档未接触用户正式槽。
- 四张 `3024×1701` 原图与 `1280×784` 联系表位于 `assets/art-intake/2026-07-26-l5-package3-preview/`，联系表 SHA-256 `a2adcb92540b362b19613bbd4649b716875d72d42c9f080d769ff8544586cc1c`。AI 实审确认加工 `2.1→2.4/10.0s` 跨重启连续、待出料为输出 `1/1` 且进度归零、目标箱催化剂 `2`，五段主体、HUD 和面板无截断或异常遮挡；等待萝卜SAMA人工确认。
- 多世界目录专项 89 项通过：覆盖空目录、名称校验、唯一 ID、四代轮转、坏主档回退、摘要更新、重命名、路径安全、回收恢复、单项损坏隔离、30 / 31 上限和旧单档发布 / 重入。完整 `sh ./scripts/check-client.sh --with-godot` 全绿，既有放置 54、操作 81、供电 79、反应器 73、物流 82、schema 138 项保持通过；同时修正一条仍要求“待接进出料 L5”的过期供电提示断言为当前“缺晶体”状态。
- 包 2 把菜单目录路径并入既有 `demo_startup_shell_check.gd`，覆盖创建、详情、载入信号、重命名稳定 ID、二次确认回收 / 恢复和 `Boot` 选档注入；完整 `sh ./scripts/check-client.sh --with-godot` 全绿。有窗口真实 `Boot` 以 32 项断言往返载入“L5 完整产线”与“新前哨对照”，核对核心、2 个建筑、2 催化剂互不串档，并证明“包 2 新建世界”不改前两档哈希。
- 包 2 通过态保留在 `/private/tmp/radishcatalyst-multi-world-package2-review`，包含三个命名世界及主档 / 备份；截图 `assets/art-intake/2026-07-26-multi-world-package2-preview/01-two-worlds-selected.png` 为 `3024×1701`。AI 两轮实审后将默认亮灰选中态修为青黑底、青描边与金色焦点框，最终列表、摘要、命名框和操作区无截断或遮挡。

## 风险与未完成项

- L2 已收口，无遗留人工验证项。
- L3-A 至 L3-F 均已归一入库；L3 实现、存档、自举、自动、截图与人工证据全部闭环，无待办。
- 人工路径准备时发现真实新档自举缺口：全图有限晶体共 17；修核心需 3 零件即 9 晶体，抵达晶体区的三座中继 + 采集器需 5 零件即 15 晶体，首批四块地板另需 1 晶体，总计至少 25。正式自动链此前注入零件只验证 L3 操作，不能替代自然资源闭环；资源修订须升级给萝卜SAMA确认。
- 萝卜SAMA确认按建议修正自举：两座大晶簇产量各由 4 提到 8，全图节点数不变、总量由 17 增至 25，恰好覆盖修核心 9 + 三中继与采集器 15 + 首批地板 1；配方、核心空仓与其余簇产量不变。
- L4 已收口，无遗留人工验证项。
- L5 与配方加工 arc 已收口，无遗留人工验证项。
- 多世界存档包 1–2 已完成；当前风险只剩包 3 在 30 项密度、损坏 / 回收状态、键盘焦点与人工启动器返回路径上的收口证据。

## 下周建议

- L4 包 1–3 的实现、自动、正式入口与人工证据已完成，专题于 2026-07-25 收口。
- 下一步按 [Slice Multi-World Save List V1](../features/slice-multi-world-save-list-v1.md) 推进包 3：保留 1 / 30 / 损坏 / 回收命名世界，用人工启动器打开同一真实列表并完成萝卜SAMA复核；不扩手动快照。
- 不为“太平面”追加独立阻塞包：设备贴地 / 悬浮 / 遮挡已由包 5 解决；核心区底板叠加等旧观察项继续留打磨清单，待 L5 完整产线画面形成后统一复核。
