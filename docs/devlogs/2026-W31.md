# 2026-W31 开发周志

更新时间：2026-07-27

## 本周目标

- 完成首次外勤战斗专题的 C1 晶腺关键样本生成与独立 C2 审阅 / 归一硬闸门。
- 三类战斗素材全部通过后，再按专题边界进入 D 接入与三实现包。

## 完成情况

- 2026-07-27 C1 晶腺关键样本首轮生成完成：独立批次 `2026-07-27-batch01` 使用 built-in `image_gen` 完成 `3 / 3` 次调用，每次只生成 1 张。
- V1 建立三叶矿化晶腺、岩甲残片与黄绿组织身份，但完整红色圆口偏机械；V2 改为不规则三叶、暗膜和断裂组织蒂，生物样本身份最完整；V3 把外围岩片与内部亮纹简化，小尺寸剪影最清楚但需防培养囊 / 人工容器读法。
- 三张 `1254×1254` chroma-key 源图、完整 prompt、源路径、尺寸与 SHA-256 均已写入批次 manifest；达到生成与图片读取上限后停手，再经萝卜SAMA明确授权切换 C2。
- C2 以统一去底和 24 / 32 / 40px 九档整数块归一完成比较；24px 过小、40px 接近敌人体量，32px 成为目标档。
- C2 选定 V2 `31×30` RGBA：629 个不透明像素、301 个透明像素、四角透明、单一连通和 7 色标准色板成立；两张联系表确认其在败亡敌人、玩家、三档晶簇和两类货物同场时保持独立生物样本身份。
- D 把 A2 V3 敌人、B2 V2 攻击 / 命中与 C2 V2 样本四张透明素材复制到 `client/assets/sprites/slice/`；包 1 只运行攻击 sheet。
- 包 1 接入显式首次充能、核心仓库优先的权威扣料、鼠标独立瞄准、左键单击 / 按住攻击、`Space` 闪避、前台输入互斥和紧凑生命 / 工具 HUD。

## 关键决策

- C1 只证明可供比较的关键样本身份候选存在，不在生成会话中选定、去底、裁切、量化、拼联系表或接入运行时。
- C2 排除仍有机械圆口的 V1 和规整三腔偏人工培养囊的 V3；V2-32 在生物来源、小尺寸可读性和世界层区分间最平衡。
- 三类战斗素材硬闸门至此全部通过；萝卜SAMA随后明确授权进入 D 接入与包 1。
- 包 1 把攻击与闪避状态收进窄职责 `SliceCombatController`；`SliceWorld` 只保留库存扣料、面板装配和存档触发，不接敌人、样本或 schema 7。
- 充能一次性消耗 2 催化剂，确定性先扣核心仓库再扣背包；攻击 / 闪避在合成、仓库、建筑、放置、确认和暂停前台时禁用，关闭后必须重新按下。

## 验证记录

- 每张候选落盘后使用 `file` 核对为 `1254×1254` PNG RGB，并记录 SHA-256。
- 三个 Alpha 源通过透明角与残余洋红检查；九个归一候选均保持单一连通、限定色板语义和至少两个透明角。
- 总览联系表 `1560×1080`、关键联系表 `1560×720`；本次 C2 新读取 2 张联系表，会话累计读取 5 张，没有加载第三张。
- `sh ./scripts/check-client.sh --with-godot` 全量通过；新增 Package 1 专项 52 项覆盖扣料、重复充能、输入映射、四向量化、反向 / 正交移动瞄准、单击 / 按住 / 松开、UI 重按、闪避中断、碰撞、无敌和冷却。
- 真实 `Boot → StartupMenu → 载入 L5 产线副本 → SliceWorld` 有窗口复核通过 30 项断言；从输出储物箱容量受限转移 2 催化剂，原 L5 主档前后 SHA-256 均为 `cdc95d9f...9606fd`。三张原图与联系表保留在 `assets/art-intake/2026-07-27-combat-package1-preview/`。
- 视觉复核确认面板、充能核心、角色、目标与 HUD 无截断 / 遮挡；右向生效帧未裁切，比例不压过角色。图片读取累计 3 张后停止。
- 人工复核根 `/private/tmp/radishcatalyst-combat-package1-review/` 保留可载入主档和三份备份。

## 风险与未完成项

- 包 1 尚待萝卜SAMA人工确认；确认前不得进入包 2。

## 下周建议

- 人工复核包 1 的鼠标朝向、左键单击 / 按住 / 松开、`Space` 闪避与前台 UI 互斥；通过后进入包 2 的唯一敌人、受击撤离和样本回收。
