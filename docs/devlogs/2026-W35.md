# 2026-W35 开发周志

更新时间：2026-08-30

## 本周目标

- 推进[试玩反馈整改 V1](../features/slice-playtest-remediation-v1.md)包 0—2：先冻结空间 / 保存合同和职责边界，再依次收口玩家可见阻断、制造资源与装备闭环。

## 完成情况

- 萝卜SAMA确认包 0 边界后，专题补齐玩家结果、实现介质、成功证据、失败判据、文档落点，以及空间层级和保存事件矩阵。
- 新增 `SliceSaveScheduler`，统一承接普通变更脏标记、约 `2s` 防抖、强制落盘、失败保留待保存状态和旧档发布上下文；`SliceSaveService` 的校验、序列化、临时文件、备份和目录元数据职责不变。
- 新增 `SliceWorldSaveStateBuilder`，只在真正写盘时构造一次权威快照，不拥有任何可变状态；`SliceWorld` 删除物流 / 生产保存计时器、旧档发布上下文和旧 `_autosave()` 接口。
- 射击耗弹、普通受伤、生产、物流、探索揭雾和普通容器转移进入合并保存；制造、放置 / 调整、拆除、核心修复 / 充能、撤离、敌人败亡、样本拾取 / 交付、暂停返回、退出和窗口关闭强制落盘。
- `SliceWorld` 在实施中一度因新协调入口达到 1515 行，随后把无状态快照构造提取到明确职责，最终回落到 1487 行，低于 1500 行硬上限。
- 新增包 0 专项检查及 macOS / Linux、Windows 接线，覆盖保存合并、延迟、强制落盘、失败重试、迁移上下文、写盘阻断、地表 / Y 排序归属、设备足印 / 端口结构和世界根行数 / 旧计时器红线。
- 正式 `Boot` 建立 16:9 与窗口化 16:10 整改前基线，保留反应器 / 传送带 / 角色、采集器过渡格和 HUD 边缘截图及两套隔离世界。

## 关键决策

- 包 0 只冻结空间目标和记录当前偏差，不提前把 R03、R04、R06、R08、R15、R16 拆成零散修补；这些玩家可见空间问题继续由包 1 一组实施。
- 保存时机是独立领域职责，但权威状态仍属于 `SliceWorld`，文件发布仍属于 `SliceSaveService`；调度器不复制 schema、世界字段或 IO 逻辑。
- 连续脏请求不重置已经累计的防抖时间，使持续生产、物流或连射每约 `2s` 仍形成检查点，而不是必须等到所有变化完全停止。
- 非 16:9 基线不能以内部 16:9 渲染纹理冒充。最终限定截取实际 Godot 16:10 窗口，确认生产内容保持 16:9 并在上下出现黑边；该结果是 A07 / 包 4 的失败基线。

## 验证记录

- `sh ./scripts/check-client.sh` 非 Godot 全量检查通过。
- `sh ./scripts/check-client.sh --with-godot` 完整回归通过；包 0 专项 `41` 项、战斗包 3 `56` 项、远程武器 P3 `87` 项、首程 `99` 项、schema `161` 项、schema 9 `134` 项和存档目录 `92` 项均通过，其余 Godot 客户端检查同轮通过。
- 首轮 Godot 回归暴露旧战斗断言仍假设普通受伤同步写盘；更新为 `1.99s` 不写、累计到 `2s` 写入后通过。第二轮暴露首程检查使用了已过期的采集器规则片段，按当前权威引导文案修正后通过。
- 正式 `Boot` 16:9 与 16:10 基线各通过 `10` 项断言；16:9 原生截图为 `1600×900`。内部渲染纹理在 16:10 窗口仍为 `1440×810`，限定窗口截图为 `1440×1054`，清楚保留标题栏和上下黑边。
- 截图位于 `assets/art-intake/2026-08-25-playtest-remediation-package0-baseline/`；隔离世界位于 `tools/runtime-intake/review-worlds/playtest-remediation-package0-baseline-16x9/` 与 `playtest-remediation-package0-baseline-16x10/`。两者均为忽略提交的开发产物，没有写入正式 `user://saves/slice/`。

## 风险与未完成项

- 包 0 不是玩家体验通过结论。传送带仍阻挡且进入设备 Y 排序，反应器 `176×88px` 轮廓与 `3×3` 基座不一致，采集器端口过渡格仍依赖错误地表组合；这些是包 1 的主阻断。
- 普通延迟保存失败会保留脏状态并在后续窗口重试，但当前只记录运行时警告；专题退出仍要求关键财产和退出失败拥有玩家不可误解的反馈。
- 生产窗口保持 16:9 的黑边行为尚未改变；包 4 必须在 HUD 安全区与窗口模式合同中决定“保持画幅”还是“扩展布局”，不能只移动小地图。
- 当前环境仍无 `pwsh`，新增 PowerShell 包装器已按既有结构接线但未在本机实际执行。

## 包 0 当时下一步建议

- 为包 1 单独回答开工五问并确认介质：Demo 完成态使用现有遭遇状态派生；传送带进入固定地表层并取消阻挡；反应器按 `3×3` 基座重做受控上部轮廓；采集器使用显式过渡格合同。
- 包 1 必须以真实 `Boot` 检查角色穿行传送带、设备基座碰撞、端口接驳与 Demo 完成态，并对连续射击做玩家侧卡顿 / 写盘观察；不新增剧情、地图或效率系统。

## 包 1 开工记录

- 萝卜SAMA于 2026-08-25 确认进入包 1 实现；专题已冻结玩家结果、实现介质、成功证据、失败判据和文档落点。
- 源码审计确认传送带根因是基体仍挂在 Y 排序 `World` 且 `blocks_movement = true`；采集器端口缺少显式跨地表距离；Demo 交付后只有一行目标文字。
- 反应器不生成新图，直接复用仓库现有 `96×90px` 高斜角 `3×3` 资产；包 1 不引入新位图、schema、剧情或地图。

## 包 1 完成记录

- `SliceMap` 新增非 Y 排序 `GroundStructures`，传送带按建筑定义进入该地表层，`blocks_movement = false`，主体与货物 `z_index = 0`；角色和实体设备继续在 Y 排序 `World`。
- 设备阻挡仍只由 `SliceBuildingDefinition.occupied_cells()` 和旋转后足印派生；新专项确认传送带无 `Footprint`，反应器碰撞为 `96×96px`，主体纹理为现有 `96×90px`，不再使用横向越界的 `176×88px` 反应器和设备自带短传送带贴片。
- 采集器输出端口改为 `connection_distance = 2`；新增会话态 `SliceLogisticsTransitionLayer`，只在端口方向正确且实际接通时，于端口和终端带之间显示一格地表过渡。实体模拟分别把采集器晶体送入储物箱与修复核心。
- 新增独立 `SliceDemoCompletionCard`：样本交付成功且强制保存后弹出，提供“继续自由建设”和“保存并返回主菜单”；关闭后 HUD 持续显示 `Demo 已完成｜可继续自由建设`。完成事实仍只来自 `field_encounter.state == delivered`，加载已完成世界不会重弹卡片。
- 包 1 没有新增位图、剧情、地图或存档字段；schema 9 与 schema 2—8 迁移合同保持不变。`SliceWorld` 当前 `1491` 行，仍低于硬上限但后续包必须把新增业务放到明确协作者。

## 包 1 验证记录

- 新增包 1 专项及 macOS / Linux、Windows 总入口接线，`26` 项断言覆盖地表 / Y 排序父节点、传送带通行、设备足印碰撞、反应器纹理、采集器过渡、两条真实物流和完成卡会话边界。
- `sh ./scripts/check-client.sh` 与 `sh ./scripts/check-client.sh --with-godot` 完整通过；同轮建筑放置 `142` 项、建筑操作 `233` 项、反应器 `141` 项、物流 `167` 项、核心物流 `98` 项、包 0 `41` 项、包 1 `26` 项及其余客户端检查全部通过。
- 正式 `Boot` 使用隔离新档，在 `1600×900` 与实际 `1440×990` 窗口各通过 `28` 项断言。真实键盘输入证明角色穿过四格连续传送带且在反应器基座边界停止；采集器分别向储物箱和核心送入一枚晶体；真实连续鼠标开火至少三发期间主档时间不变，约 `2s` 后只发布合并结果；交付完成卡、继续建设和 HUD 持续完成态均通过。
- 两种比例各保留六张内部原图；非 16:9 额外保留一张 `1440×1054` 窗口截图，确认生产内容仍为 `1440×810` 且上下黑边未变。截图位于 `assets/art-intake/2026-08-25-playtest-remediation-package1-preview/`，隔离世界位于 `tools/runtime-intake/review-worlds/playtest-remediation-package1-16x9/` 与 `playtest-remediation-package1-16x10/`，均为忽略提交的开发证据。
- 正式夹具前三次分别被非法测试 ID、缺少工业地板支撑和 `next_building_serial` 单调性校验拒绝保存；均是证据脚本未遵守生产 schema，产品空间 / 物流 / UI 断言没有失败。夹具改为合法 ID、完整地板和同步下一序号后，两种窗口比例稳定通过，说明严格保存门禁未被绕过。
- 当前环境仍无 `pwsh`，新增 PowerShell 包装器按既有结构接线但未在本机执行。

## 包 1 收口与下一步

- 人工逐图确认角色位于传送带上方、反应器横向轮廓收回三格、采集器过渡连续、完成卡层级清楚；16:9 与非 16:9 内容区均无卡片或 HUD 裁切。
- 包 1 完成 R01、R03、R04、R06、R08、R15、R16，并补齐 A01 连续射击实机观察。A07 的上下黑边仍明确留给包 4，不把“卡片未裁切”误写为完整响应式通过。
- 下一步是为包 2 回答并确认五问：制造 / 放置分离、完整资源缺口、采集器 `50`、地板 / 中继新档 A/B、最小装备页与 schema 10 条件。确认前不进入实现。

## 包 2 开工前审计

- 制造根因已定位：`SliceWorld.craft()` 在建筑配方交换成功后立即调用 `begin_building_placement()`，而制造页本身已有独立“选中已有”按钮。推荐删除自动跳转，不另造制造 / 放置状态机。
- 当前配方表只描述直接成本，制造面板只报告单次缺料；推荐新增无状态计划模型，分别计算直接材料、基础晶体递归折算、当前可制造数、完整依赖可支撑数和足印补板上限。该模型只诊断，不隐式连锁制造，真实交换继续由现有库存与配方合同决定。
- 新档八簇有限晶体总计 `25`，现行首个通电采集器自举恰好消耗 `25`：修核心 `9`、三座中继 `9`、采集器 `6`、一批地板 `1`。历史正式全链记录 `31` 次放置和 `22` 格自动补板；包 2 推荐先留取现行 `1→4、6/4` 的 A 基线，再以 `1→8、8/6` 跑同一新档 B，成功门槛是两座仍有意义的中继、至少 `3` 晶体余量、22 格补板三批完成且首条实体产线不被范围直接抹平。
- 采集器容量当前在 `SliceCollector.BUFFER_CAP` 与 `SliceBuildingSaveCodec.COLLECTOR_BUFFER_CAPACITY` 各有一份 `10`；包 2 应只保留前者并提升到 `50`，编解码、面板与回归全部引用同一常量。该调整不需要新增存档字段，旧 `0—10` 缓冲自然落在新合法范围内。
- 当前 `SliceCombatController.current_weapon` 在 `setup()` 和 `restore_durable_state()` 中固定回到切割器；库存只能说明是否拥有步枪，不能说明玩家选择。推荐 schema 10 根字段 `equipped_weapon_id`，只允许 `cutter / pulse_rifle`，且步枪选择必须与随身背包交叉校验；schema 2—9 一律迁移为切割器。最小角色页只承接一个武器槽和现有两种武器，不扩角色成长、装备属性或通用库存拖拽。
- `SliceWorld` 当前 `1491` 行，包 2 不允许继续增加业务分支。制造计算进入独立计划模型，角色页进入独立场景，装备权威留在战斗控制器，schema 校验留在保存服务；世界根只删除制造自动跳转并接入现有协作入口。
- 以上包含存档结构升级，按开发决策闸门仍待萝卜SAMA确认；确认前不修改玩家行为、资源数值、schema、Current Plan 或 Daily Start。

## 包 2 开工与实现记录

- 萝卜SAMA于 2026-08-25 确认包 2 五问、schema 10 和数值 A/B 边界并进入实现。A 轮在冻结提交上通过真实有限晶体采集与修核心前段，确认现行三座中继自举仍消耗全部 25 晶体；旧正式驱动因包 1 已冻结固定正面设备而未完成后半段，记录为有边界的 A 前段证据，不伪报完整全链通过。
- 新增无状态 `SliceCraftingPlan`，递归读取配方并输出直接成本、基础原料折算、当前可制造数、完整依赖可支撑数和足印补板上限；它不改变库存，也不自动制造机械零件。制造成功不再自动进入放置，已有套件选择继续走权威放置入口。
- 地板产出由 `1→4` 调整为 `1→8`；中继接力范围由 `6` 调整为 `8`，设备供能范围由 `4` 调整为 `6`。采集器缓冲提升为 `50`，删除编解码中的第二份容量常量，运行、保存和 UI 统一读取 `SliceCollector.BUFFER_CAP`。
- 新增独立 `SliceCharacterPanel`，提供 HUD 可见入口、`C` 开关、一个武器槽、切割器 / 随身步枪点击与拖入；`SliceCombatController.equip_weapon()` 成为热键和面板共用的权威入口。步枪离开随身背包时立即回切割器并保存，不复制背包或引入角色成长。
- `SliceSaveService` 升级 schema 10，根对象只新增 `equipped_weapon_id`；只接受 `cutter / pulse_rifle`，装备步枪时与随身背包交叉校验。schema 2—9 确定迁移为切割器；schema 9 的首次旅程旗标与探索位图保持原值。`SliceWorldSaveStateBuilder` 与战斗恢复接通该字段。
- `SliceWorld` 当前 `1492` 行。新增制造计算、角色 UI 和装备规则均进入明确协作者，世界根只删除自动放置并转交装备恢复字段，没有继续承接业务分支。

## 包 2 验证记录

- 新增包 2 专项及 macOS / Linux、Windows 总入口接线，`42` 项断言覆盖制造停留 / 显式放置、完整依赖与补板诊断、`1→8`、`8/6`、采集器 `0—50`、角色页、非法步枪选择、schema 10 与 schema 9→10 迁移。
- `sh ./scripts/check-client.sh` 与 `sh ./scripts/check-client.sh --with-godot` 完整通过；同轮建筑放置 `145` 项、建筑操作 `233` 项、库存 `134` 项、供电 `139` 项、远程武器 P3 `87` 项、首程 `99` 项、包 0 `41` 项、包 1 `26` 项、包 2 `42` 项、schema `161` 项、schema 9 兼容 `134` 项及其余客户端检查全部通过。
- 正式 `Boot` 在 `1600×900` 与实际 `1440×990` 窗口各通过 `80` 项。B 轮真实采集 25 个有限晶体，经两座仍有布局意义的中继获得首个通电采集器并余 `3` 晶体；首线共确认 8 次设备 / 传送带、自动补 `21` 格地板，只制造三批，最终由采集器、储物箱、反应器和目标储物箱实际把 6 晶体转为 3 催化剂。
- 同一正式世界制造步枪后通过角色槽拖入装备，完全重启恢复步枪；点击切割器并再次完全重启恢复切割器。最终主档为 schema 10，装备字段与世界武器外观一致。
- 两种比例各保留七张内部原图；制造页可读到“当前可制作 ×0 / 完整依赖可支撑 ×2 / 基础折算晶体 ×6”，角色页一个槽位、两种持有武器和装备状态清楚，均无裁切。截图位于 `assets/art-intake/2026-08-25-playtest-remediation-package2-preview/`，隔离世界位于 `tools/runtime-intake/review-worlds/playtest-remediation-package2-16x9/` 与 `playtest-remediation-package2-16x10/`，均为忽略提交的开发证据。
- 非 16:9 首轮证据脚本仍按 X/Y 分别缩放鼠标坐标，导致 letterbox 下采集器目标为空；改为统一等比缩放与居中偏移后同一产品路径通过。该事实不改变 A07：生产内容上下黑边继续留给包 4。
- 当前环境仍无 `pwsh`；新增 PowerShell 包装器按既有结构接线但未在本机执行。

## 包 2 收口与下一步

- 包 2 完成 R07、R09、R10、R11、R17、R19；地板 `1→8`、中继 `8/6`、采集器 `50` 与 schema 10 正式冻结。A 轮只保留可证明的前段计数，B 轮完整首线与装备双重启构成现行通过证据。
- 下一步为包 3 回答开工五问，统一随身背包、核心仓库和设备容器的“仅持有 + 全部平铺 / 分类筛选 + 成对拖拽”，但不改变库存容量、物品所有权、schema 10 或无线回传语义。

## 2026-08-25 日终复核

- 今日开发历史由四个提交组成：`eb83b50f` 完成包 0 保存治理，`2cd1d3a5` 完成包 1 阻断修复，`050acddd` 冻结包 2 开工提案，`3bad57ab` 完成包 2 实现；工作区在日终文档审阅开始时干净。
- 按四个提交的代码差异重新复核规划、活跃专题、运行时架构、空间工厂规则、存档索引、远程武器专题和复测基线；补正保存调度职责、制造 / 放置分离、非 Y 排序传送带层、采集器跨地表过渡、容量 `50`、角色装备输入与 schema 10 双重启路径。
- 2026-08-26 的第一产物冻结为包 3 开工五问，不直接进入五类面板大改：权威状态继续属于现有库存 / 设备状态，筛选与拖拽过程保持会话态，`SliceWorld` 不承接新增 UI 业务。详细事项进入 Daily Start。
- 日终文档修改通过 `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 与 `./scripts/check-repo.sh`；仓库总门禁检查 `1439` 个文件。

## 包 3 开工与实现记录

- 萝卜SAMA于 2026-08-26 确认包 3 五问并进入实现：默认只显示实际持有物，“全部”平铺、分类仅筛选；左侧随身背包、右侧目标容器；权威数量、容量、四类上限、白名单和原子转移继续属于现有库存 / 设备状态，筛选和拖拽保持会话态。
- `SliceInventoryReadModel` 新增成对库存与任意只读内容适配；新增 `SlicePairedContainerView`，统一物品格、分类按钮、双栏、空态、整堆拖拽、`Ctrl / Control` 逐次向上取整减半、点击选择、`Enter / Space` 等价转移、右键 / `Esc` / 目标外释放取消和就地失败反馈。
- `SliceCoreStoragePanel` 收缩为共享容器外壳，删除原隐藏双栏；`SliceBuildingPanelSnapshot` 为储物箱、采集器和反应器构造只读成对容器快照，`SliceBuildingActionPanel` 删除旧物品选择器与“取出 / 放置 / 回收”死分支，只保留模式、供给筛选、维护、拆除和传送带货物回收等不可拖拽动作。
- 背包 ↔ 核心、背包 ↔ 储物箱继续经过 `SliceWorld` 现有权威 API，仅增加可选请求数量以支持半数与容量受限转移。采集器整数缓冲仍只允许取出，反应器输入 / 输出仍执行既有多物品原子回收，没有被包装成普通库存或获得手工投料能力。
- schema 10、库存容量、采集器 `50`、储物箱四类上限、设备模式和无线回传语义均未改变。`SliceWorld` 由包 2 收口时的 `1492` 行降至 `1490` 行，没有承接筛选、拖拽或按设备类型增长的新业务。

## 包 3 验证与证据

- 新增包 3 专项及 macOS / Linux、Windows 总入口接线，`44` 项断言覆盖仅持有与目录顺序、分类筛选、点击 / 键盘 / 拖拽同源、逐次减半、取消零移动、核心 / 储物箱双向守恒、容量受限、四类上限、设备方向拒绝、采集器取出、反应器原子回收和 schema 10 不变。
- `sh ./scripts/check-client.sh --with-godot` 在最终死路径清理后完整通过；同轮建筑放置 `145` 项、建筑操作 `233` 项、库存 `134` 项、供电 `139` 项、反应器 `141` 项、包 0 `41` 项、包 1 `26` 项、包 2 `42` 项、包 3 `44` 项、schema `161` 项、schema 9 兼容 `134` 项、存档目录 `92` 项及其余客户端检查全部通过。
- 正式 `Boot` 使用隔离新档，在 `1600×900` 与实际 `1440×990` 窗口各通过 `42` 项；两种比例各保留八张内部原图，逐图确认“全部”实际持有物、原料筛选、核心半数拖拽、储物箱四类阻断、采集器缓冲 / 空态和反应器原子回收没有裁切、残留结果或重复旧按钮。
- 非 16:9 额外保留一张 `1440×1054` 限定窗口截图，包含标题栏和上下黑边；内部生产内容仍为 `1440×810`，设备面板完整落在内容区。该证据只证明包 3 面板无裁切，不把 A07 误写为响应式通过。截图位于 `assets/art-intake/2026-08-26-playtest-remediation-package3-preview/`，隔离世界位于 `tools/runtime-intake/review-worlds/playtest-remediation-package3-16x9/` 与 `playtest-remediation-package3-16x10/`，均为忽略提交的开发产物。
- 正式证据夹具早期使用非法固定设备 ID 且漏铺储物箱 / 反应器支撑地板，被生产放置 / 存档门禁拒绝；改为自动合法 ID 和真实支撑地板后通过。16:10 首轮又把外窗 letterbox 偏移重复应用到 Godot 已按视口接收的合成输入，导致半数拖拽只选中未落栏；删除重复坐标变换后同一产品路径通过。两者均为证据驱动问题，产品专项和完整回归未失败。
- 外窗取证前两次因 Codex 仍在前台而误截无效区域；最终在同一操作中将 Godot 置前并延迟 `0.2s` 截取，覆盖无效图后逐图确认。当前环境仍无 `pwsh`；新增 PowerShell 包装器按既有结构接线但未在本机实际执行。

## 包 3 收口与下一步

- 包 3 完成 R13、R14；五类目标容器共用同一只读模型和直接操纵语义，真实资产安全继续由原权威 API 保证。旧空物品格、选择器和多组存取按钮不再构成第二条路径。
- 下一步是为包 4 回答并确认五问：先冻结 16:9 / 非 16:9 画幅与 HUD 安全区、现有状态到世界反馈的权威映射、核心 / 反应器代表范围、上下文电力叠层、音量控制和正式证据；确认前不批量生成动画、声音或改写窗口策略。

## 包 4 开工前审计与提案

- 2026-08-26 提交 `11f4e87e feat(ui): 完成试玩整改包 3 容器直接操纵` 后工作区保持干净，随后只读审计 HUD、小地图、核心、设备、电力线、设置与音频边界。
- `SliceHud.tscn` 的小地图右下锚点仍硬编码 `152px` 底部空隙，上下文提示还会在运行时切换大段绝对偏移；HUD 存在 `12—13px` 正文和低明度文字。生产 `1440×990` 窗口继续得到居中的 `1440×810` 内容与上下黑边。
- 小地图 `SubViewport` 仍为 `UPDATE_ALWAYS` 且覆盖层每帧重绘；当前无同场景性能对照，因此提案要求先做三轮 A/B 帧时测量，允许结论为维持现状。电力层仍常亮完整青色网络，缺少普通游玩、放置与设备检查上下文。
- 核心修复后的三种阶段主要依赖整体染色；反应器只有既有两帧加工覆盖层，其他设备多为通用红绿指示。现有核心 / 设备状态足以驱动表现，不需要新玩法字段或 schema。
- 启动与暂停设置均为占位，仓库内没有音频总线、播放器或音效资产。提案将窗口 / 全屏、主音量、音效音量和控制说明收为共享设置面板，使用独立 `user://settings.cfg`，与世界 schema 10、目录和备份隔离；首批只制作项目内可归属的短电子状态切换音。
- 因整改总览已超过细专题建议篇幅，包 4 五问、状态反馈矩阵、失败边界与首个 4A 切片已路由到 `slice-hud-and-device-feedback-v1.md`。当前仍为待确认提案，没有修改玩家行为、窗口策略、音频或存档架构。
- 萝卜SAMA于 2026-08-26 确认包 4 五问：保留 16:9 世界内容与非 16:9 上下黑边，设置独立于世界存档，先完成 4A 再进入核心 / 反应器代表反馈；当前据此开始 4A 实施。

## 包 4A 实现记录

- 新增 `SliceUserSettings` 自动加载服务和独立 `user://settings.cfg`，严格校验窗口 / 全屏、主音量和音效音量；新增 `Master / SFX` 总线。启动与暂停入口实例化同一 `SliceSettingsPanel`，滑杆即时试听、`0.35s` 合并写盘，失败显式反馈；控制说明只读，不提前实现完整重映射。
- HUD 以 `18px` 内容安全边距重排：小地图贴合右下，任务、物资、角色和提示组成互不覆盖的边缘栈；任务与小地图正文不再使用 `12—13px`，次要文字提高明度。生产画幅、相机和像素网格均未改变。
- `SlicePowerLinkPresenter` 只读取放置和设备面板上下文：普通游玩隐藏完整电网，放置中继等用电设备或检查用电设备时显示，地板与传送带不触发。`SliceWorld` 只装配协作者并压缩既有声明，最终为 `1489` 行。
- 小地图性能探针在同一可见世界交替跑三轮：`UPDATE_ALWAYS` 的 median / P95 分别为 `8.334/8.629`、`8.320/8.644`、`8.319/8.665ms`；临时 `10Hz` 候选为 `8.324/9.210`、`8.367/9.173`、`8.366/9.217ms`。聚合 median 为 `8.320 / 8.366ms`，聚合 P95 为 `8.644 / 9.210ms`；候选没有收益且 P95 约差 `6.55%`，产品维持 `UPDATE_ALWAYS`。

## 包 4A 验证与证据

- 新增包 4A 专项和 macOS / Linux、Windows 总入口接线，`47` 项断言覆盖设置独立持久化、共享面板防抖重读、总线、HUD 安全边距 / 字号 / 提示、小地图刷新决策及电力层三种上下文。
- `sh ./scripts/check-client.sh` 与 `sh ./scripts/check-client.sh --with-godot` 完整通过；同轮包 0 `41` 项、包 1 `26` 项、包 2 `42` 项、包 3 `44` 项、包 4A `47` 项，建筑放置 `145` 项、建筑操作 `233` 项、库存 `134` 项、供电 `139` 项、反应器 `141` 项、schema `161` 项、schema 9 兼容 `134` 项和存档目录 `92` 项均通过。
- 正式 `Boot` 使用隔离设置与世界根，在 `1600×900` 和 `1440×990` 外窗各通过 `46` 项；后者内部内容严格为 `1440×810`，`computer-use` 外窗截图确认上下黑色信箱区等宽、HUD 完全位于内容区。两种比例各保留六张内部原图，逐图复核启动 / 暂停共享设置、浅色基地、晶体区、普通隐藏电网、放置高亮和设备检查均无裁切或遮挡。
- 首次完整回归分别暴露 `AudioBusLayout` 资源缺少类型头与旧首程检查仍要求“小地图位于提示上方”，均按生产合同修正后全量通过；首次 16:9 正式驱动因 macOS 全屏退出尚未稳定而得到错误窗口尺寸，增加有界等待和三次尺寸收敛后两种比例通过。上述均为资源 / 证据合同问题，没有放宽产品安全边界。
- 截图位于忽略提交的 `assets/art-intake/2026-08-26-playtest-remediation-package4-preview/`，隔离世界与设置、原始日志位于 `tools/runtime-intake/`；没有污染正式 `user://saves/slice/` 或用户设置。

## 包 4A 收口与下一步

- 4A 完成 R02、R18、R20 与 A02、A03、A06、A07 的基础部分；用户设置与世界 schema 10 的隔离已同步到存档归属架构真相源，小地图不因无收益的猜测降频。
- 下一步按已确认状态矩阵进入 4B，只实现核心四状态与反应器四状态的一次性视觉 / 电子音反馈；代表路径人工通过前不扩展采集器、储物箱和中继。

## 2026-08-26 日终复核

- 今日功能与规划历史由三个提交组成：`11f4e87e` 完成包 3 成对容器直接操纵，`555d0c7d` 提出并冻结包 4 五问，`562c2906` 完成包 4A 共享设置、HUD 安全区、上下文电力叠层和小地图 A/B；日终文档审阅开始前工作区干净。
- 按三个提交的合计 `46` 文件差异复核规划、活跃专题、运行时结构、设备 / 分类库存专题、应用设置与世界存档边界。发现 `Slice Runtime Systems` 仍写着“包 3 待统一”、储物箱按钮存取、供电层无上下文和 `SliceWorld 1492` 的旧口径，已同步为共享成对容器、`SlicePowerLinkPresenter`、独立 `UserSettings` 与现行 `1489` 行职责。
- `Feature Documents` 索引与整改总览仍把包 4 写成待确认，统一设备面板 / 分类库存专题也未记录包 3 接续；本轮补齐 4A 已完成、4B 下一步和后续直接操纵边界。存档归属模型已准确记录 `user://settings.cfg` 与 schema 10 隔离，无需再次改写。
- 复核同时修正周志中包 4A 专项 `45 → 47` 的残留计数；正式 `Boot` 两种比例各 `46` 项、完整 Godot 回归、小地图性能原始值和忽略目录证据均与代码及日志一致。
- 明日首个产物是 4B 八态权威谓词 / 边沿表与核心、反应器持续视觉代表样本；持续视觉成立后才接一次性 SFX，任一代表失败都不扩 4C。
- 日终只修改文档，不启动新功能或 Godot；文档预算、文本卫生、差异检查和仓库总门禁通过后形成独立文档提交。

## Candidate Quality 门禁迁移

- 2026-08-28 对比 Radish 系列兄弟仓库后，确认 `Repo Hygiene` 应继续作为可定位失败的内部组件，默认分支只绑定稳定聚合 context `Candidate Quality`；这使后续客户端构建、Godot 或平台检查可以接入聚合依赖，而无需反复迁移远端 ruleset。
- `PR Checks` 新增 `Candidate Quality` 聚合 job，使用 `if: always()` 汇总依赖；当前只依赖 `Repo Hygiene`，但失败、取消或跳过均不能得到聚合成功。Release workflow 继续保留独立 `Repo Hygiene`，不冒充 PR 候选门禁。
- ruleset 模板、治理检查器、单元测试、ADR 和 ruleset 说明同步冻结新 context。远端当前仍要求旧 `Repo Hygiene`；必须先由包含本次 workflow 的 PR 实际产生并通过 `Candidate Quality`，再单独授权切换远端 required context，避免迁移窗口阻断全部 PR。

## 包 4B 实现记录

- 2026-08-30 按已确认状态矩阵完成核心 / 反应器代表反馈。新增无状态 `SliceDeviceFeedbackState`，以既有核心修复 / 充能 / 交付、反应器供电 / 加工 / 输出事实解析八种稳定表现态和六类边沿事件；首次观察只建立基线，读档恢复不伪造边沿。
- `SliceCoreVisual` 在既有主体上增加三段状态构件、故障 / 锁定形状、慢呼吸 / 循环节奏和过渡脉冲；`SliceReactor` 复用加工覆盖层并增加三段状态构件、断电形状和阻塞双段标记。两者都以形状、段数或节奏提供颜色之外的第二通道。
- 新增 `SliceFeedbackAudio`，从源码确定性生成六种短电子 PCM 并输出 `AudioStreamWAV`；修复、充能、样本安装、反应器失电、开工和阻塞统一走 `SFX`，不增加音乐或常驻机器噪声。反应器阻塞提示设置 `1.25s` 冷却。
- `SliceWorld` 删除读档中核心战斗状态恢复前的中间视觉刷新，避免把稳定读档误判为新状态；其余反馈业务均留在纯解析器和设备自身节点，最终为 `1485` 行。schema 10、二值供电和反应器生产合同未改变。

## 包 4B 验证与证据

- 新增包 4B 专项及 macOS / Linux、Windows 总入口接线；`71` 项断言覆盖八态谓词 / 优先级、边沿去重、首次观察静默、核心与反应器持续构件、阻塞冷却、确定性音频、`SFX` 路由、世界根接线和 schema 10 不变。
- `sh ./scripts/check-client.sh --with-godot` 在最终脉冲时序修正后完整通过；同轮包 0 `41` 项、包 1 `26` 项、包 2 `42` 项、包 3 `44` 项、包 4A `47` 项、包 4B `71` 项，建筑放置 `145` 项、建筑操作 `233` 项、库存 `134` 项、供电 `139` 项、反应器 `141` 项、schema `161` 项、schema 9 兼容 `134` 项和存档目录 `92` 项均通过。
- 正式 `Boot` 使用隔离世界和设置，在 `1600×900` 与 `1440×990` 外窗各通过 `63` 项；后者内容保持 `1440×810` 与上下黑边。两种比例均记录核心四态、反应器四态和稳定读档原图，接触表与原图逐项确认故障 / 锁定、断电 / 阻塞形状、分段数量和安全区一致；动画节奏、提示播放、`Master / SFX` 静音与读档不重放由同一正式运行时断言确认。
- 首次正式夹具把反应器固定 ID 写成非法值，生产 schema 正确拒绝保存；改用合法自动 ID 后通过。首版截图又过早捕获了过渡脉冲，夹具改为先断言脉冲出现、再等待清除后记录持续态；由此补上核心 / 反应器状态切换后即时刷新脉冲，避免确定性检查存在一帧空档。
- 截图位于忽略提交的 `assets/art-intake/2026-08-30-playtest-remediation-package4b-preview/`；正式日志位于 `tools/runtime-intake/2026-08-30-playtest-remediation-package4b/`，隔离世界位于 `tools/runtime-intake/review-worlds/playtest-remediation-package4b-16x9/` 与 `playtest-remediation-package4b-16x10/`。没有污染正式世界或用户设置。当前环境仍无 `pwsh`，Windows 包装器已按既有入口接线但未在本机执行。

## 包 4B 收口与下一步

- 4B 代表路径通过，R05 与反应器代表反馈已形成稳定实现；自动证据确认音频播放与总线 / 静音合同，但不冒充主观人工听感结论。
- 下一步进入 4C，只把同一权威派生、形状 / 分段 / 节奏与离散边沿合同扩展到采集器、储物箱和中继；三类设备成立后回到整改专题 S0 新档全链，不提前并入传送带拖铺、无线回传语义或基地级工业总览。

## 包 4C 实现记录

- 2026-08-30 审计采集器缓冲 / 生产、储物箱模式 / 无线传输和中继供电的既有权威事实后，冻结采集器断电 / 采集中 / 满载三态、储物箱断电 / 供应 / 传输待机 / 运行 / 阻塞五态和中继断电 / 联网两态。`SliceDeviceFeedbackState` 只增加这些纯谓词和三类共用边沿，不新增玩法或存档字段。
- `SliceCollector` 与 `SliceStorage` 在自身节点内增加分段构件、断电 / 流向 / 满载 / 阻塞形状、动画节奏、过渡脉冲和局部播放器；新增窄类 `SlicePowerRelay` 与专用场景，只在通用实例壳上呈现中继闭合环 / 断开触点。`SliceBuildingCatalog` 只把中继定义指向该窄场景，`SliceWorld` 没有新增按类型分支并保持 `1485` 行。
- 储物箱阻塞只在既有无线传输循环同时持有模式、箱内内容、核心修复与目标容量时派生；供应态不猜测传送带下游是否取货，采集器普通非空缓冲不误报阻塞，中继不伪造负荷或吞吐。
- `SliceFeedbackAudio` 增加失电、恢复运行和阻塞三种确定性短电子 PCM，统一走 `SFX`。首次观察、稳定刷新和读档恢复静默，不增加常驻机器噪声；设备退出场景树时停止并释放运行时音频流。

## 包 4C 验证与证据

- 新增包 4C 专项及 macOS / Linux、Windows 总入口接线；`75` 项断言覆盖十种状态谓词、共用边沿、首次观察静默、视觉第二通道、节奏、冷却、确定性 PCM、`SFX` 路由、窄中继场景、`SliceWorld <= 1485` 和 schema 10 无反馈字段。
- `sh ./scripts/check-client.sh --with-godot` 最终完整通过；同轮包 4B `71` 项、包 4C `75` 项、供电 `139` 项、反应器 `141` 项、schema `161` 项、schema 9 兼容 `134` 项和存档目录 `92` 项及其余客户端检查全部通过，没有残留音频播放对象退出泄漏。
- 正式 `Boot` 使用隔离世界与设置，在 `1600×900` 和 `1440×990` 外窗各通过 `85` 项；后者内部内容严格为 `1440×810` 并保留上下黑边。两种比例各记录七张原图，逐图确认设备家族断电、采集运行 / 满载、储物箱供应 / 传输待机 / 运行 / 阻塞、中继联网和稳定读档；调整证据角色位置后，玩家不再遮挡采集器且相机世界视野保持不变。
- 动画节奏、边沿去重、实际播放器触发、`Master / SFX` 静音和读档不重放由同一正式运行时断言覆盖；视觉接触表与关键原图完成逐图复核。本轮仍只报告实际播放 / 路由合同，不把自动断言写成主观听感通过。
- 首轮完整回归暴露旧供电专项会离树推进储物箱、播放器却尝试发声；产品侧收紧为只在场景树内播放并补退出清理。随后 4C 专项夹具过早读取初始态且源码行数断言未正确去除末尾空行，均修正夹具而未放宽产品合同。供电专项一度在退出时报告运行时 WAV / playback 泄漏，增加设备退出清理和 `0.20s` 音频服务收尾后消失。
- 正式证据首轮在手工暂停 `SliceWorld` 后早于子设备 `_process` 捕获状态，驱动改为在每个证据边界显式调用设备公开刷新入口；构图复核又发现角色遮挡采集器，最终只移动证据角色并用相机局部偏移保持原世界视野。两项均属于证据确定性与可读性修正，不改变玩家逻辑。
- 截图位于忽略提交的 `assets/art-intake/2026-08-30-playtest-remediation-package4c-preview/`；正式驱动位于 `tools/runtime-intake/2026-08-30-playtest-remediation-package4c/`，隔离世界位于 `tools/runtime-intake/review-worlds/playtest-remediation-package4c-16x9/` 与 `playtest-remediation-package4c-16x10/`。没有污染正式世界或用户设置。当前环境仍无 `pwsh`，Windows 包装器已按既有入口接线但未在本机执行。

## 包 4 收口与下一步

- 4A—4C 全部通过，整改包 4 完成 R02、R05、R18、R20 与 A02、A03、A06、A07；核心、反应器、采集器、储物箱和中继均形成不依赖单一颜色的世界态反馈，用户设置继续与 schema 10 隔离。
- 下一步回到整改专题 S0，只使用真实新档连续执行 20—40 分钟全链并检查九项退出条件；S0 通过前不切入 A04 传送带拖铺、A05 无线回传语义、A09 工业总览或新内容层。

## 2026-08-30 日终复核

- 今日功能历史由两个提交组成：`d564d32c` 完成包 4B 核心 / 反应器八态与六种边沿提示，`8c9aefc4` 完成包 4C 采集器三态、储物箱五态、中继两态及三种共用边沿提示；两者均已包含专项、完整 Godot 回归、双比例正式 `Boot` 和规划同步。
- 按两个提交的合计代码差异复核 `SliceWorld`、核心 / 反应器 / 采集器 / 储物箱 / 中继、纯状态解析、确定性 PCM、供电专项与客户端检查入口；确认新增内容都是现有权威事实的派生表现，schema 10、二值供电、物流、容器容量、设备持久字段与锁定主体像素均未改变，`SliceWorld` 保持 `1485` 行。
- 对照审阅 Current Plan、Daily Start、整改总览、HUD / 设备反馈专题、切片运行时架构、供电专题、设备家族、分类库存 / 通电储物箱、保存归属、兼容规则、Godot 运行时指南和开发复测基线。发现供电专题仍把包 5 的“常态供电树 + 红绿灯”和 L3 反应器占位提示写成现行表现，已补正为 4A 上下文连线、4B 反应器真实四态与 4C 中继闭合环 / 断开触点；其余相关真相源与代码一致。
- 明日事项已明确为当前正式 `Boot → 新建世界` 的 `SliceWorld` S0：隔离真实新档连续游玩 `20—40` 分钟，不使用冻结旧 `GameRoot` 开发基线、GM、状态注入或正式证据夹具；主路径在 16:9 完成，窗口化非 16:9 补关键节点，并保留完全重启、世界态可读性和人工试听证据。
- `AGENTS.md` / `CLAUDE.md`、保存归属、运行时兼容、Godot 验证规则和冻结旧 Demo 开发基线的职责没有变化，因此不做无关更新。日终只修改文档，不启动 Godot 或继续功能开发；以文档、文本、差异和仓库治理门禁形成独立提交。
