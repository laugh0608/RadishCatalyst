# 2026-W29 Development Log

周期：2026-07-13 至 2026-07-19

## 本周目标

- 完成项目级复盘的决策落地：把已确认的像素 2D 介质、单基地 + 单远征区结构和仓库处置方案沉淀为全仓库统一口径。
- 收尾章程剩余要素；介质证据轮由萝卜SAMA择时启动，本周不自行启动实现或素材生成。

## 完成情况

- 2026-07-13 萝卜SAMA确认两项复盘决策：首个交付版收缩为单基地场景 + 单远征小区域（12 区纵切与多区域大地图降级为长期愿景）；旧路线文档采用激进精简 + 归档处置。
- 2026-07-13 批 1 上位口径统一：`project-definition.md` 与 `visual-and-ui-direction.md` 定位改为像素风 2D 俯视；新建 `docs/reference/pixel-art-and-grid-standard.md` 作为介质机械真相源（32px 玩法网格、多格建筑、限定调色板、左上光向、虚拟分辨率证据轮定档、AI 出稿 → 归一 → 点修管线、介质证据轮口径）。
- 2026-07-13 批 2 美术生产真相源重写：`ai-art-prompts.md` 重写为 V2 像素提示词库（像素风格块、负面提示、按证据轮排批次）；`free-asset-pack-candidates.md` 转向 CC0 像素包并列基底；`development-decision-gates.md` 架构升级点与案例链对齐像素口径。
- 2026-07-13 批 3 features 归档：60 个旧 Demo V1 专题 `git mv` 至 `docs/archive/features-demo-v1/` 并补归档说明；`features/README.md` 重建为精简当前视图（当前无活跃专题，指向复盘与证据轮）。
- 2026-07-13 批 4 planning / design 对齐：`demo-scope-and-playable-slice.md`、`vertical-slice` 三件套、`base-action-choice-prototype.md`、`resource-and-frontline-loop-rework.md` 归档至 `docs/archive/planning-demo-v1/`；`current.md`、`daily-start.md`、`planning/README.md` 重写为像素切片口径；复盘文档补录产品结构、生产协议、仓库处置三项已确认结论；5 个 12 区时代 design 文档加口径说明并修复归档断链；创始总纲定位行同步。
- 2026-07-13 批 5 根级同步：`CLAUDE.md` 与 `AGENTS.md` 的项目定位、执行分层、画面主介质三处口径切换为像素；`docs/README.md` 索引重建。
- 2026-07-13 章程建议稿写入复盘文档：目标玩家（切片期朋友试玩者 / 长线自动化 + 动作冒险交集玩家）、核心体验（一句话 + 每 10 分钟三感受节拍）、明确不做清单、五条完成定义与 6 个玩家动词；待萝卜SAMA定稿后同步 `project-definition.md`。
- 2026-07-13 介质证据轮执行清单就绪：S1 到 S6 六个生成会话拆分（锚点 / 地面 / 设备 / 角色 / 增量 / 立绘）、C1 到 C4 地面完整提示词、D1-W 行走帧表、B6 采集器、E1 晶体簇与三表情立绘提示词、多对象整版调整规则、归一与拼合预览流程；启动权留萝卜SAMA。
- 2026-07-13 新建 `docs/reference/pixel-evidence-round-prompts.md`：全部提示词拼装完成的生图交接副本，可整篇丢给生图 AI 执行单个会话段落；真相源仍为提示词库，冲突以库为准。
- 2026-07-13 介质证据轮 `S1` 完成：萝卜SAMA以 Codex `image_gen` 按交接副本生成 3 张像素反应器候选，规范落盘 `assets/art-intake/2026-07-13-batch01/` 并写 manifest。
- 2026-07-13 `S1` 审阅与锚点定稿：96px 目标尺寸缩小复核，`v1` 在提示词还原、剪影可读与色域纯净三项全胜；`v2` 侧管黄铜偏出色域，`v3` 英雄建筑级密度缩小糊并（留档作后续大型建筑体量参考）。萝卜SAMA定稿 `v1` 为像素风格锚点，入库 `assets/reference/pixel-style-anchor.png`，旧写实锚点在参考 README 中标注降级。
- 2026-07-13 `S2` 地面 tile 会话完成并通过审阅：3 张候选（岩地 / 金属平台 / 晶体地）挂锚点生成、规范落盘 batch02。零依赖 Python 拼贴脚本复核：2x2 无缝拼贴三张全部通过（c2 八角板阵列对齐最干净），色域、均匀光照、像素簇清洁度合格，全部保留定稿。
- 2026-07-13 `S2` 复核确立地面归一口径：32px 单 tile 压缩会把宏观纹理塌成噪点（c1 / c3 岩板消失、c2 大板变细格网），128px 宏观预览则成立；确认沿用 `R1` 宏块结论——AI 地面源图归一为 128x128 的 4x4 宏块（16 个 32px tile）按宏块坐标铺放，口径已写入 `pixel-art-and-grid-standard.md`。下一步 `S3` 首屏设备会话。
- 2026-07-14 `S3` 首屏设备会话完成并通过审阅：核心双态 / 三连储存 / 整备台 3 张挂锚点生成、规范落盘 batch01。家族感与锚点一致（八角底座、暗青灰金属、青能量窗、琥珀灯），全部保留定稿。96px 目标尺寸复核：核心双态确认同机同角度、仅状态差异（左受损缠线暗核碎屑、右修复密封亮核），储存液位条自带储量读法，整备台工具墙功能识别强。记录两项归一注意点：核心双态需从横版切成 damaged / repaired 两张独立 sprite；核心态无八角基座（作嵌地损坏物合理，不补）。下一步 `S4` 角色会话（静态 + 行走帧表，最高风险项）。
- 2026-07-14 `S4` 角色会话完成并通过审阅（最高风险项一次过，未用重 roll 额度、未退 CC0）：静态形象 d1 + 四帧行走帧表 d1-w 挂锚点生成、规范落盘 batch02。48x64 逐帧切分复核：4 帧确认同一角色（头盔 / 青面罩 / 背包反应器 / 肩灯 / 配色跨帧稳定），行走循环姿态连贯、朝向一致、剪影清晰；d1 静态作主角基准形象成立，与锚点设备同族。记录两项归一注意点：帧表整体明度低于 d1，归一需统一提亮到 d1 基线；帧表为单向 4 帧（符合证据轮口径，多方向留后续 / CC0 补位）。下一步 `S5` 二三屏增量会话。
- 2026-07-14 `S5` 二三屏增量会话完成并通过审阅：采集器 b6 / 污染地面 c4 / 三规格晶体簇 e1 挂锚点生成、规范落盘 batch03。采集器与 S3 设备并列同族；c4 污染地 128 宏块 2x2 无缝拼贴合格，是 c1 / c3 同底的污染变体（同星球不同区域读法成立）；e1 三规格同族递进，青色与晶体地 / 核心能量窗同色系，天然对应小 / 中 / 大采集价值。
- 2026-07-14 介质证据轮重复性证据成立（阶段性结论）：用零依赖 Python 合成脚本把 S1-S5 全部定稿素材归一拼成第二屏（基地：修复核心 + 储存 + 整备台 + 采集器 + 角色，浅暖砂岩地）与第三屏（晶体区：晶体地 + 三规格晶簇 + 受损核心残骸 + 角色）。6 批次 / 5 独立会话产出的资产在同画面透视、光向、色系、比例上浑然一体，读成同一款游戏的两个区域——写实路线四个月未达到的整景一致性，像素管线可稳定复制。粗拼仅内部验证（近黑底抠图有毛边、无接地阴影、纯网格摆位），不入库；真实接入的宏块地面 / 接地阴影 / 精细摆位会再上台阶。证据轮 S1-S5 全绿，剩 S6 立绘小样。
- 2026-07-14 `S6` 主角立绘会话完成并通过审阅（证据轮收官）：同一提示词三张三表情候选、不挂像素锚点、规范落盘 batch04。三张各自内部三表情确认同一人（脸型 / 发型 / 外骨骼 / 青领环 / 抱持头盔跨表情稳定），干净线稿 + 柔和赛璐璐，白 + 青灰外骨骼与世界层青 / 琥珀同色系呼应，立绘与像素世界不违和。萝卜SAMA定稿 `v2`（年轻亲和向、三表情辨识度最高）为主角立绘基准，入库 `assets/reference/protagonist-portrait-anchor.png`；`v1` 老兵向、`v3` 均衡向留候选。
- 2026-07-14 介质证据轮全部收官（S1 锚点 / S2 地面 / S3 设备 / S4 角色 / S5 增量 / S6 立绘全绿）：像素 + 2D 俯视网格介质通过第一屏成立、二三屏重复性、最高风险行走帧表与二次元立绘四项验证，且全程未触发失败闸门、未退 CC0 兜底。介质可行性证明成立，恢复实现的美术前置条件满足；下一步由萝卜SAMA决定进入首个切片实现包（素材归一入库 + 单基地场景接入）还是先补产品结构与生产协议决策产物。
- 2026-07-15 相机虚拟分辨率定档 960x540：以证据轮定稿素材把同一基地场景分别拼成 960x540（2 倍）与 640x360（3 倍）两版 1080p 取景并排对比，960x540 在视野、设备精致度与工厂一屏可读性上占优，640x360 使本批细密度素材发噪；萝卜SAMA拍板 960x540，口径写入像素规范，介质证据轮待决口径全部清零。复盘余项只剩章程定稿与首个切片实现包指定。
- 2026-07-15 章程定稿与项目级暂停结束：萝卜SAMA确认一页章程（目标玩家、核心体验、明确不做、完成定义、6 个玩家动词）并指定首个切片实现包。章程要点同步 `project-definition.md`（目标玩家、核心体验一句话与节拍），复盘文档转为决策存档。建立当前唯一活跃专题 [Slice Base First Screen Integration V1](../features/slice-base-first-screen-integration-v1.md)（三包串行：归一管线入库、单基地场景与相机、玩家接入），`current.md`、`daily-start.md`、`features/README.md` 切换到「切片实现：基地首屏接入」阶段，实现开发恢复。
- 2026-07-15 切片包 1「归一管线与素材入库」完成：新增零依赖归一脚本 `tools/normalize_pixel_asset.py`（整数块逐通道中位数降采样、近黑底去背〔边界泛洪 + 封闭孔判定〕、median cut 限色 + 标准锚点吸附、地面 128x128 宏块、横版多对象切分、明度基线匹配、2x2 拼贴与接缝比率自查）与批处理驱动 `tools/normalize_slice_pack1.py`。证据轮定稿素材 19 件入库：14 张精灵（reactor 96x94；核心双态按要求切成 `outpost_core_damaged` / `outpost_core_repaired` 各 91x89 共享调色板；storage 110x89、workbench 107x92、collector 87x90；engineer 静态 + 4 行走帧统一 48x64 帧盒底部锚定；晶体簇小/中/大 22/32/47 同因子保比例），4 张 128x128 地面宏块（rock / metal_platform / crystal / polluted），立绘 v2 原图不归一复制 `client/assets/portraits/protagonist_portraits.png`。
- 2026-07-15 包 1 归一自查记录：地面 2x2 拼贴无缝、接缝比率 x/y 均 ≤1.3（金属平台 0.23/0.50 最干净）；全部精灵 x4 放大剪影目检通过，管体孔洞正确透明、无底色残留毛边；产物主色吸附命中像素锚点标准色（`shadow_deep` / `metal_mid` / `metal_highlight` / `energy_teal` 等）。两类一轮不达标按两轮规则二轮修正达标：严格 10 色量化实证产生玻璃球色带与金属偏褐（预览留档），定稿口径为"自适应 median cut 上限 32 色 + 锚点吸附距离 24"（调色板标准表不变）；c4 污染橄榄色一轮被并入砂色，地面二轮改"上限 32 色 + 吸附距离 12"后 `#9D9469` 橄榄池色保留。行走帧表实测明度增益 1.025 对齐 d1 静态基线（W29 审阅预估的暗差实测轻微）。
- 2026-07-15 发现验证入口存量问题：`check-client.sh` 聚合自 07-13 features 归档批次起即失败——37 个旧 demo 检查脚本引用 41 个已归档文档路径（全部在 `docs/archive/features-demo-v1/` 有对应文件），归档批次死链复查只覆盖了活跃文档、未覆盖检查脚本。逐项复跑分类：8 项通过（含静态数据、场景引用、industrial-tech-spine、scene-art-foundation 等与本包素材相关项），37 项仅因文档路径失败，0 项因其他原因失败——包 1 新增素材未破坏任何检查。旧检查属本专题冻结项，本包不修改，升级萝卜SAMA决策修复方式。
- 2026-07-15 检查入口存量问题修复：萝卜SAMA拍板机械改路径方案，37 个冻结旧检查中的 41 处 `docs/features/` 引用统一指向 `docs/archive/features-demo-v1/` 对应文件，不改任何检查语义与断言文本。`check-client.sh` 聚合恢复全绿（exit=0），包 2 / 包 3 的默认验证基线与 `dev -> master` Repo Hygiene CI 解除阻塞。
- 2026-07-16 切片包 2「单基地场景与相机」完成：新建 `client/scenes/slice/BaseFirstScreen.tscn`（40x24 tile 世界；c1 岩地按宏块坐标整组铺放，2x1 宏块 c2 金属平台承托核心区；损坏核心居中偏上，反应器 / 储存 / 整备台环绕，左下与下缘留包 3 行走空间）与共享 `client/assets/tiles/slice/ground_tileset.tres`（32px tile 双 atlas）。入口接线：`新游戏` → 切片场景，`载入存档` → 旧 `GameRoot` 链路不变，`boot.gd` 只增不删、启动壳检查断言全保留。
- 2026-07-16 相机口径实现记录：首选"视口直接 960x540"方案实测打破冻结旧 HUD 布局检查（`check-client-scenes` 按工程视口校验旧面板边界），按"不改冻结检查"边界改用等价实现——视口维持 1920x1080 + `canvas_items` 整数拉伸 + 全局 nearest 采样，切片场景 `Camera2D.zoom = 2` 实现 960x540 相机虚拟分辨率，1080p 下与视口方案逐像素等价、整数缩放约束不变。旧 UI 的 1920x1080 画布合同保持原样，像素 UI 口径留 HUD 换皮专题统一。
- 2026-07-16 包 2 验证：摆位先以包 1 产物零依赖合成 960x540 期望构图并两轮调整定稿（核心视野纵向约 38% 处，留行走带）；实机以 SceneTree 脚本走真实 `Boot` → `新游戏` 链路截图 1920x1080，2x 降采样后与期望构图逐像素一致（平均 RGB 差 0.000、最差像素 0），"画面与拼合预览同构"成立；`--with-godot` `--import` 单项通过、无报错，补验了包 1 出口的素材导入项，19 个 `.import` sidecar 一并入库；`check-client` 静态全绿。
- 2026-07-16 切片包 3「玩家接入」完成，专题三包全部收口：新建 `SlicePlayer.tscn` + `slice_player.gd`（`CharacterBody2D` + `AnimatedSprite2D`，idle 静态帧 / walk 4 帧 8fps，单向帧 + `flip_h` 表达左右，脚部 24x14 碰撞，140px/s 八向自由移动）；`BaseFirstScreen` 四设备加 32px 网格对齐足印碰撞（核心 2x2 tile、反应器 / 储存 / 整备台各 3x2 tile）、首屏边界围墙（可行走区 x160..1120 / y128..640 与取景一致）、玩家出生点 (400, 576) 左下空地；设备与玩家统一收进 `World` y_sort 容器，前后遮挡正确。
- 2026-07-16 包 3 验证：运行时 SceneTree 脚本走真实 `Boot` → `新游戏` 链路模拟输入，10 项断言全过——出生即 idle、移动播 walk、上行被反应器足印精确阻挡（y=526.0 与碰撞理论值一致）、阻挡无侧漂、右移触发翻转、沿足印边缘滑动、松键回 idle；行走 / 静止实机截图留档 `assets/art-intake/2026-07-16-pack2-preview/`；`check-client` 静态全绿（新增 `.gd` 补 uid sidecar），`--import` 无报错。阶段退出条件中"三包收口、截图与验证入周志"已达成，下一专题择序待萝卜SAMA确认。
- 全程验证：每批执行 `./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 全绿；归档后活跃文档零死链（`devlogs/` 与 `archive/` 保留历史原貌不改写）。

## 关键决策

- 介质与结构决策的完整记录以 `docs/planning/project-purpose-and-solo-ai-development-review.md`「已确认结论」为准；本周志只记录落地过程。
- 归档采用 `git mv` 保留历史，不删除文件；客户端代码与旧素材继续冻结，证据轮通过后再定清理清单。
- `milestone-review-checklist.md` 判定为介质无关的通用流程文档，保留不归档。
- `devlogs/`、`archive/`、`reference/` 历史材料（外部建议、旧概念文档）保留原貌，不做口径改写。
- 介质证据轮明确未启动；启动是萝卜SAMA的显式决策，执行规范已就绪。

## 验证记录

- `./scripts/check-docs.sh`：批 1 曾报 `visual-and-ui-direction.md` 282 行超软上限，压缩重复条目后全绿；其余各批全绿。
- `./scripts/check-text-files.sh`：各批全绿。
- `git diff --check`：各批全绿。
- 死链复查：`git grep` 扫描活跃文档对已归档 planning / features 文件的引用，结果为零。
- 包 1 归一（07-15）：`check-text-files.sh` 全绿；`check-docs.sh` 全绿；48 项客户端静态检查逐项复跑 8 通过 / 37 仅文档路径失败（存量）/ 0 其他失败；19 件产物 PNG 解码与尺寸口径核验通过；自查预览（x4 剪影、2x2 拼贴、严格 10 色对照）留档 `assets/art-intake/2026-07-15-pack1-preview/`（不入库）。
- 检查路径修复（07-15）：`sh ./scripts/check-client.sh` 聚合全绿（exit=0，48 项静态检查全过）；`check-text-files.sh`、`git diff --check` 全绿。
- 包 2 场景与相机（07-16）：`check-client` 静态全绿；`--import` 单项无报错；正式入口链路实机截图与期望构图逐像素一致（对照与实机图留档 `assets/art-intake/2026-07-16-pack2-preview/`，不入库）；`check-text-files.sh`、`check-docs.sh`、`git diff --check` 全绿。
- 包 3 玩家接入（07-16）：运行时输入模拟 10 项断言全过（真实入口链路）；`check-client` 静态全绿；`--import` 无报错；行走 / 静止实机截图留档；`check-text-files.sh`、`check-docs.sh`、`git diff --check` 全绿。

## 风险与未完成项

- 显示口径为"1920x1080 画布 + 切片相机 zoom=2"的等价实现：非 1080p 整数倍窗口下由 `canvas_items` 整数拉伸保证不糊边，但旧 HUD / 启动菜单仍按 1920x1080 画布布局；像素 UI 口径与旧 HUD 换皮留后续专题统一。
- workbench 底缘保留源图自带的少量亮色高光点，交人工点修阶段处理；行走帧表为单向 4 帧，多方向留后续 / CC0 补位（口径不变）。
- 旧运行时聚合检查与新切片场景并存的适配未评估；本专题只要求 `check-client` 静态通过与新场景无导入错误，不新建检查脚本。
- `development-retest-baselines.md` 仍指向旧纵切运行时；新切片首屏成立后需建立新复测基线。
- 客户端 8.2 万行 GDScript 与旧素材冻结保留，清理清单在专题收口后另定。

## 下周建议

- 按专题三包串行推进：包 1 归一入库 → 包 2 基地场景与相机 → 包 3 玩家接入；每包收口截图与验证记入周志。
- 日常实现由执行会话推进；介质口径变更、两轮失败复盘、专题切换仍升级架构会话与萝卜SAMA。
