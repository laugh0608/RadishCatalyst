# 2026-W37 Development Log

周期：2026-09-07 至 2026-09-13（Asia/Shanghai）

## 本周目标

同规则 Godot Demo 画面与操作已获认可；正式三维工厂入口、独立存档和扩建基础已提交。当前收敛计时与持续负载验收，精细美术暂缓。

## 2026-09-07 产线亲测反馈

- 萝卜SAMA表示整体效果很满意；指出设备下方名称 / 状态遮挡视野，55° 偏俯视，希望试 50°；设备出入口不明显，传送带只能单格点击。两张终末地参考图用于接口实体与方向辨识，不复制外部资产或解冻多端口设备。
- 沿 [Web First Production Line V1](../features/web-first-production-line-v1.md) 增补同专题修正合同；移除场地 DOM 名称按钮，详情留在选中面板，状态灯 / 进度条保留；视角试调为 50°；真实端口边缘增加实体门框、短接台与顶面 / 接口箭头，观察和建造均常显。
- 按住拖动形成正交路径预览，快速跨格补齐、回拖缩短、转弯跟随路径；松开前统一校验后铺设。阻挡 / 越界 / 构件不足停止延伸，Esc、指针取消、离开场地或 UI、窗口失焦撤销预览。保持单击、R 转向、精确落位以及原生产 / 回收规则。

## 验证记录

- 联合 Node 检查共 21 项通过：原 18 项加拖动路径 / 实际输送、预览边界与提交原子性 3 项。首次运行 19 项通过，两项 HTTP 检查因沙箱禁止 loopback 监听失败；获准沙箱外重跑全部通过，失败与成功日志分别保留。
- 专用 Chrome 实际以 `1440×900` 经构件栏 / X、Z 按钮放三机，再用鼠标按下、跨格移动、松开铺下 5 + 4 段带；预览期间构件仍为 24，松开后剩 15。实测 Esc 与拖到 UI 取消不扣件，回拖加转弯铺下 3 段，遇已有带停止并铺下前缀 2 段，单击再铺 1 段，构件剩 9。
- 主线真实入仓 4 件后继续复核；点击三维中间带段 `(-4,0)`、拆回并收到 1 个晶体，耗尽后目标进入“补回输入缺口”；通过精确落位补回该格后目标进入“已恢复”，记录入仓 24 件。未调用内部模型方法、修改隐藏状态或加速生产。
- 六张截图已查看：建造接口、五格拖动预览、观察运行、左转 30°、恢复生产与窄屏。50° 默认画面和转角下，常驻文字已移除，真实端口框 / 箭头可辨；`390×844` 页面宽度与滚动宽度均 390，场景高度 326，无横向溢出。窄屏只复核布局，不冒称手机硬件操作验收；专用浏览器控制台 0 错误 / 0 警告。
- 过程失败保留：CLI 多语句调用首次语法不匹配；初次落位预览随指针移出厂坪而按钮禁用，改用已有 X / Z 控件准备设备；靠反应器端口的点击命中设备门框，改点清晰可见的中段传送带后完成拆除。没有把这些尝试记为成功路径。指针取消 / 系统失焦的撤销分支本轮仅代码复核，没有伪记为真实设备输入验证。
- CLI 自动输出曾落到仓库根的 `.playwright-cli/`，其 YAML 缺末尾换行使首次治理失败；本次生成的文件整体移入约定忽略目录后再执行最终检查。日志目录为 `tools/runtime-intake/check-runs/web-production-feedback-2026-09-07/`，截图目录为 `assets/art-intake/2026-09-07-web-production-feedback/`，均忽略提交。

- 最终仓库治理通过（1611 文件），文档预算、文本规范与 `git diff --check` 均通过。验证专用 Chrome 已关闭，用户原有应用内试玩页保留；刷新后加载本次修改，同时会按现有规则重置场地。修改未提交、未推送。

## 2026-09-07 接口箭头简化

- 萝卜SAMA复评认为上下两个箭头太花，要求只保留下方一枚。移除全部设备端口顶面的重复箭头，保留下方连接台白色流向箭头与原接口框。
- `node --check` 与差异空白检查通过；独立浏览器通过可见控件放三机，在 50°、1.3 倍缩放下截图复核。`07-single-port-arrow.png` 已查看，确认上方箭头消失；本次仅删展示几何，未重跑上一轮生产 / 拖动全链。

## 2026-09-07 传送带转角外形修正

- 萝卜SAMA提供折线产线截图，指出传送带没有拐弯。根因是显示层只旋转直带，未根据输入邻居生成弯道，侧面护栏横挡物流接口；上一轮“回拖加转弯”证明了路径方向与构件数量，没有覆盖弯道外形，不能作为本问题的视觉通过证据。
- `beltInputs` 根据真实邻带方向与设备出口确定入口；单个侧向入口生成弧形带面、内外护栏和沿弯道的横条，多入口生成开放汇流带面，无有效输入时保留直带。场景在邻居拆除 / 转向 / 补回时重算形状签名；物品按实际 `entry` 与出口走弧线，生产节拍、容量、守恒与现有汇流规则不变。
- 联合 Node 检查 23 项通过，新增八种转角端点、沿轨迹射线命中带面 / 不穿护栏检查，以及真实设备端口、错向、汇流、转向和拆除的入口判定。沙箱内首跑 21 项通过，两项 HTTP 因 loopback 监听权限失败；沙箱外重跑全部通过，分别保存 `corners-node.log` / `corners-node-approved.log`。
- 独立 Chrome `1440×900` 以可见控件放三机、真实鼠标拖出五段输入带和八段折线输出带，构件剩 11。`50° / 1.2` 倍缩放截图 `08-curved-route.png` 已查看，四个转角的弧形带面与护栏连续；选中 `(2,2)` 弯道，面板确认带上催化剂，`09-cargo-corner-0` 至 `3` 四帧已查看，记录货物沿弧线转入下一格，此时累计入仓 21 件。
- 再经画面选中 `(2,1)` 入口邻带，暂停后拆回并回收 1 催化剂；`10-corner-source-removed.png` 显示下一格恢复直带，补回南向带后的 `11-corner-source-restored.png` 显示弯道恢复。继续生产后入仓由 57 增至 58，构件仍剩 11；本步骤证明邻带拆补及恢复输送，不冒称完整缺料 / 回压重测。
- 上述截图均已直接查看，无隐藏状态改写或加速生产；日志与截图继续保存在本日约定忽略目录。未重跑 Godot、手机硬件或正式客户端回归。

## 2026-09-07 用户复评认可

- 萝卜SAMA在弯道修正后提供三机弯道产线截图，明确反馈“不错，效果我很满意”。本轮画面与转角修正按用户视觉认可收口，不继续追加同类视觉微调。
- 专题与 Current / Daily 同步本次实际反馈；下一开发包仍待讨论。本次仅文档更新，不重跑运行时检查、不提交或推送代码。

## 风险与下一步

本轮修正后的视觉效果已获萝卜SAMA认可；具体扩建目标、独立操作评价与正式量产美术仍需后续讨论。本轮不改正式客户端 / 存档、依赖或平台，不进行 Godot 回归、推送与发布。

## 2026-09-07 本地提交与正式技术路线评估

- 按萝卜SAMA要求，前述产线交互与转角修正已本地提交为 `7098c5db`（`feat(web): 完成产线建造交互与转角表现`），未推送；此前“未提交”描述保留为当时事实。
- 萝卜SAMA追问是否仍在做 Demo，随后同意转向正式技术路线评估。本次核对 Web 模型 / 渲染 / 驱动、Godot 二维节点与存档耦合、既有三维样件及产品平台约束，并查阅 Three.js、Electron、Godot 官方文档。
- 新增 [Production Client Technology Assessment V1](../architecture/production-client-technology-assessment-v1.md)，推荐 Godot 3D 桌面正式化，以已认可 Web 画面 / 操作为迁移验收参照；列出可复用与必须适配的边界、两条路线的相对成本、未测项和首个正式开发包建议。Current / Daily / 架构索引同步路由。
- 建议的 100 台设备 / 1,000 段带首档与更大压力档均未实测，也未获冻结；本机旧小场景提交耗时不能证明 Windows 大工厂性能。正式平台、介质、新旧存档兼容和开发包仍需确认，未更改代码、依赖、存档或旧验证结果。
- 本次文档预算检查、仓库治理（1612 文件）与 `git diff --check` 通过；没有启动 Godot 或浏览器，没有新增性能实测。评估文档本轮未提交、未推送。

## 2026-09-07 审阅交接

- 萝卜SAMA要求提交工作区更改、关闭试玩服务器并等待审阅。核实 `tools/visual-studies/web-topdown-3d/server.mjs`（PID 73044）后以 SIGTERM 停止，确认 4318 端口已无监听。
- 本轮评估及规划文档纳入本地提交，不推送；技术推荐仍待审阅，不启动迁移或下一开发包。

## 2026-09-08 独立 Godot 产线 Demo 授权

- 萝卜SAMA明确要求先做独立 Godot 3D 产线，与已认可 Web 版本直接对照后再决定正式路线；已授权开工，不包含正式迁移。开工 `dev` 工作区干净。
- Current / Daily、Web 专题、Godot 视觉专题和技术评估同步新决定，建立同规则 Demo 合同。复用 Web 自制模型形状与配色，独立工程内实现同一有限规则；保留原 Web 和 Godot 视觉入口。
- 本包不修改正式 `client/`、存档、液气或多人，不安装依赖或引入外部资产。窗口与定向证据待实施后记录；服务器此前已关闭。

## 2026-09-08 Godot 同规则产线实现与复核

- 在 `tools/visual-studies/topdown-3d/production/` 新增 `Production.tscn`，按状态模型、输入、HUD 和视图划分职责；旧独立 `Main.tscn` 与 Web 源码保持不变。没有正式客户端 / 存档服务依赖。
- 从既有 Web Three.js 自制几何导出 14 份 GLB（约 1.6MiB），包括同尺寸设备、单枚端口箭头、场坪、角色及七种标准带面；保存 Web 来源与资源 SHA-256 清单。未安装依赖、下载或引入第三方资产；带面按实际输入邻居旋转 / 切换，货物沿同一弧线行走，网格及三角形拾取对应真实拓扑。
- 定向入口 `sh tools/check-godot-production-demo.sh`：10 组操作序列、1,891 个完整状态检查点与 40 个弯道轨迹点全部一致，覆盖生产、缺料 / 回压 / 恢复、原子拆回 / 拖铺、公平汇流、连续拆改、移动碰撞及物料 / 构件守恒。Web 模型生成预期，Godot 独立重放；模型夹具与窗口操作分开。最终结果在 `tools/runtime-intake/check-runs/godot-production-line/model-result.json`。
- 最终完整窗口 `run-1788869561-40669`：61 项通过，10 张 `1440×900` 原图逐张查看。实际独立 Production 入口从空场坪使用鼠标 / 键盘放三机、5 条输入带和 8 条四转角输出带，完成快速跨格、回拖、Esc / UI 取消、单击补接、选择、拆回及回收物投入；未调用内部生产方法加速或修改隐藏状态。
- 完整路径状态：约 26 秒首件入仓；输入中段拆除后约 68 秒实际缺料，`completed=6 / delivered=5`；补接后约 90.55 秒新批次入仓并标记恢复，`completed=7 / delivered=7`。拆输出至反应器 `input=2 / output=1` 停止新批次，补回后约 110.1 秒 `delivered=8`。最终构件仍为 13 带在场 / 11 带随身，回收物投入仓后清空；晶体当量与生成总量相等。
- 最终补充窗口 `run-1788870451-44581`：28 项通过，3 张原图已查看。地面点击与真实按键驱动角色绕至机后，碰撞停在设备足印外，绕到机前；放置不能压人，暂停和重开取消 / 确认保持正确状态。失焦取消用窗口生命周期信号注入，确认弹窗用可见按钮 `pressed` 信号；这些不冒称原生鼠标自动化或用户手感验收。
- 截图位于 `assets/art-intake/2026-09-08-godot-production-line-preview/` 下对应运行目录。已与 Web `2026-09-07-web-production-feedback/08-curved-route.png` 原图直接对照；校正首轮偏亮的照明与旧 810 高度后，固定为 50° / 25°、同源模型与单箭头，四个真实弯道、端口、缺料 / 回压状态及人物前后遮挡可辨。此处为实现复核，用户最终画面与操作偏好待亲测。
- 本机 Godot `4.7.stable.official.5b4e0cb0f`、Forward+ / Metal、Apple M5。三机 / 13 带、`1440×900`、1.2 倍缩放，8 秒单调时钟采样 961 帧，帧间隔 p50 `8.323ms`、p95 `8.788ms`、p99 `9.064ms`、最大 `9.312ms`；198 draw calls / 46,510 渲染图元。每帧 advance 调用 p95 `0.179ms`，其中包括不触发固定步进的调用；不是 GPU 帧时间、单次模拟步预算或大型工厂结论，未测 Windows 目标机和长时运行。

### 本包排错与证据边界

- GDScript 首轮解析发现 `ready` 与原生信号重名、`Vector2i` 无 `dot`；修正后通过。Web 预期生成器的容量夹具最初覆盖回收箱时未扣除原内容当量，守恒断言立即失败；修正夹具记账，不修改 Web 规则。
- 沙箱内完整 UI headless 检查读取系统字体失败；按要求在沙箱外重跑入口成功。模型和导入采用仓库既有自包含 Godot 副本，无新复制 / 安装或签名；其 macOS 证书读取 `ret != noErr` 为隔离环境噪声，未把字体错误当通过。日志、夹具与截图留在规定忽略目录；引擎 shader cache 仍使用本 Demo 独立 app_userdata，无游戏存档。
- 完整窗口首轮 `run-1788868749-38249` 有 7 项失败：窗口失焦暂停导致耗尽等待超时，验证脚本也点击了观察态隐藏构件栏；第二轮 `run-1788869206-39057` 仅输出恢复等待因再次失焦而失败。两轮失败均保留，不以截图标签冒称当时状态达成。随后改为等待真实焦点并按实际推进时间计算生产等待，最终完整路径全过。
- 补充窗口初次 `run-1788869929-42849` 首次输入与失焦等待失败；`run-1788870099-43352` 人物路径通过后卡在确认弹窗焦点等待；`run-1788870215-43591`、`run-1788870372-44372` 的原生 / 嵌入确认窗鼠标注入未触发确认。停止继续调整同类坐标，按运行时指南改用可见按钮信号检查取消和确认，最终补充检查通过。Computer Use 只读看到了窗口，激活接口返回未激活 / Raise 不可用；没有把失败的 UI 动作记作成功。
- 已执行独立导入、定向模型核对、客户端静态检查、文档 / 文本 / 仓库治理和 diff 检查。未启动正式 Boot、未重跑完整正式客户端 Godot 套件、未验证大型工厂、未迁移正式客户端 / 存档、未推送或发布。实现与证据交给萝卜SAMA亲测，正式路线仍未决定。

### 本次交接

- 最终定向、文档预算、文本规范、仓库治理（1661 文件）和 `git diff --check` 通过。资源与脚本已完成独立导入；更改保留在 `dev` 工作区，未提交、未推送。
- 已打开 `异星催化 · Godot 3D 产线 Demo (DEBUG)` 新空场坪，原生窗口清单确认入口；交互日志为 `tools/runtime-intake/review-worlds/godot-production-line/handoff-20260908.log`。此前关闭的 Web 服务已为本次对照重新启动于 `127.0.0.1:4318`，`/play/` 实测 HTTP 200；只在本机监听。
- 下一步是萝卜SAMA直接比较画面、移动 / 放置 / 拖铺手感和流畅度。关闭 Demo 即重置，失焦暂停；没有据此批准正式迁移或大型工厂性能。

## 2026-09-08 本地提交与最大化模糊修正

- 按萝卜SAMA要求，已将独立 Godot 三机 Demo 和同步文档提交为 `cfa2fee4`，未推送；此前“未提交”保留为当时交接事实。
- 用户提供最大化模糊截图。修正前实机取证：窗口 `3024×1716`、保留宽高比后的内容截图 `2745×1716`，界面逻辑尺寸 `1440×900`；3D 子视口仍为 `1440×697`，被放大约 1.9 倍。旧 `SubViewportContainer` 按逻辑尺寸分配渲染目标，未吸收根窗口 canvas 拉伸比例。
- 改用 `TextureRect` 呈现独立 3D 纹理；窗口 / 场地尺寸变化时，根据根窗口拉伸比例将渲染目标同步到显示像素。最大化实测为 `2745×1329`，宽屏 `1920×1080` 时为 `1728×836`，恢复 `1440×900` 后回到 `1440×697`。保持 4× MSAA、100% 3D 比例和原 UI 布局，鼠标输入与投影回读采用对应比例转换。
- 定向窗口 `run-1788871477-48212`：26 项通过、0 失败；4 张原图与修正前同尺寸图直接查看，地面线条、角色和设备边缘更清晰。最大化鼠标放三机、拖铺五格输入带、选择带，以及宽屏转视角后选反应器、缩回窗口后取消预览均通过。结果和截图分别位于既定 `check-runs/godot-production-line/` 与 `2026-09-08-godot-production-line-preview/` 的该运行子目录。
- 取证脚本首次因未等 `scene_changed` 读到空场景，修正等待后才获得有效修正前数据，保留失败日志。首轮修正后窗口 `run-1788871384-48049` 的输入全过，但 3 个尺寸断言失败；查验 PNG 实际尺寸，确认测试误将 `ViewportTexture.get_size()` 的额外缩放报告值作为帧缓冲大小，改读 `get_image().get_size()` 后全过，产品代码未因该测试误差追加放大。
- 本次未更改正式客户端、模型规则、存档、依赖或画风，未重跑正式 Boot、完整生产计时路径或性能采样；之前 1440×900 的小场景帧时间不能当作最大化性能证据。

- 本次独立 Godot 导入、26 项分辨率 / 输入窗口检查、4 张原图复核、文档预算、仓库治理（1663 文件）、客户端静态检查、shell 语法与 `git diff --check` 通过；清晰度修正纳入单独本地提交，不推送。

## 2026-09-08 Godot 复评认可与正式工厂首包设计

- 萝卜SAMA明确认可当前 Godot 画面与操作，同时表示精细度尚不满意、现阶段不必抠细节，并要求继续下一步。按此前建议推进可保存、可扩建的正式工厂首包设计，不重复要求同范围画面认可；该反馈不被扩写为量产美术定稿、完整生产亲测或大工厂性能通过。
- 开工 `dev` 工作区干净，现有 Demo / 清晰度提交为 `cfa2fee4`、`5e1701b9`，尚未推送。核对 Boot、启动菜单、旧存档 / 调度 / 快照构造，以及独立产线模型、相机与输入；旧服务绑定 schema 10 / 二维世界，Demo 场坪与矿点写死、每步重建索引和截断 delta 均不能直接作为正式扩建基础。
- 新建 [Factory Foundation And Persistence V1](../features/factory-foundation-and-persistence-v1.md)，把玩家成果定义为从正式入口新建工厂、手建两条线、保存退出后跨进程继续。按 P1 正式入口与状态、P2 保存恢复、P3 扩建与规模推进；设备种类与精细美术暂缓。
- 本次建议值为 64×64 固定区域、普通预发采集器 8 / 反应器 16 / 终端仓 8 / 带 256；工程档使用同规则、明确单列的供给配置创建 100 台设备 / 1,000 段带。候选目标为 20Hz 模拟、60 FPS 和 30 分钟运行；均为待审阅合同，没有新增这些负载的实测结果。
- 新建 [Factory World State And Save V1](../architecture/factory-world-state-and-save-v1.md)，建议新工厂 schema 1 / 独立 `user://saves/factory/`，旧二维入口与 schema 10 保留，不自动导入旧档。补充权威状态、非持久索引、原子命令、保存节拍、源哈希校验、备份恢复、失败退出与同世界单写入方；明确元数据是摘要，不能覆盖主档真相。
- Current / Daily、产品方向、专题和架构索引同步本次接续；旧 Web / Godot 研究保留为参照，技术评估区分评估当时与后续 Demo 证据。根入口和正式 `client/` 本轮不改；具体接入、存档和规模合同审阅后再进入实现。
- 本轮仅文档设计与源码核对，没有启动 Godot / 新窗口、修改正式存档、安装依赖、引入资产或改变运行中的试玩服务。文档预算、文本规范、仓库治理（1665 文件）及 `git diff --check` 通过；本轮设计纳入本地提交，不推送。


## 2026-09-08 正式工厂实施进展与窗口中断

- 萝卜SAMA确认首包接入、独立存档与 P1–P3 范围，授权开始实施。正式 `client/` 增加工厂目录、Boot 菜单入口、64×64 / 25 矿点、常驻占用 / 实例 / 邻接索引、多设备生产与独立 schema 1 存档；保留旧二维入口和 schema 10。根入口只同步已确认的产品 / 介质边界。当前更改尚未提交或推送，完整首包仍在收尾。
- 纯状态 62 项、Web 同规则核对 1,839 检查点通过；存档故障 44 项通过。完整精度 JSON 读写，整数数量和批次严格校验；浮点读回有约一个 ULP 的解析差异，时钟往返检查采用绝对误差 `1e-12` 秒，文件发布按原始内容哈希核对。
- `boot-1788875751-65167` 从正式 Boot 检查新建、鼠标三机与 13 带、保存返回 / 继续和旧二维新建：33 项通过，5 张截图；前轮 `boot-1788875627-64915` 因测试脚本 typed array 参数错误最终超时，已保留失败记录。补充双线 `operation-20260908-a` 使用实际鼠标 / 按键与真实生产时间：写入进程 42 项、读入进程 24 项通过，覆盖移动到第二矿区、两线产出、带货 / 半批保存、跨进程恢复、失败退出留在世界 / 取消 / 重试、补线、最大化旋转拾取与缩回。确认弹窗用可见按钮 `pressed` 信号，不能称为用户亲测。
- 跨进程数据检查写入 14 项 / 读取 5 项、活跃锁竞争 2 项通过；原进程退出后显式恢复 3 项通过。发现 `OS.is_process_running()` 只接受本实例创建的子进程，不能判断另一个客户端；改用系统只读进程查询，无法确认时保留锁。沙箱阻断退出进程查询时确实保持阻断，在沙箱外重测恢复通过。最初失败的检查进程已停止，未触碰用户世界。
- 旧启动壳回归、旧存档目录 92 项、schema 回归 161 项通过。客户端静态、文档预算和文本规范检查通过；新状态 / 存档检查已接入 Shell / PowerShell 客户端入口，PowerShell 本机无运行时，未执行其入口。
- 工程夹具由正式命令构建 25 条线，每条采集器 / 工作反应器 / 缺料备用反应器 / 仓各一台、40 带，共 100 台 / 1,000 带。按真实规则推进约 3,000 秒后带货 1,000，75 台回压 / 25 台缺料，物料守恒；没有隐藏供料或直接填库存。状态准备耗时约 56.6 秒，模拟步 p95 1.39ms。
- 满载短测 `performance-1788876159-67028`：1080 窗口 / 最大化、局部 / 拉远四档各约 12 秒；最大化拉远实际 3D 为 `2745×1420`，帧间隔 p95 12.985ms、模拟步 p95 1.661ms，保存 95–100ms 左右。截图已检查局部与最大化拉远原图；这是短测，不能作为连续运行通过。GPU 时间未取得，Windows 未测。
- 随后启动的持续生产采样反复 `grab_focus()`，干扰萝卜SAMA操作其他任务；用户明确要求停止抢焦点并已关闭窗口。已确认测试进程退出，移除本包所有 `grab_focus()`，长测失焦即中断；本轮不再打开窗口。30 分钟测试未完成，只有第一档约 450 秒与第二档部分进度，不判为通过。原始 `performance-sustained.log` 保留；执行中修改 Shell 入口导致进程退出后旧读取位置报语法错误，磁盘最终脚本 `sh -n` 通过。
- 焦点修正仅执行 `--headless --check-only` 的性能 / 操作检查脚本解析与 Shell 语法检查，通过；未重新开窗验证。持续采样第一档活动墙钟与模型推进时间尚有差异，需进一步核对引擎 delta 与失焦边界，不以现有纯状态断言宣称时钟长期验证通过。后续收口、部分截图审阅、最终治理和完整长测仍需补齐。

## 2026-09-08 收工提交与文档审阅

按萝卜SAMA要求，先保存工作区，再回顾当天全部提交并进行独立文档收尾；全程未重开窗口，未推送。

| 当日提交 | 修改与本次审阅 |
| --- | --- |
| `cfa2fee4` `feat(godot)` | 独立三机产线、同源模型、Web 规则对照与窗口验证；保留其无存档、关闭重置的样件边界，更新后续正式工厂路由 |
| `5e1701b9` `fix(godot)` | 3D 目标按实际像素分配，拾取 / 拖铺转换与多窗口检查；核对正式工厂也使用 TextureRect / SubViewport 同比例逻辑，不将旧三机性能外推 |
| `d75075ce` `docs(factory)` | 定义 P1–P3、64×64、多矿点、独立存档与工程目标；根据后续确认和代码把活跃文档的待审阅 / 未实现描述改为实际进展 |
| `978d6250` `feat(factory)` | 正式 Boot、工厂状态 / 视图 / 独立存档、锁与检查工具，83 文件；长测中断及计时问题随提交保留，未声称首包全部完成 |

- 逐项核对 Boot / StartupMenu、工厂规则 / 模型 / 输入 / 视图、codec / store、保存协调、负载检查和两个平台检查入口。文档明确工厂自行调度保存，未实例化旧 `SliceSaveScheduler`；输入直接处理物理键码，并未新增全局 InputMap；正式资源只加载自身目录，14 个 GLB 的来源清单另补使用说明。
- 模型虽保留传入时间余量，正式应用仍接收引擎 delta。首档约 450 秒活动墙钟对应约 447.9 秒模拟推进、最长帧间隔约 1.174 秒；保留为未解释的风险，不将短测 p95 通过等同于没有长停顿。明日优先核对时钟 / 暂停 / 失焦 / 保存边界，并复查性能检查中断能否终止全部后续阶段。
- 同步 Current / 次日 Daily、首包与架构合同、产品方向、技术评估及相关索引；补充正式 / 旧二维入口的适用范围，修正根 README 的旧 ARPG 定位与客户端入口说明。历史批次正文保留当时事实，通过顶部接续说明区分后来的正式接入，未批量改写旧设计。
- 运行时指南明确双根隔离、模型时间与真实持续时钟分别验证、信号调用不等同鼠标命中，以及退出码和真实错误日志需共同判断。窗口不抢焦点、关闭后不自动重开的约束继续有效。
- 提交实现前重新执行：仓库治理 1,735 文件、客户端静态、文档预算、文本规范、diff 检查均通过；无窗口 Web 对照 1,839 检查点、状态 62 项、存档故障 44 项再次通过，证据 `save-1788879350-71969`。Godot 出现既有 macOS `ret != noErr` 证书噪声，未见真实脚本错误。
- 本次收尾没有重跑跨进程、规模、窗口或完整客户端 Godot 套件；Windows / PowerShell 未执行。双线原图补审阅、30 分钟持续生产和其余合同证据留在明日清单，不扩展新玩法或打磨美术。
- 最终文档审阅后，文档预算、文本规范、仓库治理（1,736 文件）和 diff 检查通过；本次文档变更单独提交，次日计划日期为 2026-09-09。

## 2026-09-09 计时修正与首包无窗口验收补充

- 萝卜SAMA确认按当天计划开工。开工 `dev` 干净；沿现有 P1–P3 修复和补证据，没有新增设备、经济玩法、依赖、存档 schema 或旧世界转换。窗口可用时段仍待答复，本轮未打开窗口。
- 仓库隔离目录的 `clock-probe.gd` 用真实引擎循环注入 100 / 1,174 / 100ms 停顿。最终探针约 `1.178947s` 长帧得到 `0.135296s` delta，总墙钟 `2.160722s`、累计 delta `1.134685s`，差 `1.026037s`。这与 [Godot Node 文档](https://docs.godotengine.org/en/stable/classes/class_node.html#class-node-private-method-process) 的 delta 限制一致，证明原上层会漏时；昨日混合失焦与采样边界的全部 `2.1s` 差额未逐帧还原。
- 新增窄职责 `active_clock.gd`，正式 app 按单调微秒时钟归集活动区间，暂停 / 失焦时结算余量，加载后开始新会话；仍按 20Hz / 每帧 8 步处理，保存 schema 1 的既有时间 / 余量字段保持。正常同步保存停顿计入生产，失败退出等待不补产；脏状态保存和 30 秒生产保存使用实际时间，移动保持原有限制。
- `clock-1788953919-86165`：正式 Boot 无窗口检查 28 项通过。长帧段墙钟与模型加余量均为 `1.216006s`，累计引擎 delta 仅 `0.20072s`；延迟保存段墙钟 `0.241107s`，模型加余量差在浮点误差内。覆盖暂停 / 失焦、余量存读、恢复不补产、长帧后脏状态自动保存、失败留存 / 取消 / 重试 / 二次确认放弃；焦点与按钮使用信号注入，不能称原生窗口或亲测。
- 补查发现保存失败和“不保存退出”两个独占弹窗同属根窗口，触发真实 Godot exclusive-child 错误。二次确认改为失败对话框子窗口；随后取消断言仍失败，核对 [Godot 对话框实现](https://github.com/godotengine/godot/blob/master/scene/gui/dialogs.cpp) 确认取消会延迟隐藏，检查改为等待处理帧后验证，未靠修改产品隐藏行为迁就测试。最终取消留在暂停世界、确认放弃不改主档、弹窗重试完成原退出意图均通过；原生窗口还需复验。
- 性能检查改为阶段返回值与中断状态控制整段流程。最终 `performance-1788954031-87562` / `performance-1788954033-87570` 分别注入准备 / 采样期失焦，均仅尝试 `1080-local`、零完成阶段，停止采样并写中断记录；没有继续最大化、截图或后续阶段保存。此次为无窗口控制流检查，不产生帧率通过结论。
- 持续采样新增同一已处理边界的墙钟 / 推进 / 余量变化守恒（10 微秒数值容差），保留每帧记录、模拟步耗时、自动保存耗时，Boot 同步加载与等待帧耗时分列；未改变原帧率 / 模拟步 / 负载预算。原生焦点与 30 分钟前台持续结果保持待验。
- `scale-process-1788953586-84845` 复用昨日真实规则生成的满载快照，独立写 / 读进程各 107 项通过：100 台 / 1,000 带与 1,000 在途货物，全字段恢复；两个进程分别对 25 条线连续拆回载货带、重建新 ID、推进和核对守恒，增量占用 / 邻接与重建一致，继续 `87.25s` 轨迹一致。没有将满载回压说成全设备加工。
- `scale-merge-result.json`：210 项通过。正式命令构造 100 台 / 1,000 带，把一个真实矿源改接另一路输入，原线断开、19 段带明确作边缘备用；300 秒模型推进内实际同时争用 56 次，两侧各获 28 次，两个矿源均实际供料、下游产出、物料守恒。四侧越界命令原子拒绝，合法边缘带可拆回 / 重建；无窗口模型推进不替代边缘移动 / HUD / 规模操作画面。
- 已逐张审阅 `assets/art-intake/2026-09-08-factory-foundation-preview/operation-20260908-a/` 的 5 张原图：双线、载货弯道、恢复时输入缺口、最大化旋转后的设备详情和缩回布局对应记录。原图是昨日代码的证据，不用于替代今日时钟和弹窗修正的窗口复验。
- 新时钟检查进入 Shell / PowerShell 客户端 Godot 入口；工厂专用入口增加 `clock` / `interruption` / `scale-process` / `scale-merge`，导入也检查真实错误日志，避免退出码 0 冒充通过。更新 Current / Daily、功能 / 架构合同和运行时指南；根入口未修改。
- 排错记录：探针首跑相对日志路径被 Godot 解释为 user 路径，改用规定目录绝对路径重跑；时钟检查首轮缺 `decode` 的 ID 参数，补齐后解析通过。安装位置的 Godot 导入曾被沙箱阻止写全局编辑器配置，未认作成功；改用仓库既有自包含 Godot 导入通过，无新安装或全局配置更改。Boot 无窗口检查遇系统字体沙箱错误，按要求在沙箱外重跑；新增脚本 uid 未导入时静态检查失败，完成导入后通过。只忽略已确认的 macOS `ret != noErr` 噪声。
- 已复跑 Web 对照 1,839 检查点、状态 62 项与存档故障 44 项（`save-1788953376-83967`），均通过；原跨进程写 14 / 读 5、持锁 1 / 竞争 2 / 退出恢复 3 项通过。中断监听进一步前移至 Boot 启动前，准备 / 采样检查复跑通过（`performance-1788954431-90144` 及同次采样批次）。
- 最终文档预算、文本规范、仓库治理（1,742 文件）、客户端静态、Shell 语法和 diff 检查通过；更改留在 `dev` 工作区，未提交或推送。完整客户端 Godot 套件、Windows / PowerShell、修正后窗口 / 长测未执行，首包仍待收口。

## 2026-09-09 本地提交交接

- 萝卜SAMA要求提交上述更改。此次提交范围为单调活动时钟、失败退出弹窗层级、规模 / 中断定向检查及同步文档，沿 `dev` 本地提交，不推送。
- 下一步优先执行隔离正式 Boot 的短窗口复验与地图边缘操作，再安排四档共 30 分钟持续生产；通过后交付亲测。窗口可用时段尚未确认，首包整体验收与 Windows 待验状态保持不变。

## 2026-09-09 窗口补验与中断

- 上述修正已提交为 `cff8b0ef`，未推送。萝卜SAMA随后确认开始窗口复验及接续长测；本轮只增加忽略目录中的一次性 `window-review.gd`，正式客户端代码未修改。
- `window-review-1788954820-91986` 从双根隔离 Boot 载入规模汇流快照：窗口长帧区间墙钟 `1.34983s`、模型加余量 `1.34983000000028s`；可见暂停 / 继续按钮、三次鼠标选带 / 拆回 / 补接与守恒通过。四个边缘先设置附近位置，再由实际按键走最后一段，均停在约 `±31.7`；相机跟随、HUD 不穿透、最大化与缩回已留图。预置位置不计作玩家移动证据。原生尺寸主图 01–06 已逐张审阅，场坪边界、角色、带与 HUD 可辨。
- 该轮共 66 项、2 项失败，原因是对话框视口的合成鼠标未触发二次确认与重试；没有把整轮写成通过。其 `07-discard-confirmation` / `08-returned-world-list` 文件名不能证明对应状态已经发生，保留失败原貌，不用作通过证据。
- 改用系统鼠标后，`window-review-1788954970-92314` 实际打开二次确认并点击“返回”，窗口层级和取消回到保存失败提示通过，二次确认原图已审阅；未及时完成“重试保存”系统点击，等待超时，整轮 18 项、2 项失败。没有因测试超时修改产品保存逻辑。
- 延长等待预算后的 `window-review-1788955153-92694` 在 Boot 菜单阶段未成功进入工厂列表，临时脚本访问空列表报错；随后记录原生根窗口失焦并中断退出。补充菜单空值与退出状态检查，解析通过。脚本操作与原生工具观察的时序需要分开，下一次须先等待日志进入 `NATIVE_BUTTON_WAIT` 再使用系统级工具；没有确认失焦原因，不归咎于用户。
- 已停止自动开窗，向萝卜SAMA询问是否方便重新开窗；30 分钟持续生产尚未启动，原生重试 / 最终确认放弃及完整存读窗口路径仍待补齐。全部新证据位于 `check-runs/factory-foundation-v1/` 与 `assets/art-intake/2026-09-09-factory-foundation-preview/` 对应批次；未触碰正式存档、推送或发布。

## 2026-09-09 后台长测启动

- 萝卜SAMA确认方便继续，并明确允许挂在后台。正式工厂仍遵循失焦暂停；本次改为一次性 headless Boot 真实时钟检查，只在测试中注入活动焦点信号，不修改产品暂停逻辑，也不替代前台渲染性能、原生焦点或鼠标弹窗验收。
- 忽略目录新增 `background-soak.gd` / `run-background-soak.sh`，双根隔离载入 100 台 / 1,000 带的真实供给产线；以实际单调时钟运行，每分钟记录守恒、时钟差额、积压、交付、自动保存与内存，逐帧数据另存。结束保存后自动启动第二进程，对比全量状态和接续 `87.25s` 的轨迹。
- 首次解析命令未指定隔离日志，沙箱日志访问导致 Godot 启动失败；改用仓库日志并按权限要求执行后解析通过。首个 8 秒试跑因尚无完成交付而失败，保留 `background-soak-smoke-20260909-a`；短测改为断言已实际采集，交付断言只用于至少 120 秒的检查，未修改生产规则。随后 `background-soak-smoke-20260909-b` 写入 16 项 / 独立读取 5 项通过。
- `background-soak-long-20260909-a` 已于约 20:10 启动，目标真实 `1800s`，限帧 60，不打开窗口；截至启动记录尚无最终结果，不记作通过。日志、进度、原始采样和隔离存档均保留在 `tools/runtime-intake/check-runs/factory-foundation-v1/` 的同名批次。

## 2026-09-09 后台长测完成

- `background-soak-long-20260909-a` 的运行器最终状态为 `complete`；写入进程 133 项、独立读取进程 5 项全部通过，两个进程 PID 不同，日志无真实 `SCRIPT ERROR` / `ERROR:`。运行器顺序核对进程退出和日志后才写入完成状态，未重新开窗或重复启动。
- 实际单调时间 `1800.001092s`，模拟完成 36,000 步；交付 4,375 件。107,719 条原始帧记录时间单调，逐帧“推进 + 余量变化 − 墙钟”的最大绝对差额为 `1.0898e-9s`，最大余量 `0.049999s`；每分钟及结束时的规模、物料 / 批次守恒、活动状态和无保存失败断言通过。
- 59 次自动保存均成功，耗时 p95 `118.174ms`、最大 `149.608ms`。模拟单步 p95 `3.473ms`、最大 `48.34ms`；限帧 60 的无窗口循环间隔 p95 `18.821ms`、最大 `151.535ms`，保存停顿仍包含在计时记录内。这些值不是渲染帧率或 GPU 性能结论。
- 长测写入从隔离正式 Boot 进入；独立读取使用正式 `Store` 接口，逐字段比对最终状态，并验证恢复后接续 `87.25s` 与预期轨迹一致、可再次保存。两份结果中的共享 `scope` 描述不能用来证明读取进程走过 Boot 窗口。最终主档及三代备份保留在同批次 `worlds/86af9f468c63e61d82cf02c76490f899/`，未触碰正式存档。
- 本轮收口更新 Current Plan / Daily Start；产品代码和失焦暂停契约未修改。后台生产 / 计时 / 存档稳定性通过，前台四档持续渲染性能、原生重试 / 最终放弃及其余窗口路径仍待验，首包不判整体验收。文档预算、仓库治理（1,742 文件）与差异空白检查通过；未推送或发布。

## 2026-09-09 原生保存复验与前台性能启动

- 上述窗口 / 后台结果及首包专题已提交为 `2eef577a`，未推送。萝卜SAMA随后要求继续，授权接续原生保存路径与四档前台性能。运行时产品代码未改；临时与正式检查脚本、记录按实际问题修正。
- `native-save-1788958506-19432`、`native-save-1788958674-20528` 均在首次按钮操作前失焦中断，各 2 项 / 1 失败；第一轮原生工具返回项目管理器，但改用 macOS 应用启动后仍有初始失焦，不能将两轮原因都归结为工具绑定。检查改为首次实际点击前只等待，随后才启用原有失焦中断，不请求焦点。
- `native-save-1788958756-20820` 完成诊断操作，结果文件为 49 项 / 0 断言失败，但日志有按钮释放后读取 `text` 的脚本错误，因此整轮不判通过。另确认提前布置的未保存差异会在等待鼠标时触发自动保存弹窗；改为返回按钮 `button_down` 时才用正式拆回命令布置差异与发布故障，不改变产品自动保存行为。按钮文字在等待前保存，避免点击销毁界面后再访问节点。
- `native-save-1788959483-23647` 53 项全部通过，退出完成且日志无脚本错误。全部按钮由系统鼠标操作：Boot 工厂入口 / 继续 / 暂停 / 保存、失败后二次确认取消、重试保存返回、再次载入、最终确认放弃返回、再次载入及正常保存返回。重试后的拆改落盘；取消和最终放弃保持原主档字节，最终重进恢复被放弃的设备并保留先前已重试保存的改动。
- 唯一世界的列表选择、故障和两处合法拆回属于检查布置，不计作玩家建造证据；重进发生在同一 Boot 进程，跨进程完整状态证据仍取此前独立检查。01–05 原图均已审阅，失败说明 / 确认文案和按钮可辨，世界列表与恢复画面符合路径。原生焦点在主窗口与所属弹窗间切换的事件保留；尚未把该结果扩写为原生关闭窗口保存或全量亲测通过。
- 约 21:20 启动 `performance-1788959983-24439`，以系统鼠标点击准备页后进入既有性能脚本；准备页不注入焦点、不改变正式采样、分辨率或预算。四档各 450 秒，首档已记录实际生产进度，当前尚无最终结果。日志为 `check-runs/factory-foundation-v1/foreground-performance-review.log`；原始采样与截图使用同批次目录，失焦 / 关闭即中断，不抢回窗口。

## 2026-09-09 前台中断与诊断数据补齐

- `performance-1788959983-24439` 随后在首档 `1080-local` 中断，结果 `interrupted=true`、`phases=[]`；仅尝试第一档，没有继续其他档位，也未自动重开。旧脚本将失焦与关闭合并记录，本批次不能进一步确认是哪一种事件。
- 中断前保留 10,298 帧、`166.187607s` 活动时间，帧间隔 p95 `32.176ms`、最大 `111.215ms`；各 30 秒区间 p95 约 `31.58–32.35ms`，高于 `16.7ms` 预算，并非只有一次保存尖峰。局部时间归集差额在约 `5.2e-12s` 内。此为中断样本的观察，不能记作完成一档或正式性能通过。首档原图已审阅；GPU 时间未取得，不能凭这些数据确定 CPU / GPU 瓶颈。
- 发现旧中断文件仅保存帧记录，丢失当时的逐帧模拟耗时、模拟步和保存样本；正式 `factory_performance_check.gd` 现在为完整及中断记录保留这些数据，附采样窗口 / 3D 分辨率与缩放，并单列 `focus_lost` / `window_closed` / `focus_unavailable` 原因。未修改生产、渲染算法、失焦规则或性能预算。
- 无窗口准备中断 3 项、采样中断 5 项通过；采样中断前先发生真实模拟步和真实保存，最终落盘确认保留 3 帧、4 个逐帧模拟记录、1 个模拟步及 1 次成功保存，后续阶段未进入。批次为 `performance-1788961474-31261` / `performance-1788961476-31274`。各阶段开始即清空样本，避免后续准备期中断时误带上一档数据；准备中断的所有样本与采样上下文确认为空。首次沙箱运行遇系统字体与内存信息权限错误，失败日志另存 `interruption-warmup-font-denied.log`；按权限要求在沙箱外重跑后通过，没有过滤这些真实错误冒充成功。
- 客户端静态、文档预算、仓库治理（1,742 文件）及差异空白检查通过；未执行全量 Godot 套件或 Windows 检查。下一步优先短窗口诊断前台流畅度，再重跑四档长测；原生关闭窗口保存及独立重进仍待补，首包尚未整体验收。

## 2026-09-09 前台帧间隔定向诊断

- 萝卜SAMA要求继续推进后，使用隔离正式 Boot 与相同 100 台 / 1,000 带空物料工程线诊断；产品代码、画质、实际分辨率和预算未改。一次性 `short-performance-profile.gd` / `frame-scheduling-profile.gd` 位于忽略的 `check-runs/factory-foundation-v1/`，系统鼠标启动；保留父检查的失败断言，帧调度对照允许记录超预算后继续下一对照段，但失焦仍停止整轮。
- `performance-1788962053-33222` 实际 `45.003956s`、3,640 帧，帧间隔 p95 `16.476ms`、模拟步 p95 `1.762ms`。因空线尚未交付，7 项中“实际交付”1 项失败，整轮不通过；不能为了短测删掉生产判定。延长至 90 秒覆盖填线和交付。
- `performance-1788962464-33573` 实际 `90.011152s`、5,211 帧、交付 100 件，7 项中帧预算 1 项失败：帧间隔 p95 `33.719ms`、最大 `124.226ms`。模拟步 p95 `2.508ms`，无持续积压，计时差额约 `2.94e-12s`；3 次自动保存成功，耗时约 `103–117ms`。各 15 秒区间帧 p95 `30.25–34.82ms`，波动不只来自自动保存。
- 在节点处理前 / 后各加一个诊断节点计时，90 秒样本节点处理跨度 p95 `6.272ms`；3D 视口渲染 CPU p95 `2.187ms`，渲染准备 p95 `0.790ms`。这些分别测得的分位数不能直接相加或从整帧相减来推算 GPU 等待。引擎监视器与视口测量也不保证同帧对齐；API 含义见 [RenderingServer](https://docs.godotengine.org/en/stable/classes/class_renderingserver.html#class-renderingserver-method-viewport-get-measured-render-time-cpu)。本机启用视口测量后 GPU 值仍全部为零，记为不可用，不作零 GPU 开销结论。
- 调度设置实测为 VSync 开启、屏幕报告 120Hz、`Engine.max_fps=0`、低处理器模式关闭。`performance-1788962902-35318` 原计划同步开启 / 关闭 / 恢复各 60 秒，仅在测试进程改变设置：开启段 `60.009909s`、交付 25 件、帧 p95 `18.089ms`；关闭段在 `29.596110s` 收到 `focus_lost`，局部帧 p95 `15.057ms`，恢复段未进入。退出前恢复原同步模式；工程配置未改。这提示帧调度值得继续排查，但前后恢复对照缺失、生产阶段不同，不能据此定因或选择永久关闭同步。
- 尝试对 PID 35318 运行 3 秒 `sample` 时进程已退出，工具报告 `pid_for_task` 失败、没有生成调用栈报告；命令退出码 0 不能当采样成功。已按失焦规则停止，没有重开；前台时段已询问，尚未收到接续答复。30 分钟长测未启动。
- 两轮普通短测原图和同步关闭段载货原图已审阅，HUD 与场地保持同一配置。原始 CPU / 帧记录、保存样本及 `frame-profile-summary-20260909.json` 保留；未把中断对照记为性能通过。`native-close-review.gd` 已准备并通过语法检查，但原生关闭 / 独立 Boot 重进尚未执行。
- 接续先补齐帧调度对照与有效调用栈，确定原因后再选择修正；继续保留原生关闭保存与四档长测缺口，不凭模拟预算通过修改渲染配置。

## 2026-09-09 收工审阅与次日交接

按 Asia/Shanghai 的当日范围核对本地全部分支提交，并逐项对照代码与当前文档：

| 提交 | 今日推进 | 文档审阅结果 |
| --- | --- | --- |
| `cff8b0ef` | 单调活动时钟、失败退出弹窗层级；时钟、满载跨进程、汇流、中断检查与 Shell / PowerShell 路由 | 时钟与保存协议无需改方案；补清活动帧 8 步属于协调层、重试保存按退出意图分支，以及已取得的原生证据 |
| `2eef577a` | 窗口补验与 30 分钟后台稳定性记录 | 保留后台注入焦点、独立读取仅走 Store 的限制，不扩写为前台渲染或独立 Boot 通过 |
| `0f787b5a` | 中断采样数据与明确原因；原生保存复验及前台诊断记录 | 同步专题入口、架构、产品接续与运行时指南，修正仍称原生弹窗未验的旧口径；帧超预算与不完整对照保持未完成 |

- 已先提交工作区更改，再做本次纯文档收口；不追加产品代码、模型、画质、存档格式或系统配置变更。旧二维基线和工程结构入口已检查，仍正确路由到三维工厂专题，无需扩大修改。
- Daily Start 计划日期改为 `2026-09-10`：先补齐帧调度归因及原生关闭保存 / 独立重进；原因明确、必要修正完成且短测稳定后，再安排四档共 30 分钟前台长测，最后核对亲测退出条件。Windows 与完整客户端 Godot 套件保持如实待验。
- 萝卜SAMA明确今天收工，此前前台时段询问不再等待；今晚不再启动窗口、长测或自动接续。已有隔离存档、截图和失败记录保留，未推送或发布。
- 收工验证：客户端静态、文档预算、文本卫生、仓库治理（1,742 文件）与差异空白检查通过；未重跑 Godot 运行时或 Windows 检查。最后以独立文档提交保存本次审阅和次日事项。

## 2026-09-12 性能归因、生产视图优化与原生关闭保存

萝卜SAMA确认按当前规划继续首包收口。开始时 `dev` 工作区干净，HEAD 为 `2196f5fa`；本轮沿 P1–P3 范围推进，不新增玩法、依赖、存档格式或发布动作。

### 同状态性能对照与证据范围

- 修正一次性诊断脚本的对照条件：三段原先串行推进同一个世界，生产阶段不同；新脚本每段从正式规则生成并经 codec 校验的同一 `90s` 快照开始，暂停完成截图 / 预热后再运行真实前台时间。夹具预推进不计作运行时证据。负载始终 100 台 / 1,000 带，窗口报告 `1920×1080`，实际内容图 `1728×1080`、3D `1728×894`，4× MSAA；画面留边与 3D 像素分别记录，不把窗口报告当 3D 大小。
- `performance-1789191783-3730` 同步开启 / 关闭 / 恢复各约 90 秒完成，22 项检查中 3 项帧预算失败，无中断；各段交付 225 件、推进 1,800 步。三份原生 `sample` 报告确实生成，文件 `same-state-20260912-a-<phase>.sample.txt/json` 保留 PID、退出码及采样墙钟起止。采样开始分别为 UTC `05:43:40.657`、`05:44:55.555`、`05:46:24.364`，各采样 3 秒，符号整理结束亦记录；这些时段有工具干扰，整段统计未删除干扰帧，不作为无干扰性能通过依据。
- 主线程调用栈出现 `IOSurfaceSharedEvent waitUntilSignaledValue` 和 `CAMetalLayer nextDrawable`，Godot 内部多数符号未展开，不能仅凭等待栈断言 GPU 算法或 VSync 为唯一原因。默认 Metal 视口 GPU 测量仍全零，明确记为不可用。

| 批次 / 段 | 秒数 | 帧 p95 ms | 模拟步 p95 ms | 节点处理跨度 p95 ms | 绘制调用 p95 |
| --- | --- | --- | --- | --- | --- |
| `3730` 同步开启 | 90.003 | 33.550 | 3.229 | 7.202 | 557 |
| `3730` 同步关闭 | 90.011 | 17.459 | 2.562 | 6.077 | 557 |
| `3730` 同步恢复 | 90.027 | 28.654 | 2.896 | 6.930 | 557 |
| `4266` 省去重复更新 / 原 Metal | 90.009 | 24.946 | 1.138 | 3.434 | 557 |
| `4891` 临时 Vulkan / 原效果 | 90.025 | 24.537 | 1.109 | 3.213 | 557 |
| `5867` 再共享状态灯 / 原 Metal | 90.007 | 21.237 | 1.113 | 3.324 | 508 |

- 后三批完整 ID 为 `performance-1789192231-4266`、`performance-1789192808-4891`、`performance-1789193700-5867`；各 8 项中仅帧预算 1 项失败，均交付 225 件。最终批次计时差额约 `9.65e-12s`，最大余量 `<0.05s`，2 次采样内自动保存全部成功；完整帧、逐帧模拟、步耗时、保存与视口计时均保留。分位数不相加 / 相减推算未测开销；不同进程及干扰条件有差异，不把全部性能改善归于补丁。
- 临时 Vulkan 维持 Forward+、VSync 和原画质，仅由进程参数选择，不写工程配置；其 3D GPU p95 为 `22.240ms`，不能转记成 Metal GPU 时间。由此补齐渲染侧观察，但不支持永久切驱动即可解决的结论。系统只读记录为接电、无已记录热 / 性能告警；不据此排除全部系统干扰，也未关闭其他应用。
- `performance-1789193233-5380` 继续在 Vulkan 下以同一状态运行原效果 / 临时无 SSAO / 临时无阴影 / 恢复各 45 秒，每段交付 125 件，帧 p95 为 `22.420 / 17.913 / 15.959 / 16.265ms`，3D GPU p95 为 `19.049 / 17.498 / 15.028 / 15.843ms`。无阴影段绘制调用 p95 从 557 降至 280；恢复标志确认 SSAO / 阴影均开启、VSync 回到 1。恢复原效果本身也变快，说明存在顺序 / 预热或运行时波动，不能把分段差额全归于单个效果；本批次只作归因，不算四档验收。
- 原生操作工具曾在读取已退出进程时出现项目管理器；独立恢复前退出该管理器后选中实际验证进程。没有重新运行用户关闭的测试，也未把工具误选窗口记作产品失败。启动页布局需待窗口激活后重新观察再点击；正式采样期间未抢焦点。

### 两项有边界的视图修正

- `view.gd` 根据模型身份、命令 revision 与固定步 time 更新货物显隐 / 材质 / 变换、状态灯和进度条，省去未变生产状态在多个渲染帧中的重复提交。人物、相机、预览、选择仍逐帧处理；暂停命令、显式 invalidate 与模型替换会刷新，未减少模拟步或改变物流规则。
- 原状态灯每台建立同形 SphereMesh 与独立材质；现共享一个同形网格和四种只读状态材质，按实例反馈切换材质引用。保持几何细节、发光、阴影与原图一致，不通过降低灯质量节省开销。共享相同网格 / 材质有利于 Forward+ 自动实例化，依据 [Godot 官方说明](https://docs.godotengine.org/en/stable/tutorials/performance/optimizing_3d_performance.html#use-automatic-instancing)，实际收益仍取本机数据。
- 最终静态内存监视值约 `424.6MB`，上一个仅缓存更新的 Metal 批次约 `513.7MB`；两批独立进程，作为观测值记录。基准同状态五张 Metal 图（同步三段、缓存、共享状态灯）SHA-256 都为 `a8c0538d8f84e167101c61c62f856cf231d3c70bcef3422bbc9e131a4497fee2`；已审阅原实现、缓存后及最终共享状态灯原图，画面、阴影、HUD 保持一致。
- 一次性 `view-equivalence-20260912.gd` 与原 view 对比 28,806 项，覆盖空带、载货 / 半批、连续帧、生产步、暂停拆回 / 投入、预览 / 选择、显式重建及模型替换；`view-equivalence-20260912-c.log` 最终通过且无错误。首轮断言虽通过，但测试构造未入树 App 导致资源泄漏，保留原失败日志；改用明确 UI 夹具并完整清理后重跑通过。
- 新增正式 `factory_view_state_check.gd`，最终 15 项通过，覆盖共享状态不串线、生产更新、暂停命令、预览 / 人物和同 revision / time 的替换模型。首次夹具错误假设 `93.25s` 必有进度 `<0.9` 的晶体带，空筛选导致脚本错误；已选择合法载货带，不放宽真实生产判定。Shell 增加 `view-state` 定向入口，完整客户端 Shell / PowerShell Godot 路径均纳入此检查；Windows / PowerShell 未执行。

### 系统关闭保存与独立 Boot 恢复

- `native-close-20260912-a` 写入 PID 4492、读取 PID 4713。两次均注入工厂与旧切片隔离根，经正式 Boot 工厂入口和继续按钮进入；所有按钮与系统关闭均使用原生鼠标。初始工程产线由合法规则预推进 `93.25s`，与玩家建造证据分开。
- 写入阶段实际生产后暂停，保留载货带与半加工批次；实际点击系统关闭按钮，17 项通过：正式 quit 意图、全部未保存状态落盘、主档字节改变和退出前释放锁。读取阶段首个模拟帧前比较所有状态字段，验证不同 PID 及重新取得写入锁，继续生产后正常保存返回，15 项通过。日志无真实脚本错误；读取启动前的失焦记录保留，不作为采样中断或有效操作时长。
- `01-paused-before-native-close.png` 与 `02-independent-boot-reloaded.png` 原图已审阅；第二张拍于重进并继续生产后，完整原存档比较取首帧前断言，不能用截图里的后续数量代替恢复瞬间。原图根为 `assets/art-intake/2026-09-12-factory-foundation-preview/native-close-20260912-a/`。
- 主档、备份、预期关闭快照和两阶段结果保留在 `tools/runtime-intake/check-runs/factory-foundation-v1/native-close-20260912-a/`；该路径不访问用户正式档。此项关闭了原生正常关闭 / 独立 Boot 重进缺口，不替代 Windows 锁、其他平台或用户亲测。

### 本轮结论与接续

- 两项低风险渲染优化有状态、原图及绘制调用证据；原生关闭保存闭环完成。GPU 分项 / 同步恢复对照已从缺失变为有数据，但原配置短测依然超预算，尚未达到启动四档长测的条件。
- 继续现有 P3，不再凭一次 p95 或关闭效果结果叠加修改；下一轮先控制预热和顺序，获取可重复的 GPU 分项与帧等待证据，再决定保持原画质的修正。不能宣布唯一根因已找到，也不永久改驱动、同步或效果。
- 当前规划、日常交接、首包专题、架构及运行时指南已同步。四档前台长测、完整客户端 Godot 套件、Windows / PowerShell 和用户亲测仍待完成；本轮不推送、不发布，不宣布阶段退出。

- 本轮最终验证：Godot 无窗口导入、生产画面状态 15 项、最终与原 view 等价比较 28,806 项、客户端静态、文档预算、仓库治理（1,744 文件）及 `git diff --check` 通过。原生关闭写 / 读日志无错误；诊断日志中的超帧预算失败保留，不合并成全绿结论。

## 2026-09-12：诊断准备页首次铺满修正

- 萝卜SAMA指出临时诊断窗口先在左上角显示小画面，稍后才铺满，并授权修正。原准备页继承最大化配置后又写入 `1100×720`，立即挂载控件；这是诊断入口自身的初始化冲突。未记录旧版本原生 resize 回调，不能断言具体是哪次系统事件使旧画面恢复，也不将此现象归因于尚未加载的工厂负载。
- 新增 `client/scripts/checks/factory_diagnostic_gate.gd`，8 个忽略提交的既有临时诊断入口改为调用它：`short-performance-profile`、`frame-scheduling-profile`、`foreground-performance-review` 及 9 月 12 日的 `same-state-frame`、`cached-view`、`vulkan`、`render-effects`、`shared-status` 五个 profile。各段负载、时长和采样实现保留；新建临时准备页的调用方式进入运行时指南。
- 准备页明确采用最大化和自适应铺满，不写小窗口尺寸；渲染后核对原生尺寸、实际图像像素与布局，跨帧确认尺寸稳定再开放按钮。5 秒为明确失败上限，不是显示延时；等待点击上限保持 180 秒。原内容比例和自动关闭策略在开始 / 取消时恢复，关闭处理连接释放后返回调用方。
- 尺寸守卫两轮失败复盘：首轮误用带拉伸变换的 `ViewportTexture.get_size()`，返回 `4763×2680`；改读实际图像后，第二轮发现原等比留边策略使 `3024×1716` 窗口只渲染 `3024×1701` 内容，等待无法解决契约冲突。收尾明确准备页采用铺满策略，而正式 Boot 仍恢复原策略；没有增加固定等待、放宽像素一致判定或修改产品画质。最初还修正一次 Dictionary 布尔表达式的类型推断解析错误。失败日志和结果保留，未记为通过。
- `tools/runtime-intake/check-runs/factory-foundation-v1/gate-20260912/` 最终 `metal-c` 25 项通过、`vulkan-c` 12 项通过；按钮就绪分别约 54 / 44 毫秒（从准备页函数开始计时，不代表进程冷启动时长）。首次可见帧的原生窗口与实际图像均为 `3024×1716`；Metal 另覆盖 `1400×900` 窗口和恢复最大化。四角像素填满、全区域布局、原设置恢复及清理均通过。
- Metal 使用原生鼠标一次点击按钮进入双存档根隔离的正式 Boot；Vulkan 使用系统关闭按钮取消，返回 false 且未进入 Boot / 采样。原生工具读取退出进程时误开项目管理器，随后退出并只读确认无剩余 Godot 进程。原图在 `assets/art-intake/2026-09-12-diagnostic-gate-preview/`；Metal / Vulkan 首帧、Metal 小窗口及 Boot 原图已审阅，最大化原生窗口亦已复核。
- 导入首轮保存全局编辑器设置被沙箱拒绝，经本轮明确授权重跑导入成功；8 个迁移入口的 Godot `--check-only` 单独复核。仅此准备页修正，不改变当前规划与 P3 性能结论，不执行工厂负载或长测；Windows 和完整客户端 Godot 套件未运行。
- 最终客户端静态、文档预算、仓库治理（1,746 文件）与 `git diff --check` 全部通过；两份最终窗口日志无脚本错误。公共实现和使用说明进入本地提交，临时入口与测试产物按既有规则保留在忽略目录，不推送。

## 2026-09-12：重复短测与 GPU 采样干扰排查

- 萝卜SAMA确认继续 P3。开始时 `dev` 工作区干净，HEAD 为 `2e143811`；本轮未修改产品实现、画质、默认驱动、同步或预算。一次性入口与分析脚本保留在 `tools/runtime-intake/check-runs/factory-foundation-v1/gpu-breakdown-20260912/`，所有运行仍从双存档根隔离的正式 Boot 进入。
- 三组完整对照均重复两段原配置，每段从同一合法 `90s` 生产快照开始，暂停预热固定 15 秒后生产 45 秒。预热按墙钟计时，不因帧率不同改变预热时长；夹具预推进和暂停预热不计为真实生产。Vulkan 仅通过该进程启动参数选择；未关闭 SSAO、阴影或 MSAA。

| 批次 | 测量配置 | 两段帧 p95 ms | 两段模拟步 p95 ms | 两段 3D GPU p95 ms |
| --- | --- | --- | --- | --- |
| `performance-1789212421-22248` | Vulkan，视口计时及 `--gpu-profile` 分项时间戳 | 44.838 / 39.725 | 1.053 / 1.060 | 42.558 / 38.880 |
| `performance-1789212681-23133` | Vulkan，仅视口计时 | 24.281 / 21.934 | 1.204 / 1.117 | 23.757 / 21.661 |
| `performance-1789212910-25623` | 默认 Metal，无额外 GPU 计时 | 20.595 / 20.853 | 1.087 / 1.155 | 不可用 |

- 三组各 15 项检查中仅两段帧预算失败，无失焦或其他断言失败；不能将整组写成通过。每段完成 900 步、交付 125 件，余量 `<0.05s`，计时差额约 `6e-13s`；各发生一次采样内自动保存且成功。默认 Metal 两段自动保存为 `95.275 / 128.681ms`，仍包含在完整帧记录内，没有删除保存停顿以降低分位数。
- 窗口报告 `1920×1080`，正式采样实际内容图 `1728×1080`、3D `1728×894`，4× MSAA、VSync 1、缩放 1。六张同状态基准截图 SHA-256 均为 `a8c0538d8f84e167101c61c62f856cf231d3c70bcef3422bbc9e131a4497fee2`；已审阅 Vulkan 仅视口计时首段和默认 Metal 第二段原图，场景、阴影与 HUD 一致。启动早期 inventory 的视口尺寸不代替预热后的正式采样尺寸。
- 分项采样与仅视口计时的 Vulkan 帧时间差异明显，提示测量本身造成扰动；两组是独立进程，仍可能有运行时波动，不能将差额全部视为精确的采样开销。默认 Metal 移除额外 GPU 计时后仍重复超预算，说明问题不能仅归咎于诊断计时。
- 分项批次保留原始 GPU 标记、视口计时和 `gpu-summary.json`：两段分别有 170 / 172 个不同标记帧，未出现相邻负时间差。SSAO、阴影、opaque 与 resolve 标签耗时较高但波动明显，例如 SSAO 中位数两段为 `8.398 / 0.471ms`；这些受扰动数据不支持将 SSAO 定为唯一根因，也不能把分位数相加 / 相减推算未测耗时。渲染统计绘制调用 p95 保持 508，阴影绘制为 247，可见物体绘制 p95 为 230；统计口径分别保留。
- 核对 [Godot 渲染计时实现](https://github.com/godotengine/godot/blob/master/servers/rendering/rendering_server_default.cpp)：本机原始 GPU 时间按纳秒、CPU 按微秒换算毫秒，与实测数量级一致。上游 [Metal 时间戳接口](https://github.com/godotengine/godot/blob/master/drivers/metal/rendering_device_driver_metal.cpp) 的结果填零、写入为空实现，与本机 GPU 全零一致；这不是 GPU 开销为零或硬件不能采样的证据。运行时指南补充测量开销、原始单位与有效轨迹要求。

### 原生 Metal 采样接续边界

- 本机已有完整 Xcode / Instruments，`xcrun xctrace list templates` 确认存在 `Metal System Trace`；首次沙箱执行因 Instruments 缓存目录权限失败，经授权重跑查询成功，未安装依赖。已读取 record / export 帮助，尚未实际运行 record，不能把工具可用记作取得轨迹。
- `native-metal-a.log` 准备页约 79 毫秒就绪，但未成功点击开始，180 秒后 `PROFILE_START_TIMEOUT`；没有进入正式工厂，不算性能运行。原生工具在读取已退出应用时打开项目管理器，随后关闭，再启动自动进入正式 Boot 的一次性诊断入口。
- 自动入口 `performance-1789214427-28895` 已进入正式工厂，在 `native-metal` 暂停预热期间收到 `focus_lost`；3 项检查中 1 项中断失败，`phases=[]`，没有完成生产采样或生成有效 `.trace`。失焦原样保留，不推断原因；已停止自动重开并询问桌面是否方便，不能用中断记录填补原生采样缺口。
- 下一步仍是对实际 Godot PID 取得短时原生轨迹并记录对应生产时段，区分 GPU 执行、提交与显示等待；原配置短测稳定前不启动四档长测。已完成的三组对照可直接复用，不再凭受扰动分项值增加代码优化或降低画质。完整客户端 Godot 套件、Windows 与亲测仍待验。
- 本轮文档预算、仓库治理（1,746 文件）及 `git diff --check` 通过。三组完整运行日志仅有已记录的帧预算失败；初始 headless API 枚举日志另有系统证书读取错误，保留原日志，未将该枚举当成运行时通过证据。当前仅提交正式文档与接续记录，临时诊断与产物留在忽略目录；未推送或发布。

## 2026-09-12：原生 Metal 轨迹与限帧对照

- 用户点击准备页后取得有效 10.906 秒 Metal 轨迹；完整 90 秒生产无中断，帧 p95 21.227ms 仍超预算。581 次 drawable 等待 p95 19.225ms，每帧 GPU 活动并集 p95 15.073ms；二者重叠，不能相加或相减。
- 原不限帧 / 临时 60 FPS / 恢复各 45 秒完成，帧 p95 20.131 / 20.772 / 18.104ms，均超预算；生产、保存、原图和设置恢复正常，不采用限帧。首次中断由用户确认为误切换窗口，失败批次保留。
- 两轮完整批次、原图 hash、采集范围、检查和失败记录迁至 [Metal 呈现诊断记录](../reference/factory-metal-presentation-review-20260912.md)，避免周志继续堆叠逐项诊断数据。本地提交分别为 `f16184b7`、`f189b69c`。

## 2026-09-12：呈现时钟与对象核对收口

- 本轮只导出和分析已有轨迹，未启动窗口。校正呈现时间导出异常，581 个时间与显示交换对齐误差最大 83.704ns；581 对原始等待与导出完全一致，但无 drawable 标识。
- 否决 seed 等于 Drawable ID 的候选映射；361 条可比 surface 全部不一致，中间延迟推算撤回。319 条显示依赖通过帧号、surface 和提交时刻核对，提交至显示 p95 56.919ms；42 条不符项保留排除，子集不代表全部帧或回收时长。
- 已有轨迹不能证明具体回收唤醒关系，此轮离线分析收口。下一项改为实际 Godot 构建的获取 / 提交 / 释放源码审计，先明确可修正问题或补测位置；不重复限帧值试验，不先构建引擎。方法、复盘和后续取证要求见上述诊断记录。
- 收口验证：一次性离线审计脚本通过；文档预算、仓库治理（1,747 文件）与 `git diff --check` 通过。本轮仅分析产物和文档变更，未运行客户端 / Godot 回归；结果本地提交，不推送。
