# 2026-W17 Development Log

周期：2026-04-20 至 2026-04-26

## 本周目标

- 完成新仓库基础初始化。
- 整理 `docs/` 目录结构，统一文档英文文件名。
- 建立 LF、UTF-8、无 BOM 的仓库文本规范。
- 引入轻量分支治理、PR 模板、ruleset 模板和仓库卫生检查。
- 对齐 `AGENTS.md` / `CLAUDE.md` AI 协作规则。

## 完成情况

- 将早期文档整理为 `product/`、`architecture/`、`design/`、`reference/`、`archive/` 等分区。
- 新增 `docs/README.md` 作为文档入口。
- 新增 `.gitattributes`、`.editorconfig`，并配置仓库本地 Git 使用 LF。
- 新增 `client/`、`server/`、`assets/`、`tools/` 基础目录占位。
- 新增 `AGENTS.md`，明确项目定位、文档规则、工程偏好和目录职责。
- 新增 `CLAUDE.md`，与 `AGENTS.md` 保持基本同步。
- 新增 `.github/PULL_REQUEST_TEMPLATE.md`。
- 新增 `.github/rulesets/master-protection.json` 与说明文档。
- 新增 `.github/workflows/pr-check.yml` 与 `.github/workflows/release-check.yml`。
- 新增 `scripts/check-text-files.ps1` 与 `scripts/check-text-files.sh`。
- 新增 `docs/adr/0001-branch-and-pr-governance.md`。
- 新增 `docs/planning/current.md`。

## 关键决策

- `docs/` 是当前唯一正式文档源。
- `dev` 作为日常开发与文档集成分支。
- `master` / `main` 作为稳定主线，只通过 Pull Request 合并。
- 当前 CI 只设置 `Repo Hygiene`，不引入尚不存在的 Godot 构建或测试门禁。
- 单个源码文件原则上不超过 1000 行，硬上限 1500 行。
- 首版仍以单机完整闭环为目标，联机只做架构预留。

## 验证记录

- `git diff --check`
- `pwsh ./scripts/check-text-files.ps1`

## 风险与未完成项

- 远端 `dev` 分支和 GitHub ruleset 仍需要在 GitHub 仓库中实际创建 / 导入。
- 当前没有 Godot 工程，因此尚无 Godot 导入、构建、导出或自动化测试基线。
- 当前只有仓库卫生门禁，未来进入原型开发后需要补充真实项目验证。

## 下周建议

- 创建远端 `dev` 分支，并按 `.github/rulesets/README.md` 导入默认分支 ruleset。
- 补 `product/mvp-feature-list.md`。
- 补 `design/core-gameplay-loop.md`。
- 补 `design/resources-and-process-chain.md`。
- 准备初始化 Godot 4.x 客户端工程。
