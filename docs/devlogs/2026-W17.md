# 2026-W17 Development Log

周期：2026-04-20 至 2026-04-26

## 本周目标

- 完成新仓库基础初始化。
- 整理 `docs/` 目录结构，统一文档英文文件名。
- 建立 LF、UTF-8、无 BOM 的仓库文本规范。
- 引入轻量分支治理、PR 模板、ruleset 模板和仓库卫生检查。
- 对齐 `AGENTS.md` / `CLAUDE.md` AI 协作规则。
- 明确开发者文档、玩家 Wiki 和官方工具三条内容线的边界。

## 完成情况

- 将早期文档整理为 `product/`、`architecture/`、`design/`、`reference/`、`archive/` 等分区。
- 新增 `docs/README.md` 作为文档入口。
- 新增 `.gitattributes`、`.editorconfig`，并配置仓库本地 Git 使用 LF。
- 新增 `client/`、`server/`、`assets/`、`tools/` 基础目录占位。
- 新增 `AGENTS.md`，明确项目定位、文档规则、工程偏好和目录职责。
- 新增 `CLAUDE.md`，与 `AGENTS.md` 保持基本同步。
- 新增 `.github/PULL_REQUEST_TEMPLATE.md`。
- 新增 `.github/rulesets/master-protection.json` 与说明文档。
- 新增 `.github/workflows/pr-check.yml` 与 `.github/workflows/release-check.yml`。
- 新增 `scripts/check-text-files.ps1` 与 `scripts/check-text-files.sh`。
- 新增 `docs/adr/0001-branch-and-pr-governance.md`。
- 新增 `docs/planning/current.md`。
- 新增 `docs/product/project-definition.md`，先固定项目定义、核心元素、故事方向、UI 与客户端原则。
- 新增 `docs/product/player-wiki-and-official-tools.md`，定义玩家 Wiki 与官方工具的内容边界。
- 新增 `docs/product/visual-and-ui-direction.md`，明确工业科幻现场感、冒险 HUD、基地设备面板、地图和玩家工具的视觉一致性。
- 新增 `docs/product/worldbuilding-premise.md`，明确玩家作为前哨工程战斗员的身份，以及异星资源、化工基地、生态、污染、秘境和主线故事的关系。
- 新增 `docs/architecture/platform-and-compatibility.md`，明确 Windows 首发优先、Web 试玩预留、Android 远期评估、离线单机、本地存档和结构化数据原则。
- 新增 `docs/design/core-gameplay-loop.md`，把基地准备、外出探索、带回成果、基地加工和成长解锁串成后续设计基准。
- 新增 `docs/design/maps-and-regions.md`，定义地图层级、首颗星球区域角色和多星球扩展原则。
- 新增 `docs/design/resources-and-process-chain.md`，定义资源类型、化工链表达、副产物污染和官方工具数据要求。
- 新增 `docs/design/character-progression-and-equipment.md`，定义角色成长、装备、技能、Build 与基地生产的咬合方式。
- 归档四张视觉方向概念图到 `assets/concept-art/`。
- 新增 `wiki/` 目录，作为未来玩家 Wiki 源内容。
- 新增 `official-tools/` 目录，作为未来在线合成台、工艺链规划器和地区资源标点等官方工具入口。

## 关键决策

- `docs/` 是当前唯一正式文档源。
- `dev` 作为日常开发与文档集成分支。
- `master` / `main` 作为稳定主线，只通过 Pull Request 合并。
- 当前 CI 只设置 `Repo Hygiene`，不引入尚不存在的 Godot 构建或测试门禁。
- 单个源码文件原则上不超过 1000 行，硬上限 1500 行。
- 首版仍以单机完整闭环为目标，联机只做架构预留。
- 联机方向确定为先采用 Minecraft 式多形态联机：首版按 2~5 人协作设计，长期预留 2~10 人专服或高性能房间扩展；更远期大型多人在线只保留演进口子，不进入首版和首个联机版本范围。
- `docs/` 只承载开发者文档，玩家可见内容转入 `wiki/`。
- 面向玩家的官方辅助工具使用 `official-tools/`，不与仓库脚本 `tools/` 混用。
- 游戏数据、玩家 Wiki 和官方工具未来应尽量共享结构化数据源。
- 当前阶段先完成“项目到底是什么、讲什么故事、看起来像什么、如何发布与兼容”的上位定稿，再拆首版 MVP 功能清单。
- 视觉方向确定为清晰克制的工业科幻现场界面，叠加危险异星生态和化工设施可读性。
- 世界观方向确定为前哨工程战斗员在异常星球建立化工基地，通过生产、探索、污染治理和秘境调查逐步揭开催化现象。
- 平台方向确定为 Windows 桌面优先，Web 作为试玩和传播渠道预留，Android 只做远期评估。
- 长期探索方向允许不同星球或大型区域形成差异，但基地设备、UI、图标、数据 ID 和工程识别语言应保持统一；星球差异主要来自生态、地貌、污染类型、资源体系和基地适应需求。
- 核心玩法循环正式收束为“基地准备远征，远征带回样本和资源，基地转化为装备、工艺和通行能力，再推动玩家进入更危险区域”。
- 角色成长方向确定为不做脱离基地的传统刷怪升级，而是通过武器、防护服、模块、消耗品和技能 Build 承接基地后勤。
- 角色与存档方向确定为玩家可拥有多个角色，不同角色可有不同初始能力和天赋；世界进度、基地资源、关键装备和主线推进默认归属世界存档。

## 验证记录

- `git diff --check`
- `pwsh ./scripts/check-text-files.ps1`

## 风险与未完成项

- 远端 `dev` 分支和 GitHub ruleset 仍需要在 GitHub 仓库中实际创建 / 导入。
- 当前没有 Godot 工程，因此尚无 Godot 导入、构建、导出或自动化测试基线。
- 当前只有仓库卫生门禁，未来进入原型开发后需要补充真实项目验证。
- 玩家 Wiki 和官方工具目前只有目录与策略，不应被误认为已有可公开内容或可运行工具。

## 下周建议

- 创建远端 `dev` 分支，并按 `.github/rulesets/README.md` 导入默认分支 ruleset。
- 复核项目定义、视觉 UI、世界观、发布形态、Wiki / 官方工具和联机存档文档是否存在口径冲突。
- 复核核心循环、地图区域、资源链和角色成长桥接文档是否足以支撑首版 MVP 拆分。
- 在上位方向和桥接文档稳定后补 `product/mvp-feature-list.md`。
- 准备初始化 Godot 4.x 客户端工程。
