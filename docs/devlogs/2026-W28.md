# 2026-W28 Development Log
周期：2026-07-06 至 2026-07-12

## 本周目标

- 收束「首版 Demo 表现层重建第一版」的 `R0` 首分钟体验重基线。
- 以新档第一分钟截图序列作为本周主证据，确认开局、修复核心、晶体路径和采集提示连续成立。
- `R0` 成立后再恢复 60 秒路径录像和 `P3` 旧层清除判断；不新增玩法、区域、任务、存档字段或素材批次。

## 完成情况

- 2026-07-06 审阅 2 / 5 / 6 号截图：5 号污染边界基本可用；2 号晶体矿脉仍被旧晶体层、目标标签和旧基地视觉抢读；6 号核心稳定站点位已修回核心方向，但相机偏左，旧调试块和空场压过核心站主体。
- 针对 2 号，强化 `DemoPresentationFieldZones` 的表现层收尾优先级，补充旧基地视觉层、旧路径 / 身份层、首屏旧层和目标指引层压低，并在字段焦点内压低交互 label / focus ring。
- 针对 6 号，新增 `DemoPresentationCoreStation` 专用脚本，在核心站焦点范围内压低旧路线、非核心身份层、旧核心 run / handoff 层和目标指引；核心站场景挂接该脚本。
- 将 6 号截图定位点从核心入口左侧移到核心站主体附近，避免左侧空场和锚定桥调试块成为第一眼主读法。
- 2026-07-06 复审最新 2 / 6 号截图：2 号旧流程图层已退下，虽仍偏资源作业点但可作为晶体矿脉证据；6 号核心稳定站主体成为第一眼中心。结合此前 5 号污染边界基本可用，`P2` 三区定点截图证据成立。
- 2026-07-06 根据四张实机截图复判：旧语义轮廓 / 旧色块仍会运行时抢读，首屏设备过早堆叠，HUD 过小且截断，人物 sprite 直接旋转，核心交互后目标断裂；撤回 `P2` 成立判断，暂停录像与 `P3`。
- 插入 `R0` 首分钟体验重基线：硬禁用旧视觉层和交互语义轮廓，开局只保留损坏核心，修复后才放开晶体 / 残骸采集，收紧交互范围，放大 HUD，角色改为固定朝向 / 翻转 / 轻微步态摆动。
- 2026-07-07 补正式入口可见性：`Boot.tscn` 直接挂正式 `StartupMenu`，运行时复用该节点；补齐首分钟基线对方向标签、核心交接层和核心运行色块层的硬禁用。
- 2026-07-07 沿正式地图链路补修复核心后的晶体区视觉引导：晶体路径道具、工作灯和残骸提示随首分钟状态门显示，下一目标不只依赖 HUD 文案。
- 2026-07-07 扩展首分钟当前目标选择：`quest.scout_crystal_field` 会优先选中当前可采集晶体簇，同时远距离仍要求目标在玩家朝向前方。
- 2026-07-08 审阅首分钟截图序列：开局与核心修复方向成立，但靠近晶体采集时旧第一工业路径层、旧晶体资源层和路线 / 轮廓语言回流；在 `GameRoot` 表现层刷新末尾再次执行 `R0` 旧层禁用，并补资源链运行时检查复现旧层自刷新后被压回。
- 2026-07-08 针对实机截图仍未形成“晶体路径出现”的问题，复用现有管线、工作灯、线缆卷轴、补给箱和工具箱资产补核心到晶体的中段路径，并纳入首分钟修复前 / 修复后显隐检查。
- 2026-07-08 收尾审阅今天两笔提交后，确认仍留在 `R0` 首分钟体验重基线：下一步不是开新玩法或删旧层，而是重跑正式入口截图序列验证旧层回流和晶体路径读法。
- 2026-07-09 架构级路线复盘：萝卜SAMA 提出实机始终收敛不到 `2026-06-25-demo-first-screen-wide-reference.png`。逐层对比判定根因不是资产数量，而是生产方式——参考图是一张一次性画出的整幅场景（地形 / 光影 / 投影 / 色调同源），当前实机是把各自独立生成的 sprite 摆在近黑空背景上，缺连续地形、统一光影、接地阴影和构图。决策：插入 `R1` 地形+光影地基包（地形→平台→光照色调→接地阴影→构图，地基不成立不摆物件），并把验收口径从"真实资产渲染"改为"场景成立四要件"，平铺不算通过；已同步 `current.md`、`daily-start.md` 和本专题文档。
- 2026-07-09 `R1` 工程半落地：新增 `GroundBedFoundation`（`client/scripts/map/demo_ground_bed.gd`），用既有真实岩地 tile 把 `GroundTileMap` 覆盖扩展到填满可视地面（tile `[-18,-11]..[24,11]`，可导出调整），并关闭旧深色 `Background` 色块，解决"空背景 + 悬浮物"。只放置真实 tile、不做程序绘制。`sh ./scripts/check-client.sh` 通过；Godot 4.7 headless 导入无 script/parse 报错；仓库无多余改动。
- 2026-07-09 定位更深根因（转明日待决策）：核对 `ai-art-prompts.md` 发现风格锚点与调色板是"暗青灰"（岩地 `#151C1E`、`dark teal-gray basalt`、暗青反应堆锚点），而宽幅参考图是"浅暖荒漠地面 + 暗金属建筑"。参考图像游戏靠浅地面与暗建筑的图/底对比；当前地面与设备同为暗青、明度贴死、无图底分离，铺满也塌成暗色虚空。判定：只要地表仍按暗青生成，工程再对也到不了参考图——属风格锚点内部矛盾，需萝卜SAMA 做美术方向决策（是否把地表 / 平台改浅暖），Claude 不改锚点、不生成美术。
- 2026-07-11 完成全项目审阅并启动整改：确认内容规模、运行时检查可信度和关键承载面已成为 Demo 收口风险；建立 `Demo Scope Convergence And Runtime Health V1`，将 `H0` 运行时真相修复设为 `R1` 前置闸门，不扩玩法、不新增检查脚本。
- 2026-07-11 萝卜SAMA 接受调色板建议：地表 / 平台改为浅暖砂岩灰，建筑保持暗青灰金属；反应器锚点约束设备，宽幅参考图约束场景图底对比。已同步提示词库和阶段入口。
- 2026-07-11 `H0` 收口：开局当前目标优先范围由 58px 调整为 70px（普通交互仍为 34 / 56px 且远距离要求朝向）；后期检查状态补齐首分钟完成标记；相位行动台检查补方向姿态和空值保护；旧引导层、旧视觉层、旧 HUD 紧凑尺寸断言改为当前 `R0 / R1` 契约。统一 Godot 入口完整通过，主线恢复 `R1`。
- 2026-07-11 `R1` 浅暖地面素材接入：审定 C1 到 C4 各 3 张同风格候选，原图归档到忽略提交的 `assets/art-intake/2026-07-11-batch06/`；缩放到 64px 后做 4px 边缘 wrap 校正，并沿既有四条稳定资源路径组装为 `192x64` 三变体 atlas。三个场景 TileSet 已声明全部变体；`GroundBedFoundation` 按 tile 坐标确定性选取真实 C1 贴图，不旋转、镜像或生成视觉内容。2x2 拼贴未见硬接缝，`sh ./scripts/check-client.sh` 通过。
- 2026-07-11 `R1` 连续地形实机验收：第一轮正式新档无黑底且已转为浅暖砂岩，但 64px 裂纹重复和半透明青灰平台矩形成为主读法，判定不成立；唯一根因修正把 C1 三个真实候选保留为 4x4 宏块 atlas，并按宏块坐标确定性选取，第二轮正式新档中重复退到次级、连续地形成立。
- 2026-07-11 `R1` 基地平台落座：复用 C2 三个真实候选和现有 `MetalPlatformTileMap`，以 4x4 宏块保留大板材关系，重建有边界的不透明暖灰金属平台。正式新档与修复核心后截图确认平台承托核心、玩家和基地路径设备，与浅暖砂岩形成清楚图底关系，没有恢复旧色块或均匀 64px 测试砖格。
- 2026-07-11 复盘图像密集型会话中断：同一会话累计多次完整 PNG 工具结果后发生长时间重连并被中断。新增仓库级稳定性约束：一个素材类别一个生成会话、单会话最多 3 张、生成与审阅 / 接入分离、项目图片立即复制到 `art-intake`、中断后先清点落盘结果；同步 `AGENTS.md`、`CLAUDE.md` 与提示词真相源。
- 2026-07-11 日终复核今日五笔提交：`H0` 恢复统一运行时真相，浅暖 C1 到 C4 atlas 完成接入，图像生成会话稳定性约束已同步协作文件，连续地形与基地平台通过正式入口截图验收；代码、专题、阶段入口和周志现已统一指向 `R1` 的统一光向与整体色调步骤。
- 2026-07-12 `R1` 统一光向与整体色调收口：确认现有运行时资产没有 normal map，设备与角色贴图已烘焙同一左上光，因此不接入只会均匀增亮的 `DirectionalLight2D`；正式地图新增 `SceneColorGrade`，首屏与晶体路径活跃 sprite 归一逐件 RGB 染色并保留状态 alpha，玩家移除额外过曝增亮。
- 2026-07-12 两轮实机截图复核：首轮全屏偏黄褐，唯一一次根因修正把场景级调色改为压红、保蓝的中性日光；第二轮新档开局 / 修复核心后确认浅暖地面、暗青金属、青色能量、琥珀灯和晶体冷色区域差异同时成立，第 3 步通过，下一步进入接地阴影。
- 2026-07-12 `R1` 接地阴影首轮收口：新增可复用 `GroundShadow` 原生 `Sprite2D` 组件，复用目标贴图透明轮廓，按物件高度贴合脚底并统一向右下压缩投影；首屏核心、玩家、可见基地设备和修复后晶体簇、采集器、工作灯、卷轴、工具箱及箱体均已接入，平台、管线和地面贴花保持无夸张投影。
- 2026-07-12 正式入口截图判定第 4 步通过：新档开局与修复核心后均确认关键物件明确落座，阴影未形成大片黑污，也未遮挡平台纹理、青色能量、琥珀灯、玩家交互环或 HUD 提示；下一步进入构图收拢，不做第二轮阴影调参。
- 2026-07-12 `R1` 构图收口：运行时重建非对称基地平台与晶体地面轮廓，首屏可见道具形成核心主体、右侧支撑物和下方前景簇；修复后主晶体保持既有玩法锚点，卫星簇、工作灯、箱体和残骸沿右上 / 右下形成清楚主次。
- 2026-07-12 两轮构图截图复核：首轮修复后画面成立，但新档平台下半部仍是大片无功能金属空场；唯一根因修正只收紧平台轮廓，第二轮新档 / 修复后均确认焦点、疏密、前中后景成立，基地首屏场景成立四要件全部通过。
- 2026-07-12 最终参考图复核撤回 `R1` 通过结论：萝卜SAMA 提供正式新档实机截图并对照宽幅参考图，确认现有画面仍是透视 / 比例不统一的独立切图贴在正交平台上，工业密度不足且原型 HUD 占比过大。四要件只能作为必要条件，不能代表整景达到目标。
- 2026-07-12 触发两轮失败闸门并完成架构升级：停止缩平台、挪 sprite、补 tile 和阴影调参，暂停 `P2` / `P3`；批准 `R2`“统一宽幅静态场景板 + 同透视交互前景”路线。场景板承载地形、平台、道路、静态管线和背景设施，但不得烘焙玩家、HUD、状态核心或交互设备。
- 2026-07-12 `R2-A` 第一轮审阅无定稿候选，第二轮以 `v1` 为基底修正晶体方位、出站路径、插槽和 HUD 安全区；最终 3 张候选仍没有一张同时满足路径、`1 + 3~4` 插槽、静态 / 交互语义和 HUD 避让，正式判定第二轮失败并停止同类生成。
- 两轮证据索引：第一轮生成会话 `019f55a0-ace1-78f2-8a93-f3e6043ad138`、审阅会话 `019f55c2-f6c4-7472-8451-3ab993cd68af`；第二轮生成会话 `019f55e2-5b9a-74b0-a674-1cdc11600092`、最终审阅会话 `019f5630-75d0-7f22-983a-f191d0debb73`。候选留在忽略提交的 `assets/art-intake/2026-07-12-batch01/` 与 `batch02/`。
- 2026-07-12 项目级停手：失败主要来自生成模型的多约束空间控制不足，以及单张生成式场景板与精确玩法装配需求不匹配。萝卜SAMA 决定先把证据与边界写入仓库，暂停全部实现开发，重新考虑项目目的、产品结构、工程架构和个人 + AI 的职责分工。
- 2026-07-12 收紧分支拓扑治理：阶段 PR 统一使用 `merge commit`；每次默认分支 PR 合并后，必须先将新的 `master` / `main` merge commit 快进回 `dev` 并推送，再开始下一轮开发；无法快进时只允许检查拓扑后普通 merge 回流，不重写共享 `dev` 历史。
- 2026-07-12 项目级复盘首批结论确认：第一目的定为证明产品可行性（20 到 40 分钟可被朋友完整玩完的切片）；美术介质由写实 2.5D 整景切换为像素 + 2D 俯视网格（32px 玩法网格、建筑多格占位、虚拟分辨率由证据轮定档）；参照系为星露谷 + Factorio + 银河破裂者（仅取循环）+ CrossCode 式对话立绘，二次元立绘限 UI 层；恢复实现前先完成介质证据轮，细节见复核文档「已确认结论」。

## 关键决策

- 本轮不生成新素材、不扩玩法，只处理截图证据暴露出的旧层遮挡、标签抢读和定位取景问题。
- 5 号污染边界暂按基本可用处理，除非后续录像暴露黄绿 tile 重复感抢主读法。
- 不继续按单张截图做同类调参；先用新档第一分钟截图序列验证 `R0`，通过后再恢复路径录像判断。
- 2026-07-09 决策：不再把"真实资产已接入"当作画面成立证据；`R1` 起以"场景成立四要件"（连续地形铺满、统一光向色调、接地阴影、构图）为验收硬口径，缺任一条即不成立。`R0` 首分钟流程读法逻辑保留复用，不回滚。
- 不新增临时工作台或临时 Demo 场景名；正式 Godot 入口必须打开可见，并沿 `Boot` / `GameRoot` / 地图链路解决表现层问题。
- `H0` 只删除过期契约、修复检查宿主和当前路径真实回归；不为旧检查恢复已禁用视觉层，也不逐文件启动 `RefCounted` 辅助脚本。
- `R1` 地形首次失败只允许一次同类根因修正；宏块 atlas 第二轮成立后才进入平台，不继续做第三轮地形调参。平台本批只解决落座与图底关系，统一光向、接地阴影和构图留在既定后续步骤。
- `R1` 光色不为没有 normal map 的既有资产强接 `DirectionalLight2D`；光向以同批资产的烘焙左上光为准，场景节点只承担整体日光平衡和区域色调关系。首轮偏暖后只修正一次全局调色，第二轮通过后不再逐件调色。
- `R1` 接地阴影只使用复用目标纹理的原生 `Sprite2D` 组件，不以 `_draw()`、`Polygon2D`、`ColorRect`、脚本生成贴图或新素材补投影；第一轮截图成立后冻结阴影参数，后续只在构图移动中保持层级与同向关系。
- `R1` 构图不为追求密度提前显示未解锁设备；以平台轮廓、现有道具分簇、晶体主次和负空间建立层次。主晶体视觉必须服从既有首分钟交互锚点，不能为摆图破坏路径契约。
- 最终宽幅参考图对照优先于 `R1` 四要件的局部通过判定；现有实现只作 `R2` 过渡，不再继续同类调参。`R2` 按场景板生成、审阅接入、交互资产生成、正式入口整合四包串行推进，每个素材类别使用独立图像会话。
- `R2-A` 第二轮失败后不再执行上述后续包；不把失败简单归因于某一张构图，也不启动第三轮提示词修正。现有 Demo 目标、参考图、区域规模和 AI 美术主管线全部转为待复核假设。
- 实现开发暂停期间只允许只读分析和经萝卜SAMA确认的项目章程 / 结构决策更新，不用工程整理、修 bug、补检查或生成素材维持表面进度。
- `dev` 与稳定主线采用闭环拓扑，不再只做 `dev -> master` / `main` 单向 PR；`merge commit` 合并后的默认分支必须在下一轮开发前回流到 `dev`。
- 写实 2.5D 整景不再作为任何验收对照；像素介质证据轮先于一切引擎实现，同类两轮失败退向扁平几何风或 CC0 像素包基底。

## 验证记录

- `sh ./scripts/check-client.sh`
- `sh ./scripts/check-client.sh --with-godot`
- `./scripts/check-docs.sh`
- `./scripts/check-text-files.sh`
- `git diff --check`
- 2026-07-08 旧层回流修正：`sh ./scripts/check-client.sh` 通过；`./scripts/check-client.sh --with-godot` 仍阻塞在既有 `vertical-slice-flow` / `first-hour` 运行时预期与当前 `R0` 首分钟门控不一致，未作为本次通过证据。
- 2026-07-08 晶体路径补包：`sh ./scripts/check-client.sh` 通过。
- 2026-07-08 收尾文档：`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 通过。
- 2026-07-11 `H0`：`sh ./scripts/check-client.sh` 通过；`sh ./scripts/check-client.sh --with-godot` 完整运行并通过全部入口；未逐文件启动辅助脚本，未出现 `SCRIPT ERROR` 或崩溃弹窗。
- 2026-07-11 `R1` 浅暖地面 atlas：四类 12 个变体的 2x2 拼贴边缘复核通过；`sh ./scripts/check-client.sh` 通过。
- 2026-07-11 `R1` 连续地形与基地平台：正式 `Boot` / `GameRoot` / `VerticalSliceMap` 入口完成第一轮新档、第二轮新档、平台新档和修复核心后截图；`sh ./scripts/check-client.sh`、`sh ./scripts/check-client.sh --with-godot`、`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 通过。
- 2026-07-12 `R1` 光色：正式入口完成首轮与根因修正后第二轮新档开局 / 修复核心后截图；`sh ./scripts/check-client.sh`、`sh ./scripts/check-client.sh --with-godot`、`git diff --check` 通过。
- 2026-07-12 `R1` 接地阴影：`sh ./scripts/check-client.sh` 与 `sh ./scripts/check-client.sh --with-godot` 完整通过；正式 `Boot` / `GameRoot` / `VerticalSliceMap` 入口完成第一轮新档开局 / 修复核心后截图并判定通过。
- 2026-07-12 `R1` 构图：主晶体首次右移触发 `vertical-slice-flow` 路径契约并已恢复原交互锚点；随后 `sh ./scripts/check-client.sh --with-godot` 完整通过，根因修正后 `sh ./scripts/check-client.sh` 通过，正式入口完成两轮新档 / 修复核心后截图并于第二轮判定通过。
- 2026-07-12 分支拓扑治理：`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 通过。
- 2026-07-12 复盘首批结论文档：`./scripts/check-docs.sh`、`./scripts/check-text-files.sh`、`git diff --check` 通过。

## 风险与未完成项

- `R1` 与 `R2-A` 均已失败并停止；当前风险不是缺少下一包，而是在未重新确认项目目的和可持续生产方式前继续消耗时间。
- `P2` / `P3`、`R2-B` 到 `R2-D`、工程整改和全部玩法实现均暂停；旧截图与候选只作历史证据。
- Godot 聚合运行时入口已在 `H0` 恢复全绿，但检查体量、历史长尾覆盖和字符串哨兵比例仍偏高；`H3` 保持冻结，不在项目级复盘期间安排重构。
- Godot runtime 检查会刷新 `.import` 文件，本轮已还原导入噪声；后续提交前仍需确认不混入无关资产导入差异。
- C1 宏块过渡、平台阶梯和晶体资产家族差距共同证明 tile + 独立切图介质不足；这些实现暂作过渡，不再横向修补。
- `R0` 只是首分钟基线，不包含完整装备面板、便携工具面板、技能树和科技树；这些需要后续单独设计为首小时体验包。

## 下一步事项

- 确认项目第一目的、目标玩家、核心体验、完成定义和明确不做。
- 比较更适合个人 + AI 的产品形态、场景规模、工程结构与美术生产方式，形成生产职责和介质证据计划。
- 决定现有代码、场景、美术、检查和文档的保留、冻结、归档或替换边界；结论确认前不恢复实现。

## 下周建议

- 不排新的实现包；先完成项目目的、产品结构与个人 + AI 开发方式复核，再决定是否以及以何种形式继续 RadishCatalyst。
