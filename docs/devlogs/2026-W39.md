# 2026-W39 开发周志

周期：2026-09-21 至 2026-09-27（Asia/Shanghai）

## 本周目标

- 沿已确认的工厂 P1–P3 收口顺位，补齐第二版诊断离线分析及首轮窗口分段证据。
- 保留原画质、提交策略、生产与存档合同；不把快样本或诊断通过当成整包验收。

## 2026-09-25：第二版离线分析与有界窗口验证

### 完成与范围

- 萝卜SAMA确认开始当天建议的收口工作。起点为干净的 `dev`、`e02d5a17`；未修改正式客户端、引擎源码、二进制、依赖或根协作入口。
- 在既有忽略目录 `tools/runtime-intake/check-runs/factory-foundation-v1/engine-diagnostic-20260912/postwait-v2/` 增加 `analyze_postwait.py`、`test_analyze_postwait.py`、`check_offline.py` 和本批 `audit_run.py`。一次性诊断继续留在隔离目录，不加入正式游戏依赖。
- 分析器先校验 revision、来源、时钟、序号、零丢弃、成对区间与调用层次，再解析 fence 创建 / 销毁、命令创建 / 提交 / 重置。指针复用按创建身份区分；信号编码不解释为 GPU 完成；超时保留失败报告并返回非零。
- 每个绘制帧按端点切分成互不重叠区间，保留未覆盖余量和提交后分段；嵌套原始跨度另列，不相加分位数。frame 0 回调保持未分配；生产范围用 Mach / Godot 锚点的保守误差界限筛选。
- 启动前告知，经沙箱授权运行原 `run-postwait.py on`；用户实际点击准备页后完成 15 秒预热 / 45 秒生产，未失焦、未中断，未追加窗口批次。

### 离线验证与分析失败复盘

- 最终 34 项测试在普通与 `-O` 模式各通过，覆盖缺失单端 / 整对 / 单帧阶段、指针复用、错误身份、跨线程、信号与提交顺序、初始值 0、frame 0 回调、超时、损坏数据及未知事件。
- `offline-20260925-133737/result.json` 保留 8 项执行记录：两种测试模式，以及正常 / 超时 / 丢事件 / 畸形 CSV / 不完整生产日志 / 禁止覆盖既有输出的命令行检查。超时 CLI 非零退出且保留失败报告；无效输入不生成成功报告。
- 原二进制 SHA-256 `11d33c50560b7e80281ea9901b622b824fe85e4f541111891bc9fbce6cc3fb1a` 与 8 份插桩源码 hash 复核一致；未重新构建引擎。
- 实测首次离线分析误把 `drawableID=0` 当成空 drawable；原记录同时保留非零 texture，后续 clear / present 也使用该编号。修正为依据 texture 指针判断空获取，加入有效零编号与空对象反例。
- 第二次误要求捕获结束前所有 fence 已销毁。源码确认 `finalize_display()` 在删除 RenderingServer 后 flush，随后才删除 DisplayServer；macOS DisplayServer 析构再删除 RenderingDevice 并释放 fence。因此保留 3 个捕获末尾仍存活的身份，不虚构销毁或宣布完整析构已测。复用前缺失 free 仍失败；两次失败日志 `analysis-a.log` / `analysis-b.log` 保留，最终 `analysis-c.log` 通过。

### 实测结果与证据

| 项目 | 实际结果 |
| --- | --- |
| 诊断批次 | `postwait-v2/on-20260925-133319/`，PID 11173，进程退出 0 |
| 正式 Boot 生产结果 | `factory-foundation-v1/performance-1790314409-11173/`，8 项检查通过 |
| 事件完整性 | 359,359 条；dropped / write_error / close_error 均为 0；全部 7,682 个绘制作用域闭合 |
| 身份与等待 | 16,273 个命令创建身份、15,371 次原生命令指针复用；8,590 次等待全部成功；3 个 fence 无地址复用，析构未进入捕获 |
| 生产时段 | 45.0071 秒，5,390 个生产绘制帧；锚点不确定度 2.083 微秒 |
| 帧与模拟步 | 正式帧 p95 `8.600ms`、最大 `104.179ms`；单步 p95 `2.284ms`；900 步 |
| 分段跨度 | 绘制 p95 / 最大 `8.388 / 9.570ms`；最后提交后 p95 / 最大 `0.011 / 6.804ms`；生产等待 p95 / 最大 `0.0005 / 6.791ms` |
| 生产与守恒 | 交付 125 件；模拟推进 45 秒，余量变化 0.0071 秒；计时误差约 `-5.4e-13s`，最大余量不足 0.05 秒 |
| 保存 | 采样内自动保存 1 次成功、`98.630ms`；采样后保存成功、`87.876ms` |
| 设置 | 原 Metal / Forward+、VSync、4× MSAA；窗口 `1920×1080`，3D `1728×894`；历史渲染设置核对一致 |

- 原始应用帧记录仅 1 个区间超过 `16.7ms`：第 3601 项、`102.525ms`（应用帧与正式 frame_samples 端点口径不同，最大值不混用）。该区间 `[59661527,59764052]` 微秒包含自动保存 `[59661529,59760159]`，同区间绘制 frame 24478 仅 `1.743ms`。依据同轮端点定位到保存占据主要时间，不把它归因于 fence / GPU 或删出统计。
- 与 9 月 13 日官方批次 `performance-1789307606-99676` 的初始完整快照相同；最终存档状态仅 `remainder` 不同，本轮为 `0.036275s`，设备、库存、加工、物流、累计生产及模型步进时间一致。不是独立新进程恢复验证。
- 原图 `assets/art-intake/2026-09-25-factory-foundation-preview/performance-1790314409-11173/on-postwait-v2.png` 与历史官方原图均已实际审阅，场景、状态、HUD 与设置无可见改变。两张 `1728×1080` 原图有 8 个像素不同，最大通道差 6；保留逐像素记录，不声称字节一致，也不臆测差异原因。
- `events.analysis.json`、`run-audit.json`、`image-comparison.json`、原始 CSV、日志、隔离主档与备份均保留。实测未出现的 fence 复用、初始零等待和帧外回调只有离线覆盖；GPU 完成时刻、系统 drawable 回收因果及标记开销未测。

### 治理验证

- `./scripts/check-docs.sh` 通过，无篇幅警告；`./scripts/check-repo.sh` 通过（1,750 文件）；`git diff --check` 通过。本轮未修改客户端，因此未重跑完整客户端 Godot 套件，未执行 Windows / PowerShell 检查。

### 结论与下一步

- 第二版分析器及本轮捕获范围内的窗口区间 / 身份验证完成；本次未复现此前持续慢绘制，未修复或解释历史基线波动。
- 按已定快样本停止条件结束本轮窗口诊断，不追加整体开关、GPU 采样或渲染调度补丁。下次先回到正式版本原配置复核；原配置短测稳定后再安排四档各 450 秒前台长测，见 [Daily Start](../planning/daily-start.md)。
- 四档长测、完整客户端 Godot 回归、Windows / PowerShell 与用户亲测仍待完成，P1–P3 未整体验收；产品方向、冻结项与退出合同不变。


## 2026-09-25 接续：正式版本短测通过，长测因失焦中断

### 范围与正式版本复核

- 萝卜SAMA要求继续下一步，起点 `10698e06`、`dev` 工作区干净。沿现有入口执行正式版本原配置复核，没有修改客户端、渲染设置、生产规则或验收预算。
- `postwait-v2/official-20260925-135157/` 经实际准备页点击完成 15 秒预热 / 45 秒生产，使用 `4.7.stable.official.5b4e0cb0f`，记录开关关闭、未产生事件 CSV、无额外 GPU 时间戳。结果目录 `factory-foundation-v1/performance-1790315545-14852/`，退出 0，8 项检查通过。
- 生产 `45.007296s`、5,389 帧、900 模拟步、交付 125 件；帧 p95 / 最大 `8.680 / 102.841ms`，模拟步 p95 `2.266ms`，最大余量不足 `0.05s`，计时误差约 `-5.76e-13s`。采样内自动保存 1 次成功、`97.996ms`，采样后保存成功、`83.934ms`。
- 初始完整状态与本日诊断构建相同，最终状态仅正常余量不同；渲染设置、实际生产分辨率一致。`official-postwait-v2.png` 原图已审阅，与本日 `on-postwait-v2.png` 字节一致，SHA-256 `43d8df96b7c7729e445072e6be2704a004de1d47a4fb8bf8dd5eb1876df069b4`。不据两次独立进程的帧差估算插桩开销。

### 四档长测尝试与停止

- 正式短测达标后，告知四档各 450 秒 / 总计约 30 分钟，经沙箱授权启动既有 `foreground-performance-review.gd`；准备页实际点击后通过正式 Boot 进入从空物料启动的工程产线。
- 结果目录 `factory-foundation-v1/performance-1790315800-15236/`，只尝试 `1080-local`，约 220.7 秒时收到 `focus_lost`，进程退出 1；`phases=[]`，没有任何完整通过档位，也未进入后续三个档位。未归因失焦来源、未抢焦点、未自动重开。
- `interrupted-samples.json` 保留 26,411 个原始帧、4,414 个模拟步及 7 次成功自动保存（`82.577–101.043ms`）；最后已记录活动时间 `220.697779s`。首尾共同边界计时漂移约 `1.58e-11s`，最大记录余量 `0.04527s`，只说明已有部分记录可核对，不补算失焦尾段或拼接为完整长测。
- 部分原始帧 p95 `8.674ms` 仅供诊断，不作为性能验收；本次结束原因是失焦，而非已完成档位的预算失败。`1080-local.png` 起始原图已审阅，不能代替阶段结束证据。
- `foreground-20260925-after-official/` 保留启动命令、正式二进制 / 入口 hash、完整日志、`audit.py` / `audit.json`。隔离主档、备份与中断样本继续保留，未写用户正式存档。

### 无窗口回归与待办

- 前台测试退出后补跑 `sh ./scripts/check-client.sh --with-godot`。首次沙箱执行在 `factory-clock` 阶段报告 FreeType 系统字体加载错误，虽然 28 项时钟断言无失败，入口仍按错误日志返回 1，未记为整套通过；失败日志为 `check-client-with-godot.log`。
- 按疑似沙箱字体服务限制请求授权，在沙箱外沿同一隔离流程重跑，退出 0；全部静态检查及 74 个 Godot 入口（含导入）执行完成，原始日志无未预期错误。工厂状态 / 存档 / 时钟 / 视图及旧切片等覆盖以现有 Shell 入口为准，不能代替窗口或 Windows 验证。此对照仅确认本次主机执行通过，不扩写成所有字体错误都可忽略。
- 通过日志为 `check-client-with-godot-host.log`；74 份原始日志位于仓库忽略目录 `.godot-check-runs/godot-shell-17577-20260925060342/`，失败批次为 `.godot-check-runs/godot-shell-16870-20260925060138/`。`client-check-audit.json` 核对实际执行列表与 Shell 入口完全一致，未漏后续阶段。
- 四档长测仍需另一次连续前台时段从头完成；当前环境与代码未变时，复用已完成的正式短测和本机无窗口回归，不再把分析器或完整 Shell Godot 套件列为未做。Windows / PowerShell 与用户亲测仍单列待验，首包不宣称整体验收。

- 萝卜SAMA随后要求先提交工作区并推进其他项目任务，四档长测等待其完成其他任务后明确通知再开始；期间不启动前台窗口。

## 2026-09-25 接续：首包验收证据审计

- 按要求先将正式短测、中断长测和完整 Shell 回归记录提交为 `529c61dd`；随后在首包范围内核对 Boot → 世界列表 → Store → 场景接受候选、保存退出与锁释放的实现 / 检查对应关系，没有启动 Godot 或改动产品代码。
- `store.gd`、`codec.gd` 和 `world_menu.gd` 最近修改仍为 `978d6250`。检查确认备份候选在视图重建后经 `accept()` 发布，未来版本中止回退；列表 / 恢复按钮有业务连接，但已有 44 项存档和跨进程锁检查直接调用 Store，不能外推列表、按钮和 HUD 的窗口验收。本轮未发现需要立即修改这条调用链的确定性缺陷，不等于完整正确性证明。
- 离线脚本 `check-runs/factory-foundation-v1/acceptance-audit-20260925/audit.py` 核对 16 份历史结果的断言数 / 完整失败列表、本日存档日志及 10 个相关源码 hash，结果保留为同目录 `result.json`；输出禁止覆盖。未重跑已有测试、改写历史结果或重新宣称画面通过。
- 发现专题 P1 的“剩余正式 Boot 窗口路径”无法说明实际缺口；新增玩家路径表，明确新建 / 双线使用 `push_input` 合成输入，部分按钮用信号；原生保存失败 53 项及关闭 / 独立重进 17 / 15 项可复用。早期窗口 66 项中 2 项失败继续保留，只引用已审阅非弹窗路径，07 / 08 文件名不作成功证据。
- 将仍缺界面证据落实为备份恢复与坏档 / 未来版本反馈、锁提示与显式恢复、旧二维已有世界载入画面。只细化既有验收合同，未解冻新玩法或新增预算；补验使用新隔离副本，不能破坏历史通过存档。用户亲测、四档长测和 Windows / 目标硬件依然分别待验。
- 本轮文档预算、仓库治理（1,750 文件）及 `git diff --check` 通过；产品代码未变，复用本日完整客户端回归，未重复启动 Godot。

## 2026-09-25 接续：正式四档前台长测全部通过

### 范围与执行

- 萝卜SAMA明确要求“开始四档长测”。起点 `476faab0`、`dev` 工作区干净；先告知约 30 分钟连续前台条件，经沙箱授权启动正式 Godot，实际准备页点击后进入原 `foreground-performance-review.gd`。未拼接前次中断样本，未抢焦点或改动产品 / 检查入口。
- 启动根 `check-runs/factory-foundation-v1/foreground-20260925-141938/` 保留 `launch.json`、`source-manifest.json`、日志、`completion.json` 与离线 `audit.py` / `audit.json`。正式二进制 SHA-256 `445c6f95030e2ca767dd921be1e91bd99e50c3703f91d22a22cd31216c93a80f` 与此前正式短测 / 长测启动记录一致；入口 hash 一致，12 个产品 / 检查来源文件运行前后 hash 未变。
- 结果 `factory-foundation-v1/performance-1790317200-22165/`，PID 22165；正式 `4.7.stable.official.5b4e0cb0f`、Apple M5、Metal / Forward+，VSync 与 4× MSAA 保持。退出 0，29 项检查、0 失败，`interrupted=false`，日志无 `SCRIPT ERROR` / `ERROR:`。

### 四档结果

| 档位 | 活动秒数 | 实际 3D 像素 | 帧 p95 / 最大 ms | 单步 p95 ms | 本档交付 | 采样内保存次数 / 最大 ms |
| --- | --- | --- | --- | --- | --- | --- |
| 1080 局部 | 450.003705 | 1728×894 | 8.726 / 106.207 | 2.385 | 1,000 | 15 / 99.206 |
| 1080 拉远 | 450.002904 | 1728×894 | 8.562 / 103.252 | 1.612 | 1,125 | 15 / 97.691 |
| 最大化局部 | 450.002404 | 2745×1420 | 8.808 / 124.584 | 2.331 | 1,125 | 15 / 123.276 |
| 最大化拉远 | 450.006666 | 2745×1420 | 9.696 / 104.322 | 1.558 | 1,125 | 15 / 98.770 |

- 共 `1800.015679s`、213,300 个采样帧、36,000 个模拟步；采样区间合计交付 4,375 件，四档均满足帧 p95 ≤16.7ms、单步 p95 ≤10ms。最大记录余量不足 `0.05s`，最大逐帧计时漂移约 `4.04e-10s`，低于既定 `10μs` 数值容差。逐帧端点、单步计数 / 分位数和阶段汇总交叉核对通过。
- 60 次采样内自动保存全部成功，耗时范围 `81.236–123.276ms`；阶段结束保存分别 `79.120 / 84.272 / 82.508 / 77.823ms`。保存停顿保留在帧统计中，不能将通过 p95 写成没有卡顿。表中为正式应用 frame_samples 口径；原始边界帧最大值不同，未混用。GPU 时间未取得。
- Boot 同步加载 `300.611ms`，含等待帧合计 `585.282ms`。阶段末静态内存约 499–501MB；最大化显存约 636MB。这些端点值不等于峰值或内存泄漏排除结论。
- 正常保存返回后释放写入锁；最终主档 sequence 66、模型时间约 `1804.5s`、入仓 4,400 件、1,100 个实体，主档及三代备份保留。准备 / 切档也有正常推进，因此最终存档数值不同于四个采样区间合计，不混称测试只推进 1,800 秒。未在本轮再做独立进程恢复。

### 画面与接续边界

- 四张原图 `1080-local.png` / `1080-wide.png` / `max-local.png` / `max-wide.png` 均已实际审阅，位于 `assets/art-intake/2026-09-25-factory-foundation-preview/performance-1790317200-22165/`；画面、HUD、局部 / 拉远和载货生产状态正常。普通原图 `1728×1080`，最大化 `2745×1716`，与 3D 区域大小分列；这些是各档采样前的截图，不伪称阶段结束图。
- 本机四档持续性能缺口关闭；早先失焦中断及历史慢绘制记录保留，当前通过不解释历史波动或诊断标记开销。后续优先补备份 / 坏档 / 锁反馈与旧二维载入界面证据，再交付亲测；Windows / PowerShell 与目标硬件仍待验，P1–P3 不宣称整体验收。
- 测试已自动退出，没有自动启动其他窗口；同步 Current / Daily、首包专题、运行时指南和呈现诊断记录。正式客户端未修改，完整 Shell 客户端回归复用本日已通过批次。
- 最终文档预算、仓库治理（1,750 文件）和 `git diff --check` 通过；同步架构文档的验证状态，保存协议及运行时边界不变。本轮文档更改留在工作区，未提交或推送。

## 2026-09-25 接续：界面缺口补齐与亲测交付

- 萝卜SAMA要求提交并继续。先经治理 / 文档 / diff 检查，将四档长测及接续记录提交为 `78c4d479`；随后按首包清单补剩余界面证据，未修改正式客户端或冻结边界。
- 新隔离批次 `tools/runtime-intake/check-runs/factory-foundation-v1/menu-review-20260925-a/` 从保留的 `operation-20260908-a` 普通双线主档复制出五个世界，分别布置备份恢复、全部候选无效、未来 schema 2、独占锁与未知锁；旧二维从本日 `startup-world-list` 复制 schema 10 主档和元数据。`fixture-manifest.json` 记录来源与初始逐文件 hash，故障不写入原通过批次。
- 启动前告知，经授权和实际准备页点击后，用正式 Boot 双根隔离、可见列表 / 按钮的合成鼠标输入完成检查。55 项断言通过，进程退出 0；主进程及独立持锁进程日志无脚本错误。临时脚本 `review.gd`、日志、结果、文件保护审计及主档 / 备份保留于同批次。
- 备份世界先显示“可从备份恢复”，继续后 HUD 显示恢复来源并发布有效主档；发布时来源 hash 不变。全部候选无效 / 未来版本世界的继续按钮禁用，并显示明确原因；另保留原始错误细节，未据自动通过宣称文案已达量产标准。
- 独立 Godot 进程 PID 24990 使用真实 Store 获取写入会话，持锁 15 秒；父窗口可见写入锁、继续被阻断、活跃持锁者恢复被拒绝。子进程自然退出且故意保留锁后，经“恢复遗留锁”显示成功、重新选择并继续世界；未知所有者始终拒绝回收，未使用假 PID 或超时清锁。
- 从工厂返回旧启动菜单，经“载入存档”与列表选择载入复制的 schema 10 世界，恢复核心能量 11，且服务路径指向隔离根。不是新建截图，也不把这次标记字段验证扩写为旧世界全部状态等价证明；全量旧回归沿用本日已通过套件。
- 11 张原图位于 `assets/art-intake/2026-09-25-factory-foundation-preview/menu-review-20260925-a/`，均逐张审阅：01 / 02 备份列表与恢复 HUD，03 / 04 坏档与未来版本拒绝，05 未知锁保护，06 / 07 活跃锁阻断，08 / 09 退出恢复与世界重入，10 / 11 旧列表与载入画面。暂停截图通过测试调用；合成鼠标不称系统原生鼠标或用户亲测。
- 离线核对确认两个拒绝世界及未知锁世界全部文件字节未变，工厂与旧二维历史源档 hash 未变，成功世界的写入锁已释放。`audit.json` 保留 11 图 hash 和输入范围；未触碰用户正式存档。
- 亲测入口已准备于 `tools/runtime-intake/review-worlds/factory-foundation-v1/playtest-20260925/`：`start.sh` 启动正式 Boot，工厂 / 旧二维均隔离，每次保留独立日志并复用亲测世界；不自动输入、预布置设备或清档。README 与首包专题给出普通新建 → 双线 → 载货 / 半批保存 → 关闭重启继续的路径。只完成入口解析与 Shell 语法检查，尚未启动用户亲测。
- 临时检查与亲测入口各有一次 Variant 返回值的类型推断解析错误；补明确 `String` 后解析通过。原失败日志 `parse.log` / `playtest-parse.log` 与成功日志分别保留，Godot 解析退出码 0 未被误判为成功。实际窗口只运行一轮并通过，无重复开窗。
- 本机既定工程证据具备，转入用户亲测；Windows / PowerShell、目标硬件及用户体验确认仍待验，首包不宣布整体验收。当前无运行中的检查窗口，不重复四档长测或解冻后续玩法。
- 最终文档预算、仓库治理（1,750 文件）和 `git diff --check` 通过；正式客户端未改，复用本日完整 Shell 回归。本轮接续记录单独本地提交，未推送。

## 2026-09-25：亲测反馈与下一包建议

- 萝卜SAMA反馈“测了，没啥问题”，并询问下一步。记录为本次亲测未反馈明显问题；未提供逐项操作记录，不补写具体测试时长、覆盖项或 Windows 验收结论。
- 本机工程证据及亲测反馈已具备，建议按既定顺位收口开发机首包，并进入“发现 → 试制 → 稳定生产 → 获得能力 → 反哺生产”的功能专题设计。Windows / PowerShell 与目标硬件验证保持单列待办。
- 本次只记录反馈并提出下一包建议，未切换实施专题、解冻玩法或修改客户端。下一包先明确有限区域的玩家路径、生产成果用途、状态 / 存档、反馈、资产及验收，再确认实施范围。

## 2026-09-25 接续：发现与生产循环设计

- 萝卜SAMA同意开始下一包设计。先核对正式工厂规则、模型、库存操作、导入资产与严格 schema 1 校验；确认取料仍依赖拆回、反应器只支持固定配方，新包必须显式包含取放 / 配方与多物料存读工作。
- 新增 `Factory Discovery And Production V1` 提案：固定矿区内发现矿壳样本、试制并量产解壳剂、两次永久开路、富集晶体自动开采并回流生产。列出物品 / 配方 / 成本、地图与资产表达、失败恢复、状态守恒、HUD、D1–D4 分包及玩家证据。数值与 20–40 分钟节奏为设计估算，未作实机结论。
- 推荐新建勘探世界采用独立规则 / 地图及工厂 schema 2，已有基础工厂继续 schema 1、不插入新地形、不迁移旧二维。此为待确认方案，未修改客户端、正式存档合同字段、用户档或资产；未启动 Godot，也未引入外部依赖。
- 开发机首包根据已具备的工程证据和亲测反馈收口；Windows / 目标硬件最终验收仍单列。同步 Current / Daily、专题索引、长期设计入口及存档合同的事实状态；下一步审阅新包边界，确认后先做 D1 场景与内容验证。
- 本轮 `./scripts/check-docs.sh`、`./scripts/check-repo.sh`（1,751 文件）及 `git diff --check` 通过；补核新专题相对链接与批次成本算术。仅文档变更，未执行客户端测试，未把既有长测作为新玩法验证；更改保留工作区，未提交或推送。

## 2026-09-25 接续：电力设计顺位与文档提交

- 萝卜SAMA要求提交工作区更改，并强调电力系统不可遗漏。核对产品支柱中的电力供能及旧二维二值电网边界；此前发现循环提案只写预供电，没有明确基础电力的接续位置，本次补齐。
- Current / Daily 与发现循环专题同步将基础电力列为下一项优先设计：发电自举、输配电、供需负荷、停复电、存档与玩家反馈。先完成电力专题并联合确认先行或合包顺序，再冻结新世界生产 / 存档合同；现有吞吐与节奏仅为充分供电的设计前提。
- 本轮仅补规划并提交前序亲测收口与设计文档；未修改客户端、冻结电力参数、移植旧二维电网或启动 Godot。
- 提交前 `./scripts/check-docs.sh`、`./scripts/check-repo.sh`（1,751 文件）与 `git diff --check` 通过；本次仅文档，未重跑客户端或四档长测。按要求在 `dev` 本地提交，未推送。

## 2026-09-25 接续：生产统计参考与设计

- 萝卜SAMA补充统计系统需求，提供下载目录“终末地UI参考”中的 `Screenshot_2026-08-09-16-08-58-590_com.hypergryph.endfield.jpg`。已直接查看原图（3200×1440），核对物料行、仓储、产消趋势、当前与理论每分钟数据、收藏 / 排序 / 筛选及区域 / 时间选择；未将截图未展示的算法当作该游戏事实。
- 新增 `Factory Production Statistics V1`：建议单世界物料简报、短时间窗、库存分解、实际 / 理论产消及设备定位，并与电力供需页联合设计。明确内部搬运不重复计产、在制与取消记账、模拟时间采样及有限历史跨进程接续，原有累计计数不能冒充完整统计系统。参数 / 字段仍为提案。
- 同步 Current / Daily、专题索引及发现生产包：基础电力、统计与生产事件在新世界合同冻结前共同确认，页面随真实玩法纳入验收。未修改客户端、用户档或外部参考文件，未启动 Godot、下载或复制外部资产。
- 文档预算、仓库治理（1,752 文件）与 `git diff --check` 通过。本轮为文档设计，未执行客户端或窗口测试；更改保留工作区，尚未提交。

## 2026-09-25 接续：基础电力与联合开发包设计

- 萝卜SAMA同意开始基础电力可实施设计。起点 `7f329d67`，`dev` 工作区干净；核对正式生产步进、当前两物料计数、存档权威与资产。已有 14 个工厂 GLB 缺少电源 / 配电节点，旧二维电网和 sprite 不计为可直接接入的三维能力。
- 新增 `Factory Power And Grid V1`，推荐两套 120 kW 封装电源、12 个配电节点、12 / 6 格显式连接和公平比例降速；带与仓不耗电，燃料 / 储能 / 构件经济后置。定义末步不足 50ms 的能量、无电不开批、停复电保留进度、并网不重复供电和取消批次不退电；全部为待确认设计值，未改产品规则。
- 发现、电力与统计方案统一为一个新世界版本提案：D1 联合验证矿道 / 接线与两个电力模型，D2-A 先基础供电和记账、D2-B 接多配方 / 开拓状态，D3–D4 完整路径与亲测。现行 schema 1 / 旧二维世界不迁移；字段、模型制作范围与参数须联合确认后实施。
- 统计对齐全世界历史曲线、当前分网缺口和逐步能量；明确图表采用完整秒桶及真实覆盖时长，未完成桶保存后接续，边界事件恰计一次。避免用全厂富余抵消孤网缺口，也不拿当前拓扑拼接历史电网。Current / Daily、专题索引与相关提案同步。
- 设计算术执行 6 个 50ms 场景及孤网 / 多源分摊检查，共 20 条断言通过；结果在忽略目录 `tools/runtime-intake/check-runs/factory-power-v1/design-review-20260925/proposal-arithmetic.json`。只证明提案公式的示例计算，不代表客户端仿真、性能或玩法平衡验证。
- 本轮未修改客户端、生成 / 导入模型、启动 Godot、安装依赖或接触用户存档；设计文档保留工作区，未提交或推送。
- 文档预算、仓库治理（1,753 文件）和 `git diff --check` 通过；本次只验证设计文档与示例算术，未执行客户端测试、窗口验收或重跑长测。
