# Factory Foundation And Persistence V1

更新时间：2026-09-25

状态：方案已确认；正式入口、独立存档和扩建基础已实现。原生保存返回 / 重进、系统关闭保存与独立 Boot 恢复、30 分钟后台稳定性已有证据。9 月 12 日补齐性能对照与两项渲染优化，9 月 13 日完成两版隔离诊断构建；历史原画质短测有超预算与基线波动；9 月 25 日第二版离线分析和首轮窗口分段通过，本轮快样本未复现持续慢绘制；同日正式原配置短测及完整 Shell 无窗口回归通过，前次长测因失焦中断，随后经明确通知四档各 450 秒完整通过；同日补齐剩余界面证据，萝卜SAMA亲测反馈“没啥问题”；开发机首包交付收口，Windows / 目标平台最终验收仍待完成。

本专题范围为基础工厂 schema 1；上述完整回归与长测发生于 D1–D2 代码变更前。后续勘探工厂 schema 2 的实现与验收见[发现生产联合专题](factory-discovery-and-production-v1.md)，旧首包成绩不直接覆盖新版本。

## 决定与玩家结果

萝卜SAMA认可当前独立 Godot 3D Demo 的画面与操作，同时明确精细度尚不满意、当前不必抠细节，并要求继续推进下一步。本包以 Godot 3D 为设计路线，形成可以保存、继续经营和扩建的工厂基础；不再追加同范围的画风比较。认可不等于量产美术定稿，也没有新增大型工厂性能通过的结论。

完成后，玩家通过正式启动入口创建工厂世界，亲手接通第一条产线，再在同一区域建设第二条产线；关闭程序后重新进入，设备、库存、带上物品与加工进度保留，继续生产。先让当前三种设备可多台使用，新增设备种类与产物换构件的经营循环另包设计。

本专题定义玩家范围与分包。状态、存档、并发写入与新旧隔离的实现合同见 [Factory World State And Save V1](../architecture/factory-world-state-and-save-v1.md)；既有对照依据见 [Godot First Production Line Demo V1](godot-first-production-line-demo-v1.md)。

## 开工十问

1. 新动作：创建 / 继续工厂世界，在多个矿点建造多条产线，保存退出后接着调整。
2. 世界变化：已投入的构件和生产成果长期保留，不随一次窗口关闭而清空。
3. 产品价值：为“探索资源 → 生产 → 扩建”提供可积累的基地；本包不新增探索内容。
4. 感知时长：目标在 5–10 分钟完成接通、保存和重进；第二条线的建设时长由试玩记录，不冒称已验证。
5. 系统需求：需要独立工厂状态、格位 / 端口索引和工厂存档；不建立通用插件、ECS 或联网框架。
6. 素材范围：沿用已认可的设备、角色、配色、视角与光照，只补扩大场坪需要的重复地面组织和相机跟随。
7. 成功证据：正式 Boot 输入路径、跨进程存读、物料守恒、故障恢复、窗口截图和明确负载下的性能记录。
8. 失败处理：按输入、状态、文件发布或渲染瓶颈归因；规模不达标须记录实际限制，不通过增加画面细节掩盖。
9. 旧系统取舍：保留旧切片及其存档，不移植旧战斗、核心任务、无线仓库或二值电网规则。
10. 阶段价值：玩家第一次能积累一座可扩建工厂；经济驱动、长期探索和量产美术仍是后续包。

## 已确认首包范围

下列新数值属于已确认实施合同；既有 Demo 和 Web 不随本文更改。

| 项目 | 首包方案 | 验收意义 |
| --- | --- | --- |
| 正式入口 | 保留 `client/scenes/boot/Boot.tscn`，启动菜单新增“工厂世界”入口；既有二维世界仍可进入 | 交付进入正式客户端的成果，旧世界不会被误当作三维世界加载 |
| 地图 | 一块 `64×64` 固定格区域、固定晶体矿点表；无随机地图或区域加载玩法 | 足够亲手建设多条线，不以增加空地面积代表规模通过 |
| 普通新世界供给 | 采集器 8、反应器 16、终端仓 8、传送带 256；初始物料为空，预供电 | 与 Demo 一样用有限预发构件验证基础，不提前引入制造与电力经济 |
| 工程负载世界 | 同地图与生产规则，专用检查供给为采集器 25、反应器 50、终端仓 25、带 1,000 | 100 台设备 / 1,000 段带首档；明确区别于普通新世界的构件预算 |
| 资源 | 固定地图提供 25 个互不重叠的合法采集落位，首条线附近保留易读的矿点；矿源暂不枯竭 | 多采集器由真实矿点支持，禁止到处放采集器或用隐藏自动供料 |
| 相机 | 默认 50° / 25°，Q / E 转角、滚轮缩放；随人物移动观察同一片场地 | 扩建后可到达并观察新产线，世界边缘、HUD 与落点有实际复测 |
| 长期状态 | 工厂专属世界列表、保存 / 返回、正常关闭时保存、继续世界和备份恢复 | 第一次跨进程继续经营，失败不会清空或覆盖成果 |
| 美术 | 保留同源 GLB、4× MSAA 与按实际像素渲染；仅修正阻碍辨识或操作的问题 | 保持已认可体验，模型细节、材质和角色动画润色暂缓 |

工程负载世界只由检查入口创建，使用明确标记的静态供给配置；不出现在普通“新建世界”的选择里。它仍通过正式命令校验建造并运行，不直接改库存或复制静态 Mesh 冒充生产。地图与供给配置使用稳定版本标识，修改时须重新审核兼容关系。

## 保持的生产与操作规则

- 采集器 / 仓 2×2、反应器 3×3、带 1×1；设备固定朝向与端口位置沿用 Demo。只放开多个实例和多个合法矿点，不新增设备旋转、端口模式或带速档位。
- 采集 1 晶体/秒、缓冲 50；反应器原子消耗 2 晶体，10 秒产 1 催化剂，输入 2 / 输出 1；终端仓与回收箱各容量 200。终端仓继续只收料，不暗中变成会发料的物流设备。
- 每带格一件、1 格/秒、真实正交弯道、不反向对撞、公平汇流；占用与满仓引发回压。缺料和堵塞状态按单台实例派生，不用一台设备的恢复冒充另一台恢复。
- 原子放置、拖铺、回拖、取消、拆回与回收物投入沿用对照合同；拆回须先校验全部内容能否回收，在制批次折回 2 晶体，不丢料或复制料。
- WASD / 方向键按屏幕方向移动，点空地移动，建造态可走；UI 阻断鼠标落地，人物不穿设备，设备不压人。加载、退出和失焦取消未提交预览。
- 暂停、失焦和退出不继续生产；无离线收益。后台返回与高负载积压须由明确模拟时钟处理，不能简单截断 delta 后悄悄少产。
- Demo 的固定三处参考框、全局断路教学旗标不成为正式经营状态。首包提示说明如何接通、查看状态与保存；累计入仓从真实输送事件统计，手动投入不冒充产出。

## 玩家路径与界面反馈

1. 从 Boot 打开“工厂世界”，新建并命名。只在初始主档成功发布后进入；创建失败留在菜单，显示原因与重试入口。
2. 从空场坪放置采集器、反应器、终端仓并接带；第一件催化剂真实入仓。界面可查看各台设备缓冲、进度、缺料 / 回压和库存。
3. 走到另一矿点建设第二条独立产线；第一条线继续运行。拆断其中一条并补接，分别观察两条线，确保反馈与实例身份对应。
4. 在“带上有料、反应器正在加工”的时刻保存并返回；显示本次保存结果。关闭程序，再从 Boot 进入同一世界，恢复数量、进度和位置，继续获得新产物。
5. 游戏内保留显式保存与“保存并返回”动作；点击窗口关闭也走保存流程。保存失败留在当前暂停世界，提供重试、取消退出和经明确确认的“不保存退出”。
6. 主档损坏而备份有效时，告知恢复来源，完整重建通过后才发布恢复结果；无有效候选时保留文件并在列表显示错误，不能自动创建空场坪。

工厂世界列表首包提供新建、继续和保存状态信息；不追加复制世界、跨世界携带、云同步、导入 / 导出或永久删除功能。旧世界已有列表功能继续保留。

## 实施顺序与退出条件

| 包 | 交付 | 本包退出证据 |
| --- | --- | --- |
| P1 正式工厂状态与入口 | 在 `client/` 建立窄工厂目录与 Boot 路由，适配同三机画面 / 输入；配置化场地、构件与实例身份 | Boot 空场坪操作、单线 Web 规则对照、世界 / HUD 坐标与旧启动入口回归通过；P1 单独不宣称已可长期经营 |
| P2 保存与继续 | 工厂 codec / 存档服务 / 目录、保存时机、独占写入、候选重建和失败反馈 | 跨进程存读、半批 / 载货 / 回压恢复、坏档 / 备份 / 写入失败和旧档保护通过 |
| P3 扩建与规模 | 64×64 多矿点、多实例、相机跟随、占用索引与必要的分区渲染，形成可手建多线和工程负载世界 | 多线实际操作、缺料 / 堵塞互不串线、不同分辨率及首档性能 / 存读记录通过 |

每包先完成对应定向验证再进入下一包；单点失败按真实职责修正。正式依赖不得反向引用 `tools/visual-studies/`；只将必要的同源模型、规则与操作合同适配到正式目录，保留原 Demo / Web 作为对照。

首包完成时必须同时具备 P1–P3 证据，不能以 P1 已开窗或 P2 能写 JSON 宣称整个包通过。若扩大范围导致必须更换画风、存档方案或运行时语言，则重新说明影响；不会自行升级成整仓重写。

## 规模与流畅度验证合同

| 档位 | 负载与用途 | 判定 |
| --- | --- | --- |
| 操作路径 | 至少两条真实独立产线，含人物移动、放置、拖铺、拆回、缺料 / 回压与恢复 | 有窗口输入链与截图；体验由萝卜SAMA复评 |
| 首档 | 100 台设备、1,000 段带、最多 1,000 个在途物品 | 本包工程目标；记录实际活跃 / 缺料 / 堵塞数量，不能只测空设备或静态货物 |
| 扩展档 | 1,000 台设备、10,000 段带 | 本包不做、不作为容量承诺；首档完成后再讨论 |

- 首档分别覆盖局部可见、拉远大面积可见、持续生产、接近满载、汇流和连续拆改；拉远不必在一个屏幕硬塞整张地图。状态由正式规则和明确检查夹具生成，夹具来源必须记录。
- 先在当前 macOS 开发机测 `1920×1080` 与最大化实际像素，分别保留记录；窗口逻辑大小、实际 3D 分辨率、画质和硬件写入证据。
- 采用 20Hz 生产步进；首档目标 60 FPS、帧间隔 p95 ≤16.7ms、单次模拟步 p95 ≤10ms，前台持续运行 30 分钟无持续积压。保存 / 读档停顿单列，不用平均 FPS 遮住卡顿。
- 记录完整帧间隔、模拟步耗时、draw calls、内存及保存 / 加载阶段耗时；能取得 GPU 时间时单列，不能以 CPU 提交耗时推算 GPU 表现。
- Windows 仍是产品目标平台；目标机 CPU / GPU / 内存尚未指定，因此本机通过只能作为开发机证据，Windows 性能验收保持未完成，不能擅自以虚拟机结果替代。
- P1–P3 本机证据成立后可交付萝卜SAMA亲测，Windows 项单独保持待验；开发机交付与目标平台最终验收分别记录。
- 以上数值是已确认的工程验收目标，不是当前实测结果或最低配置承诺；若首档失败，记录瓶颈和实际值，不通过缩小负载或降低实际分辨率冒称通过。

## 验证、隔离与交接

- 状态：配置与命令输入校验、占用 / 稳定 ID、材料和构件守恒、单线对照、多线独立性、汇流公平、帧率变化下的推进与暂停。
- 存档：精确状态往返、半批 / 满缓冲 / 转角载货保存、退出重启、未来版本拒绝、重复 ID / 越界 / 超容量 / 非有限数拒绝、并发写入阻断、主档与备份故障。
- 玩家路径：真实 Boot → 工厂列表 → 新建 / 继续 → 多线操作 → 保存返回 → 新进程继续；旧二维新建 / 载入路径单独回归。合成输入、按钮信号与用户亲测分别记录。
- 画面：继承现有按像素渲染检查，复核普通 / 最大化 / 缩回、转角、遮挡、相机跟随边缘和 HUD 输入阻断；精细度不作为本包追加美术的理由。
- 自动与手工验证分别使用 `tools/runtime-intake/check-runs/factory-foundation-v1/`、`tools/runtime-intake/review-worlds/factory-foundation-v1/`。Boot 的旧切片和新工厂目录均注入隔离根，防止启动时旧迁移服务触碰用户正式档。
- 截图存于 `assets/art-intake/<date>-factory-foundation-preview/`，实际目录与检查结果写周志；存档证据保留最终可重进世界。
- 文档与静态入口按仓库规范执行；实施后扩展客户端静态 / Godot 定向检查，保留旧覆盖。Godot 和窗口检查前告知，不以旧独立 Demo 入口替代本包 Boot。

## 当前实现与证据边界

`978d6250` 已接入正式 `client/`，两份根入口同步已确认的产品 / 介质边界；授权已覆盖 P1–P3，不再将接入或 schema 1 写成待审阅。以下仅汇总实际进展，详细批次和失败见 [W37 周志](../devlogs/2026-W37.md)。

| 包 | 当前实现与已测证据 | 尚需收口 |
| --- | --- | --- |
| P1 | Boot 工厂菜单、新建空场坪、64×64 / 25 矿点、增量索引、相机跟随；状态 62 项、Web 对照 1,839 检查点、Boot 33 项及旧入口回归；双线 5 张原图已审阅，补充四侧边缘移动 / 相机 / HUD 原图 | 按下表区分合成输入与原生 / 亲测；旧二维载入画面已补齐；亲测收到无明显问题反馈，未补写逐项覆盖 |
| P2 | 独立 schema 1、保存 / 候选恢复 / 世界锁；存档故障 44 项、满载写 / 读各 107 项及锁定向通过；时钟 / 失败退出 28 项；原生取消 / 重试 / 最终放弃 / 返回 / 重进 53 项、后台恢复通过；9 月 12 日系统关闭保存 17 项、独立 Boot 重进 15 项通过 | 备份 / 坏档提示与遗留锁恢复窗口已补齐；Windows 文件 / 进程锁未实测 |
| P3 | 双线操作写入 42 项 / 读取 24 项；100 台 / 1,000 带满载短测；规模汇流 210 项、两矿源实际争用 56 次各获 28 次；准备 / 采样中断、窗口长帧计时 / 暂停恢复 / 三次规模拆改、30 分钟后台稳定性通过 | 本机四档各 450 秒已于 9 月 25 日通过；亲测收到无明显问题反馈；Windows 待验，历史波动及观察开销未定 |

- 工程夹具为 25 条线，各含 1 采集器、1 工作反应器、1 缺料备用反应器、1 仓和 40 带。满载时 1,000 件在途、75 台回压、25 台缺料；这不等于 100 台全在加工，也没有用静态货物代替真实推进。
- 本机 Apple M5 / Godot 4.7 / Forward+ Metal / 4× MSAA 下，各约 12 秒的 1080 窗口与最大化、局部与拉远满载短测通过预算；最大化拉远 3D 为 `2745×1420`，帧间隔 p95 `12.985ms`、模拟步 p95 `1.661ms`，保存约 `95–100ms`。加载记录包含等待 30 帧，不能当纯 IO 耗时；GPU 时间未取得。
- 9 月 8 日长测被关闭窗口中断，首档约 450 秒活动墙钟只推进约 447.9 秒；原记录含失焦与约 1.174 秒长帧。9 月 9 日无窗口探针复现引擎长帧 delta 漏时，正式应用改用单调活动时钟；Boot 定向中 1.216006 秒活动时间完整进入模型及余量，后续窗口长帧计时亦通过。原历史差额未逐帧还原，修正后四档前台持续性能于 9 月 25 日完成，原中断记录不拼接进新批次。
- 萝卜SAMA允许后台运行后，隔离 headless Boot 完成真实 `1800.001092s`，写入 133 项 / 独立 Store 读取 5 项通过；交付 4,375 件，59 次自动保存全部成功，完整状态及后续 `87.25s` 轨迹一致。该检查仅注入测试活动焦点，产品失焦暂停不变；不替代前台帧率、GPU、原生焦点或弹窗验收。原始数据与失败批次边界见 W37 周志。
- 性能检查已用返回值和中断状态终止整个阶段序列；准备 / 采样失焦信号注入均不进入后续阶段，原生失焦亦有中断记录。完整与中断记录保留帧、逐帧模拟、单步及保存样本、采样尺寸与中断原因；Boot 同步加载耗时与等待帧数分列。四档完整前台验收于 9 月 25 日通过，临时帧调度对照或局部样本仍不计通过。
- `sh tools/check-factory-foundation.sh <mode>` 的 `state` / `clock` / `view-state` / `interruption` / `process` / `legacy` / `scale` / `scale-process` / `scale-merge` 均无窗口。`clock` 走隔离 Boot 并注入焦点 / 弹窗按钮信号；`interruption` 不产生性能通过结论。规模存读 / 中断需要已有 `scale` 快照；汇流夹具保持 100 台 / 1,000 带，将一路真实矿源改接共享输入，19 段备用带不伪称活跃物流。
- `boot`、`operation-write <batch>` / `operation-read <同一 batch>`、`performance`、`sustained` 会开窗口。`sustained` 从空物料工程线开始，四档各 450 秒，不能在用户占用桌面时自动启动。`check-client` 默认仅静态；Godot 模式包含状态 / 存档 / 时钟 / 生产画面状态，但不包含所有跨进程、Web、窗口或持续负载检查。

- 9 月 12 日同状态同步开 / 关 / 恢复各 90 秒完成，三份有效调用栈保留；不再将未恢复的旧对照作为唯一依据。随后省去未变生产状态的重复渲染提交，状态灯共享同形网格和只读材质；与原实现 28,806 项状态比较通过，最终基准截图与原图 SHA-256 相同。状态灯优化后原配置短测绘制调用 p95 从 557 降至 508，模拟步 p95 `1.113ms`，节点处理跨度 p95 `3.324ms`；帧 p95 `21.237ms` 仍高于 `16.7ms`。不同批次含运行时波动，不能把帧差全部归于补丁。
- Godot 内置的默认 Metal GPU 时间接口仍不可用；临时 Vulkan 与效果对照取得 GPU 数据，但未永久改驱动、同步或画质。恢复原效果后表现也发生变化，尚不能把瓶颈唯一归结为 Metal、SSAO 或阴影；关闭效果的诊断段不计验收。系统关闭 / 独立重进原图已审阅，该阶段仍等待前台长测与亲测；后续长测结果见下文，详细历史批次及失败见 W37 周志。

- 后续三组同状态重复对照完成，每段固定预热 15 秒、生产 45 秒。默认 Metal 无额外 GPU 计时仍为 `20.595 / 20.853ms`；Vulkan 分项值受到采样干扰，不能作为关闭效果的依据。每段交付 125 件、自动保存成功、计时守恒，六张基准图字节一致。随后已取得默认 Metal 的有效原生轨迹：drawable 等待 p95 `19.225ms`、合并每帧 GPU 活动 p95 `15.073ms`，采样所属 90 秒帧 p95 `21.227ms`；不能将两项分位数相加或将受采样干扰的运行当成验收。后续原不限帧 / 临时 60 FPS / 恢复各 45 秒已完成，帧 p95 `20.131 / 20.772 / 18.104ms`，临时限帧未改善；设置恢复，不采用为正式方案。已有轨迹的离线核对已收口，仍缺少 drawable 回收与等待结束的对象关联；实际引擎调用顺序审计及 9 月 13 日已授权的两版隔离诊断构建均已完成，第一版 CPU 顺序已验证但性能根因未明；第二版新增事件分析与首轮窗口实测于 9 月 25 日补齐，本轮生产帧 p95 `8.600ms`，未复现历史持续慢绘制，见 [Metal 呈现诊断记录](../reference/factory-metal-presentation-review-20260912.md)。不把重复短测当作长测通过。

9 月 25 日 34 项离线测试在普通 / `-O` 模式各通过，359,359 条实测事件零丢弃、8,590 次等待无超时。捕获在显示设备析构前结束，3 个存活 fence 单列；生产状态与历史对照仅正常时间余量不同，原图存在 8 个像素差异并已实际审阅。唯一 `102.525ms` 原始长帧包含 `98.630ms` 自动保存，同区间绘制仅 `1.743ms`；不删出统计、不归因于 GPU。详细失败分析及证据见 [W39 周志](../devlogs/2026-W39.md)。

同日后续正式版本原配置短测 8 项通过，帧 p95 `8.680ms`，原图与本日诊断图字节一致。四档长测首档 `220.697779s` 因 `focus_lost` 中断，26,411 帧和 7 次成功自动保存仅作部分证据，不计整档通过；没有启动后三档或自动重开。完整 Shell Godot 套件首次沙箱字体错误后，经授权主机重跑 74 个入口（含导入）全部通过；该次窗口长测未完成；同日后续完整结果如下，Windows / PowerShell 与亲测仍待验。

9 月 25 日经萝卜SAMA明确通知重新开始，正式 Godot `performance-1790317200-22165` 四档各 450 秒全部通过（29 项、0 失败，未失焦），共 `1800.015679s`、213,300 帧和 36,000 模拟步。帧 p95 `8.726 / 8.562 / 8.808 / 9.696ms`，单步 p95 `2.385 / 1.612 / 2.331 / 1.558ms`，最大余量不足 `0.05s`；60 次采样内自动保存成功，保存尖峰仍计入帧统计。窗口实际 3D 为 `1728×894` / 最大化 `2745×1420`，未降低画质；四张阶段起始原图已审阅。此项关闭本机持续性能缺口，不解释历史慢绘制、不代表窗口故障路径或 Windows / 用户亲测全部完成。批次明细见 [W39 周志](../devlogs/2026-W39.md)。

### 玩家路径证据核对

2026-09-25 对照现有合同、实际检查脚本和保留结果，细化原“剩余 Boot 窗口路径”；不新增玩法或验收预算，也不要求把已有原生通过项全量重跑。下列批次均位于 `tools/runtime-intake/check-runs/factory-foundation-v1/`，先离线核对，随后 `menu-review-20260925-a` 补齐 55 项窗口检查与 11 张原图；历史画面审阅结论取 W37 周志，本日批次取 W39 周志。

| 路径 | 可复用证据与边界 | 后续核对 |
| --- | --- | --- |
| Boot 新建空场坪、首线、返回继续 | `boot-1788875751-65167` 33 项；`factory_window_driver.gd` 通过 `push_input` 合成鼠标 / 按键，部分列表 / 旧入口用信号 | 已有窗口业务链，不称原生新建或用户亲测；在亲测中确认普通新建体验 |
| 双线操作与跨进程恢复 | `operation-20260908-a` 写 42 / 读 24；真实时间生产，输入合成、弹窗信号；`scale-process-1788953586-84845` 满载写 / 读各 107 | 已有操作与全字段恢复证据；后续亲测整体反馈无明显问题，非本项单独签验 |
| 长帧、四侧边缘、相机 / HUD、规模拆改 | `window-review-1788954820-91986` 66 项中 2 项弹窗失败；仅复用已核对的非弹窗路径与 01–06 原图 | 不把整批或 07 / 08 截图计作通过；弹窗另取下行成功批次 |
| 保存失败取消 / 重试 / 放弃、关闭与重进 | `native-save-1788959483-23647` 53 项；`native-close-20260912-a` 写 17 / 读 15，独立 PID；系统鼠标 / 关闭已有证据 | 代码与环境无相关变化时可复用，不继续笼统列为未测 |
| 备份恢复、全坏 / 未来版本阻断 | `factory_save_check.gd` 44 项，本日完整 Shell 套件通过；验证候选、来源哈希、发布与原文件保护，未经过列表 / HUD 画面 | `menu-review-20260925-a` 已补列表、恢复来源 HUD 与拒绝反馈，主档 / 来源保护已核对；合成输入，非亲测 |
| 活跃锁阻断与遗留锁恢复 | `process-1788954289-88441` 持锁 1 / 竞争 2 / 恢复 3；直接调用 Store，未经过恢复按钮 | 同批已补真实独立持锁进程的阻断、退出后显式恢复与继续；未知所有者原锁保持。列表 / 按钮使用合成鼠标 |
| 旧二维保留入口 | Boot 33 项含旧入口新建原图；本日 Shell 套件覆盖旧启动壳 / schema / catalog，属于无窗口业务证据 | 同批 10 / 11 原图已核对旧列表及载入画面，schema 10、已存核心能量 11 恢复；旧源档未改 |

本机已列明的界面缺口关闭：本批从新隔离副本经正式 Boot 进入，主档 / 备份及旧二维历史来源 hash 保持；拒绝世界与未知锁文件字节未变。55 项检查不等于原生鼠标或用户亲测，暂停截图使用测试调用，结果与输入边界详见 [W39 周志](../devlogs/2026-W39.md)。四档长测与本次短窗口均已退出，不自动追加窗口。

### 用户亲测交付

本机工程证据已具备，2026-09-25 萝卜SAMA反馈“测了，没啥问题”。记录为本次亲测未反馈明显问题，不补写具体操作覆盖；Windows / PowerShell 及目标硬件仍单列待验，开发机交付与目标平台最终验收分开。

独立入口位于 `tools/runtime-intake/review-worlds/factory-foundation-v1/playtest-20260925/`。在项目根执行 `sh tools/runtime-intake/review-worlds/factory-foundation-v1/playtest-20260925/start.sh`，复用正式 Boot、普通供给及双隔离存档根；不注入操作 / 产线，不自动删除世界。入口已通过 Shell 语法与 Godot 解析，随后已收到萝卜SAMA亲测无明显问题反馈。

最短亲测路径：工厂世界 → 普通新建 → 按目标建首线、第二矿区建另一条线 → 验证拖铺 / 取消、拆回与产出 → 载货 / 半批时保存返回并关闭 → 用同一入口重启继续。重点反馈操作是否清楚、两线是否独立、状态恢复是否符合预期和是否有可感知卡顿；保留亲测主档与备份。详细本地说明位于入口目录 `README.md`。

开发机首包交付据上述工程证据与亲测反馈收口，Windows / 目标平台最终验收单列。接续设计见 [Factory Discovery And Production V1](factory-discovery-and-production-v1.md)；方案待确认，设备新品类与美术精修继续暂缓。

## 相关真相源

- [Factory World State And Save V1](../architecture/factory-world-state-and-save-v1.md)
- [Production Client Technology Assessment V1](../architecture/production-client-technology-assessment-v1.md)
- [Production-Centered Direction](../product/production-centered-direction.md)
- [Godot Runtime Verification Guide](../reference/godot-runtime-verification-guide.md)
