# Slice First Playable Journey V1

更新时间：2026-07-28

状态：**活跃，包 2 人工计时落在 20 到 40 分钟，但发现体验阻力，暂不进入包 3**。承接已收口的基地、建造供电、物流反应器、多世界存档和首次外勤战斗专题，第一次从真实新档验证 20 到 40 分钟完整切片，而不是继续增加同层功能。

## 专题目的

现有专题已经分别证明六个玩家动词和关键系统可运行，但此前的正式入口证据按包拆分，后段路径会从 L5 保留世界开始。它们不能证明不了解项目的玩家能从新档自然走到切片终点。

本专题只解决一个问题：

**玩家能否从真实 `Boot → 新建世界` 出发，在没有状态注入和开发者口头指导的情况下，修复前哨、建立首条化工链、完成首次外勤并把关键样本交回基地。**

## 玩家结果

完成后，玩家应在一段连续体验中：

1. 从启动列表创建并进入新世界。
2. 采集晶体、制造修复零件并让前哨核心恢复运行。
3. 放置工业地板、供电、采集、储存、传送和反应器设备，产出 2 份催化剂。
4. 把催化剂交给核心完成工具充能，理解工业成果为何服务外勤。
5. 前往同图远征区，经历攻击、闪避、受击或撤离，击败唯一敌人并拾取关键样本。
6. 返回基地交付样本，获得抗蚀内衬与 `120` 最大生命的固定成长。
7. 在自然节点退出并重新载入后，仍能继续且不会卡死。

六个既定动词——移动、采集、战斗、建造 / 放置、加工、交互——必须在这条路径中自然发生；本专题不新增第七个动词。

## 决策闸门十问

1. **玩家新增什么动作？** 不新增动作；新增的是把六个已有动词组成可独立完成的一条旅程。
2. **世界发生什么变化？** 核心从损坏到修复，自动化链从无到有，工具从未充能到可战斗，敌人与样本进入完成态，角色最大生命从 `100` 提升到 `120`。
3. **如何服务基地与冒险闭环？** 基地产出的催化剂解锁战斗，外勤样本又永久强化角色，形成第一次双向回路。
4. **5 到 10 分钟内如何感知？** 每段都要出现清楚的工程、冒险或成长变化；完整路径必须在 20 到 40 分钟内结束。
5. **是否需要通用系统？** 包 1 不新增系统。若证据显示引导阻断，包 2 优先使用现有权威状态派生固定阶段目标，不复活旧通用任务链。
6. **能否复用现有地图和素材？** 能；限定为当前单基地 + 单远征区和现有像素资产，不新增地图或素材类别。
7. **成功证据是什么？** 自动断言、真实 `Boot` 有窗口输入链、关键节点截图、保留存档、萝卜SAMA人工复核，最后再由不了解项目的朋友盲测。
8. **失败怎么办？** 先按路径、理解、节奏、视觉或存读分类；只修阻断项。连续两次同类实机失败时停手升级，不堆提示或调参。
9. **是否复活旧系统？** 否；不复活旧 `GameRoot`、旧任务链、旧三槽存档、旧地图或旧 HUD。
10. **是否更接近切片完成？** 是；这是章程五条完成定义此前缺失的端到端证据。

## 权威路径与状态

路径固定为：

```text
Boot
→ StartupMenu 新建世界
→ 新档采集与手持合成
→ 修复核心
→ 建造供电、采集、物流和反应器链
→ 自然产出 2 catalyst
→ 核心首次充能
→ 晶体区唯一敌人
→ key_sample 拾取
→ 核心交付
→ corrosion_lining / 120 生命
```

`SliceSaveService` schema 7、多世界目录、现有世界状态和静态定义仍是唯一真相源。完整路径不得用测试注入直接写入最终库存、建筑、战斗或交付状态；允许测试脚本通过正式输入加速等待，但不得绕过玩家动作和权威规则。

## 实现包

### 包 1：真实新档全链基线与缺口审计

- 不修改玩法结果，先从真实 `Boot → 新建世界` 运行完整路径。
- 记录各阶段到达时间、交互次数、资源收支、自然保存点、失败恢复和需要猜测或口头解释的位置。
- 至少在化工链运行中或外勤前完成一次“保存返回 → 完全重启 → 载入继续”。
- 在核心修复、产线运行、工具充能、样本携带、样本交付五个关键节点抓图；多图先合成联系表。
- 输出按 `P0` 卡死 / 崩溃、`P1` 无法理解或超时、`P2` 打磨观察项分类的缺口清单。
- 一次性驱动脚本默认放 `/private/tmp`；只有形成稳定回归价值后才进入仓库。

包 1 出口：全链能够完成并形成可信时长基线，或准确定位阻断位置；不以“测试脚本跑完”代替视觉和路径判断。

2026-07-27 包 1 完成：真实 `Boot → StartupMenu → 新建世界` 无最终状态注入，依次完成 25 个有限晶体自举、37 个可再生晶体采收、31 次放置、自然产线、加工中重启续跑、2 催化剂取料与充能、三次鼠标攻击、样本拾取和核心交付。253 项断言通过；全程记录 84 次交互、101 次按键、48 个合成产物及约 390.7 秒最低生产等待。最终 schema 7 保存 `delivered / 120`、31 栋建筑和三份备份；隔离根为 `/private/tmp/radishcatalyst-first-playable-journey-package1/`，六张原图和联系表位于 `assets/art-intake/2026-07-27-first-playable-journey-package1-preview/`。

包 1 缺口分级：

- `P0`：无；完整路径、一次完全重启续跑、最终保存和世界摘要均成立。
- `P1` 理解：修复核心后 HUD 只显示“首次充能：催化剂 0/2”，没有把采集器、供电、工业地板、储物箱、传送带和反应器组成一条产线的下一步告诉新玩家；自动路径依赖已知布局，不能作为无人指导证据。
- `P1` 可读性：主要资源、目标与战斗 HUD 没有稳定底板或描边，在浅色基地 / 晶体地上对比不足；关键通知本身有描边，不是同一根因。
- `P2` 节奏：84 次交互、101 次按键和 31 次放置存在重复劳动风险；自动化使用传送和加速 tick，不能判定真实 20 到 40 分钟，留给包 2 人工计时，不在包 1 调配方或资源。

### 包 2：只修阻断路径、理解与节奏

- 只处理包 1 证明会阻断完成定义的 `P0` / `P1`。
- 优先修根因：权威状态、交互优先级、资源预算、空间路径和目标派生；不靠长说明或连续弹窗遮盖问题。
- 如需阶段目标，使用紧凑 HUD 和已有状态派生固定序列；不建立通用任务图、剧情系统或教程框架。
- 把“修核心后到首次充能”拆成由现有状态派生的短目标：先建立通电采集，再搭供电反应器与双端物流，最后取得 2 催化剂；配方 / 放置面板只补完成当前动作所需的一行规则。
- 给现有资源、目标、生命和战斗 HUD 增加同一套紧凑半透明承载底板或等效高对比处理；不借机做 HUD 换皮。
- 每个可分割修正执行匹配专项；玩家可见修正必须从真实入口复核。

包 2 出口：内部新档路径能在 20 到 40 分钟内稳定完成，无口头指导、不可逆卡死和崩溃。

2026-07-27 包 2 自动与正式入口证据完成：`SliceWorld` 只从核心、通电设备、建筑储物箱、背包与遭遇状态派生 `修核心 → 通电采集 → 通电反应器 → 双端物流 → 取催化剂 → 核心充能 → 外勤`，没有新增存档字段或任务图；合成面板显示当前一步规则，主要 HUD 与交互提示使用统一半透明高对比承载。专项 40 项和完整 Godot 回归通过；真实 `Boot → 新建世界` 无终态注入以 267 项断言再次完成整条旅程，记录 84 次交互、103 次按键、31 次放置、48 个合成产物与约 390.7 秒最低生产等待。九张原图及联系表位于 `assets/art-intake/2026-07-27-first-playable-journey-package2-preview/`，视觉复核确认浅色基地 / 晶体地上的资源、目标、战斗与提示清晰且无截断；schema 7 主档和三份备份保留在 `/private/tmp/radishcatalyst-first-playable-journey-package2/`。剩余出口证据只有萝卜SAMA从干净人工复核目录完成真实计时与无口头指导判断。

2026-07-27 萝卜SAMA人工计时落在 20 到 40 分钟，完整体验总体可完成且没有新增 `P0`，但包 2 暂不收口：

- `P1` 节奏 / 交互：前期大部分时间等待采集器产晶；HUD 承载过大挡视野；设备位置难精调；中继放置缺电网距离预览；设备占地所需地板不清楚，也缺少鼠标连续铺地或先预览设备再补地板的自然流程。
- `P2` 表达：设备建造与背包仍是纯文字面板，应在后续 UI 专题转向图形化格子、图标与状态表达。
- 架构级升级点：整体画面仍显得“太平”，3/4 俯视体积感不足；这会触及投影、素材锚点和场景组合口径，不在执行会话内直接调参。

#### 包 2A：生产节奏与紧凑 HUD

1. **玩家结果：**第一台采集器通电后，生产继续可见但不再由纯等待主导；默认 HUD 只保留当前目标、关键资源、生命和当前上下文，完整套件信息回到合成 / 放置上下文。
2. **实现介质：**不改配方、25 晶体自举、资源总量或 schema；只调整采集器确定性生产周期，并收紧既有 `SliceHud` 场景布局与上下文文本。
3. **成功证据：**37 个可再生晶体和反应器加工的理论最低生产等待从约 `390.7s` 降到 `60s` 内；真实新档仍能自然完成自举和产线，HUD 常驻高度明显降低且无截断 / 遮挡。
4. **失败判据：**生产变成瞬时领奖、旧档采集进度失效、HUD 缩小后当前目标或关键资源不可读，或仍需靠新增长说明解释产线。
5. **文档落点：**包边界与结果留在本专题，实施与验证写入 W31 周志；阶段方向未变，不因本包扩写规划入口。

证据归因：包 1 / 包 2 自动全链均记录 37 个可再生晶体与约 `390.7s` 最低生产等待，其中采集器按 `10s × 37 = 370s` 占主要部分；人工复核进一步确认等待主导感。首轮选择把采集周期从 `10s` 调整为 `6s`，不改每次产出、配方或总需求，理论最低生产等待降至约 `242.7s`。旧 schema 7 中已经累计的 `0–10s` 进度仍需可读取，超过新周期的进度在载入后于下一生产 tick 结算。

2026-07-28 包 2A 自动与正式入口证据完成：采集周期调整为 `6s`，保存校验继续接受旧 `0–10s` 进度，载入时按当前周期结算；常驻 HUD 从四行资源 / 套件 / 目标收为两行目标与关键资源，顶部左侧承载由 `192px` 降到 `114px`，套件全集保留在合成上下文。完整 Godot 回归通过；真实 `Boot → 新建世界` 仍以 267 项断言完成 84 次交互、103 次按键、31 次放置和 48 个合成产物，最低生产等待降为 `242.7s`。九图联系表与基地 / 晶体区关键原图确认无截断、遮挡或浅地对比回归。

2026-07-28 萝卜SAMA人工视觉反馈未通过：紧凑 HUD 虽减少遮挡，但仍是矩形底板加纯文字的草稿读法，不像正式游戏 UI。当前停止继续调同类尺寸、透明度和字号；生产节奏改用停在“核心已修复、采集器已通电”的独立存档复核，不要求从新档重跑。HUD 后续需先做 UI 介质与组件层级复盘，不能把图形化面板继续当作 `P2` 延后，也不与包 2B 放置实现混改。

2026-07-28 萝卜SAMA从阶段档确认 `6s/个` 仍然太慢，明确建议 `1s/个`。当前采集周期按实机反馈调整为 `1s`，37 个可再生晶体等待降为 `37s`，连同两轮反应器与物流的理论最低生产等待约 `57.7s`；配方、资源需求、每次产出和 schema 均不变，完整 Godot 回归通过。

生产节奏人工复核根为 `/private/tmp/radishcatalyst-production-rhythm-review/`：schema 7 世界已修复核心，三段中继和采集器通电，玩家停在采集器旁，采集 buffer 近零；主档与三份备份保留。该档直接读取当前 `1s` 周期，不需重建，也不作为全链完成证据。

2026-07-28 萝卜SAMA人工确认 `1s/个` 的生产速度可以接受。生产节奏退出包 2A；HUD 改由可执行子专题 [Slice HUD Gameplay Shell V1](slice-hud-gameplay-shell-v1.md) 承载，以任务舷窗、物资槽、角色生命条、敌人目标条和操作键帽替代整行文字底板。该路线不改世界美术介质、玩法状态或 schema，包 2B 继续冻结到 HUD 人工通过。

#### 包 2B：鼠标放置与空间反馈

1. **玩家结果：**放置预览跟随鼠标网格；地板可连续铺设；设备足印、缺地板格、中继连接距离和设备供电覆盖在确认前可见。
2. **实现介质：**复用既有建筑定义、占用、电网与放置控制器；鼠标目标解算、动态格子高亮和范围叠层进入窄职责组件，`SliceWorld` 只协调权威校验与落盘。
3. **成功证据：**真实输入可完成鼠标单放、地板连续铺设、旋转、取消和 UI 输入互斥；预览与最终占用 / 供电结果一致，现有键盘 `E` 路径不回归。
4. **失败判据：**点击穿透 UI、拖动重复扣料、预览范围与权威网格不一致，或继续把放置分支堆入已接近硬上限的 `SliceWorld`。
5. **文档落点：**本专题记录交互契约；实现与验证进入周志。纯文字面板图形化和 3/4 投影 / 素材路线不并入本包。

包 2B 输入契约先定为：放置态左键确认、`E` 保留兼容、`R` 旋转、`Esc` 取消；只有工业地板允许按住左键跨格连续铺设，设备保持单次确认；鼠标位于前台 UI 时不得落地。动态格子与电力范围只作交互辅助，不改变像素资产主介质。

### 包 3：完整验收与陌生玩家盲测

- 从干净世界完成一次正式入口全链，保留主档与三份备份。
- 萝卜SAMA先复核关键节点、时间、HUD、读档和完成态。
- 内部基线通过后，再交给一位不了解项目且不预设自动化经验的朋友，无人指导完成盲测。
- 只记录试玩者是否理解目标、为何出门 / 返回、是否愿意继续以及最想要什么；不在试玩过程中即时改规则。

包 3 出口：章程五条完成定义全部有证据。无论继续、调整或停止，第一轮产品可行性结论都能成立。

## 成功与失败判据

成功必须同时满足：

- 陌生玩家从新档在 20 到 40 分钟内完成整条路径。
- 玩家能用自己的话说明正在做什么、为什么出门、为什么回基地。
- 全程由正式像素场景、设备、角色、敌人和 HUD 表达，不依赖 debug 色块、流程图地图或长说明。
- 至少一次中途退出 / 载入后可继续，零不可逆卡死、零崩溃。
- 试玩者能回答是否愿意继续以及最想要什么。

以下任一情况视为未通过，不继续增加内容：

- 需要开发者口头提示才能找到目标或完成设备链。
- 正常操作稳定超过 40 分钟，或资源重复劳动掩盖了闭环。
- 存读、交互优先级、空间阻挡或资源预算造成不可逆卡死。
- HUD 继续加字仍不能让玩家理解路径。
- 同一类问题连续两次采用同类修正仍未通过实机复核。

## 边界

- 不新增敌人、Boss、装备词条、技能树、地图、化工配方或建筑类别。
- 不做立绘对话、八方向动画、viewport 整数重构、世界视觉介质换线或打磨清单；当前 HUD 组件重构只按子专题范围执行。
- 不做联机、角色 / 世界拆档、发布、打包或试玩分发平台建设。
- 不删除旧运行时层，不做与全链阻断无关的重构。
- 包 2 内部全链与人工计时通过前，不进入朋友盲测。

## 文档与验证

- 阶段与当前包入口：`docs/planning/current.md`、`docs/planning/daily-start.md`。
- 完成定义：[Project Purpose And Solo AI Development Review](../planning/project-purpose-and-solo-ai-development-review.md)。
- 阶段复核：[Milestone Review Checklist](../planning/milestone-review-checklist.md)。
- 决策边界：[Development Decision Gates](../process/development-decision-gates.md)。
- 运行时方法：[Godot Runtime Verification Guide](../reference/godot-runtime-verification-guide.md)。

默认先运行静态 `sh ./scripts/check-client.sh`。启动 Godot、执行 `--with-godot` 或有窗口验证前先告知萝卜SAMA；玩家可见结果必须以正式入口截图和人工路径为主证据。
