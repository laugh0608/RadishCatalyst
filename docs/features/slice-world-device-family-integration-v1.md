# Slice World Device Family Integration V1

更新时间：2026-08-08

状态：**架构方向已于 2026-08-08 获萝卜SAMA确认，联合包 1 等价地基已完成；正式设备、端口和 schema 8 尚未切换。**[分类库存与通电储物箱 V1](slice-category-inventory-and-powered-storage-v1.md) 是联合 schema 8 前置合同。

## 玩家结果

把已通过 Gate A 的设备家族真实接入第一可玩切片：大型设施保持固定正面，物流口与传送带一格一格真实对接，储物箱成为通电的本地供给 / 无线回库设备，核心仓库和采集器成为空间物流端点，供电线只表达真实二值连接；旧档保留建筑、物料和加工状态，并为端口迁移后的断链提供可恢复路径。

本专题承接 [视觉层级与色彩分离 V1](slice-visual-hierarchy-and-color-separation-v1.md)，不重开设备设计。`target-world-v01.png` 已人工通过，但只锁定画面目标，不代表以下架构和玩法已经实现。

## 不可变输入

| 资产 | 锁定候选 | 尺寸 | SHA-256 |
| --- | --- | --- | --- |
| 核心 V5 | `assets/art-intake/2026-08-05-batch09-native-redraw/core_native_redraw_v5.png` | `152×128` | `e5ab5a430e4f725b4ac61eeb1d170d93b0f9d217b794e4716cab7ed5fc447bc3` |
| 反应器 V10 | `assets/art-intake/2026-08-05-batch09-native-redraw/reactor_native_redraw_v10.png` | `184×88` | `c96d9a4423cbe3e28f76d56483194365719524bc6b4e302ebdce33993d64e930` |
| 采集器 V1 | `assets/art-intake/2026-08-05-batch12-support-device-native-normalization/collector_native_v1.png` | `96×112` | `8c9fca2cbac02ddc91a6b2940a16cbb6e17dd61be322f45e0cfaa7964404639d` |
| 储物箱 V1 | `assets/art-intake/2026-08-05-batch12-support-device-native-normalization/storage_native_v1.png` | `104×80` | `e764156c5670c24375c753ecc8d7629e8c00400b4d18c6dfdd3456b1962f5184` |
| 中继 V2 | `assets/art-intake/2026-08-05-batch12-support-device-native-normalization/power_relay_native_v2.png` | `48×76` | `22608d88ec311929c3e4b7c051caa3dbd2a8fe365cf944a00c2d902ce02d408f` |
| 世界目标 | `assets/reference/visual-direction/target-world-v01.png` | `960×540` | `ffe56b4c99b0f5d53cf13490ad9f5cd5f742fae84942cc0ab846b3e73541647d` |

- 五张透明设备候选只能逐字节复制并调整场景锚点，禁止重新生成、重画、缩放、旋转、补画端口或派生第二套主体。
- 核心 V5 只覆盖修复态；确认本方案即接受受损态暂时继续使用现行受损核心，修复后才切换 V5。中继通断、反应器加工与消费者通电继续使用允许的动态状态灯 / 既有小型 overlay，不修改锁定主体像素。

## 包 1 前现状审计

| 耦合面 | 当前运行事实 | 迁移风险 |
| --- | --- | --- |
| 贴图与足印 | `SliceBuildingDefinition.normalized_rotation()` 同时选择四向贴图、交换足印长宽，并影响原点、中心、底边排序、碰撞与交互框。 | 固定正面若只换单帧，旧旋转仍会从其他路径改变几何或锚点。 |
| 物流端口 | 储物箱只有一个随旋转变化的双向口；反应器的对置 `IN / OUT` 随旋转变化；物流网、放置叠层、方向诊断和设备面板都调用这些结果。 | 新储物箱左入右出、反应器左入右出不能靠把 `allows_rotation` 改成 `false` 静默获得。 |
| 供电 | 采集器 / 反应器的供电探针随旋转变化；中继按足印中心建图。`powered_connections()` 只给核心—中继和中继—中继边，不提供中继—消费者表现边。 | 固定正面的逻辑供电探针、设备顶部画线锚点和中继端子必须分离，否则视觉锚点会改变供电判定。 |
| 放置与调整 | 放置控制器以 `rotation_index` 驱动预览；`R` 修改它。调整暂存原格与原旋转，提交 / 取消后重建占用、供电和物流。 | 固定设施仍需可移动和无损取消，但不再允许 `R` 改变朝向；传送带必须继续旋转。 |
| 存档 | schema 7 对每个建筑严格保存 `{instance_id, building_id, origin_cell, rotation, state}`，`rotation` 必须为 `0–3`；载入后按它恢复建筑，再派生占用、供电和物流。 | 直接改变 schema 7 中同一字段的语义会让旧档静默变形，也会让旧客户端误读新档。 |
| 核心与采集器物流 | 核心是 `SliceMap.tscn` 中固定 `Sprite2D`，不是建筑实例；`core_storage` 只支持背包手动存取、充能和交付。物流网只登记传送带、储物箱与反应器。采集器缓冲同样只能手动取出。 | Gate A 的核心左入右出和采集器右出都尚不存在；只换图会显示无法使用的假端口。 |

结论：`building_rotation` 不是单纯视觉参数，而是贴图、空间几何、物流、供电、调整与存档的共享不变量；核心 / 采集器也缺少目标稿所展示的物流职责。正式接入必须做版本化、原子化迁移。

## 目标合同

### 方向、贴图与端口

建筑定义改为显式声明三件互不替代的事实：

1. `orientation_mode`：`fixed_front` 或 `cardinal`，只决定玩家是否可按 `R` 及 `rotation` 是否有方向语义。
2. `visual_mode`：固定单帧或四向固定帧；运行时仍禁止旋转像素图。
3. `logistics_ports`：每个端口有稳定 ID、`input / output` 角色、本地端口格、外向方向和 `fixed_local / follows_orientation` 策略，不再用一个可空字段兼任多种设备。

| 对象 | 朝向 / `R` | 固定物流合同 | 供电角色 |
| --- | --- | --- | --- |
| 前哨核心 | 场景固定正面 / 无 `R` | 左侧 `IN`、右侧 `OUT`；只在修复后启用 | 唯一电源 |
| 晶体采集器 | `fixed_front` / 无 `R` | 右侧 `OUT`，只输出 `crystal` | 现行二值消费者 |
| 基础反应器 | `fixed_front` / 无 `R` | 左侧 `IN` 只收 `crystal`；右侧 `OUT` 只出 `catalyst` | 现行二值消费者 |
| 储物箱 | `fixed_front` / 无 `R` | `supply` 只启用右 `OUT`；`transfer` 只启用左 `IN` | 新二值消费者 |
| 电力中继 | `fixed_front` / 无 `R` | 无物流口 | 现行二值中继 |
| 传送带 | `cardinal` / 保留 `R` | 输出方向继续跟随 `rotation` | 被动设施 |
| 工业地板 | 固定 / 无 `R` | 无物流口 | 被动承载 |

- 固定设施保存的规范旋转值恒为 `0`；`building_rotation` 仅继续服务传送带及未来明确声明为 `cardinal` 的对象。
- 当前所有待切换设施均为正方形足印，规范化不会改变 `origin_cell`、占用格、碰撞或底边排序；代码仍须通过显式方向模式保证未来非正方形设施不会复用旧耦合。
- 反应器固定端口等价于旧 `rotation = 1` 的左入右出；储物箱从旧单一双向口升级为左入右出；采集器新增右出。端口内格和外接格必须落在 32px 网格上，sprite 的可见领口只负责对齐，不反向决定拓扑。
- 以足印左上格为局部原点：采集器 `OUT (1,1) → RIGHT`；反应器 `IN (0,1) → LEFT`、`OUT (2,1) → RIGHT`；储物箱 `IN (0,1) → LEFT`、`OUT (1,1) → RIGHT`；核心固定 2×2 足印为 `IN (0,1) → LEFT`、`OUT (1,1) → RIGHT`。外接格恒为端口格加外向方向。
- 储物箱模式、逐类容量、输出筛选、无线传输和面板合同以 [分类库存与通电储物箱 V1](slice-category-inventory-and-powered-storage-v1.md) 为唯一真相源；本专题只负责让锁定 sprite、固定端口、供电探针和世界锚点与该状态一致。

### 物流端点与所有权

- `SliceLogisticsGrid` 改为登记统一的窄端点描述，而不是按 `SliceStorage` / `SliceReactor` 写多组并行映射；储物箱、反应器、采集器和核心分别提供端点与权威库存 / 缓冲引用。
- 核心继续使用现有 `core_storage`，不创建第二份核心库存；采集器继续使用现有 `buffer`，不新增重复计数。自动搬运和手动操作必须读写同一真相源并发出既有刷新 / 自动存档信号。
- 物理传送带白名单保持 `crystal`、`catalyst`；机械零件与建筑套件可手动存取但不进入首版工业物流，关键样本继续走独立状态。每次传输先确认目标可接收，再从源移除，保持回压和守恒。
- 核心未修复时两个端点均不存在；修复后左口入库、右口按现行 `crystal → catalyst` 稳定顺序输出。采集器右口只在缓冲非空时输出，手动“取出全部晶体”继续保留。
- 储物箱 `supply` 模式只从右口输出选中类别，`transfer` 模式只从左口接收并按 `5s / 50件` 无线写入同一 `core_storage`；无线链不是第二份库存，也不占用核心物理左口。

### 供电判定与表现锚点

- 二值电网、核心 6 格、中继 6 格和设备 4 格范围不变；不引入发电量、耗电量、端子容量或负荷存档。
- 固定设施的逻辑供电探针使用稳定足印中心，不再跟随 sprite、物流口或旧旋转。核心 / 中继建图位置与设备顶部画线锚点分开。
- Gate A 锁定的设备画线锚点按 PNG 左上角计为核心 `(84,16)`、储物箱 `(52,18)`、反应器 `(92,22)`、采集器 `(48,2)`；中继西 / 北 / 东 / 前端子为 `(4,14)`、`(24,3)`、`(43,14)`、`(24,23)`。运行时从 texture-local 像素转换到世界坐标，不把这些数值用于范围判定。
- 供电父节点和消费者连线继续全部派生且不进存档；同一布局按距离与稳定实例 ID 得到确定结果。Gate A 四个端子各自唯一是基准截图合同，不构成新的四连接容量限制。
- 储物箱升级为真实消费者：手动访问不受断电影响，存储模式自动供货与传输模式无线回库需要通电；Gate A 的第四条中继线因此可以由真实电网结果派生，不再是假状态。

## schema 8 与旧档迁移

1. schema 8 由本专题与分类库存专题共同拥有，只能在联合包 2 一次启用；建筑继续保留必填 `rotation`，固定设施 / 地板只能为 `0`，传送带仍允许 `0–3`。
2. schema 2–7 先走现有迁移得到 schema 7 结构，再联合迁移逐类库存、储物箱模式和设备方向：原样保留稳定 ID、建筑 ID、`origin_cell`、全部物品、反应器在制批次 / 进度、传送带货物 / 进度 / 合流游标；只去掉旧容器总容量、补储物箱模式状态，并把固定设施 / 地板旋转规范为 `0`。
3. 不自动移动设施、旋转传送带或重排玩家产线。供电和物流本来就是派生状态，载入后按新固定探针 / 端口重建；旧反应器 `rotation = 1` 可自然保持左右链，其他旋转及旧储物箱单口线路可能断开。
4. 兼容承诺是“世界可载入、持久状态与物料不丢、布局不被偷偷搬动、断链可诊断并可修复”，不是任意旧端口方向的拓扑逐边不变。为避免终端带上货物卡死，包 2 必须给传送带面板增加容量预检后的单件货物回收；玩家回收后才能调整或拆除旧终端带。
5. 迁移成功、当前 schema 校验和世界重建全部通过后才写 schema 8；原候选与备份仍按现有多世界回退规则保留。重复迁移必须幂等，schema 8 保存—重启—载入结果一致。

## 实现分包

### 包 1：库存 / 方向 / 端点模型与等价回归

- 状态：**已完成。**六类现行建筑均显式声明 legacy `cardinal` 与现行视觉模式；`building_rotation`、四向贴图、足印、调整和 schema 7 数值不变。
- 按分类库存专题包 1 引入物品定义与容器 profile；同时引入方向模式、显式端点描述、逻辑供电探针和视觉锚点，先用 legacy profile 表达现行容量、四向储物箱 / 反应器、采集器和中继。
- 把物流登记与状态快照收敛到统一端点结构，保留当前传输顺序、回压、公平轮询和面板文本；此包不复制锁定资产、不升 schema、不启用新端点。
- 出口：现有库存、放置、四向端口、供电、物流、反应器、schema 2–7 和正式入口基线全部逐值不变。

### 包 2：分类库存 + 通电储物箱 + 可放置设备联合原子切换

- 与分类库存专题包 2 同一提交启用背包 / 核心 / 储物箱逐类容量、动态分类格、储物箱真实供电 / 双模式 / 无线回库和 schema 8。
- 逐字节接入采集器 V1、反应器 V10、储物箱 V1、中继 V2；原子启用固定正面、采集器右出、反应器左入右出和储物箱模式端口，禁用固定设施 `R`，完成旧档迁移与传送带货物回收。
- 不得先换 sprite 后留旧端口、先启用新容量后仍写 schema 7，或先显示模式开关再使用旧被动储物箱。
- 复用并重新定位现有反应器加工 overlay；中继通断只用锁定 V2 主体加动态状态灯。若锚点无法与锁定像素对齐则停手升级，不补画主体。

### 包 3：核心 V5 与核心物流原子切换

- 受损阶段保持现行素材；修复动作、读档修复态与预览在同一包切至核心 V5，并同时启用左 `IN` / 右 `OUT`。
- 核心物流直接读写 `core_storage`，保留手动存取、充能优先级、样本交付和 `core_repaired` 真相源；不新增库存或存档字段。
- 自动检查覆盖未修复无端点、修复后双端点、满仓回压、物理白名单、无线回库、手动 / 自动交错和重启守恒。

### 包 4：真实供电线、状态反馈与场景锚点

- 将二值图的逻辑位置与画线锚点分离，按真实父子 / 供电结果输出中继线；不伪造被动设施连线，不把端子数量变成电网容量。
- 校准五类设备的 sprite、阴影、交互、碰撞、排序、状态灯和加工 overlay；保持现行足印、相机、地表与 UI 结构。
- 储物箱线必须读取真实 `powered` 与模式状态；断电时线和自动行为同帧失效，手动库存操作继续可用。

### 包 5：迁移演练、正式入口与人工闸门

- 自动矩阵覆盖逐类容量、储物箱两模式 / 输出筛选 / 无线脉冲、四个旧旋转、断电 / 加工 / 满仓、端口方向、采集器 / 核心输出、带货回收、schema 2–8、坏主档回退和重复保存。
- 正式 `Boot` 分别验证真实新档与 schema 7 隔离旧档：新家族、完整自然产线、核心入 / 出库、保存重启、端口断链诊断与修复；保留最终测试档和截图。
- 截图至少覆盖受损核心、修复态核心、固定设施放置 / 调整、完整产线和旧档迁移后断链状态。完整 `check-client --with-godot`、专项断言、逐图复核和萝卜SAMA人工确认后才收口。

## 包 1 实施与验证

- 新的非持久化 `SliceLogisticsEndpoint` 绑定设备现有库存；物流网已统一登记端点、传输、拓扑、放置预览和面板快照，不再按储物箱 / 反应器维护并行映射。
- legacy 储物箱仍是单一双向口与 `POWER_PASSIVE`；反应器仍是对置 `IN / OUT`，source phase 明确保持全部储物箱先于反应器。采集器和核心没有提前获得端点。
- 逻辑供电探针与画线视觉锚点已分离；中继逻辑位置、三段既有供电树和世界画线坐标逐值不变，没有提前绘制消费者连线。
- 放置 `124`、供电 `96`、物流 `141`、反应器 `125`、存档 `203` 项断言及完整 `check-client --with-godot` 通过；首次导入发现并修复一处面板 `bool` 类型推断后全绿。
- 本包没有复制五张锁定候选、替换正式世界素材、修改端口坐标 / 供电角色 / schema 或写入新存档字段；作为内部等价重构，不另做玩家可见截图闸门。

## 不做

- 不重新生成、重画、旋转或缩放五张锁定主体，不借本专题新增受损核心或第二套中继 / 反应器主体。
- 不新增配方、建筑、货物种类、物流优先级、分流器、蓝图、有限发电、负荷、储能或电网容量；分类库存额外不做重量、背包升级或拖拽排序。
- 不改 32px 网格、现行足印、核心位置、世界相机、战斗、任务、联机或 UI 侧舷窗结构。

## 已确认架构结论

1. schema 8 对持久数据无损，但允许因固定端口而重派生旧电力 / 物流连接；不自动搬动玩家布局，以断链诊断和带货回收保证可修复。
2. 核心仓库每类 `99999`、背包普通物品每类 `200` 且不限制类型数、储物箱 `4×200`；储物箱是通电的存储 / 传输双模式设备。
3. 受损核心继续使用现行素材，修复态使用锁定核心 V5；中继通断和反应器加工只加动态反馈，不派生或重画锁定主体。
4. 包 1 只做等价地基；分类库存、储物箱模式、固定正面、显式端口与 schema 8 在包 2 联合原子切换，随后才进入核心 V5、供电线和正式入口收口。
